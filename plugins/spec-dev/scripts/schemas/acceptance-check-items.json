{
  "type": "object",
  "required": ["items", "coverage_note"],
  "properties": {
    "items": {
      "type": "array",
      "minItems": 1,
      "items": {
        "type": "object",
        "required": ["check", "dimension", "tier", "ops", "result", "evidence_ref", "notes"],
        "properties": {
          "check": { "type": "string" },
          "dimension": {
            "type": "string",
            "enum": ["unit", "integration", "e2e", "visual", "a11y", "perf-web", "perf-api", "other"]
          },
          "tier": { "type": "string", "enum": ["D", "A", "X"] },
          "requirement_ref": { "type": "string" },
          "ops": { "type": "string" },
          "result": { "type": "string", "enum": ["pass", "warn", "fail", "unverified"] },
          "evidence_ref": { "type": "string" },
          "notes": { "type": "string" },
          "recheck": {
            "type": "object",
            "required": ["status"],
            "properties": {
              "status": { "type": "string", "enum": ["维持", "降级", "翻案"] },
              "evidence_ref": { "type": "string" }
            }
          }
        }
      }
    },
    "coverage_note": { "type": "string" }
  }
}
