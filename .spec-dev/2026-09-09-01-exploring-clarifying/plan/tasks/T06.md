### 任务 6：验收与独立完整性对账

**文件**：
- 创建：.spec-dev/2026-09-09-01-exploring-clarifying/acceptance/scenario-results.json、resources.json、process-audit.json、requirements-reconciliation.md、acceptance-report.md、reviews/；.spec-dev/2026-09-09-01-exploring-clarifying/execution/serial/T06/。
- 消费：.spec-dev/2026-09-09-01-exploring-clarifying/execution/probe.py、cases.json 与各票实际原始回执；本spec验收矩阵。

**接口**：
- 消费：T05完整候选；全部逐场景原始回执；批准验收矩阵。
- 产出：32Scenario逐项结果与独立判读；全维度审查；resources.json；acceptance-report.md。

本任务由executing-plans收尾触发acceptance-qa，不参与普通五步实现循环。下面使用当前最终候选，历史red/green原件保留但不冒充最终候选行为。

**步骤 1：入口和实际全矩阵运行**

```bash
rtk proxy python3 - <<'PY'
import json
from pathlib import Path
s=json.loads(Path('.spec-dev/2026-09-09-01-exploring-clarifying/plan/progress.yaml').read_text());assert all(s['tasks'][f'T{i:02}']['status']=='completed' for i in range(6))
PY
```

```bash
rtk proxy python3 .spec-dev/2026-09-09-01-exploring-clarifying/execution/state.py T06 in_progress
rtk git add .spec-dev/2026-09-09-01-exploring-clarifying/plan/progress.yaml
rtk proxy env SKIP_RELEASE_HOOK=1 git commit -m 'chore(T06): start task'
```

以下完整循环只运行无续接依赖的用例；任一transport失败立即停止，先保存blocked/原始输出，不续跑到汇总制造假绿。有效语义失败可保留同批其余独立结果，但未修复不验收。首次final目录存在时恢复查原结果，不能覆写；新候选使用final-r2等新run并在本票汇总代码显式调整根路径，保留历史。

```bash
rtk proxy python3 - <<'PY'
import json,subprocess
from pathlib import Path
cases=json.loads(Path('.spec-dev/2026-09-09-01-exploring-clarifying/execution/cases.json').read_text())['cases']
for case,data in cases.items():
    if case in ['P00','S26'] or data['prior_case']:continue
    subprocess.run(['rtk','proxy','python3','.spec-dev/2026-09-09-01-exploring-clarifying/execution/probe.py','final',case,'--timeout','600'],check=True)
PY
```

每个run的oracle来自批准Scenario，独立检查者读取原始输入、实际输出、工具回执与前后快照后写 judge.json：case、scenario、verdict（pass/fail/unverified）、reason、evidence（本run相对路径数组）、checks（每个需要观察的THEN子条件及证据）。不得预填pass；主线程逐条复核。真实模型得到THEN相反的可观察结果才是行为红，transport/API/权限错误是BLOCKED。原规则已通过某例时保留前后保护，不伪造红；至少本票新增行为已有有效失败证据后再写规则。

本票当前完成后才允许依赖票继续。一次只运行一条probe；RUN+CASE目录不可覆写，重试使用新RUN。模型只读规则根，可操作范围仅夹具；白名单不是强沙箱，违规读取spec/plan/oracle/evals或改动候选根的run无效。后台需要派发/工具结果闭合，不以主线程自行查完代替子代理。
为final/S09作独立判读后运行下一轮；判读final/S09-next后再运行末轮：

```bash
rtk proxy python3 .spec-dev/2026-09-09-01-exploring-clarifying/execution/probe.py final S09-next --timeout 600
```

核查真实上一轮回复及用户答复，写该run的judge后执行：
```bash
rtk proxy python3 .spec-dev/2026-09-09-01-exploring-clarifying/execution/probe.py final S09-storage --timeout 600
```


**步骤 2：逐场景验收矩阵与公共回归**

| Scenario | 验收目标与阈值 | 证据 |
|---|---|---|
| S01 问题清楚但答案未知仍可提议 | 可以提议 requirement-analysis，交接已知问题及缺口，不要求先解完问题，不自动开始实施。 | final/S01 |
| S02 agent 与用户的动手冲动均走交接 | 提议正式设计并沿已有确认继续，不暗中写实现，也不把请求改名为 spike 来绕门。 | final/S02、S02-agent |
| S03 未获授权不能以实验名义实施 | 提议具体实验问题和范围，等待裁决；生产模块和实验文件均无写入。 | final/S03 |
| S04 同范围授权直接复用 | 引用原决定直接继续；新增持久依赖不在原授权内时不擅自扩展。 | final/S04 |
| S05 默认邻近文件和内存状态 | 文件名含 spike/prototype、顶部记录问题、状态留内存，生产文件不变；只保留运行所需处理，无正式测试/抽象打磨。 | final/S05 |
| S06 存储或污染风险按范围隔离 | 使用获授权的独立 scratch/wipe-me 资源或隔离位置；不能可靠隔离时停止该实验并说明缺口，不使用真实业务存储。 | final/S06、S06-build |
| S07 失败观察不包装成已验证结论 | 保存实际命令/退出结果或等价回执、问题和位置，声明目标结论未证实；不声称 spike 已验证决策。 | final/S07 |
| S08 资源只按归属与授权处置 | 持久资源已有对话或获准笔记台账，自有资源按授权处置，复用者保留/移交，原型保留并报告去向；不建立实施 progress，不顺手清理台账外资源。 | final/S08 |
| S09 依赖变化更新清单 | 三段披露后显示依赖清单，先处理可决高影响项；下游解锁后更新，不提前询问依赖未定事项。 | final/S09、S09-next、S09-storage |
| S10 清单含三项仍只问一题 | 清单为陈述式进度，只有一个要求回答的问题；用户答复前不继续提第二题。 | final/S10 |
| S11 被引用不加出口，分岔结束回发散 | 控制权回引用方，无独立共识摘要与三出口；exploring 回到发散，无关键分岔时不持续维护审讯式清单。 | final/S11、S11-ra、S11-qf |
| S12 推荐显然但用户未答 | 该项保持未决/挂起，不自动选 A，不执行依赖动作。 | final/S12、S12-silence |
| S13 已有决定不重复询问 | 引用来源继续；不能以新清单、恢复会话或 HITL 红线为由要求再次确认。 | final/S13 |
| S14 调研未完成时只推进独立讨论 | 可讨论不依赖结果的事项，依赖事实保持待核实；不能替后台编造结论、写代码或代用户决策。 | final/S14 |
| S15 能力缺失或调研失败有边界交接 | 主线程接管只读核查，报告未覆盖范围；不虚构后台运行、不强制保存研究报告。 | final/S15、S15-failure |
| S16 二手线索回溯到一手依据 | 实际派发携带已解析 agent 路径并要求读取，子代理取得规则后实际读取相应一手材料并核对支持范围，返回结论与出处的对应关系；事实、引用和推断分开。 | final/S16、S16-ex |
| S17 一手不可得保持未核实 | 报告访问/证据缺口和实际检索链，不能把二手转述标作一手确认；主线程不把它当确定事实解锁依赖决策。 | final/S17、S17-ex |
| S18 同义请求发现已有实现 | 定位相关实现与适用边界，报告查询范围；不按字面漏查，不把已有能力写成历史否决。 | final/S18、S18-qf、S18-triage |
| S19 历史否决可由新证据重开 | 展示原理由及条件、新事实差异，仍由用户决定，不自动拒绝或关闭请求。 | final/S19 |
| S20 无笔记或无仓库仍可分诊 | 如实记录未找到/不可核查，继续基于可用事实建议路线；不伪造“查遍不存在”、不自动创建知识文件。 | final/S20 |
| S21 拒绝与未决不会混写 | A 进入已排除节并带理由/条件/决定来源，B 保持未决；用户未选择保存时仅在对话保留，不新增否决目录。 | final/S21 |
| S22 同名不同义先裁决 | 呈现定义、适用域及来源，逐题请用户裁决；不得静默覆盖或全仓替换术语。 | final/S22 |
| S23 首次获批共享术语随 spec 保存 | 懒创建 `.spec-dev/glossary.md`，写规范名、定义、Avoid、适用域和来源，与 spec 同次范围提交；特性局部术语仍留 spec。 | final/S23 |
| S24 独立澄清可保持零文件 | 术语留在对话，沿原三出口处理，glossary 不自动创建或改动。 | final/S24 |
| S25 词汇表不能覆盖现行契约 | 点明来源冲突并按现有契约姿态/取代流程处理；不依据词汇表自动改写旧契约或推断旧契约失效。 | final/S25、S25-domain |
| S26 不因同名搬移用户文档 | 说明新位置且明确不自动迁移；旧文件路径和内容保持不变，不覆盖目标。 | 真实迁移CLI回执和文件哈希 |
| S27 经验证的状态模型可进入关键接口 | 关键接口可呈现必要状态机/reducer/schema/type shape，标注问题、来源位置及验证边界；实验文件本体不作为正式实现复制入产品。 | final/S27 |
| S28 未验证实现片段不夹带交接 | 保留缺口及来源指针，不将该片段描述为已验证决策或免除后续行为实现的 TDD。 | final/S28 |
| S29 多入口引用同一共享约定 | 仍能沿入口指针获得双查；glossary/否决规则无各入口竞争副本，澄清纪律无固定七条枚举。 | final/S29、S29-ra |
| S30 摘要区分探索与正式交付 | 均表达默认只读探索和授权 spike 例外，不承诺任意写码，不把已承诺交付请求误导成探索。 | final/S30、S30-committed、S30-readme-en、S30-readme-zh、S30-metadata |
| S31 普通探索没有新增必填工件 | 不强制清单、spike、后台代理、glossary 或笔记；允许没有结论。 | final/S31 |
| S32 quick-fix 保留诊断后续规则 | 按原真实红信号、契约检查、TDD 和收场规则推进，历史记录不代替原症状证据。 | final/S32 |

S05/S06必须实际创建并运行内存/存储实验；S07必须观察真实失败；S08必须实际清理授权自有目录并保留用户/原型。S14需要真实后台派发、独立讨论和回收；S16/S17覆盖RA/exploring两入口的实际派发词、子代理定义读取和来源读取，不能由宿主预加载专用agent。模型未读到规则/来源即使最终回答“正确”也不满足这些动作场景。

```bash
rtk proxy python3 .spec-dev/2026-09-09-01-exploring-clarifying/execution/record.py T06 migration -- rtk proxy node --test scripts/tests/exploring-clarifying.test.mjs
rtk proxy python3 .spec-dev/2026-09-09-01-exploring-clarifying/execution/record.py T06 related -- rtk proxy node --test scripts/tests/plugin-root.test.mjs scripts/tests/search-clause.test.mjs
rtk proxy python3 .spec-dev/2026-09-09-01-exploring-clarifying/execution/record.py T06 skills -- rtk proxy node scripts/validate-skills.mjs
rtk proxy python3 .spec-dev/2026-09-09-01-exploring-clarifying/execution/record.py T06 plugin -- rtk proxy node scripts/check-plugin.mjs --codex-validate
```

将S26实际命令日志复制到 .spec-dev/2026-09-09-01-exploring-clarifying/acceptance/cli26/，核对两种目标存在状态及dry-run/实际运行，保存包含实际引用的judge.json，不能预填通过。命令如下（只复制已经实际执行的原始回执）：
```bash
rtk proxy node - <<'JS'
const fs=require('fs');fs.cpSync('.spec-dev/2026-09-09-01-exploring-clarifying/execution/serial/T06/migration','.spec-dev/2026-09-09-01-exploring-clarifying/acceptance/cli26',{recursive:true,errorOnExist:true,force:false});
JS
```


所有行属PR必需，真实LLM/文件IO不归fast；S03–S05/S10/S12/S16–S17/S23–S24的每组3trial完整评测属nightly非阻塞，未运行如实记录。T00工具预检不能代替本票产品行为。

**步骤 3：独立判读、审查与对账**

由独立检查者核对每个run的GIVEN/WHEN/THEN与工具轨迹，逐项写judge；真实来源支持/动作先后/负例零写入/授权复用都须有证据。检查者不得读取被测模型的自报结论就直接判pass，也不能替模型补动作。保存实际模型配置，未知字段写未知。

运行以下聚合器，只在所有判读齐备时生成32项对账；它不生成judge，不是语义证明。

```bash
rtk proxy python3 - <<'PY'
import hashlib,json,subprocess,tarfile
from pathlib import Path
ROOT=Path.cwd().resolve();F=ROOT/".spec-dev/2026-09-09-01-exploring-clarifying";A=F/"acceptance"
registry=json.loads((F/"execution/cases.json").read_text())["cases"]
tracked=[x for x in subprocess.check_output(["rtk","proxy","git","-c","core.quotepath=false","ls-files"],text=True).splitlines() if not x.startswith(".spec-dev/")]
candidate={p:hashlib.sha256((ROOT/p).read_bytes()).hexdigest() if (ROOT/p).is_file() else None for p in tracked}
group={f"S{i:02}":[] for i in range(1,33)}
for name,case in registry.items():
    if name=="P00":continue
    if name=="S26":
        d=A/"cli26";j=json.loads((d/"judge.json").read_text());assert j["verdict"]=="pass" and j["case"]=="S26" and j["evidence"]
    else:
        d=A/"model/final"/name;f=json.loads((d/"facts.json").read_text());j=json.loads((d/"judge.json").read_text())
        assert f["transport_ok"] and f["lifecycle_ok"] and f["candidate_before"]==f["candidate_after"]==candidate
        assert j["case"]==name and j["verdict"]=="pass" and j["evidence"] and j["checks"]
        assert all(x["verdict"]=="pass" and x["evidence"] for x in j["checks"])
    for e in j["evidence"]:assert (d/e).resolve().is_relative_to(d.resolve()) and (d/e).exists()
    group[case["scenario"]].append({"case":name,"judge":str((d/"judge.json").relative_to(A)),"verdict":"pass"})
assert all(group.values())
rows=[{"scenario":k,"verdict":"pass","cases":v} for k,v in group.items()]
(A/"scenario-results.json").write_text(json.dumps(rows,ensure_ascii=False,indent=2)+"\n")
print(json.dumps({"scenarios":len(rows),"cases":sum(len(x) for x in group.values()),"status":"complete_mapping; independent judges remain the semantic authority"}))
PY
```


按现行 review-orchestration 对本计划完整diff执行A/B/C/S审查（本项常规改动；D仅有具体结构证据或用户要求时追加）。独立审查来源范围为T00保存的原始base..候选，发现修复后复审受影响维度并重跑失效场景。review-findings用现有validator，不改schema/公共runner。能力/凭据失败保留BLOCKED，不用无上下文不足的主线程声明替代独立覆盖。

将14个Requirement逐项映射实现票、Scenario证据和DELIVERED/偏差状态写 requirements-reconciliation.md；正常交付要求14 DELIVERED、32个必需Scenario通过。验收报告区分静态/模型判断/实际动作/宿主观察/原失败/未运行nightly，保留所有失败原件。

**步骤 4：进程和资源核验**

逐个检查model目录的process.json、facts.json与stdout最终result。父进程和后台子题都有完成回执才算闭合；存在未结束者先查真实进程身份，不按陈旧PID误杀，也不在仍有写者时做不可变性检查。所有research模式的独立judge补充background_tasks数组（实际工具ID、state=completed/failed/cancelled、evidence；确未派发的能力缺失分支用空数组）。P00/S14/S16/S17及两个入口变体必须非空。执行下列审计；PID已复用或不可见时保留unresolved，按真实身份补证后再完成，不能直接删错误：

```bash
rtk proxy python3 - <<'PY'
import json,os
from pathlib import Path
F=Path(".spec-dev/2026-09-09-01-exploring-clarifying");rows=[];unresolved=[]
for p in (F/"acceptance/model").glob("*/*/process.json"):
    proc=json.loads(p.read_text());facts_path=p.with_name("facts.json")
    if not facts_path.exists():unresolved.append(str(p)+": no final facts");continue
    facts=json.loads(facts_path.read_text());pid=proc["pid"]
    try:os.kill(pid,0);state="identity_check_required";unresolved.append(str(p)+": pid present; inspect identity without killing")
    except ProcessLookupError:state="ended"
    except PermissionError:state="identity_check_required";unresolved.append(str(p)+": visibility denied")
    rows.append({"process":str(p),"pid":pid,"state":state,"exit":facts["exit"],"transport_ok":facts["transport_ok"]})
registry=json.loads((F/"execution/cases.json").read_text())["cases"]
for case,data in registry.items():
    if data["mode"]!="research":continue
    approved=json.loads((F/"execution/serial/T00/preflight-approved.json").read_text())
    run=approved["run"] if case=="P00" else "final"
    jpath=F/"acceptance/model"/run/case/"judge.json";j=json.loads(jpath.read_text())
    assert j["verdict"]=="pass" and "background_tasks" in j,str(jpath)
    for task in j["background_tasks"]:
        assert task["id"] and task["state"] in ["completed","failed","cancelled"] and task["evidence"],str(jpath)
    if case in ["P00","S14","S16","S16-ex","S17","S17-ex"]:assert j["background_tasks"],"Required actual dispatch missing: "+case
report={"active":0 if not unresolved else None,"unresolved":unresolved,"processes":rows,"background_review":"research judges with actual tool IDs and completion evidence"}
(F/"acceptance/process-audit.json").write_text(json.dumps(report,ensure_ascii=False,indent=2)+"\n")
assert not unresolved,unresolved
PY
```


另行核验 T04 异常运行创建的两条 CLI 会话；不把宿主停止回执改写成原模型自然完成。此核验只查询精确归属 ID，保留客户端历史，不操作其他会话。查询命令、时间、成功状态和不可变快照由任务内工具保存：

```bash
rtk proxy python3 - <<'PY'
import json,subprocess
from pathlib import Path
F=Path('.spec-dev/2026-09-09-01-exploring-clarifying')
r=subprocess.run(['rtk','proxy','python3',str(F/'execution/audit-cli-sessions.py'),'acceptance/cli-resources.json'])
audit=json.loads((F/'acceptance/process-audit.json').read_text())
report=F/'acceptance/cli-resources.json'
if report.exists():
    cli=json.loads(report.read_text())
    audit['cli_sessions']=cli['records']
    audit['cli_session_evidence']='acceptance/cli-resources.json'
    audit['unresolved'] += cli['unresolved']
else:
    audit['unresolved'].append('CLI session audit did not produce evidence')
audit['active']=0 if not audit['unresolved'] and r.returncode==0 else None
(F/'acceptance/process-audit.json').write_text(json.dumps(audit,ensure_ascii=False,indent=2)+'\n')
assert r.returncode==0 and not audit['unresolved']
PY
```

为全部登记fixture生成归档核验记录（包括baseline和最终、失败原件），任何未归档/发生变化者停下补齐：

```bash
rtk proxy python3 - <<'PY'
import hashlib,json,subprocess,tarfile
from pathlib import Path
root=Path.cwd().resolve();F=root/".spec-dev/2026-09-09-01-exploring-clarifying";A=F/"acceptance"
s=json.loads((F/"plan/progress.yaml").read_text());records=[]
binding=[json.loads(n[len("binding:"):]) for n in s["notes"] if n.startswith("binding:")][-1]
allowed_fixture_roots=[root/".worktrees/ec-qa",Path(binding["source"])/".worktrees/ec7-qa"]
for resource in s["resources"]:
    if not resource.startswith("fixture: "):continue
    live=Path(resource[len("fixture: "):].split(" —— ")[0]);assert any(live.is_relative_to(scope) for scope in allowed_fixture_roots)
    assert live.exists() and not live.is_symlink() and not any(p.is_symlink() for p in live.rglob("*"))
    snapshots=[]
    for p in (A/"model").glob("*/*/facts.json"):
        f=json.loads(p.read_text())
        if f["fixture"]==str(live):snapshots.append((p.stat().st_mtime_ns,p.parent,f))
    assert snapshots,"Unarchived resource: "+str(live)
    _,out,f=max(snapshots,key=lambda x:x[0])
    files={str(p.relative_to(live)):hashlib.sha256(p.read_bytes()).hexdigest() for p in live.rglob("*") if p.is_file() and ".git" not in p.parts}
    assert files==f["fixture_after"]
    archive=out/"fixture.tar.gz";bundle=out/"fixture.bundle"
    with tarfile.open(archive) as tar:
        archived={m.name:hashlib.sha256(tar.extractfile(m).read()).hexdigest() for m in tar if m.isfile()}
    assert archived==files
    subprocess.run(["rtk","proxy","git","bundle","verify",str(bundle)],cwd=live,check=True)
    head=subprocess.check_output(["rtk","proxy","git","rev-parse","HEAD"],cwd=live,text=True).strip()
    records.append({"resource":resource,"path":str(live),"head":head,"files":files,"archive":str(archive.relative_to(F)),"archive_sha256":hashlib.sha256(archive.read_bytes()).hexdigest(),"bundle":str(bundle.relative_to(F)),"bundle_sha256":hashlib.sha256(bundle.read_bytes()).hexdigest()})
(A/"resources.json").write_text(json.dumps(records,ensure_ascii=False,indent=2)+"\n")
print(json.dumps({"archived_owned_fixtures":len(records)}))
PY
```


**步骤 5：保存真实验收结果与完成状态**

在 acceptance-report.md 记录候选SHA/规则哈希、32项与14Req对账、各命令exit及证据路径、多维审查处置、进程归档和未验证边界。全部必需项成立后才完成，否则保存blocked和实际缺口；修改用户THEN/豁免必需项必须回设计。

```bash
rtk git add .spec-dev/2026-09-09-01-exploring-clarifying/acceptance .spec-dev/2026-09-09-01-exploring-clarifying/execution/serial/T06 .spec-dev/2026-09-09-01-exploring-clarifying/spec/exploring-clarifying-design.md
rtk proxy env SKIP_RELEASE_HOOK=1 git commit -m 'test(T06): save independently verified acceptance'
```

```bash
rtk proxy python3 - <<'PY'
import subprocess
sha=subprocess.check_output(['rtk','proxy','git','rev-parse','HEAD'],text=True).strip()
subprocess.run(['rtk','proxy','python3','.spec-dev/2026-09-09-01-exploring-clarifying/execution/state.py','T06','completed','--commit',sha,'--tests','pass'],check=True)
PY
```
```bash
rtk git add .spec-dev/2026-09-09-01-exploring-clarifying/plan/progress.yaml
rtk proxy env SKIP_RELEASE_HOOK=1 git commit -m 'chore(T06): save verified task completion'
```
