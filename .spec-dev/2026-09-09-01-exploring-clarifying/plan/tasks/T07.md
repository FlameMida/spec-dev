### 任务 7：本地交付与资源清理

**文件**：
- 修改：.spec-dev/2026-09-09-01-exploring-clarifying/spec/exploring-clarifying-design.md、plan/progress.yaml、acceptance/acceptance-report.md；.spec-dev/roadmaps/2026-09-03-01-skill-ecosystem-absorption.md。
- 证据：.spec-dev/2026-09-09-01-exploring-clarifying/execution/serial/T07/。
- 删除资源仅限progress.resources中本计划拥有、已归档核验的fixture、实施worktree和分支。原隔离资源保持所有权。

**接口**：
- 消费：T00实际来源/资源归属；T06完整验收与审查；前序全部完成。
- 产出：真实本地合并/取代回写/资源处置/sync_commit；roadmap delivered；全部completed。

**步骤 1：入口、最终全库与失败归属**

恢复时先读progress.notes中的binding和execution/serial/T07/merge.json：已有真实合并回执且其SHA仍是来源HEAD祖先时，在来源工作区核对产品树后续清理/锚定；不要从已删除worktree重跑创建、全库或合并。下面步骤1—3用于尚未完成合并的路径；成功合并后的代码未变时复用已保存全库证据。已有检查目录使用新的LABEL记录需要补跑的检查。

只检查T00—T06完成，不要求T07也completed：
```bash
rtk proxy python3 - <<'PY'
import json
from pathlib import Path
s=json.loads(Path(".spec-dev/2026-09-09-01-exploring-clarifying/plan/progress.yaml").read_text())
assert all(s["tasks"][f"T{i:02}"]["status"]=="completed" for i in range(7))
assert s["tasks"]["T07"]["status"] in ["pending","in_progress","blocked"]
PY
rtk proxy python3 .spec-dev/2026-09-09-01-exploring-clarifying/execution/state.py T07 in_progress
rtk git add .spec-dev/2026-09-09-01-exploring-clarifying/plan/progress.yaml
rtk proxy env SKIP_RELEASE_HOOK=1 git commit -m 'chore(T07): start verified delivery'
rtk proxy python3 .spec-dev/2026-09-09-01-exploring-clarifying/execution/record.py T07 full -- rtk proxy node --test scripts/tests/*.test.mjs
rtk proxy python3 .spec-dev/2026-09-09-01-exploring-clarifying/execution/record.py T07 skills -- rtk proxy node scripts/validate-skills.mjs
rtk proxy python3 .spec-dev/2026-09-09-01-exploring-clarifying/execution/record.py T07 plugin -- rtk proxy node scripts/check-plugin.mjs --codex-validate
```

全部真实执行通过且0fail/skip才继续。相关范围内失败回归属任务修复；范围外失败在来源干净检出上复跑同一测试，来源也失败则报告既有失败交用户裁决，不静默忽略；来源通过则本次回归。来源dirty先厘清归属，不覆盖。保存blocked时用state.py并单独提交progress。前序修复后需重做受影响验收，不复用失效PASS。

**步骤 2：退役与取代核对**

```bash
rtk proxy rg -n 'test\(|Scenario:|Superseded|REMOVED' scripts/tests/plugin-root.test.mjs scripts/tests/search-clause.test.mjs scripts/tests/exploring-clarifying.test.mjs .spec-dev/2026-09-09-01-exploring-clarifying/spec/exploring-clarifying-design.md
```

本spec只部分取代major-upgrade的澄清核心纪律，其他条款保留。测试退役需现行Scenario失联且原Requirement已删除/取代；历史非规范命名保守豁免，不能因编号重复删测试。无候选记录无孤儿测试，有候选按已有授权边界处理。执行以下取代回写，仍保持旧spec active：

```bash
rtk proxy python3 - <<'PY'
import datetime,re
from pathlib import Path
p=Path('.spec-dev/2026-08-26-01-major-upgrade/spec/major-upgrade-design.md')
t=p.read_text();heading='### Requirement: 澄清核心纪律（新增第 0 条自我披露）'
assert t.count(heading)==1
marker='> **Superseded ('+datetime.date.today().isoformat()+')** — by .spec-dev/2026-09-09-01-exploring-clarifying/spec/exploring-clarifying-design.md#requirement-澄清核心纪律与可见清单 — 原文保留仅作历史参考。'
section=t.split(heading,1)[1].split('### Requirement:',1)[0]
assert '**Superseded (' not in section,'Already completed: verify recovery evidence'
t=t.replace(heading,heading+'\n\n'+marker,1)
t='\n'.join(x for x in t.splitlines() if not (x.startswith('> **Superseded-pending') and '.spec-dev/2026-09-09-01-exploring-clarifying/spec/exploring-clarifying-design.md' in x))+'\n'
p.write_text(t)
PY
```

```bash
rtk git add .spec-dev/2026-08-26-01-major-upgrade/spec/major-upgrade-design.md
rtk proxy env SKIP_RELEASE_HOOK=1 git commit -m 'docs(T07): apply approved partial supersession'
```


将核对结果写execution/serial/T07/closure-checks.md，指出最终必需矩阵已通过、nightly实际状态及资源归属。以下提交使实施检出干净：
```bash
rtk git add .spec-dev/2026-09-09-01-exploring-clarifying/execution/serial/T07
rtk proxy env SKIP_RELEASE_HOOK=1 git commit -m 'chore(T07): save final validation and closure checks'
rtk git status --short
```


**步骤 3：合并回实际来源**

以下脚本从binding读取来源，验证当前检出/分支/干净状态。来源HEAD不是实施HEAD祖先时，先在实施worktree集成来源变化并复验，不直接合并未经验证的新组合。冲突停止并保存实际阻塞事实，不force或自动解冲突。

```python
import json,subprocess
from pathlib import Path
root=Path.cwd().resolve();F=Path(".spec-dev/2026-09-09-01-exploring-clarifying")
s=json.loads((F/"plan/progress.yaml").read_text())
b=[json.loads(n[len("binding:"):]) for n in s["notes"] if n.startswith("binding:")][-1]
assert str(root)==b["worktree"]
source=Path(b["source"]).resolve()
def git(*args,cwd=root):
    return subprocess.check_output(["rtk","proxy","git",*args],cwd=cwd,text=True).strip()
assert git("branch","--show-current")==b["branch"]
assert git("branch","--show-current",cwd=source)==b["source_branch"]
assert not git("status","--porcelain=v1") and not git("status","--porcelain=v1",cwd=source)
candidate=git("rev-parse","HEAD")
if b["owned"]:
    subprocess.run(["rtk","proxy","git","merge-base","--is-ancestor",git("rev-parse","HEAD",cwd=source),candidate],cwd=root,check=True)
    # If source advanced outside candidate ancestry, integrate it in the owned worktree and revalidate first.
    subprocess.run(["rtk","proxy","env","SKIP_RELEASE_HOOK=1","git","merge","--no-ff",b["branch"],
                    "-m","Merge verified exploring clarifying"],cwd=source,check=True)
    merged=git("rev-parse","HEAD",cwd=source)
else:
    receipt=root/F/"execution/serial/T07/external-merge-receipt.json"
    assert receipt.exists(),"Retained isolation: wait for actual original-mechanism merge receipt; keep T07 in_progress"
    r=json.loads(receipt.read_text())
    assert r["source"]==str(source) and r["source_branch"]==b["source_branch"]
    merged=r["merge_commit"]
    subprocess.run(["rtk","proxy","git","merge-base","--is-ancestor",merged,"HEAD"],cwd=source,check=True)
product=[p for p in git("ls-files").splitlines() if not p.startswith(".spec-dev/")]
assert product
assert not git("diff","--name-only",candidate,merged,"--",*product,cwd=source),"Merged product differs: revalidate before cleanup"
out=source/F/"execution/serial/T07/merge.json";assert not out.exists()
out.parent.mkdir(parents=True,exist_ok=True)
out.write_text(json.dumps({"source":str(source),"source_branch":b["source_branch"],"candidate":candidate,
    "merge_commit":merged,"product_tree_verified":True,"owned_worktree":b["owned"]},ensure_ascii=False,indent=2)+"\n")
subprocess.run(["rtk","proxy","git","add",str(F/"execution/serial/T07/merge.json")],cwd=source,check=True)
subprocess.run(["rtk","proxy","env","SKIP_RELEASE_HOOK=1","git","commit","-m","chore(T07): record actual merge receipt"],cwd=source,check=True)
print("CONTINUE_FROM_SOURCE",source)
```


本计划默认本地通道，不push。复用隔离需要原机制的真实回执：external-merge-receipt.json只在收到真实合并证据后由主线程保存，字段source、source_branch、merge_commit均为实际值；等待时T07保持in_progress，不能以“移交”当合并。脚本核对目标祖先和产品树，回执不存在绝不伪造。

**步骤 4：在存活来源工作区逐项清理**

切换工具cwd到merge脚本输出的实际source，不能继续在将删除的实施worktree执行。先重新确认T06记录的模型进程均已结束，资源无未归档新变化。执行：

```python
import hashlib,json,os,shutil,subprocess,tarfile
from pathlib import Path
root=Path.cwd().resolve();F=Path(".spec-dev/2026-09-09-01-exploring-clarifying")
p=F/"plan/progress.yaml";s=json.loads(p.read_text())
b=[json.loads(n[len("binding:"):]) for n in s["notes"] if n.startswith("binding:")][-1]
assert str(root)==b["source"]
def git(*args,cwd=root):
    return subprocess.check_output(["rtk","proxy","git",*args],cwd=cwd,text=True).strip()
assert git("branch","--show-current")==b["source_branch"]
assert not git("status","--porcelain=v1")
receipt=json.loads((F/"execution/serial/T07/merge.json").read_text())
assert receipt["product_tree_verified"]
subprocess.run(["rtk","proxy","git","merge-base","--is-ancestor",receipt["merge_commit"],"HEAD"],check=True)
audit=json.loads((F/"acceptance/process-audit.json").read_text());assert audit["active"]==0 and not audit["unresolved"]
records=json.loads((F/"acceptance/resources.json").read_text())
by_resource={r["resource"]:r for r in records};assert len(by_resource)==len(records)
def save(message):
    tmp=p.with_suffix(".tmp");assert not tmp.exists()
    tmp.write_text(json.dumps(s,ensure_ascii=False,indent=2)+"\n");os.replace(tmp,p)
    subprocess.run(["rtk","proxy","git","add",str(p)],check=True)
    subprocess.run(["rtk","proxy","env","SKIP_RELEASE_HOOK=1","git","commit","-m",message],check=True)
try:
    cli_resources=[r for r in s['resources'] if r.startswith('cli-session: ')]
    if cli_resources:
        subprocess.run(['rtk','proxy','python3',str(F/'execution/audit-cli-sessions.py'),'execution/serial/T07/cli-recheck.json'],check=True)
        cli_path=F/'execution/serial/T07/cli-recheck.json'
        cli=json.loads(cli_path.read_text());assert not cli['unresolved']
        cli_by_id={r['short_id']:r for r in cli['records']}
        for evidence,expected_hash in cli['evidence_hashes'].items():
            assert hashlib.sha256((F/evidence).read_bytes()).hexdigest()==expected_hash
        snapshot=F/cli['immutable_snapshot'];assert snapshot.resolve().is_relative_to(F.resolve())
        subprocess.run(['rtk','proxy','git','add',str(cli_path),str(snapshot)],check=True)
        subprocess.run(['rtk','proxy','env','SKIP_RELEASE_HOOK=1','git','commit','-m','chore(T07): verify stopped owned CLI sessions'],check=True)
        for resource in cli_resources:
            sid=resource[len('cli-session: '):].split(' —— ')[0]
            r=cli_by_id[sid]
            assert r['state']=='stopped_by_host' and r['remaining_listed'] is False
            assert r['closure_origin']=='host_cleanup_after_interruption' and r['client_history']=='retained'
            s['resources'].remove(resource)
            s['notes'].append('T07: CLI active resource discharged after fresh query: '+r['full_id']+'; host stop, client history retained; evidence '+str(cli_path))
            save('chore(T07): discharge one stopped CLI resource')
    for resource in list(s["resources"]):
        if not resource.startswith("fixture: "):continue
        r=by_resource[resource];wt=Path(r["path"])
        assert any(wt.is_relative_to(scope) for scope in [Path(b["worktree"])/".worktrees/ec-qa",Path(b["source"])/".worktrees/ec7-qa"]) and not wt.is_symlink()
        archive=F/r["archive"];bundle=F/r["bundle"]
        assert hashlib.sha256(archive.read_bytes()).hexdigest()==r["archive_sha256"]
        assert hashlib.sha256(bundle.read_bytes()).hexdigest()==r["bundle_sha256"]
        if wt.exists():
            assert git("rev-parse","--show-toplevel",cwd=wt)==str(wt)
            assert git("rev-parse","HEAD",cwd=wt)==r["head"]
            assert not any(x.is_symlink() for x in wt.rglob("*"))
            live={str(x.relative_to(wt)):hashlib.sha256(x.read_bytes()).hexdigest()
                  for x in wt.rglob("*") if x.is_file() and ".git" not in x.parts}
            assert live==r["files"],"Fixture changed after archived audit"
            shutil.rmtree(wt)
        assert not wt.exists()
        s["resources"].remove(resource);s["notes"].append("T07: archived fixture removed or verified absent: "+str(wt))
        save("chore(T07): record one archived fixture cleanup")
    for resource in list(s["resources"]):
        if not resource.startswith("fixture-scope: "):continue
        scope=Path(resource[len("fixture-scope: "):].split(" —— ")[0])
        assert scope in [Path(b["worktree"])/".worktrees/ec-qa",Path(b["source"])/".worktrees/ec7-qa"] and not scope.is_symlink()
        assert not any(x.startswith("fixture: ") for x in s["resources"])
        if scope.exists():
            assert not any(p.is_file() or p.is_symlink() for p in scope.rglob("*")),"Remaining files outside archived ledger"
            for empty_dir in sorted(scope.rglob("*"),key=lambda x:len(x.parts),reverse=True):empty_dir.rmdir()
            scope.rmdir()
        s["resources"].remove(resource);s["notes"].append("T07: empty owned fixture scope removed or verified absent")
        save("chore(T07): record empty fixture scope cleanup")
    if b["owned"]:
        wt=Path(b["worktree"])
        resource=next((x for x in s["resources"] if x.startswith("worktree: "+str(wt)+" —— ")),None)
        if resource is None:
            assert not wt.exists(),"Worktree exists outside remaining ledger; do not delete"
            assert subprocess.run(["rtk","proxy","git","show-ref","--verify","--quiet","refs/heads/"+b["branch"]]).returncode!=0
        if wt.exists():
            assert git("rev-parse","--show-toplevel",cwd=wt)==str(wt)
            assert git("branch","--show-current",cwd=wt)==b["branch"]
            assert not git("status","--porcelain=v1",cwd=wt)
            subprocess.run(["rtk","proxy","git","merge-base","--is-ancestor",b["branch"],"HEAD"],check=True)
            subprocess.run(["rtk","proxy","git","worktree","remove",str(wt)],check=True)
        exists=subprocess.run(["rtk","proxy","git","show-ref","--verify","--quiet","refs/heads/"+b["branch"]]).returncode==0
        if exists:
            subprocess.run(["rtk","proxy","git","merge-base","--is-ancestor",b["branch"],"HEAD"],check=True)
            subprocess.run(["rtk","proxy","git","branch","-d",b["branch"]],check=True)
        assert not wt.exists()
        if resource is not None:
            s["resources"].remove(resource);s["notes"].append("T07: owned implementation worktree and branch cleaned")
            save("chore(T07): record implementation cleanup")
    else:
        s["notes"].append("T07: original isolation retained after verified external merge; deletion belongs to original mechanism")
        save("chore(T07): retain original isolation ownership")
    assert all(x.startswith("retained-worktree: ") for x in s["resources"]),s["resources"]
except Exception as e:
    s["tasks"]["T07"]["status"]="blocked";s["current"]=None
    s["notes"].append("T07 cleanup blocked; retain uncleared ledger: "+type(e).__name__+": "+str(e))
    save("chore(T07): preserve cleanup failure checkpoint")
    raise
```


每项删除前核对快照/文件哈希/独立Git HEAD，每项完成单独保存progress并提交。worktree和branch分别成功后才销账；失败保留未清部分并blocked。CLI 会话仅依据宿主停止及最新查询作非删除式销账，不删除客户端共享日志、会话历史、缓存或其他会话，也不把原模型中断改记为自然闭合。只读保留/移交的原隔离资源不删除。台账外目录、共享缓存、用户工作不处理。

**步骤 5：锚定真实合并与最终完成**

确认原模型失败/未运行nightly已在报告诚实列明，不把交付状态写成首次自主成功。以下脚本在来源工作区更新真实merge SHA、roadmap和交付说明，再单独保存T07完成状态：

```python
import json,re,subprocess
from pathlib import Path
F=Path(".spec-dev/2026-09-09-01-exploring-clarifying")
root=Path.cwd().resolve();s=json.loads((F/"plan/progress.yaml").read_text())
b=[json.loads(n[len("binding:"):]) for n in s["notes"] if n.startswith("binding:")][-1]
assert str(root)==b["source"] and all(x.startswith("retained-worktree: ") for x in s["resources"])
receipt=json.loads((F/"execution/serial/T07/merge.json").read_text());sha=receipt["merge_commit"]
subprocess.run(["rtk","proxy","git","merge-base","--is-ancestor",sha,"HEAD"],check=True)
spec=F/"spec/exploring-clarifying-design.md";text=spec.read_text()
assert text.startswith("---\n") and "\n  status: active\n" in text
fm,body=text.split("---",2)[1:]
assert re.search(r"^  sync_commit: .*$",fm,re.M)
fm=re.sub(r"^  sync_commit: .*$","  sync_commit: "+sha,fm,flags=re.M)
lines=body.splitlines()
for i,line in enumerate(lines):
    if line.startswith("> Roadmap"):
        lines[i]="> Roadmap skill-ecosystem-absorption #7，AB-20/21/22/26/23/25/31/27；用户已批准设计与实施流程，必需验收和本地交付已完成，证据见本特性acceptance及plan/progress.yaml。"
    elif line.startswith("> 独立审查"):
        lines[i]="> 独立设计审查 Approved，14 条 Requirement / 32 个 Scenario；实施与验收证据见 ../acceptance/acceptance-report.md，真实进度见 ../plan/progress.yaml。"
spec.write_text("---"+fm+"---"+"\n".join(lines)+"\n")
road=Path(".spec-dev/roadmaps/2026-09-03-01-skill-ecosystem-absorption.md")
r=road.read_text();a,tail=r.split("### #7 exploring-clarifying",1);capsule,beyond=tail.split("### #8 p2-watchlist",1)
capsule+="\n- **本地交付结论**：T00—T07完成，14Req/32Scenario必需验收与全库验证通过；实际合并 "+sha+"。资源/来源见execution/serial/T07/merge.json及progress.notes。原模型失败及nightly状态保留，未push/发布。下一项#8按原留观清单评估，不自动启动全部P2。\n\n"
r=a+"### #7 exploring-clarifying"+capsule+"### #8 p2-watchlist"+beyond
r="\n".join(line.replace("| in-progress |","| delivered |") if line.startswith("| 7 |") else line for line in r.splitlines())+"\n"
road.write_text(r)
report=F/"acceptance/acceptance-report.md"
with report.open("a") as out:out.write("\n## 本地交付\n\n实际合并 "+sha+"；源工作区 "+str(root)+"。资源处置见progress.notes；未push或发布。本结论不改写历史模型失败和未运行nightly。\n")
subprocess.run(["rtk","proxy","git","add",str(spec),str(road),str(report)],check=True)
subprocess.run(["rtk","proxy","env","SKIP_RELEASE_HOOK=1","git","commit","-m","chore(T07): anchor verified local delivery"],check=True)
subprocess.run(["rtk","proxy","python3",str(F/"execution/state.py"),"T07","completed","--commit",sha,"--tests","pass"],check=True)
subprocess.run(["rtk","proxy","git","add",str(F/"plan/progress.yaml")],check=True)
subprocess.run(["rtk","proxy","env","SKIP_RELEASE_HOOK=1","git","commit","-m","chore(T07): save final completion"],check=True)
```


```bash
rtk git status --short
rtk git log -3 --oneline
rtk proxy git diff --check
```


预期工作区干净、spec sync_commit指向真实且已验证的目标提交、T00—T07全部completed/current=null、resources仅允许明确移交保留的条目。若任何合并/清理/锚定未完成，保留实际等待或blocked，不执行完成脚本。
