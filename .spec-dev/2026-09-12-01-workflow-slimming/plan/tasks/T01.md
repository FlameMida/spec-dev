### 任务 1：证据留存规则与历史夹具收口

**文件**：
- 修改：`.gitignore`（末尾追加）
- 修改：`skills/executing-plans/references/integration-groups.md:51`
- 修改：`skills/acceptance-qa/SKILL.md:150`（「输出约定」段）
- 索引移除（磁盘保留）：所有已跟踪且命中新规则的 `.spec-dev/**/execution/**`、`.spec-dev/**/acceptance/**`（`acceptance-report.md` 除外）
- 来源工作区（`git -C "$SRC"`，`$SRC` 取 T00 notes 的来源工作区绝对路径）：提交 6423 个未提交删除，不恢复任何文件
- 测试：`scripts/tests/workflow-slimming-T01.test.mjs`（新建）

**接口**：
- 消费：T00 notes 的来源工作区绝对路径
- 产出：`.gitignore` 三条规则；T12 步骤 8 的历史清理沿用同一路径集合

**步骤 1：写失败测试**

```js
import {test} from 'node:test';
import assert from 'node:assert/strict';
import {execFileSync} from 'node:child_process';
import path from 'node:path';
import {fileURLToPath} from 'node:url';
const root=path.resolve(path.dirname(fileURLToPath(import.meta.url)),'../..');
const ignored=p=>{try{execFileSync('git',['check-ignore','-q','--no-index',p],{cwd:root,stdio:'ignore'});return true}catch{return false}};
test("S7.1 证据与验收产物被忽略",()=>{
  assert.equal(ignored('.spec-dev/2099-01-01-01-x/execution/serial/T01/green/facts.json'),true);
  assert.equal(ignored('.spec-dev/2099-01-01-01-x/acceptance/model/smoke-1/S01/turn-1/events.jsonl'),true);
  assert.equal(ignored('.spec-dev/2099-01-01-01-x/acceptance/review-A.json'),true);
});
test("S7.2 报告、spec 与 plan 仍被跟踪",()=>{
  assert.equal(ignored('.spec-dev/2099-01-01-01-x/acceptance/acceptance-report.md'),false);
  assert.equal(ignored('.spec-dev/2099-01-01-01-x/spec/x-design.md'),false);
  assert.equal(ignored('.spec-dev/2099-01-01-01-x/plan/progress.yaml'),false);
});
```

**步骤 2：运行测试确认失败**

运行：`rtk proxy node --test scripts/tests/workflow-slimming-T01.test.mjs`
预期：S7.1 FAIL 为 AssertionError：`false !== true`（规则尚不存在）；S7.2 PASS。

**步骤 3：写最小实现**

`.gitignore` 末尾追加：
```
# spec-dev 执行证据与验收产物留在本地，只有验收报告进 git
.spec-dev/**/execution/
.spec-dev/**/acceptance/*
!.spec-dev/**/acceptance/acceptance-report.md
```

`integration-groups.md:51` 句尾追加：
> execution/ 整目录不进 git（仓库 `.gitignore`）；record.json 与日志留在本地工作区，evidence_paths 指向本地文件，跨机器复核以 acceptance-report.md 的摘要为准。

`skills/acceptance-qa/SKILL.md:150`「输出约定」段末追加：
> acceptance/ 下除 acceptance-report.md 外不进 git（仓库 `.gitignore` 规则）；JSON 回执、日志与 model/ 夹具留在本地。

**步骤 4：运行测试确认通过，移除索引，收口来源工作区**

运行：`rtk proxy node --test scripts/tests/workflow-slimming-T01.test.mjs`
预期：2 PASS。

在 worktree 内把已跟踪的证据文件移出索引（磁盘保留）并核对 S7.3：
```bash
git ls-files -ci --exclude-standard -z | xargs -0 git rm --cached -q --
git ls-files | grep -E '\.spec-dev/[^/]+/(execution/|acceptance/)' | grep -vc 'acceptance/acceptance-report\.md$'   # 预期 0
```

来源工作区收口（提交全部 6423 个删除，不恢复）：
```bash
A=.spec-dev/2026-09-11-01-skill-instruction-streamlining/acceptance
git -C "$SRC" add -A -- "$A"
git -C "$SRC" status --short | grep -vc '^D '     # 预期 0：只剩已暂存的删除
SKIP_RELEASE_HOOK=1 git -C "$SRC" commit -m "chore(evidence): drop deleted acceptance artifacts (6423 files)" -m "Spec-Guard: off workflow-slimming (facet coexistence)"
git -C "$SRC" status --short | wc -l                # 预期 0
```

**步骤 5：提交**

```bash
git add .gitignore skills/executing-plans/references/integration-groups.md skills/acceptance-qa/SKILL.md scripts/tests/workflow-slimming-T01.test.mjs
SKIP_RELEASE_HOOK=1 SKIP_OPENAI_SYNC_CHECK=1 git commit -m "feat(T01): evidence stays local; only acceptance reports are tracked" -m "openai.yaml 无需同步：仅改输出约定" -m "Spec-Guard: off workflow-slimming (facet coexistence)"
```
（步骤 4 的 `git rm --cached` 已进入暂存集，随本提交一起提交。）
