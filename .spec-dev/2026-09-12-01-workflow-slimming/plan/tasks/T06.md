### 任务 6：受控运行器对齐

**文件**：
- 修改：`scripts/lib/review_store.py:50,72-73,281-283,423`
- 修改：`scripts/lib/review_process.py:20,41-42`
- 修改：`scripts/lib/review_broker.py:131`
- 测试：`scripts/tests/controlled-review.test.mjs`（改既有 actor 名与 critic 期望，追加四例）

**接口**：
- 消费：T05 定义的 `critic` 取值与 `evidence` 元素字段
- 产出：`initialize` 接受 `critic`/`evidence`；roles `regular=['AS','BC']`；`tasks()` 仅在有候选或 `critic=='always'` 时生成 critic-1；`context()` 返回 `execution_evidence`

**步骤 1：写失败测试**

在 `controlled-review.test.mjs` 末尾追加：
```js
test('S3.1 常规档派发两路 AS 与 BC',t=>{
 const f=fixture(t);const s=invoke(['status','--run',f.run]);assert.deepEqual(s.data.tasks.map(x=>x.actor),['AS','BC']);
});
test('S3.2 零发现时无反驳无critic即可完成',t=>{
 const folder=mkdtempSync(path.join(tmpdir(),'review-client-'));t.after(()=>rmSync(folder,{recursive:true,force:true}));
 const f=fixture(t,{client:fakeClient(folder)});const r=invoke(['run','--run',f.run,'--budget-seconds','20']);
 assert.equal(r.data.status,'completed',JSON.stringify(r));assert.ok(!r.data.tasks.some(x=>/^(refute|critic)-/.test(x.actor)));
});
test('S3.3 tests 为空时以宿主 evidence 为证据',async t=>{
 const evidence=[{task:'T01',phase:'green',command:['node','--test','x.test.mjs'],exit_code:0,stdout_sha256:'a'.repeat(64),stderr_sha256:'b'.repeat(64)}];
 const f=fixture(t,{tests:[],evidence});const b=await broker(t,f.run,'AS');
 const c=await b.call('context');assert.equal(c.error,false);assert.deepEqual(c.data.execution_evidence,evidence);
 const r=await b.call('submit_report',cleanReport());assert.equal(r.error,false,JSON.stringify(r));
});
test('S3.5 critic=always 零发现仍派critic',t=>{
 const folder=mkdtempSync(path.join(tmpdir(),'review-client-'));t.after(()=>rmSync(folder,{recursive:true,force:true}));
 const f=fixture(t,{client:fakeClient(folder),critic:'always'});const r=invoke(['run','--run',f.run,'--budget-seconds','20']);
 assert.equal(r.data.status,'completed',JSON.stringify(r));assert.ok(r.data.tasks.some(x=>x.actor==='critic-1'));
});
```
既有用例同步改动（regular 档角色由 A/B/C/S 变为 AS/BC，零发现不再派 critic）：
- 全部 `broker(t,f.run,'A')`→`'AS'`；`broker(t,f.run,'B')`→`'BC'`；断言中 `actor==='A'`/`'B'`→`'AS'`/`'BC'`；路径 `actors/A/`→`actors/AS/`。
- R01：`run_test` 的 `actor:'B'` 越权参数→`'BC'`。
- R04「零发现不放行缺少角色和critic的运行」→ 改名「零发现不放行缺少角色的运行」，断言 gaps 含 `'AS'` 与 `'BC'`，不含 `'critic'`。
- fakeClient：`actor=='B'`→`'BC'`（candidate/low）；`actor in ['A','S']`→`['AS','BC']`（merge/cross-merge）；`mode=='dynamic-d' and actor=='C'`→`'BC'`；critic-1 的 gaps `{'actor':'S',…}`→`'AS'`。
- 需要 critic 但零发现的用例在 fixture extra 加 `critic:'always'`：`late`、`supplement`、`last-candidate`、`late-d`、`last-d`、`partial-context`、`page-resume`、`skip-supplement-context`、「实际worker派发保留覆盖契约」（其循环 actors 改为 `['AS','BC','critic-1']`）。
- 「有界补查」`dynamic-d` 的期望 actors 改为 `['D']`；`supplement` 的期望 `'supplement-S'` 改为 `'supplement-AS'`；「补查者必须实际读取」断言中的 `supplement-S` 同改 `supplement-AS`。
- 「初审维度不读取其他维度结论」：`test_receipts.every(x=>x.actor==='BC')`。

**步骤 2：运行测试确认失败**

运行：`rtk proxy node --test scripts/tests/controlled-review.test.mjs`
预期：S3.1 FAIL（deepEqual `['A','B','C','S']` ≠ `['AS','BC']`）、S3.2 FAIL（tasks 含 critic-1）、S3.3 FAIL（init 报「测试定义为空或ID重复」）；S3.5 在旧代码上 PASS——它守住的是保留路径（critic 常驻），不作为本票的红；改名后的既有用例因 `AS`/`BC` 未获派发 FAIL。

**步骤 3：写最小实现**

review_store.py：
- `:50` `roles = {'small': ['AS'], 'regular': ['AS', 'BC'], 'large': [...]}`（large 不变）。
- `:72-73` 两行替换为：
```python
    evidence = config.get('evidence', [])
    keys = {'task', 'phase', 'command', 'exit_code', 'stdout_sha256', 'stderr_sha256'}
    if not isinstance(evidence, list) or any(not isinstance(x, dict) or set(x) != keys for x in evidence):
        raise ValueError('evidence必须为execution回执数组')
    if not config['tests'] and not evidence:
        raise ValueError('tests为空时必须提供evidence')
    if len({x['id'] for x in config['tests']}) != len(config['tests']):
        raise ValueError('测试ID重复')
    critic = config.get('critic', 'always' if tier == 'large' else 'on-findings')
    if critic not in ['on-findings', 'always']:
        raise ValueError('critic必须为on-findings或always')
```
  并在 `data = {**config, …` 字典中加入 `'critic': critic, 'evidence': evidence`。
- `:282` `todo.append({'actor': 'critic-1', …})` 与 `:283` `if completion(run, 'critic-1'):` 改为：
```python
    critic_needed = bool(first) or manifest(run)['critic'] == 'always'
    if critic_needed: todo.append({'actor': 'critic-1', 'depends': dims + (['refute-1'] if first else [])})
    if critic_needed and completion(run, 'critic-1'):
```
- `:423` `final_critic = …` 与其后 `if completion(run, final_critic):` 块改为：
```python
        critic_tasks = [t['actor'] for t in all_tasks if t['actor'].startswith('critic-')]
        final_critic = ('critic-2' if 'critic-2' in critic_tasks else 'critic-1') if critic_tasks else None
        if final_critic and completion(run, final_critic):
```
  （块内其余行缩进不变）。

review_process.py：
- `:20` `dimensions = {'A', 'B', 'C', 'S'} if role == 'AS' else {'B', 'C'} if role == 'BC' else {role.split('-')[0]}`。
- `:41` A 描述「必须亲自run_test取得所有相关测试回执。」→「tests非空时必须亲自run_test取得回执；tests为空时以context.execution_evidence为测试证据，不得编造回执。」；`:42` AS 描述中「必须亲自run_test，」同样替换为「tests非空时亲自run_test、为空时用execution_evidence，」。
- 描述表新增：`'BC': '代码质量与项目规范合并一路：可维护性、简洁性、DRY，以及适用项目约定与已有工具；必须引用真实规则。'`。

review_broker.py `:131` 在 `'reports': reports,` 之前插入 `'execution_evidence': data.get('evidence', []),`。

**步骤 4：运行测试确认通过**

运行：`rtk proxy node --test scripts/tests/controlled-review.test.mjs scripts/tests/review-findings.test.mjs`
预期：全部 PASS。

**步骤 5：提交**

```bash
git add scripts/lib/review_store.py scripts/lib/review_process.py scripts/lib/review_broker.py scripts/tests/controlled-review.test.mjs
SKIP_RELEASE_HOOK=1 git commit -m "feat(T06): runner tiers 1/2/5, host evidence, conditional critic" -m "Spec-Guard: off workflow-slimming (facet coexistence)"
```
