### 任务 7：本地三时点测试

**文件**：
- 修改：`skills/writing-plans/references/task-templates.md`（T00 步骤 3 段）
- 修改：`skills/writing-plans/references/plan-format.md`（「## 相关测试范围」段）
- 修改：`skills/using-git-worktrees/SKILL.md:99-101,125`
- 修改：`skills/executing-plans/SKILL.md:85`（阶段 3 第 1 项）
- 测试：`scripts/tests/workflow-slimming-T07.test.mjs`（新建）

**接口**：
- 消费：无
- 产出：plan-format「相关测试范围」推导规则第 2 条：自有测试 + 直接 import/require 被改源文件的既有测试；三时点字面量「任务内 / 最终任务 / 手动」供 T08 引用

**步骤 1：写失败测试**

```js
import {test} from 'node:test';
import assert from 'node:assert/strict';
import {readFileSync} from 'node:fs';
import path from 'node:path';
import {fileURLToPath} from 'node:url';
const root=path.resolve(path.dirname(fileURLToPath(import.meta.url)),'../..');
const raw=f=>readFileSync(path.join(root,f),'utf8');
test("S4.1 T00 声明失效不回退全量",()=>{
  const s=raw('skills/writing-plans/references/task-templates.md');
  assert.match(s,/不回退完整测试套件/);assert.doesNotMatch(s,/回退运行完整测试套件/);
  const w=raw('skills/using-git-worktrees/SKILL.md');
  assert.match(w,/不回退全量/);assert.doesNotMatch(w,/回退完整\n?测试套件/);
});
test("S4.2 相关测试范围按自有测试与直接导入方推导",()=>{
  const s=raw('skills/writing-plans/references/plan-format.md');
  assert.match(s,/直接 import\/require/);assert.doesNotMatch(s,/路径判定，不做依赖分析/);assert.match(s,/回归不在任务内跑/);
});
test("S4.3 任务内不跑回归",()=>{
  assert.match(raw('skills/executing-plans/SKILL.md'),/不在任务内跑回归或完整套件/);
});
```

**步骤 2：运行测试确认失败**

运行：`rtk proxy node --test scripts/tests/workflow-slimming-T07.test.mjs`
预期：三例 FAIL 为 AssertionError。

**步骤 3：写最小实现**

task-templates.md T00 步骤 3 中「然后按计划头部「相关测试范围」运行基线验证：…行为与现状一致。」整段 →
```
然后按计划头部「相关测试范围」运行基线验证：有声明 → 只跑声明范围（声明为空 → 跳过测试并注明；
声明命令执行报错或工具不可用 → 改跑本特性自有测试文件——声明节列出的新增/修改测试，尚不存在则跳过——
并注明声明已失效、建议修订计划，**不回退完整测试套件**：完整套件只在最终任务跑一次）；
计划无该节（旧版计划）→ 跑该计划任务文件中出现的测试文件，同样不跑完整套件。
```

plan-format.md「## 相关测试范围」段：
- 第 2 条「无工具 → 按 spec `covers` 与影响面推导测试文件/目录清单（路径判定，不做依赖分析）。」→「无工具 → 范围 = 本特性将新增/修改的测试文件 + 直接 import/require 被改源文件的既有测试（grep 导入语句判定，一层即止，不做传递闭包）；不按目录 glob 圈入无关测试。」
- 段末「本声明约束任务 0 基线验证；最终任务的全量验证不受本节约束（全量安全网）。」→「本声明约束任务 0 基线验证与各任务步骤 4（只跑本任务目标测试与本范围内的自有测试，回归不在任务内跑）；最终任务的全量验证不受本节约束（全量安全网，每特性一次）。」

using-git-worktrees SKILL.md：
- `:100-101`「声明命令报错 → 回退完整\n测试套件，并注明声明已失效、建议修订计划）」→「声明命令报错 → 改跑该计划列出的自有测试文件，注明声明已失效、建议修订计划，不回退全量）」。
- `:101`「无计划或计划无该节 → 运行项目完整测试命令」→「无计划 → 运行项目完整测试命令；计划无该节 → 跑其任务文件中出现的测试文件」。
- `:125` 表行「基线只跑声明范围（空声明→跳过并注明）」→「基线只跑声明范围（空→跳过并注明；声明失效→自有测试，不回退全量）」。

executing-plans SKILL.md `:85` 第 1 项句尾追加「；测试只跑本任务目标测试与计划「相关测试范围」内的自有测试，不在任务内跑回归或完整套件（最终任务一次全量）」。

**步骤 4：运行测试确认通过**

运行：`rtk proxy node --test scripts/tests/workflow-slimming-T07.test.mjs scripts/tests/skill-streamlining-T03.test.mjs`
预期：全部 PASS（T03 的 `/零测试|SKIP/`、`/已就绪不强制重装/` 断言所在行未改）。

**步骤 5：提交**

```bash
git add skills/writing-plans skills/using-git-worktrees skills/executing-plans scripts/tests/workflow-slimming-T07.test.mjs
SKIP_RELEASE_HOOK=1 SKIP_OPENAI_SYNC_CHECK=1 git commit -m "feat(T07): tests run at three local points; no full-suite fallback per task" -m "openai.yaml 无需同步：触发描述未变" -m "Spec-Guard: off workflow-slimming (facet coexistence)"
```
