### 任务 5：收尾审查编排文本

**文件**：
- 修改：`skills/executing-plans/references/review-orchestration.md:9,17,35,41-48,64,83`
- 修改：`skills/executing-plans/SKILL.md:30,109-112`
- 修改：`skills/executing-plans/evals/evals.json:17,162,166,167`
- 修改：`skills/executing-plans/agents/openai.yaml:3`
- 修改：`agents/code-reviewer.md:77`
- 修改：`README.zh-CN.md:15,197`、`README.md:15,197`
- 测试：`scripts/tests/workflow-slimming-T05.test.mjs`（新建）

**接口**：
- 消费：无
- 产出：review-orchestration「维度与路数」表：小 diff 一路 AS / 常规两路 AS + BC / 大变更五路；config 键 `critic`（`on-findings` 默认，large 默认 `always`）与 `evidence`（元素 `{task,phase,command,exit_code,stdout_sha256,stderr_sha256}`）。T06 按此实现

**步骤 1：写失败测试**

```js
import {test} from 'node:test';
import assert from 'node:assert/strict';
import {readFileSync} from 'node:fs';
import path from 'node:path';
import {fileURLToPath} from 'node:url';
const root=path.resolve(path.dirname(fileURLToPath(import.meta.url)),'../..');
const raw=f=>readFileSync(path.join(root,f),'utf8');
test("S3.6 路数表为 1/2/5 且读回执不复跑",()=>{
  const s=raw('skills/executing-plans/references/review-orchestration.md');
  assert.match(s,/\| 常规 \| 两路 AS \+ BC/);assert.doesNotMatch(s,/四路 A\/B\/C\/S/);
  assert.match(s,/facts\.json/);assert.doesNotMatch(s,/独立复跑相关测试，不采信自报告/);
  assert.match(s,/零候选时不派 critic/);assert.match(s,/`critic`/);assert.match(s,/`evidence`/);
});
test("S3.6 执行入口与 reviewer 同步",()=>{
  assert.match(raw('skills/executing-plans/SKILL.md'),/有候选才反驳与 critic/);
  const r=raw('agents/code-reviewer.md');assert.match(r,/facts\.json/);assert.doesNotMatch(r,/用 Bash 独立重跑本次变更涉及的测试，不采信实施者的自报告/);
});
test("S3.6 README 与 evals 同步",()=>{
  assert.doesNotMatch(raw('README.zh-CN.md'),/loop-until-dry|fan-out code-reviewer/);assert.doesNotMatch(raw('README.md'),/loop-until-dry|fans out code-reviewer/);
  assert.doesNotMatch(raw('skills/executing-plans/evals/evals.json'),/四路/);
});
```

**步骤 2：运行测试确认失败**

运行：`rtk proxy node --test scripts/tests/workflow-slimming-T05.test.mjs`
预期：三例 FAIL 为 AssertionError。

**步骤 3：写最小实现**

review-orchestration.md：
- `:9` 在「`tests`（如 …）」之后追加「、`critic`（`on-findings` 默认 / `always`；large 档默认 `always`）、`evidence`（可选，execution 回执数组，元素 `{task,phase,command,exit_code,stdout_sha256,stderr_sha256}`；提供时 `tests` 可为空数组）」。
- `:17`「A仍须亲自请求测试」→「tests 非空时 A/AS 仍须亲自请求测试；tests 为空且提供 evidence 时以该回执为证据」。
- `:35`「独立复跑相关测试，不采信自报告」→「以特性目录 execution/ 下 facts.json 记录的命令、退出码、stdout/stderr sha256 为测试证据；回执缺失、退出码非零或未覆盖本次变更文件时才复跑，并说明复跑原因」。
- `:41-46` 表体 → `| 小 diff（<100 行） | 一路 AS：A 与 S 合并，兼查 B/C 显著问题 |`、`| 常规 | 两路 AS + BC（B 与 C 合并） |`、`| 大变更或用户要求彻底/全面/审计 | 五路 A/B质量/B简洁性/C/S |`、`| D 已触发 | 在相应安排外追加一路 D |`。
- `:48`「超过该阈值至少保留常规四路」→「超过该阈值至少保留常规两路」。
- `:64`「无 fresh 则结束；否决候选也记 seen，避免反复出现」之后加一行「无高/中候选（首轮即空）→ 不派反驳、不派 critic，直接收口」。
- `:83` 段首「维度审查之后实际派发一个独立 critic，并收取其覆盖核查回执；」→「存在高/中候选、大变更档或用户要求彻底时，维度审查之后派发一个独立 critic 并收取回执；小 diff 与常规档零候选时不派 critic，由 AS 路逐条 Scenario 的 coverage_note 充当覆盖声明；派发时」。

executing-plans SKILL.md：
- `:30` Checklist 第 4 项「多维 fan-out + 对抗复核 + completeness critic」→「按规模 1/2/5 路审查，读执行回执不复跑；有候选才反驳与 critic」。
- `:112` critic 要点「一个子代理检查…」句首加「仅在存在高/中候选、大变更档或用户要求彻底时派发；零候选的小/常规档由 AS 路 coverage_note 承担覆盖声明。」。

`agents/code-reviewer.md:77`「用 Bash 独立重跑本次变更涉及的测试，不采信实施者的自报告——"声称通过但无法复现"按高严重性问题上报」→「先读特性目录 execution/ 下的 facts.json（命令、退出码、输出哈希）；回执覆盖本次变更文件且退出码为 0 即采信；缺回执、退出码非零或未覆盖变更文件时才用 Bash 复跑并说明原因——"声称通过但无回执"按高严重性问题上报」。

evals.json：`:17`「按常规四路 A/B/C/S 审查」→「按常规两路 AS/BC 审查」；`:162`「前者四路 A/B/C/S」→「前者两路 AS/BC」；`:166`「已选定四路」→「已选定五路」；`:167`「分批完成四路」→「分批完成五路」。

`openai.yaml:3` 句尾追加「；审查读执行回执不复跑，零候选不派 critic」。

README：
- zh:15「收尾多维对抗审查（fan-out code-reviewer + 契约校验 + loop-until-dry + completeness critic）」→「收尾按规模 1/2/5 路审查（读执行回执不复跑测试；有高/中候选才反驳与 critic）」。
- zh:197「全部完成后 fan-out code-reviewer 多维对抗审查（review-findings 契约校验 + 高/中发现对抗复核 + completeness critic）」→「全部完成后按规模派 1/2/5 路 code-reviewer 审查（review-findings 契约校验；读 execution/ 回执不复跑；有高/中候选才对抗复核与 completeness critic）」。
- en:15「wrap-up multi-dimension adversarial review (fan-out code-reviewer + contract validation + loop-until-dry + completeness critic)」→「a wrap-up review sized 1/2/5 routes by diff (reads execution receipts instead of re-running tests; rebuttal and critic only when high/medium candidates exist)」。
- en:197「when all tasks complete it fans out code-reviewer for multi-dimension adversarial review (review-findings contract validation + adversarial recheck of high/medium findings + completeness critic)」→「when all tasks complete it dispatches 1/2/5 code-reviewer routes by diff size (review-findings contract validation; reads execution/ receipts instead of re-running tests; adversarial recheck and completeness critic only when high/medium candidates exist)」。

**步骤 4：运行测试确认通过**

运行：`rtk proxy node --test scripts/tests/workflow-slimming-T05.test.mjs scripts/tests/skill-streamlining-T08.test.mjs`
预期：全部 PASS。

**步骤 5：提交**

```bash
git add skills/executing-plans agents/code-reviewer.md README.md README.zh-CN.md scripts/tests/workflow-slimming-T05.test.mjs
SKIP_RELEASE_HOOK=1 git commit -m "feat(T05): review routes 1/2/5, evidence-first, conditional critic" -m "Spec-Guard: off workflow-slimming (facet coexistence)"
```
