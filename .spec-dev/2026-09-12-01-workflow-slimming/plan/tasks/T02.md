### 任务 2：writing-plans 计划体量规则

**文件**：
- 修改：`skills/writing-plans/SKILL.md:4,17,38,104-136`
- 修改：`skills/writing-plans/references/task-templates.md`（「## 任务结构」标题下）
- 修改：`skills/writing-plans/agents/openai.yaml:3`
- 修改：`README.zh-CN.md:14,196,333`、`README.md:14,196`
- 修改：`scripts/tests/skill-streamlining-T03.test.mjs:24`
- 测试：`scripts/tests/workflow-slimming-T02.test.mjs`（新建）

**接口**：
- 消费：无
- 产出：SKILL.md 新节「## 计划体量」，含常量「单任务文件上限 200 行」与「逐字重合超过 30 行即视为整文件内嵌」；T03 消费 200 这个数字

**步骤 1：写失败测试**

```js
import {test} from 'node:test';
import assert from 'node:assert/strict';
import {readFileSync} from 'node:fs';
import path from 'node:path';
import {fileURLToPath} from 'node:url';
import {readPolicy} from './helpers/policy-documents.mjs';
const root=path.resolve(path.dirname(fileURLToPath(import.meta.url)),'../..');
const raw=f=>readFileSync(path.join(root,f),'utf8');
const policy=f=>readPolicy(root,f).text;
test("S1.3 skill 文本不再要求完整代码",()=>{
  const s=policy('skills/writing-plans/SKILL.md');
  assert.match(s,/## 计划体量/);assert.match(s,/关键 diff 片段/);assert.match(s,/单任务文件上限 200 行/);assert.match(s,/禁止整文件内嵌/);
  assert.doesNotMatch(s,/每步给完整代码/);
  const body=raw('skills/writing-plans/SKILL.md');assert.doesNotMatch(body,/零上下文/);
});
test("S1.3 README 与 openai.yaml 同步",()=>{
  for(const f of ['README.zh-CN.md','README.md']) assert.doesNotMatch(raw(f),/完整代码|complete code/);
  assert.match(raw('skills/writing-plans/agents/openai.yaml'),/200 行/);
});
```
同时把 `skill-streamlining-T03.test.mjs:24` 的 `assert.match(s,/每步给完整代码/)` 改为 `assert.match(s,/每步给关键片段/)`。

**步骤 2：运行测试确认失败**

运行：`rtk proxy node --test scripts/tests/workflow-slimming-T02.test.mjs scripts/tests/skill-streamlining-T03.test.mjs`
预期：新测试两例 FAIL 为 AssertionError（`## 计划体量` 不匹配 / README 仍含「完整代码」）；T03 的 S10 用例 FAIL（`每步给关键片段` 尚不存在）。

**步骤 3：写最小实现**

`SKILL.md:4` description 中「把设计拆解为零上下文工程师也能执行的 bite-sized 任务（精确文件路径、完整代码、TDD 步骤、预期输出）」→「把设计拆解为可独立验证的 bite-sized 任务（精确文件路径、改动要点与关键 diff 片段、TDD 步骤、预期输出）」。

`SKILL.md:17` 整段 →
> 假设执行计划的工程师**能读到仓库、但不了解本次设计的取舍**：技术过硬，会用工具链，但不知道我们为什么这样改。计划写清每个任务动哪些文件、改动要点、关键 diff 片段、怎么验证、参考哪些文档；不替执行者抄写他能自己读到的现行文件。DRY、YAGNI、TDD、频繁提交。

`SKILL.md:38`「计划要求每步含完整代码与精确路径」→「计划要求每步含关键 diff 片段与精确路径」。

在「## 任务结构」节（原 104 行）之后插入新节：
```markdown
## 计划体量

- **改动要点 + 关键 diff 片段 + 验收断言**：改代码的步骤给出「要改什么、改成什么」——新增函数给完整函数体；修改既有代码给被改的行及前后 1-3 行锚点；文本类文件给「旧文本 → 新文本」对照；再给一条可运行的断言或测试。
- **禁止整文件内嵌**：不把目标文件的现行内容或改后全文抄进任务，执行者自行 Read 现行文件。片段与目标文件现行内容逐字重合超过 30 行即视为整文件内嵌。
- **单任务文件上限 200 行**：`tasks/TNN.md` 超过 200 行先拆任务，仍超则把片段收敛为要点；`validate-output.mjs plan-index` 对超限文件报错 `expected: "<= 200 lines"`。
- **体量参照**：全部 `tasks/*.md` 行数之和以不超过预期代码改动行数为目标，超出时在 index「体量说明」写一句原因。
```

「禁止占位符」列表：「（把代码重复写出来——工程师可能乱序阅读任务）」→「（把该任务自己的片段写出来——工程师可能乱序阅读任务）」；「（涉及代码的步骤必须有代码块）」→「（涉及代码的步骤必须有片段或新旧对照）」。

「牢记」列表：「每步给完整代码——改代码的步骤必须展示代码」→「每步给关键片段——改代码的步骤必须展示新旧对照或新增代码，不抄整文件」。

`task-templates.md`「## 任务结构」标题下插入一行：
> 修改既有文件时步骤 3 给「旧片段 → 新片段」对照，不给整文件；新增文件给完整内容。规则见 SKILL.md「计划体量」。

`openai.yaml:3` short_description 句尾追加「；任务给关键 diff 片段而非整文件，单任务 ≤ 200 行」。

README 对照：
- zh:14「零上下文可执行的 bite-sized 任务：精确文件路径、完整代码、」→「可独立验证的 bite-sized 任务：精确文件路径、改动要点与关键 diff 片段、」
- zh:196「假设执行者零上下文」→「假设执行者能读仓库但不知本次取舍」；同行「完整代码」→「关键 diff 片段」
- zh:333「生成计划仍包含完整代码、命令和接口。」→「生成计划包含关键片段、命令和接口，单任务文件不超过 200 行。」
- en:14「executable with zero context: exact file paths, complete code,」→「executable by an engineer who can read the repo: exact file paths, change notes with key diff snippets,」
- en:196「assumes a zero-context executor」→「assumes an executor who can read the repo but not the design rationale」；同行「complete code」→「key diff snippets」

**步骤 4：运行测试确认通过**

运行：`rtk proxy node --test scripts/tests/workflow-slimming-T02.test.mjs scripts/tests/skill-streamlining-T03.test.mjs scripts/tests/skill-streamlining-T01.test.mjs`
预期：全部 PASS。

**步骤 5：提交**

```bash
git add skills/writing-plans README.md README.zh-CN.md scripts/tests/workflow-slimming-T02.test.mjs scripts/tests/skill-streamlining-T03.test.mjs
SKIP_RELEASE_HOOK=1 git commit -m "feat(T02): plans carry diff snippets, not whole files; 200-line task cap" -m "Spec-Guard: off workflow-slimming (facet coexistence)"
```
