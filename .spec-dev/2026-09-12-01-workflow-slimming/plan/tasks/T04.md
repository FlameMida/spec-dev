### 任务 4：requirement-analysis 档位裁剪

**文件**：
- 修改：`skills/requirement-analysis/SKILL.md:27,39,50,52-59,146-156`
- 修改：`skills/requirement-analysis/assets/spec-template.md:5`（其后插入档位表）
- 修改：`skills/requirement-analysis/references/spec-review.md:15,17`
- 修改：`skills/requirement-analysis/agents/openai.yaml:3`
- 测试：`scripts/tests/workflow-slimming-T04.test.mjs`（新建）

**接口**：
- 消费：无
- 产出：spec-template「档位裁剪」表（light / standard / deep 三行）；spec-review 第二步含字面量「light 档跳过本步」

**步骤 1：写失败测试**

```js
import {test} from 'node:test';
import assert from 'node:assert/strict';
import {readFileSync} from 'node:fs';
import path from 'node:path';
import {fileURLToPath} from 'node:url';
const root=path.resolve(path.dirname(fileURLToPath(import.meta.url)),'../..');
const raw=f=>readFileSync(path.join(root,f),'utf8');
test("S2.3 反模式段不再要求全流程",()=>{
  const s=raw('skills/requirement-analysis/SKILL.md');
  assert.doesNotMatch(s,/所有需求都要走完本流程/);assert.match(s,/light 档一次成稿一次批准/);assert.match(s,/不派 spec-reviewer/);
});
test("S2.2 模板标注档位",()=>{
  const s=raw('skills/requirement-analysis/assets/spec-template.md');
  assert.match(s,/档位裁剪/);assert.match(s,/\| light \| 背景与目标、非目标、已确认的关键决策、行为规范/);assert.match(s,/\| deep \| 全部小节/);
});
test("S2.1 light 一次批准",()=>{
  assert.match(raw('skills/requirement-analysis/references/spec-review.md'),/light 档跳过本步/);
  assert.match(raw('skills/requirement-analysis/SKILL.md'),/spec 草稿随完整设计同一条消息呈现/);
});
```

**步骤 2：运行测试确认失败**

运行：`rtk proxy node --test scripts/tests/workflow-slimming-T04.test.mjs`
预期：三例 FAIL 为 AssertionError（旧句仍在 / 新字面量缺失）。

**步骤 3：写最小实现**

`SKILL.md:27` 整段 →
> 需求不论大小都要经过设计批准（HARD-GATE），但设计的篇幅与流程按档位裁剪：light 档一次成稿一次批准（阶段 5 与阶段 7 合并，不派 spec-reviewer 子代理）；standard 档走完整八阶段；deep 档在 standard 之上扩大探索与方案分析。"简单"需求恰恰是未经检验的假设造成返工最多的地方——所以 light 档省的是文书，不省批准。

`SKILL.md:39` Checklist 第 7 项 →「**Spec self-review + 对抗验证** — inline 自检；standard/deep 派审查子代理；有修改则请用户再 review（light 档并入第 5 项，一次批准）」。

`SKILL.md:50`「它只调节探索规模与 spec 篇幅，**不豁免任何 Checklist 项与 HARD-GATE**。」→「它调节探索规模、spec 小节集合与批准门的合并方式，**不豁免 HARD-GATE**（任何档位都在用户批准前零实施动作）；light 档把 Checklist 第 5 与第 7 项合并为一次批准。」

`SKILL.md:52-59` 代码块内容 →
```
light    — 单文件/单模块、无新依赖、无方案分歧（如加字段、改文案）
           探索：主线程直查或 1 个子代理；方案可收敛为 1 个（说明为何无分歧）
           spec：五节——背景与目标 / 非目标 / 已确认的关键决策 / 行为规范 / 测试与验收策略
           批准：完整设计与 spec 草稿同一条消息呈现，用户一次批准 → 落盘、激活、交接；不派 spec-reviewer
standard — 默认档。跨 2-3 模块或有方案取舍
           探索：按架构层次或功能模块 3-5 个子代理；完整 2-3 方案对比
           spec：light 五节 + 术语表 / 参与者与适用行为 / 影响面 / 约束归属与拒绝的解读 / 取代与共存 / 方案设计 / 风险与边缘情况
           批准：阶段 5 设计批准 + 阶段 7 一次整体 review
deep     — 跨层架构变更、新技术栈、用户使用"彻底/全面/审计"等措辞
           探索：multi-modal sweep，按模态数派发、不设上限；方案对比含更完整的风险分析
           spec：全模板；批准同 standard，spec-reviewer 子代理必派
```

`SKILL.md` 阶段 5 列表末尾（原 156 行「用户批准前不进入阶段 6」之前）插入一条：
> - **light 档**：按 light 五节写好的 spec 草稿随完整设计同一条消息呈现；用户批准即视为阶段 5 与阶段 7 双门通过，直接进入阶段 6 落盘并按阶段 8 激活、交接

`spec-template.md:5` 之后插入：
```markdown
> **档位裁剪**（requirement-analysis 阶段 1 判定）：
>
> | 档位 | 保留小节 |
> |---|---|
> | light | 背景与目标、非目标、已确认的关键决策、行为规范（Requirements 或差量三节）、测试与验收策略 |
> | standard | light 全部 + 术语表、参与者与适用行为、影响面、约束归属与拒绝的解读、取代与共存、方案设计、风险与边缘情况 |
> | deep | 全部小节 |
>
> 有相交 active spec 时任何档位都保留「取代与共存」节；frontmatter 任何档位必填。
```

`spec-review.md:15`「**第二步——对抗验证**：派 1 个临时子代理」→「**第二步——对抗验证**（light 档跳过本步，其自检与用户批准已在阶段 5 一次完成）：standard/deep 档派 1 个临时子代理」；`:17`「**第三步——用户 review 门**：」→「**第三步——用户 review 门**（light 档不进入本步）：」。

`openai.yaml:3` short_description 句尾追加「；light 档一次成稿一次批准」。

**步骤 4：运行测试确认通过**

运行：`rtk proxy node --test scripts/tests/workflow-slimming-T04.test.mjs scripts/tests/skill-streamlining-T04.test.mjs scripts/tests/skill-streamlining-T08.test.mjs`
预期：全部 PASS（T04/T08 既有断言只依赖 references 链接与 frontmatter，不受影响）。

**步骤 5：提交**

```bash
git add skills/requirement-analysis scripts/tests/workflow-slimming-T04.test.mjs
SKIP_RELEASE_HOOK=1 git commit -m "feat(T04): tiers trim spec sections; light approves once" -m "Spec-Guard: off workflow-slimming (facet coexistence)"
```
