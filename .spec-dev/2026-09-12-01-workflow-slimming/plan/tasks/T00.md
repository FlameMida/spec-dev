### 任务 0：建立隔离工作区

**文件**：
- 修改：`.spec-dev/2026-09-12-01-workflow-slimming/plan/progress.yaml`（T00 状态、notes、resources）

**接口**：
- 消费：无
- 产出：progress.notes 中的真实值——来源工作区绝对路径、来源分支、来源 HEAD、实施工作区绝对路径、实施分支 `plan/2026-09-12-01-workflow-slimming`、created/inherited 归属；T01 与 T12 只消费这些记录，不自行推算

**步骤 1：检测已有隔离**

运行：`git rev-parse --git-dir` 与 `git rev-parse --git-common-dir`
两者不同、且 `git rev-parse --show-superproject-working-tree` 无输出（排除 submodule）
→ 已在隔离工作区，只跳过步骤 2 的创建，仍执行下面的绑定、步骤 3 与完成状态保存。

在创建前记录实际来源工作区绝对路径、来源分支与 HEAD；创建后记录实际实施工作区与分支。复用时从原隔离机制的记录核对来源，不能由 common-dir 推算来源工作区、假定来源为 main 或把复用资源改为本计划所有。用现有 append-only `notes` 保存这些真实值、created/inherited 归属及移交方式；`resources` 中复用行写保留/移交回执，不能预填删除命令。无法核实的来源明确记未知，不能据此生成合并命令。

来源工作区当前有 6423 个未提交删除（`.spec-dev/2026-09-11-01-skill-instruction-streamlining/acceptance/` 整目录）。这不是本任务的阻塞：worktree 从 HEAD 创建，不带工作区改动；该目录的处置在 T01 完成。notes 追加一行记录该事实。

**步骤 2：建立 worktree**

有原生 worktree 工具（如 EnterWorktree）或 using-git-worktrees skill 时优先使用（Codex 无原生 worktree 工具，直接走下面的手工路径）；否则手工降级：
确认 `.worktrees/` 已被忽略（`git check-ignore -q .worktrees`，本仓库 `.gitignore` 已含 `/.worktrees/`），然后
`git worktree add .worktrees/plan/2026-09-12-01-workflow-slimming -b plan/2026-09-12-01-workflow-slimming` 并切换到该目录。

**步骤 3：安装依赖并验证基线**

先读取项目声明的工具/命令、锁文件和已有环境；已就绪不强制重装。本仓库 package.json 无 dependencies，不安装；需要 `node`、`python3`、`git`、`rtk` 在 PATH。
然后按计划头部「相关测试范围」运行基线验证：有声明 → 只跑声明范围（声明为空 → 跳过测试并注明，
最终任务全量验证照跑；声明命令执行报错或工具不可用 → 回退运行完整测试套件，并注明声明已失效、
建议修订计划）；计划无该节（旧版计划）→ 运行完整测试套件，行为与现状一致。
基线测试失败 → 停下报告，先问再继续。

已知基线事实（2026-09-11 实测）：全量 253 用例中 anysearch `--check` 一例因 `spawnSync git ENOBUFS` 失败，属环境缓冲区问题；本计划相关测试范围不含该用例。若范围内出现失败，先问再继续。

将已验证的实际基线 SHA 写入 T00.commit，保存 T00 completed 检查点；步骤 2 跳过不意味着 T00 无需落盘。状态写入采用同目录临时文件 + rename，保留原 tasks/notes/resources；状态提交独立于它引用的实际基线提交。

```bash
cd .worktrees/plan/2026-09-12-01-workflow-slimming
git add .spec-dev/2026-09-12-01-workflow-slimming/plan/progress.yaml
SKIP_RELEASE_HOOK=1 git commit -m "chore(T00): bind isolated workspace and baseline"
```
