### 任务 8：test-strategy 本地三 lane

**文件**：
- 修改：`skills/test-strategy/SKILL.md:4,15-23,36,62-64,70-72,77-78`
- 修改：`skills/test-strategy/references/frontend-testing.md:5`、`skills/test-strategy/references/ai-agent-testing.md:9`
- 修改：`skills/test-strategy/agents/openai.yaml:3`
- 修改：`skills/test-strategy/evals/evals.json:7,22`、`skills/test-strategy/evals/trigger-evals.json:7`
- 修改：`skills/acceptance-qa/references/acceptance-matrix.md:36`、`skills/acceptance-qa/SKILL.md:72`
- 修改：`skills/requirement-analysis/assets/spec-template.md:128`
- 测试：`scripts/tests/workflow-slimming-T08.test.mjs`（新建）

**接口**：
- 消费：T07 的「任务内 / 最终任务」两时点
- 产出：lane 名 `fast` / `final` / `manual`；标记字面量 `manual-pending`

**步骤 1：写失败测试**

```js
import {test} from 'node:test';
import assert from 'node:assert/strict';
import {readFileSync} from 'node:fs';
import path from 'node:path';
import {fileURLToPath} from 'node:url';
const root=path.resolve(path.dirname(fileURLToPath(import.meta.url)),'../..');
const raw=f=>readFileSync(path.join(root,f),'utf8');
const files=['skills/test-strategy/SKILL.md','skills/test-strategy/references/frontend-testing.md','skills/test-strategy/references/ai-agent-testing.md','skills/test-strategy/agents/openai.yaml','skills/test-strategy/evals/evals.json','skills/test-strategy/evals/trigger-evals.json','skills/acceptance-qa/references/acceptance-matrix.md','skills/acceptance-qa/SKILL.md','skills/requirement-analysis/assets/spec-template.md'];
test("S5.1 lane 定义与消费方同步",()=>{
  for(const f of files) assert.doesNotMatch(raw(f),/nightly|PR lane|fast\/PR/,f);
  const s=raw('skills/test-strategy/SKILL.md');assert.match(s,/\| \*\*manual\*\* \|/);assert.match(s,/\| \*\*final\*\* \|/);assert.match(s,/manual-pending/);
  assert.match(raw('skills/acceptance-qa/SKILL.md'),/manual-pending/);
});
```

**步骤 2：运行测试确认失败**

运行：`rtk proxy node --test scripts/tests/workflow-slimming-T08.test.mjs`
预期：FAIL 为 AssertionError（`nightly` 仍出现于 test-strategy SKILL.md）。

**步骤 3：写最小实现**

test-strategy SKILL.md：
- `:4` description「按 IO 类型的三 Lane 调度（fast/PR/nightly）」→「按本地执行时点的三 Lane 调度（fast/final/manual）」。
- `:15-23` 节标题与表 →
```
## 三 Lane 模型（调度维度是执行时点，不是业务模块；全部本地运行，无 CI/PR 槽位）

| Lane | 时机/预算 | 内容 | 判据 |
|---|---|---|---|
| **fast** | 每任务，< 1 min | 本任务目标测试 + 特性自有测试文件；纯内存或本地可控 IO | 不碰网络与 LLM |
| **final** | 收尾一次（最终任务） | 项目完整测试套件，含真实容器/库集成与主干 E2E | 每特性只跑一次，失败按归属裁决 |
| **manual** | 用户当轮显式要求 | 模型评测、性能多轮采样、全量 E2E、迁移演练等长耗时/概率性检查 | 永不由 executing-plans 或 acceptance-qa 自动触发；交付路径上的 manual 行标 `manual-pending` |

小团队（1-5 人）不建按模块的测试选择系统；final 一次全量即安全网，选择/分片只在 final 超过 15 min 时再议。
```
- `:36`「fast 只含纯内存；真实 IO 按 PR/nightly，替身通过不能作为真实集成信心」→「fast 只含纯内存或本地可控 IO；真实集成归 final，长耗时归 manual；替身通过不能作为真实集成信心」。
- `:62`「L3 回放（fast/PR）」→「L3 回放（fast）」；`:63`「L4 廉价模型冒烟（PR，仅 agent 相关变更）」→「L4 廉价模型冒烟（manual，仅 agent 相关变更且用户要求）」；`:64`「L5 完整 eval（nightly）」→「L5 完整 eval（manual）」。
- `:70`「每行标注 Lane 归属（fast/PR/nightly）——unit/docs 行默认 fast，integration 行默认 PR，perf/eval 行默认 nightly」→「每行标注 Lane 归属（fast/final/manual）——unit/docs 行默认 fast，integration/e2e 行默认 final，perf/eval/实际模型任务行默认 manual」。
- `:72`「阶段 0 装配按 Lane 选择执行窗口；nightly 行在验收报告中标注"非阻塞"」→「阶段 0 装配按 Lane 选择执行窗口；manual 行在交付路径标 `manual-pending`，仅用户当轮显式要求时执行」。
- `:77`「集成信心来自 PR lane 真实容器」→「集成信心来自 final lane 真实容器」；`:78`「eval 属 nightly 不阻塞」→「eval 属 manual 不阻塞」。

references：`frontend-testing.md:5`「主干 E2E 3-5 条（PR）→ 全 E2E（nightly）」→「主干 E2E 3-5 条（final）→ 全 E2E（manual）」；`ai-agent-testing.md:9`「L0-L2 fast、L3 fast/PR、L4 PR（仅 agent 变更触发）、L5 nightly 永不阻塞 PR」→「L0-L3 fast、L4/L5 manual，永不阻塞交付」。

`openai.yaml:3`「按 IO 划分 fast/PR/nightly」→「按本地时点划分 fast/final/manual」。

evals：`evals.json:7` 两处「PR lane」→「final lane」；`:22`「真实 IO 按 PR/nightly 安排」→「真实集成归 final、长耗时归 manual」；`trigger-evals.json:7` prompt「把我们项目的 CI 测试分成 fast/PR/nightly 三档怎么分？」→「把我们项目的本地测试分成 fast/final/manual 三档怎么分？」。

消费方：`acceptance-matrix.md:36`「（fast/PR/nightly，语义见 test-strategy skill）」→「（fast/final/manual，语义见 test-strategy skill）」；`spec-template.md:128` 中英两处「fast/PR/nightly」→「fast/final/manual」；`acceptance-qa/SKILL.md:72`「（fast/PR 行随验收即时执行，nightly 行标注"非阻塞"）」→「（fast/final 行随验收即时执行，manual 行标 `manual-pending`、仅用户当轮显式要求时执行）」。

**步骤 4：运行测试确认通过**

运行：`rtk proxy node --test scripts/tests/workflow-slimming-T08.test.mjs scripts/tests/skill-streamlining-T11.test.mjs`
预期：全部 PASS。

**步骤 5：提交**

```bash
git add skills/test-strategy skills/acceptance-qa skills/requirement-analysis scripts/tests/workflow-slimming-T08.test.mjs
SKIP_RELEASE_HOOK=1 SKIP_OPENAI_SYNC_CHECK=1 git commit -m "feat(T08): local lanes fast/final/manual replace fast/PR/nightly" -m "openai.yaml 无需同步：acceptance-qa 仅改阶段 0 一句；test-strategy 的 yaml 已随描述同步" -m "Spec-Guard: off workflow-slimming (facet coexistence)"
```
