### 任务 2：补齐反模式、mock 分层与静态快检策略

**文件**：
- 修改：`skills/test-driven-development/references/testing-anti-patterns.md`
- 修改：`skills/test-strategy/SKILL.md`
- 修改：`skills/test-strategy/agents/openai.yaml`
- 修改：`skills/test-driven-development/evals/evals.json`
- 修改：`skills/test-strategy/evals/evals.json`
- 记录：`.spec-dev/2026-09-06-02-tdd-seam/execution/serial/T02-eval-review.md` 与验证日志；状态只写 `plan/progress.yaml`。

**接口**：
- 消费：T01：test-driven-development 的 seam 消费、红绿/纯重构路径与例外单点。
- 产出：testing-anti-patterns：反模式 6/7 与 mock 准入；test-strategy：四类依赖策略与静态快检。
- Spec/Scenario：`.spec-dev/2026-09-06-02-tdd-seam/spec/tdd-seam-design.md`；S08 S09 S10 S11 S12 S13 S14 S15。静态夹具的唯一主属见本任务新增 eval，其余为消费约束或 T05 复核。
- Lane：PR（读取仓库文件的静态检查不是纯内存 fast）；本任务使用获批 spec 的文档行为验证方式，静态红/绿只能标记规则差异，不能冒充真实模型执行。

**步骤 1：写失败场景**

在隔离工作区用 `rtk proxy python3 - <<'PY'` 执行以下 Python 块（尾部补单独一行 `PY`），只先改 eval/测试。条目已经把 GIVEN/WHEN 转为输入、THEN 转为独立期望；保留其他条目。

```python
import json
from pathlib import Path
additions = json.loads(r'''{
  "skills/test-driven-development/evals/evals.json": [
    {
      "id": "tdd-seam-s08",
      "prompt": "S08 拒绝同构自证：\nGIVEN 被测函数用 reduce 求和，测试又用同一 reduce 算出期望值\nWHEN 检查断言",
      "expected_output": "指出同义反复，改用有来源的已知样例结果，如 `[2, 5, 8]` 的手算值 `15`，而非换个函数名继续复制算法。"
    },
    {
      "id": "tdd-seam-s09",
      "prompt": "S09 重构后脆弱测试失败：\nGIVEN 私有协作者改名导致 mock 断言失败，而获批公共行为保持不变\nWHEN 诊断失败测试",
      "expected_output": "把测试移回公共边界，不为保留脆弱断言恢复旧内部结构；若公共行为实际改变，则按行为回归处理。"
    },
    {
      "id": "tdd-seam-s10",
      "prompt": "S10 数据库测试边界对照：\nGIVEN 一个测试声称验证 HTTP 响应却只查内部表，另一个明确验证获批数据库适配器契约\nWHEN 审查测试落点",
      "expected_output": "前者补回 HTTP 可观察行为断言，后者可在自身契约边界验证真实数据库；不一概禁止 DB 测试。测试夹具准备和清理不等于旁路行为断言。"
    }
  ],
  "skills/test-strategy/evals/evals.json": [
    {
      "id": "tdd-seam-s14",
      "prompt": "S14 内部实现与外部替身对照：\nGIVEN 支付业务通过声明的支付客户端边界调用外部服务，同时有内部折扣计算模块\nWHEN 设计测试替身",
      "expected_output": "可替换支付客户端并检查真实业务结果，保留内部计算；不把“自己写的外部适配器”按所有权误判为禁止替换的内部细节。"
    },
    {
      "id": "tdd-seam-s15",
      "prompt": "S15 四类依赖与集成信心：\nGIVEN 测试涉及远端 API、数据库、时钟/随机源、文件系统\nWHEN 选择策略及 Lane",
      "expected_output": "每类按上述规则得到真实依赖或替身方案；纯内存替身可以进入 fast，真实 IO 按 PR/nightly 安排；fast 全绿不能证明真实 DB/网络集成通过。"
    }
  ]
}''')
for name, entries in additions.items():
    path = Path(name)
    data = json.loads(path.read_text())
    present = {entry['id'] for entry in data['evals']}
    assert not present.intersection(entry['id'] for entry in entries), name
    data['evals'].extend(entries)
    path.write_text(json.dumps(data, ensure_ascii=False, indent=2) + '\n')
```

**步骤 2：确认旧规则差异**

逐条读取刚增加/修改的 eval 与当前入口，保存「ID｜输入｜预期｜旧规则依据｜缺失或矛盾」至本任务 eval-review。预期是本 spec 具名场景在旧规则中未完整承载；有已存在的近义机制就准确记录差异，不能把静态评读或立即符合叫作有效行为红。若所需行为已完整存在，停止对应冗余修改并记录事实。

本任务没有新增生产脚本；无需为文字镜像制造正则单测，真实模型遵循留 T05 必需冒烟。

**步骤 3：应用完整变更**

下面是逐项完整替换；在隔离工作区执行。每个旧锚点必须唯一存在，漂移时停止对应修改、读当前事实后按 index 偏差规则处理，不直接覆盖整文件。

```python
import json
from pathlib import Path
changes = json.loads(r'''[
  {
    "path": "skills/test-driven-development/references/testing-anti-patterns.md",
    "old": "## mock 过于复杂时",
    "new": "## 反模式 6：同义反复\n\n期望值来自独立真值：现行 spec、已验证字面量或独立手算例子。不能用被测算法重算 expected，改名或复制为另一个 helper 也无效。\n\n```typescript\n// BAD：把实现用的 reduce 再写一遍，只能自证。\nexpect(sum([2, 5, 8])).toBe([2, 5, 8].reduce((a, b) => a + b, 0));\n// GOOD：独立手算样例 2 + 5 + 8 = 15。\nexpect(sum([2, 5, 8])).toBe(15);\n```\n\n## 反模式 7：实现耦合与越过接口\n\n获批 seam 内的私有方法、内部协作者调用顺序、存储细节不是测试目标。若已核实公共行为不变，重构却弄坏测试，优先把测试移回公共边界，不恢复旧实现迁就断言；公共行为真的回归时修实现。\n\n```typescript\n// BAD：对外声称测下单 API，却只检查内部私有表。\nawait api.createOrder({ sku: \"A\", quantity: 1 });\nexpect(await db.internalOrders.count()).toBe(1);\n// GOOD：观察下单 API 的契约结果。\nconst order = await api.createOrder({ sku: \"A\", quantity: 1 });\nexpect(order.status).toBe(\"created\");\nexpect(order.quantity).toBe(1);\n```\n\n若批准的 seam 本身是数据库适配器，则可直接验证该适配器与真实 DB 契约；夹具准备/清理不算旁路行为断言。公共接口可以是模块函数，不强制所有测试走 HTTP。\n\n## mock 准入（单点定义）\n\n- 可替换声明的外部边界：外部 API、数据库、时钟/随机源、文件系统；策略选择见 ../../test-strategy/SKILL.md。\n- 不替换获批 seam 内自己的实现协作者、私有方法；不为了测试给生产 API 添加专用入口。\n- “自己写的适配器”也可能代表外部边界，按契约身份而非代码所有权判断。\n- 保留原规则：理解依赖链与副作用；镜像真实数据结构；断言真实业务结果，不只断言替身存在。\n- seam 声明无替换依赖时写无；策略与准入有疑义先回到批准的边界，不自行扩大替身范围。\n\n## mock 过于复杂时"
  },
  {
    "path": "skills/test-driven-development/references/testing-anti-patterns.md",
    "old": "| mock 过于复杂 | 考虑集成测试 |",
    "new": "| mock 过于复杂 | 考虑集成测试 |\n| 同构算法重算期望 | 使用独立真值 |\n| 测私有实现或旁路断言 | 回到获批 seam；不变行为下修测试位置 |"
  },
  {
    "path": "skills/test-driven-development/references/testing-anti-patterns.md",
    "old": "- \"保险起见 mock 一下\"",
    "new": "- \"保险起见 mock 一下\"\n- 用与实现同构的算法生成 expected\n- 公共行为未变而重构导致测试因私有细节失败"
  },
  {
    "path": "skills/test-strategy/SKILL.md",
    "old": "## 治理顺序铁律：flaky → 时长 → 选择",
    "new": "## 外部依赖策略\n\n准入以 ../test-driven-development/references/testing-anti-patterns.md 为单点；本节只定义选择策略，不另列竞争的禁止清单。\n\n| 依赖 | 策略 | 验证边界 |\n|---|---|---|\n| 外部 API | 在声明边界注入契约完整的客户端替身，按需做真实契约集成 | 替身绿只证明本地消费者 |\n| 数据库 | 优先真实测试库；纯内存替身只用于局部确定性行为 | 真实 DB 契约依 references/db-testing.md |\n| 时间/随机性 | 注入可控时钟或随机源 | 不 mock 被测业务算法 |\n| 文件系统 | 目的允许时用真实临时目录；需隔离时替换声明边界 | 真实文件行为按实际 IO 归 Lane |\n\n依赖注入与操作明确的窄接口用于已有交付需求，不为测试投机新建抽象。优先操作明确的客户端方法，避免在替身中解析万能 fetcher 的条件分支。fast 只含纯内存；真实 IO 按 PR/nightly，替身通过不能作为真实集成信心。模型边界下的 fake model 骨架保留。\n\n## 静态快检\n\n计划写出项目配置中已有且适用的 typecheck/静态检查命令及其来源，在编辑批次间提供快速反馈。无适用命令写“不适用”并沿用测试命令，不新增依赖或虚构 tsc。\n\n快检与行为测试分开记账：编译故障不是有效红，静态通过不能替代目标红绿、相关集成、最终全量或原 Lane 的必需检查；零测试和 SKIP 不记 PASS。任务完成和并发后继解锁仍以既有验证要求为准。\n\n## 治理顺序铁律：flaky → 时长 → 选择"
  },
  {
    "path": "skills/test-strategy/agents/openai.yaml",
    "old": "  short_description: \"三 Lane 测试调度、flaky→时长→选择治理、AI Agent 测试骨架、验收矩阵对接。\"",
    "new": "  short_description: \"测试策略：按 IO 分三 Lane，治理 flaky、时长与选择；四类外部依赖和模型边界策略；静态快检不替代行为验证。\""
  }
]''')
for change in changes:
    path = Path(change['path'])
    text = path.read_text()
    assert text.count(change['old']) == 1, f"anchor changed: {path}"
    path.write_text(text.replace(change['old'], change['new'], 1))
```

**步骤 4：核验新规则与相关回归**

逐条重新走查步骤 1 的夹具，把新入口的证据位置与 STATIC_MATCH/不匹配写入同一记录；不能只记录“已更新”。执行：

```bash
rtk proxy node --test scripts/tests/plugin-root.test.mjs scripts/tests/plan-single-format.test.mjs scripts/tests/parallel-plan.test.mjs scripts/tests/parallel-integration.test.mjs
rtk proxy node scripts/validate-skills.mjs
rtk proxy node scripts/check-plugin.mjs
rtk git diff --check
```

预期实际选中套件全部通过、0 fail/skip，静态夹具逐项符合；记录实际计数，不照抄历史计数。 修复命中自身变更的失败后重跑受影响检查。

**步骤 5：提交与状态记录**

暂存仅本任务文件和证据，按 index 的分面共存/部分取代说明生成提交正文。实施提交走仓库正常自动发版钩子；进度使用钩子完成后的真实 HEAD，不预写预期 SHA。

```bash
rtk git add skills/test-driven-development/references/testing-anti-patterns.md skills/test-strategy/SKILL.md skills/test-strategy/agents/openai.yaml skills/test-driven-development/evals/evals.json skills/test-strategy/evals/evals.json
rtk proxy node scripts/check-openai-sync.mjs
```

按 index「提交与漂移」先完成 staged drift 自检，再提交 `feat(T02): 补齐反模式、mock 分层与静态快检策略`。原子更新 progress：状态 completed，记录真实 commit；tests 写实际静态和自动回归证据摘要（不把 STATIC_MATCH 写成真实模型 pass），再单独文档提交进度与记录。按现有串行执行规则做 over/under-building 与契约锚定自检。
