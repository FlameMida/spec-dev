### 任务 0：隔离与基线

**文件**：
- 记录：`.spec-dev/2026-09-06-02-tdd-seam/plan/progress.yaml`、`.spec-dev/2026-09-06-02-tdd-seam/execution/serial/T00-baseline.log`。
- 创建资源：progress 已登记 worktree；工作区内证据随本特性归档。

**接口**：
- 消费：已提交 spec/plan、来源 main。
- 产出：已核验 worktree、原始 base_commit、相关基线。

**步骤 1：检测已有隔离**

```bash
rtk git status --short
rtk proxy git rev-parse --git-dir
rtk proxy git rev-parse --git-common-dir
rtk proxy git rev-parse --show-superproject-working-tree
rtk git worktree list
```

同特性已隔离则复用原始基线与 completed 记录；其他特性隔离不得冒充本票。主工作区未知 dirty 先厘清归属，不拷贝或清理。

**步骤 2：建立隔离**

遵循 using-git-worktrees，已有/原生机制优先。本机可用 Git 手工路径，先核验 .worktrees 已忽略、来源 main 和 HEAD：

```bash
rtk proxy git check-ignore -q .worktrees/
rtk proxy git rev-parse HEAD
rtk git worktree add .worktrees/plan-2026-09-06-02-tdd-seam -b plan/2026-09-06-02-tdd-seam
```

创建前真实 HEAD 作为原始审查 base_commit 记进 progress.notes，来源路径 /Users/maverick/feature-dev。后续 cwd 为 /Users/maverick/feature-dev/.worktrees/plan-2026-09-06-02-tdd-seam。采用现有隔离时按真实路径修订台账；禁止清理其他特性资源。

**步骤 3：依赖与基线**

Node/Git/Python 使用已有安装，根 package.json 无依赖，不运行安装。执行 index 的相关基线全部命令，保存真实退出码与计数。命令失效回退完整 Node 套件，真实失败按基线门处理，零测试/skip 不记通过。忽略规则缺失时仅补 .worktrees 条目并独立提交；正常可用不主动降级原地实施。

**步骤 4：记录完成**

原子更新 progress，记录 worktree、来源、最初基线和检查证据；使用 SKIP_RELEASE_HOOK=1 文档提交。模型试验尚未开始，不标模型通过。
