### 任务 4：贯通串并行消费与收尾审查

**文件**：
- 修改：`skills/executing-plans/SKILL.md`
- 修改：`skills/executing-plans-parallel/SKILL.md`
- 修改：`agents/implementer.md`
- 修改：`skills/executing-plans/references/review-orchestration.md`
- 修改：`agents/code-reviewer.md`
- 修改：`skills/executing-plans/agents/openai.yaml`
- 修改：`skills/executing-plans-parallel/agents/openai.yaml`
- 修改：`skills/executing-plans/evals/evals.json`
- 修改：`skills/quick-fix/evals/evals.json`
- 修改：`skills/executing-plans-parallel/evals/evals.json`
- 记录：`.spec-dev/2026-09-06-02-tdd-seam/execution/serial/T04-eval-review.md` 与验证日志；状态只写 `plan/progress.yaml`。

**接口**：
- 消费：T03：spec/四列导航/本票接口的 seam、Scenario、依赖替换边界和来源；T01/T02 的验证纪律。
- 产出：串行/implementer 同源消费，缺失/冲突 blocked；原收尾接收重构候选并按公共行为审查覆盖。
- Spec/Scenario：`.spec-dev/2026-09-06-02-tdd-seam/spec/tdd-seam-design.md`；S02 S03 S04 S05 S06 S07 S09 S11 S13 S16 S17 S18 S19 S20 S21。静态夹具的唯一主属见本任务新增 eval，其余为消费约束或 T05 复核。
- Lane：PR（读取仓库文件的静态检查不是纯内存 fast）；本任务使用获批 spec 的文档行为验证方式，静态红/绿只能标记规则差异，不能冒充真实模型执行。

**步骤 1：写失败场景**

在隔离工作区用 `rtk proxy python3 - <<'PY'` 执行以下 Python 块（尾部补单独一行 `PY`），只先改 eval/测试。条目已经把 GIVEN/WHEN 转为输入、THEN 转为独立期望；保留其他条目。

```python
import json
from pathlib import Path
additions = json.loads(r'''{
  "skills/executing-plans/evals/evals.json": [
    {
      "id": "tdd-seam-s03",
      "prompt": "S03 显式声明直接开工：\nGIVEN spec 和本票明确指定同一 seam，用户已经批准\nWHEN 开始写失败测试",
      "expected_output": "使用该 seam，不再次询问测试落点。"
    },
    {
      "id": "tdd-seam-s04",
      "prompt": "S04 存量接口唯一提取：\nGIVEN 旧计划没有 seam 标签，但批准的精确接口与 Scenario 唯一指向 CLI 的退出码和标准输出\nWHEN 恢复执行",
      "expected_output": "在本次工作记录中记下来源及提取结果，沿该 CLI 测试；不重写旧计划形态或选择私有函数作为新落点。"
    },
    {
      "id": "tdd-seam-s06",
      "prompt": "S06 spec 与计划冲突：\nGIVEN spec 指定 HTTP seam，计划却要求私有方法断言\nWHEN 串行执行者检查本票输入",
      "expected_output": "报告冲突并走偏差处理，不默默选择其中一个继续。"
    },
    {
      "id": "tdd-seam-s13",
      "prompt": "S13 编译失败不充当红证据：\nGIVEN 新测试因拼写、编译或环境缺件而报错\nWHEN 收集红证据",
      "expected_output": "记录该故障并修复/阻塞相应验证，直到观察到预期行为失败；不把非零退出码直接当作有效红，也不将 SKIP 记作 PASS。"
    },
    {
      "id": "tdd-seam-s16",
      "prompt": "S16 三个私有函数不制造三份测试：\nGIVEN 两个获批行为由三个内部函数实现且均已有公共边界测试\nWHEN 完成自检和代码审查",
      "expected_output": "不因三个函数缺少直接单测判失败；如果错误路径 Scenario 漏测，仍明确报告该覆盖缺口。"
    },
    {
      "id": "tdd-seam-s17",
      "prompt": "S17 绿后发现重复代码：\nGIVEN 目标测试已绿，执行者发现与本票相关的重复辅助逻辑\nWHEN 结束本次红绿循环",
      "expected_output": "记录重构候选，按任务流程提交和自检；收尾接收候选，而不是在每个循环里自动去重或新增功能。"
    }
  ],
  "skills/quick-fix/evals/evals.json": [
    {
      "id": "tdd-seam-s05",
      "prompt": "S05 即兴修复并入原确认环节：\nGIVEN quick-fix 无批准落点，API 与内部服务都是候选\nWHEN 形成修复方案",
      "expected_output": "主线程在原修复确认环节说明公共接口和推荐落点，一次一题确认；不先写任一候选的测试。"
    }
  ],
  "skills/executing-plans-parallel/evals/evals.json": [
    {
      "id": "tdd-seam-s07",
      "prompt": "S07 implementer 不越权修正：\nGIVEN implementer 发现落点缺失，或纠正落点需要改写集合外文件\nWHEN 检查任务可执行性",
      "expected_output": "回报 `blocked` 及来源/缺口，由主线程处理；不自行发问、修改 plan、扩大 writes 或伪造红绿结果；其他独立票按原并发规则处理。"
    }
  ]
}''')
for name, entries in additions.items():
    path = Path(name)
    data = json.loads(path.read_text())
    present = {entry['id'] for entry in data['evals']}
    assert not present.intersection(entry['id'] for entry in entries), name
    data['evals'].extend(entries)
    path.write_text(json.dumps(data, ensure_ascii=False, indent=2) + '\n')
```

**步骤 2：确认旧规则差异**

逐条读取刚增加/修改的 eval 与当前入口，保存「ID｜输入｜预期｜旧规则依据｜缺失或矛盾」至本任务 eval-review。预期是本 spec 具名场景在旧规则中未完整承载；有已存在的近义机制就准确记录差异，不能把静态评读或立即符合叫作有效行为红。若所需行为已完整存在，停止对应冗余修改并记录事实。

本任务没有新增生产脚本；无需为文字镜像制造正则单测，真实模型遵循留 T05 必需冒烟。

**步骤 3：应用完整变更**

下面是逐项完整替换；在隔离工作区执行。每个旧锚点必须唯一存在，漂移时停止对应修改、读当前事实后按 index 偏差规则处理，不直接覆盖整文件。

```python
import json
from pathlib import Path
changes = json.loads(r'''[
  {
    "path": "skills/executing-plans/SKILL.md",
    "old": "TDD 循环遵循 test-driven-development skill（红→绿→重构，每步运行验证）",
    "new": "TDD 循环遵循 test-driven-development skill（有效红→最小实现→绿）；写测试前直接消费获批 seam，存量只允许唯一提取并记录来源，缺失/冲突走主线程偏差处理。按 test-strategy 使用已有适用快检，仍执行目标测试与全部必需验证；重构候选交收尾"
  },
  {
    "path": "skills/executing-plans/SKILL.md",
    "old": "用户确认后在 worktree 内修复（修复本身同样走 TDD：先写复现测试），修复后受影响维度复审一次；裁决结果即对账定稿。",
    "new": "沿已有授权在 worktree 内处置：行为缺陷先写复现失败测试；纯重构按 test-driven-development「收尾纯重构」记录前后绿，缺保护先补刻画，不伪造红。修复后受影响维度复审一次；裁决结果即对账定稿。"
  },
  {
    "path": "skills/executing-plans-parallel/SKILL.md",
    "old": "4. 派发受当前平台实际可用名额约束，不写死限额。",
    "new": "4. 派发前从 spec、本票及依赖接口行核对 seam 来源：显式批准直接使用，存量只唯一提取已有决定；不能唯一确定或互相冲突时由主线程处理，停止受影响票。派发受当前平台实际可用名额约束，不写死限额。"
  },
  {
    "path": "skills/executing-plans-parallel/SKILL.md",
    "old": "要求 implementer 读取 agents/implementer.md 并遵守绑定、TDD、自检、回报协议",
    "new": "输入同时携带获批 seam、Scenario、依赖替换边界及来源指针，要求 implementer 读取 agents/implementer.md 并遵守绑定、TDD、自检、回报协议"
  },
  {
    "path": "agents/implementer.md",
    "old": "## 实施",
    "new": "写测试前消费获批 seam；无显式标签时只能唯一提取批准的接口与 Scenario 并回报依据。缺失、冲突或修正需越过 writes 时 blocked，交主线程，不自行询问用户或修改计划。读取范围保持本票、spec 与依赖接口行。\n\n## 实施"
  },
  {
    "path": "agents/implementer.md",
    "old": "空基线不自动豁免票内 TDD。",
    "new": "空基线不自动豁免票内 TDD。纯重构无法提供有效红的票同样回报 blocked，由主线程按既有串行路径处理；已有行为刻画通过不能充当 red，不修改结果 schema。"
  },
  {
    "path": "agents/implementer.md",
    "old": "自检仅核对 over/under-building 与契约锚定。",
    "new": "静态快检遵循 test-strategy，仅提供额外反馈，不代替目标红绿或集成验证。重构候选附位置、理由与保护证据指针交主线程收尾，不在循环里顺手整理。自检仅核对 over/under-building 与契约锚定。"
  },
  {
    "path": "skills/executing-plans/references/review-orchestration.md",
    "old": "## 审查维度（与 code-reviewer agent 的维度定义一致）",
    "new": "收尾输入同时包含实施期间记录的重构候选（位置、理由、保护证据）；按既有维度及授权处置，不新增审查轴。测试覆盖以获批 seam 的公共行为及现行 Scenario 为准，非新增函数数量。\n\n## 审查维度（与 code-reviewer agent 的维度定义一致）"
  },
  {
    "path": "skills/executing-plans/references/review-orchestration.md",
    "old": "3. 向用户征求处理方式——不自动修复；用户确认后修复走 TDD（先写复现测试），修复后受影响维度复审一次",
    "new": "3. 按 executing-plans 的既有授权与处置门处理；行为缺陷走复现失败测试，纯重构遵循 test-driven-development「收尾纯重构」，缺保护先刻画，不造假红；修复后受影响维度复审一次"
  },
  {
    "path": "agents/code-reviewer.md",
    "old": "- **测试覆盖**: 新代码是否有对应的测试",
    "new": "- **测试覆盖**: 获批 seam 的公共行为、现行 Scenario、边缘及错误路径是否有覆盖；不按新增函数数量要求直测。已核实行为不变而测试依赖私有细节时，按 testing-anti-patterns 反模式 7 修正落点；公共行为回归仍报告实现问题。"
  },
  {
    "path": "skills/executing-plans/agents/openai.yaml",
    "old": "  short_description: \"执行已批准的书面计划（plan/ 分文件形态：index.md + tasks/ + progress.yaml；存量单文件按原样读取）：默认主线程串行 TDD；显式选择且条件合格时委托并发 skill；共享完整审查、验收与本地/PR 交付闭环。\"",
    "new": "  short_description: \"串行执行批准的计划（index.md + tasks/ + progress.yaml；存量单文件按原样读取）：消费 seam、每票红绿与契约自检，重构候选交收尾；多维审查、验收和本地/PR 对账闭环。\""
  },
  {
    "path": "skills/executing-plans-parallel/agents/openai.yaml",
    "old": "  short_description: \"显式选择后以独立 worktree 执行可并发任务；先声明模型与思考强度，主线程独占进度和集成、阻塞票先核验再接管，支持任务边界切换、中断恢复、验证基线推进与全局收尾。\"",
    "new": "  short_description: \"显式 opt-in 并发计划：主线程编排与唯一状态，独立 worktree 完成获批 seam 下的红绿；缺失或冲突 blocked，集成验证后完成。\""
  }
]''')
for change in changes:
    path = Path(change['path'])
    text = path.read_text()
    assert text.count(change['old']) == 1, f"anchor changed: {path}"
    path.write_text(text.replace(change['old'], change['new'], 1))
```

**步骤 4：核验新规则与相关回归**

逐条重新走查步骤 1 的夹具，把新入口的证据位置与 STATIC_MATCH/不匹配写入同一记录；不能只记录“已更新”。执行：

```bash
rtk proxy node --test scripts/tests/plugin-root.test.mjs scripts/tests/plan-single-format.test.mjs scripts/tests/parallel-plan.test.mjs scripts/tests/parallel-integration.test.mjs
rtk proxy node scripts/validate-skills.mjs
rtk proxy node scripts/check-plugin.mjs
rtk git diff --check
```

预期实际选中套件全部通过、0 fail/skip，静态夹具逐项符合；记录实际计数，不照抄历史计数。 修复命中自身变更的失败后重跑受影响检查。

**步骤 5：提交与状态记录**

暂存仅本任务文件和证据，按 index 的分面共存/部分取代说明生成提交正文。实施提交走仓库正常自动发版钩子；进度使用钩子完成后的真实 HEAD，不预写预期 SHA。

```bash
rtk git add skills/executing-plans/SKILL.md skills/executing-plans-parallel/SKILL.md agents/implementer.md skills/executing-plans/references/review-orchestration.md agents/code-reviewer.md skills/executing-plans/agents/openai.yaml skills/executing-plans-parallel/agents/openai.yaml skills/executing-plans/evals/evals.json skills/quick-fix/evals/evals.json skills/executing-plans-parallel/evals/evals.json
rtk proxy node scripts/check-openai-sync.mjs
```

按 index「提交与漂移」先完成 staged drift 自检，再提交 `feat(T04): 贯通串并行消费与收尾审查`。原子更新 progress：状态 completed，记录真实 commit；tests 写实际静态和自动回归证据摘要（不把 STATIC_MATCH 写成真实模型 pass），再单独文档提交进度与记录。按现有串行执行规则做 over/under-building 与契约锚定自检。
