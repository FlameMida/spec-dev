### 任务 1：统一 TDD 落点、红绿与例外定义

**文件**：
- 修改：`skills/test-driven-development/SKILL.md`
- 修改：`skills/test-driven-development/agents/openai.yaml`
- 修改：`skills/quick-fix/SKILL.md`
- 修改：`skills/quick-fix/agents/openai.yaml`
- 修改：`skills/test-driven-development/references/testing-anti-patterns.md`
- 修改：`skills/test-driven-development/evals/evals.json`
- 修改：`skills/quick-fix/evals/evals.json`
- 修改：`scripts/tests/plugin-root.test.mjs`
- 记录：`.spec-dev/2026-09-06-02-tdd-seam/execution/serial/T01-eval-review.md` 与验证日志；状态只写 `plan/progress.yaml`。

**接口**：
- 消费：T00 已核验的隔离工作区；spec 的已批准 seam/红绿/纯重构/例外契约。
- 产出：test-driven-development：获批 seam 消费、行为红绿/纯重构保绿分流、例外单点；quick-fix 引用。
- Spec/Scenario：`.spec-dev/2026-09-06-02-tdd-seam/spec/tdd-seam-design.md`；S03 S04 S05 S06 S07 S16 S17 S18 S19 S20 S21 S22 S23 S24。静态夹具的唯一主属见本任务新增 eval，其余为消费约束或 T05 复核。
- Lane：PR（读取仓库文件的静态检查不是纯内存 fast）；本任务使用获批 spec 的文档行为验证方式，静态红/绿只能标记规则差异，不能冒充真实模型执行。

**步骤 1：写失败场景与确定性回归**

在隔离工作区用 `rtk proxy python3 - <<'PY'` 执行以下 Python 块（尾部补单独一行 `PY`），只先改 eval/测试。条目已经把 GIVEN/WHEN 转为输入、THEN 转为独立期望；保留其他条目。

```python
import json
from pathlib import Path
additions = json.loads(r'''{
  "skills/test-driven-development/evals/evals.json": [
    {
      "id": "tdd-seam-s18",
      "prompt": "S18 各入口与图示一致：\nTDD 正文、DOT、description/openai、反模式 5、串行和 quick-fix 引用及已有 eval"
    },
    {
      "id": "tdd-seam-s19",
      "prompt": "S19 纯重构复用已有保护：\n已授权的结构整理不改变公共接口且已有相关行为测试"
    },
    {
      "id": "tdd-seam-s20",
      "prompt": "S20 缺少保护先刻画：\n待整理路径缺乏公共行为保护测试"
    },
    {
      "id": "tdd-seam-s21",
      "prompt": "S21 并发红绿协议不被豁免：\nimplementer 被派发的票实际只需纯重构保护，无法提供新增行为的有效红证据"
    }
  ],
  "skills/quick-fix/evals/evals.json": [
    {
      "id": "tdd-seam-s22",
      "prompt": "S22 已授权纯措辞调整：\n用户已授权一次不改变规则的错别字修正及其免测处理"
    },
    {
      "id": "tdd-seam-s23",
      "prompt": "S23 技能行为不是纯文案：\n修改 Markdown 中的执行规则、API 语义或验收预期"
    },
    {
      "id": "tdd-seam-s24",
      "prompt": "S24 单点迁移及旧断言处置：\nquick-fix 当前含自有纯文案例外，plugin-root 测试要求保留它"
    }
  ]
}''')
for name, entries in additions.items():
    path = Path(name)
    data = json.loads(path.read_text())
    present = {entry['id'] for entry in data['evals']}
    assert not present.intersection(entry['id'] for entry in entries), name
    data['evals'].extend(entries)
    path.write_text(json.dumps(data, ensure_ascii=False, indent=2) + '\n')
path = Path('skills/test-driven-development/evals/evals.json')
data = json.loads(path.read_text())
item = next(item for item in data['evals'] if item['id'] == 'tdd-red-first')
item['expected_output'] = '沿获批 seam 先写失败测试，确认目标行为失败后最小实现转绿；重构候选交收尾，不插入循环，不先写实现。'
path.write_text(json.dumps(data, ensure_ascii=False, indent=2) + '\n')
```

替换存量 Scenario 测试（保留文件其他测试）：

```python
import json
from pathlib import Path
changes = json.loads(r'''[
  {
    "path": "scripts/tests/plugin-root.test.mjs",
    "old": "test(\"Scenario: 清单不再复述\", () => {\n  const qf = read(\"skills/quick-fix/SKILL.md\");\n  assert.doesNotMatch(qf, /配置文件\\/纯文案\\/一次性原型/, \"不应并列复述 TDD 例外清单\");\n  assert.ok(qf.includes(\"例外清单以 test-driven-development skill 为准\"), \"应改为引用 TDD 清单\");\n  assert.ok(qf.includes(\"纯文案\"), \"quick-fix 自有例外应显式保留\");\n});\n\n",
    "new": "test(\"Scenario: S24 单点迁移及旧断言处置\", () => {\n  const qf = read(\"skills/quick-fix/SKILL.md\");\n  const tdd = read(\"skills/test-driven-development/SKILL.md\");\n  assert.ok(qf.includes(\"例外清单以 test-driven-development skill 为准\"));\n  assert.doesNotMatch(qf, /quick-fix 另视纯文案|自有纯文案例外/);\n  assert.match(tdd, /例外清单[\\s\\S]*纯文案/);\n  assert.match(tdd, /授权/);\n});\n\n"
  }
]''')
for change in changes:
    path = Path(change['path'])
    text = path.read_text()
    assert text.count(change['old']) == 1, f"anchor changed: {path}"
    path.write_text(text.replace(change['old'], change['new'], 1))
```

**步骤 2：确认旧规则差异及实际红**

逐条读取刚增加/修改的 eval 与当前入口，保存「ID｜输入｜预期｜旧规则依据｜缺失或矛盾」至本任务 eval-review。预期是本 spec 具名场景在旧规则中未完整承载；有已存在的近义机制就准确记录差异，不能把静态评读或立即符合叫作有效行为红。若所需行为已完整存在，停止对应冗余修改并记录事实。

运行 `rtk proxy node --test --test-name-pattern='S24 单点迁移' scripts/tests/plugin-root.test.mjs`，预期 exit 1，因旧 quick-fix 仍含自有例外而断言失败；该定向命令的未选中测试是 selection skip，只证明目标红，步骤 4 全文件必须无 skip。保存原始退出码和日志，环境报错不当红。

**步骤 3：应用完整变更**

下面是逐项完整替换；在隔离工作区执行。每个旧锚点必须唯一存在，漂移时停止对应修改、读当前事实后按 index 偏差规则处理，不直接覆盖整文件。

```python
import json
from pathlib import Path
changes = json.loads(r'''[
  {
    "path": "skills/test-driven-development/SKILL.md",
    "old": "  测试驱动开发纪律——实现任何功能或修复任何 bug、在写实现代码之前使用。铁律：没有失败测试就没有生产代码。先写测试、看它失败、再写最小实现让它通过；红-绿-重构循环。例外（一次性原型、生成代码、配置文件）需征得用户同意。",
    "new": "  测试驱动开发纪律——实现功能、修复 bug 或改变行为前，沿已批准的公共测试落点先写失败测试，确认有效红后最小实现转绿。纯重构在收尾用行为测试保绿。例外按单点清单与可追溯授权处理。"
  },
  {
    "path": "skills/test-driven-development/SKILL.md",
    "old": "**总是**：新功能、bug 修复、重构、行为变更。",
    "new": "**总是**：新功能、bug 修复、行为变更走红绿循环；纯重构走本文「收尾纯重构」，不伪造红。"
  },
  {
    "path": "skills/test-driven-development/SKILL.md",
    "old": "**例外（需征得用户同意）**：一次性原型、生成代码、配置文件。",
    "new": "**例外清单（单点定义）**：一次性原型、生成代码、配置文件，以及不改变行为、契约或测试预期的纯文案。例外需要可追溯的用户授权；同一范围内已授权则引用原决定，不反复询问。技能规则、API 语义与测试预期即使写在 Markdown 中，也不是纯措辞。"
  },
  {
    "path": "skills/test-driven-development/SKILL.md",
    "old": "## 铁律",
    "new": "## 测试落点确认与消费\n\n测试落点（seam）是观察公共行为的契约边界，可为模块函数、CLI、HTTP API、数据库适配器，不限于最外层界面。优先复用已有落点，选择仍能稳定观察目标行为的较高层接口，尽量少建新接口；一个落点可覆盖多个 Scenario，不逐私有函数制造测试。\n\nspec/plan 已批准的落点直接使用，不重复确认。存量无 seam 标签但批准的精确接口和 Scenario 唯一指向同一边界时，只提取已有决定并记录来源，不迁移旧计划格式或推测新决定。\n\n没有唯一落点或 spec/plan 冲突时，停止受影响任务的测试和实现，主线程走原澄清/偏差流程，不默默择一覆盖。即兴任务在写测试前确认公共接口和落点，quick-fix 并入已有修复确认环节，一次一题。implementer 回报 blocked，不向用户另开门、不改 plan 或扩大 writes。\n\n## 铁律"
  },
  {
    "path": "skills/test-driven-development/SKILL.md",
    "old": "没有失败测试，就没有生产代码",
    "new": "没有有效失败测试，就不写新增或改变行为的生产代码"
  },
  {
    "path": "skills/test-driven-development/SKILL.md",
    "old": "先写了代码？删掉，重来。**没有例外**：不留作\"参考\"、不\"边写测试边改造它\"、不看它。删除就是删除，从测试出发重新实现。",
    "new": "未按纪律先写了行为实现？删除本次未经测试先行的实现，从失败测试重做，不留作参考；不删除无关既有工作。纯重构与已授权例外按对应规则处理，不冒充新增行为的红证据。"
  },
  {
    "path": "skills/test-driven-development/SKILL.md",
    "old": "## 红-绿-重构",
    "new": "## 红绿循环"
  },
  {
    "path": "skills/test-driven-development/SKILL.md",
    "old": "    refactor [label=\"重构\\n清理\", shape=box, style=filled, fillcolor=\"#ccccff\"];\n",
    "new": ""
  },
  {
    "path": "skills/test-driven-development/SKILL.md",
    "old": "    verify_green -> refactor [label=\"是\"];\n",
    "new": ""
  },
  {
    "path": "skills/test-driven-development/SKILL.md",
    "old": "    refactor -> verify_green [label=\"保持绿\"];\n",
    "new": ""
  },
  {
    "path": "skills/test-driven-development/SKILL.md",
    "old": "    verify_green -> next;",
    "new": "    verify_green -> next [label=\"是\"];"
  },
  {
    "path": "skills/test-driven-development/SKILL.md",
    "old": "写一个最小测试，表达应该发生什么：一个行为、清晰命名、测真实代码（万不得已才 mock）。",
    "new": "在获批 seam 写一个最小测试：一个公共行为、清晰命名、独立真值。mock 准入遵循 references/testing-anti-patterns.md，不用自己的算法重算期望值。"
  },
  {
    "path": "skills/test-driven-development/SKILL.md",
    "old": "**强制步骤，永不跳过。** 运行测试，确认：失败而非报错、失败信息符合预期、失败原因是功能缺失（不是拼写错误）。",
    "new": "**行为红绿路径的强制步骤。** 确认失败源于目标行为缺失，而非拼写、编译或环境故障；刻画已有行为遵循「收尾纯重构」，不冒充红证据。"
  },
  {
    "path": "skills/test-driven-development/SKILL.md",
    "old": "- **测试直接通过了？** 你在测既有行为，改测试。",
    "new": "- **行为缺陷测试直接通过了？** 核实是否复现目标缺陷并修正无效测试。纯重构刻画应先观察当前行为通过，不适用本条。"
  },
  {
    "path": "skills/test-driven-development/SKILL.md",
    "old": "- **测试失败？** 修代码，不是修测试。",
    "new": "- **公共行为回归？** 修实现。已核实行为未变但测试绑定私有细节时，按反模式 7 修正落点，不迁就脆弱断言。"
  },
  {
    "path": "skills/test-driven-development/SKILL.md",
    "old": "### 重构——清理\n\n只在全绿后：去重、改名、提取辅助函数。保持全绿，不加行为。",
    "new": "### 重构候选\n\n发现重复、命名或结构清理机会时，记录位置、理由与保护证据，交既有收尾，不插入每次红绿循环。每票第五步仍为提交；契约自检只查 over/under-building 与接口锚定。"
  },
  {
    "path": "skills/test-driven-development/SKILL.md",
    "old": "## 为什么顺序重要",
    "new": "## 收尾纯重构\n\n先核实授权范围及公共行为不变，再用已有行为测试记录调整前后的通过证据；缺保护时，先依据现行契约补行为刻画并观察当前实现通过，再结构调整。刻画通过不是红证据；若当前行为与需求不同，转缺陷复现红绿路径。\n\n有计划则由现有收尾审查接收候选，修复后复审受影响维度；quick-fix 使用自身收尾，不强制开启可选 acceptance-qa。处置沿已有授权边界。implementer 无法提供有效红证据的纯重构票回报 blocked，由主线程按现有串行分流处理，不改结果 schema 或普通实施票的红绿条件。\n\n## 为什么顺序重要"
  },
  {
    "path": "skills/test-driven-development/SKILL.md",
    "old": "- 代码先于测试；测试后补；测试立即通过",
    "new": "- 新增/改变行为的代码先于有效失败测试；缺陷测试未复现就立即通过"
  },
  {
    "path": "skills/test-driven-development/SKILL.md",
    "old": "**以上任何一条出现 = 删代码，从 TDD 重来。**",
    "new": "**核实违反红绿纪律后，删除本次违规行为实现并重做。** 纯重构刻画和已授权例外不误判为违规。"
  },
  {
    "path": "skills/test-driven-development/SKILL.md",
    "old": "- [ ] 每个新函数/方法都有测试",
    "new": "- [ ] 获批 seam 的公共行为、相关 Scenario、边缘与错误路径已有覆盖，不按新增函数数量要求直测"
  },
  {
    "path": "skills/test-driven-development/SKILL.md",
    "old": "- [ ] 每个测试都看着它失败过",
    "new": "- [ ] 新增/改变行为的测试观察到有效红；纯重构另有前后绿，刻画不冒充红"
  },
  {
    "path": "skills/test-driven-development/SKILL.md",
    "old": "- [ ] 每次失败原因正确（功能缺失，不是拼写错误）",
    "new": "- [ ] 红证据源于行为缺失，编译/环境故障、零测试、SKIP 不当 PASS"
  },
  {
    "path": "skills/test-driven-development/SKILL.md",
    "old": "- [ ] 每个测试都用最小实现通过",
    "new": "- [ ] 行为红绿使用最小实现；纯重构保持已有契约"
  },
  {
    "path": "skills/test-driven-development/SKILL.md",
    "old": "- [ ] 全部测试通过",
    "new": "- [ ] 目标及相关测试通过，计划最终全量和范围外失败裁决保持"
  },
  {
    "path": "skills/test-driven-development/SKILL.md",
    "old": "勾不满？你跳过了 TDD。重来。",
    "new": "按适用路径核对，不把其他路径的证据冒充自己的完成依据；例外有授权来源，重构候选已交收尾。"
  },
  {
    "path": "skills/test-driven-development/SKILL.md",
    "old": "生产代码 → 存在先失败过的测试\n否则 → 不是 TDD",
    "new": "新增或改变行为 → 有效红 → 最小实现 → 绿\n纯重构 → 行为保护 → 结构调整 → 保持绿\n例外 → 唯一清单及可追溯授权；普通实施票的红绿条件不变"
  },
  {
    "path": "skills/test-driven-development/agents/openai.yaml",
    "old": "  short_description: \"TDD 纪律：没有失败测试就没有生产代码；红-绿-重构循环与测试反模式防护。适用于实现功能或修 bug 之前；例外（一次性原型、生成代码、配置文件）需征得用户同意\"",
    "new": "  short_description: \"TDD 纪律：沿获批公共落点先红后绿；纯重构在收尾保绿；例外单点与授权复用；独立真值和测试反模式防护。\""
  },
  {
    "path": "skills/quick-fix/SKILL.md",
    "old": "- **强制 TDD**：先写复现 bug 的失败测试 → 确认红（失败原因是功能缺陷而非拼写）→ 最小实现转绿 → 重构。遵循 test-driven-development skill 铁律；例外清单以 test-driven-development skill 为准，quick-fix 另视纯文案改动为例外（均需用户同意）。",
    "new": "- **强制 TDD**：失败复现 → 确认有效红 → 最小实现 → 确认绿，遵循 test-driven-development；例外清单以 test-driven-development skill 为准，按其授权纪律处理，不另设自有例外。写测试前复用获批 seam，无唯一落点时并入步骤 3 原确认，不重复询问已有决定。重构候选记录位置、理由和保护证据，交自身修复收尾；纯重构遵循 TDD「收尾纯重构」，不因此强制开启可选验收。"
  },
  {
    "path": "skills/quick-fix/agents/openai.yaml",
    "old": "  short_description: \"轻量 bug 修复工作流：已决定要修、无设计空间的小修复——定位根因（含 spec 反查）、逐题校对、TDD 修复、可选验收；按契约影响分流规避 spec 漂移，涉及跨 spec 契约/跨模块/新依赖时升级 requirement-analysis。不适用于新功能或有设计空间的需求（用 requirement-analysis），也不适用于尚未决定要不要做的想法（用 exploring）。\"",
    "new": "  short_description: \"小修复：根因与契约逐题校对，沿已确认落点红绿修复；TDD 例外和纯重构引用单点，验收保持可选。\""
  },
  {
    "path": "skills/test-driven-development/references/testing-anti-patterns.md",
    "old": "TDD 循环走完（红→绿→重构）才能声称完成。",
    "new": "新增/改变行为完成有效红→最小实现→绿后，按既有任务与收尾纪律完成交付；纯重构遵循 TDD「收尾纯重构」，不插入每个循环。"
  },
  {
    "path": "skills/test-driven-development/SKILL.md",
    "old": "- [ ] 测试用真实代码（mock 仅在万不得已时）",
    "new": "- [ ] 测试验证真实行为，mock 遵循 testing-anti-patterns 的准入与声明依赖边界"
  }
]''')
for change in changes:
    path = Path(change['path'])
    text = path.read_text()
    assert text.count(change['old']) == 1, f"anchor changed: {path}"
    path.write_text(text.replace(change['old'], change['new'], 1))
```

**步骤 4：核验新规则与相关回归**

逐条重新走查步骤 1 的夹具，把新入口的证据位置与 STATIC_MATCH/不匹配写入同一记录；不能只记录“已更新”。执行：

```bash
rtk proxy node --test scripts/tests/plugin-root.test.mjs scripts/tests/plan-single-format.test.mjs scripts/tests/parallel-plan.test.mjs scripts/tests/parallel-integration.test.mjs
rtk proxy node scripts/validate-skills.mjs
rtk proxy node scripts/check-plugin.mjs
rtk git diff --check
```

预期实际选中套件全部通过、0 fail/skip，静态夹具逐项符合；记录实际计数，不照抄历史计数。另核对铁律、确认红、立即通过、Red Flags、完成清单、最终规则、DOT 及 quick-fix 均区分纯重构和行为红绿。 修复命中自身变更的失败后重跑受影响检查。

**步骤 5：提交与状态记录**

暂存仅本任务文件和证据，按 index 的分面共存/部分取代说明生成提交正文。实施提交走仓库正常自动发版钩子；进度使用钩子完成后的真实 HEAD，不预写预期 SHA。

```bash
rtk git add skills/test-driven-development/SKILL.md skills/test-driven-development/agents/openai.yaml skills/quick-fix/SKILL.md skills/quick-fix/agents/openai.yaml skills/test-driven-development/references/testing-anti-patterns.md skills/test-driven-development/evals/evals.json skills/quick-fix/evals/evals.json scripts/tests/plugin-root.test.mjs
rtk proxy node scripts/check-openai-sync.mjs
```

按 index「提交与漂移」先完成 staged drift 自检，再提交 `feat(T01): 统一 TDD 落点、红绿与例外定义`。原子更新 progress：状态 completed，记录真实 commit；tests 写实际静态和自动回归证据摘要（不把 STATIC_MATCH 写成真实模型 pass），再单独文档提交进度与记录。按现有串行执行规则做 over/under-building 与契约锚定自检。
