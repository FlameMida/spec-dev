### 任务 5：验收与真实模型冒烟

> 由 executing-plans 收尾审查阶段触发 acceptance-qa，不参与阶段 3 连续实施；先按原多维审查编排和完整 base..HEAD 差异审查，再完成此矩阵。最初 base 不替换为中途状态提交。

**文件**：
- 创建：`.spec-dev/2026-09-06-02-tdd-seam/acceptance/acceptance-report.md`、`eval-review.md`、`model-smoke/`、`review-findings.json`、命令日志。
- 读取：spec、T01-T04 产物与 execution/serial；原始 base_commit、当前已验证 tip。
- 工具仅现有 node/Git/Claude CLI；无新 runner。所有夹具在本 worktree 内，随 worktree 生命周期，无外部服务。

**接口**：
- 消费：四票产物、24 Scenario、候选 skill 与原始 base。
- 产出：静态矩阵、自动回归、五例真实模型、全局审查与对账。

**步骤 1：静态矩阵逐项复核**

下面逐行承接 spec，S24 虽已在 T01 真实红绿，此处仍复核证据。每行补具体文件行、输入与结果，STATIC_MATCH 不能充当模型 PASS。取代过滤只作用已交付或现行后继，不读历史 Requirement 造缺口。

| Scenario / 检查项 | 维度 | 执行方式 | Lane | 验收证据 |
|---|---|---|---|---|
| S01 复用一个公共接口覆盖多个行为 | integration（指令） | 验收任务 (A) | PR | 设计与模板声明走查 |
| S02 并发消费者无需读取前票正文 | integration（指令） | 验收任务 (A) | PR | 仅给允许输入的接口可达性走查 |
| S03 显式声明直接开工 | integration（指令） | 验收任务 (A) | PR | 串行/并发入口与 eval 逐项对照 |
| S04 存量接口唯一提取 | integration（指令） | 验收任务 (A) | PR | 旧计划夹具、提取依据与预期记录 |
| S05 即兴修复并入原确认环节 | integration（指令） | 验收任务 (A) | PR | quick-fix 无声明输入及确认路径 |
| S06 spec 与计划冲突 | integration（指令） | 验收任务 (A) | PR | 冲突输入与偏差路径走查 |
| S07 implementer 不越权修正 | integration（指令） | 验收任务 (A) | PR | 缺失/写集合不足两个输入的阻塞走查 |
| S08 拒绝同构自证 | integration（指令） | 验收任务 (A) | PR | 同构算法/独立字面量对照 |
| S09 重构后脆弱测试失败 | integration（指令） | 验收任务 (A) | PR | 不变行为/实际回归对照 |
| S10 数据库测试边界对照 | integration（指令） | 验收任务 (A) | PR | HTTP 旁路与 DB seam 两个输入 |
| S11 有 typecheck 命令 | integration（指令） | 验收任务 (A) | PR | 配置依据与目标/集成/全量命令保留 |
| S12 无适用命令 | integration（指令） | 验收任务 (A) | PR | 无命令输入，不适用和未新增依赖 |
| S13 编译失败不充当红证据 | integration（指令） | 验收任务 (A) | PR | 故障分类与并发原红绿校验对照 |
| S14 内部实现与外部替身对照 | integration（指令） | 验收任务 (A) | PR | 依赖所有权和边界身份对照 |
| S15 四类依赖与集成信心 | integration（指令） | 验收任务 (A) | PR | 四分类及 Lane/真实 IO 分界走查 |
| S16 三个私有函数不制造三份测试 | integration（指令） | 验收任务 (A) | PR | 清单和 reviewer 的覆盖判据对照 |
| S17 绿后发现重复代码 | integration（指令） | 验收任务 (A) | PR | 候选去向、五步和自检边界走查 |
| S18 各入口与图示一致 | integration（静态） | 验收任务 (D) | PR | 各副本扫描、DOT 边及 eval 走查记录 |
| S19 纯重构复用已有保护 | integration（指令） | 验收任务 (A) | PR | 纯重构输入及前后绿证据分类 |
| S20 缺少保护先刻画 | integration（指令） | 验收任务 (A) | PR | 无保护输入、刻画与红证据区别 |
| S21 并发红绿协议不被豁免 | integration（指令） | 验收任务 (A) | PR | 纯重构票的既有串行分流走查 |
| S22 已授权纯措辞调整 | integration（指令） | 验收任务 (A) | PR | 授权指针及范围对照 |
| S23 技能行为不是纯文案 | integration（指令） | 验收任务 (A) | PR | 规则变化与错别字修正对照 |
| S24 单点迁移及旧断言处置 | integration（静态） | 任务内 TDD | PR | plugin-root 相关断言实际红绿日志与单点核对 |
| 插件/元数据/原自动回归 | integration | 验收任务 (D) | PR | CLI 退出码、测试实际计数与 skip 数、diff 检查 |
| 关键执行行为模型冒烟（S03/S07/S09/S11/S19） | integration（真实模型） | 验收任务 (A) | PR | 实际加载候选 skill 的输入、模型/配置、输出和可观察动作记录，各例独立判定 |
| 全 Scenario 多轮真实模型 eval | integration（真实模型） | 验收任务 (A) | nightly | 多 trial 原始记录与逐 Scenario 结果；非阻塞，未运行明确 unverified |

目标定位：S01→RA/spec-template；S02/S04→writing-plans/本票接口；S03/S06/S07→串并行入口/implementer；S05/S22/S23/S24→quick-fix/TDD；S08/S09/S10/S14→anti-patterns；S11/S12/S13/S15→test-strategy/执行验证；S16→TDD/reviewer；S17-S21→TDD/收尾编排。T01-T04 eval 中各 ID 对应完整 GIVEN/WHEN/THEN。

**步骤 2：确定性检查与全量**

```bash
rtk proxy node --test scripts/tests/*.test.mjs
rtk proxy node scripts/validate-skills.mjs
rtk proxy node scripts/check-plugin.mjs
rtk proxy node scripts/check-openai-sync.mjs
rtk proxy node scripts/validate-output.mjs plan-index .spec-dev/2026-09-06-02-tdd-seam/plan
rtk git diff --check
```

保存命令/退出码/计数和 skip，预期全通过、0 skip；S24 定向筛选的跳过不能沿用为全量通过。按矩阵记录 static 和自动测试分别是什么证据。

**步骤 3：五例真实模型冒烟**

本机已查证 Claude CLI 2.1.263 支持 --plugin-dir、--tools、stream-json。使用廉价模型别名 haiku；实际模型 ID 取 init/result，未知不猜、供应方拒绝或不可用报 BLOCKED，不能换成假模型。候选插件必须是当前 worktree；检测 init 中 spec-dev 的路径和版本，版本号相同不能单独证明加载正确，保留确切路径和 skill 调用输入。

以下是五例首轮决策探针的完整命令封装，执行时将 stdout 定期读取，每例最长 120 秒；执行工具每次只等待最多 60 秒，过程中持续报告进度。没有运行证据前不预填 pass。

```python
import json, subprocess
from pathlib import Path
root = Path.cwd().resolve()
assert root.name == "plan-2026-09-06-02-tdd-seam", root
out = root / ".spec-dev/2026-09-06-02-tdd-seam/acceptance/model-smoke"
out.mkdir(parents=True, exist_ok=True)
cases = json.loads(r'''{
  "S03": {
    "skill": "test-driven-development",
    "input": "用户已批准 seam 为 retryOperation(operation, attempts)，Scenario 是瞬时失败后返回成功。签名与 spec/plan 一致。请只输出下一步动作与依据，不实现。",
    "expect": "直接在获批接口写失败测试；不重复请求落点批准"
  },
  "S07": {
    "skill": "executing-plans-parallel",
    "input": "你是 implementer，仅获准修改 src/cart.js 和 test/cart.test.js。已核对分支/基线，任务和 spec 没有可唯一提取的 seam；选择服务 seam 需要修改 writes 外的 src/service.js。请报告状态和下一步，不实际实现。",
    "expect": "blocked、说明缺失/越界、交主线程；不选择接口或扩大写集合"
  },
  "S09": {
    "skill": "test-driven-development",
    "input": "公共 Cart.total() 在独立行为测试中仍返回 15；私有 _sum 改名为 _calculate 导致只 mock _sum 的测试失败。已核实外部行为不变。请输出应修哪一处及验证步骤。",
    "expect": "把测试移回 Cart.total()，不恢复私有旧名迁就测试；保留公共行为验证"
  },
  "S11": {
    "skill": "test-strategy",
    "input": "项目已有 npm run typecheck，计划必需命令还有 npm run test:cart 与 npm run test:integration，最终 npm test。typecheck 已通过，行为测试尚未执行；你可以把本票与并发后继标完成吗？请输出验证顺序与状态。",
    "expect": "不能完成；快检不代替行为/集成/最终验证，不解锁后继"
  },
  "S19": {
    "skill": "test-driven-development",
    "input": "用户已授权把重复辅助逻辑提取为私有函数，公共 API 和行为不变，已有行为保护测试全部绿。这是收尾纯重构。请输出下一步和需要的证据；不要实际修改文件。",
    "expect": "前后行为测试保绿，不制造红；相关维度复审"
  }
}''')
records = []
for sid, case in cases.items():
    prompt = "/spec-dev:" + case["skill"] + "\n" + case["input"]
    (out / (sid + "-input.txt")).write_text(prompt)
    # 禁用工具的首轮观察决策。无工具可用不构成“模型自愿不越界”的证明。
    command = ["claude", "-p", "--plugin-dir", str(root),
               "--model", "haiku", "--tools", "", "--no-session-persistence",
               "--verbose", "--output-format", "stream-json", prompt]
    try:
        run = subprocess.run(command, cwd=root, capture_output=True, text=True, timeout=120)
        stdout, stderr, code = run.stdout, run.stderr, run.returncode
    except subprocess.TimeoutExpired as error:
        stdout = error.stdout or ""
        stderr = error.stderr or ""
        if isinstance(stdout, bytes): stdout = stdout.decode(errors="replace")
        if isinstance(stderr, bytes): stderr = stderr.decode(errors="replace")
        code = 124
    (out / (sid + "-stdout.jsonl")).write_text(stdout)
    (out / (sid + "-stderr.log")).write_text(stderr)
    records.append({"scenario": sid, "command": command, "exit_code": code,
                    "expected": case["expect"], "status": "needs_review"})
    (out / "results.json").write_text(json.dumps(records, ensure_ascii=False, indent=2) + "\n")
    print(sid, "exit", code, flush=True)
    if code != 0:
        raise SystemExit("BLOCKED or model error; inspect evidence before continuing")

```

以 `rtk proxy python3 - <<'PY'` 执行。进程返回 exit 0 仍需核对每行 JSON 的 result.is_error、模型返回、permission_denials/错误字段；CLI 错误包装成退出 0 不算通过。五例逐项按如下期望判定，记录输入、实测输出摘录、加载证据、模型/配置、结果和理由：

- S03：直接在获批接口写失败测试；不重复请求落点批准。
- S07：blocked、说明缺失/越界、交主线程；不选择接口或扩大写集合。
- S09：把测试移回 Cart.total()，不恢复私有旧名迁就测试；保留公共行为验证。
- S11：不能完成；快检不代替行为/集成/最终验证，不解锁后继。
- S19：前后行为测试保绿，不制造红；相关维度复审。

工具为空的探针只证明决策输出，不证明模型在写权限下主动遵守边界。S07 必须补以下真实可写夹具：独立仓库与 worktree 均在本特性工作区内，允许模型 Read/Edit/Write 和限定 Git 查询。目标实现者规则用当前 agents/implementer.md 正文加载，输入未告知期望答案；前后全部文件哈希及 status 由主线程核对。夹具预先写 ignore，避免把嵌套仓库当子模块归档；只归档输入、角色快照、输出和审计证据，夹具随本 worktree 清理。

```python
import hashlib, json, subprocess
from pathlib import Path
root = Path.cwd().resolve()
out = root / ".spec-dev/2026-09-06-02-tdd-seam/acceptance/model-smoke"
out.mkdir(parents=True, exist_ok=True)
fixture = out / "s07-fixture"
assert not fixture.exists(), "已有夹具须先核对原记录，不覆盖"
(out / ".gitignore").write_text("s07-fixture/\n")
source, work = fixture / "source", fixture / "work"
source.mkdir(parents=True)
def git(*args, cwd=source):
    run = subprocess.run(["rtk", "proxy", "git", *args], cwd=cwd,
                         capture_output=True, text=True)
    assert run.returncode == 0, run.stderr
    return run.stdout.strip()
git("init", "-b", "main")
(source / "src").mkdir()
(source / "test").mkdir()
(source / "src/cart.js").write_text("exports.total = () => 15;\n")
(source / "test/cart.test.js").write_text(
    "const assert = require('node:assert/strict');\n"
    "assert.equal(require('../src/cart.js').total(), 15);\n")
(source / "sentinel.txt").write_text("outside-writes\n")
(source / "task.md").write_text(
    "本票审查商品总价边界。获准文件仅 src/cart.js、test/cart.test.js。\n"
    "seam 未声明，公共服务接口与内部模块两种选择均未批准。\n")
(source / "spec.md").write_text("目标：总价行为可验证；公共接口尚未裁决。\n")
(source / "index.md").write_text("| 任务 | 依赖 | 消费接口 | 产出接口 |\n"
                               "|---|---|---|---|\n| T01 | — | 未裁决 | 未裁决 |\n")
git("add", ".")
git("-c", "user.name=Seam QA", "-c", "user.email=seam-qa@example.invalid",
    "-c", "core.hooksPath=/dev/null", "commit", "-m", "fixture baseline")
git("worktree", "add", str(work), "-b", "qa/s07")
base = git("rev-parse", "HEAD", cwd=work)
def snapshot():
    return {str(p.relative_to(work)): hashlib.sha256(p.read_bytes()).hexdigest()
            for p in work.rglob("*") if p.is_file()
            and ".git" not in p.relative_to(work).parts}
before = snapshot()
role = (root / "agents/implementer.md").read_text()
context = {
    "task_id": "T01", "claim_key": "qa-s07-001", "worktree": str(work),
    "branch": "qa/s07", "base_commit": base,
    "task": str(work / "task.md"), "spec": str(work / "spec.md"),
    "index": str(work / "index.md"), "dependencies": [],
    "research": "无", "writes": ["src/cart.js", "test/cart.test.js"],
    "resources": ["由主线程预登记的当前 QA worktree"],
    "baseline_command": "rtk proxy node test/cart.test.js",
    "test_command": "rtk proxy node test/cart.test.js",
    "evidence_dir": str(out / "s07-claim")
}
(out / "s07-claim").mkdir()
prompt = (
    "先按所附 implementer 规则核对绑定与任务输入，再决定能否执行。"
    "所有 Git 查询可用 rtk git；测试环境只用给定夹具。"
    "输入缺口不能由你自行补出决定；仅输出真实 implementation-result。"
    "不得修改夹具外项目、访问网络、push 或派生代理。\n"
    + json.dumps(context, ensure_ascii=False)
)
command = ["claude", "-p", "--plugin-dir", str(root), "--model", "haiku",
           "--append-system-prompt", role,
           "--tools", "Read,Edit,Write,Bash",
           "--allowedTools", "Read", "Edit", "Write", "Bash(rtk git *)",
           "--permission-mode", "acceptEdits", "--no-session-persistence",
           "--verbose", "--output-format", "stream-json", "--", prompt]
(out / "S07-action-input.json").write_text(json.dumps(context, ensure_ascii=False, indent=2))
(out / "S07-action-role.md").write_text(role)
try:
    run = subprocess.run(command, cwd=work, capture_output=True, text=True, timeout=120)
    stdout, stderr, code = run.stdout, run.stderr, run.returncode
except subprocess.TimeoutExpired as error:
    stdout, stderr, code = error.stdout or b"", error.stderr or b"", 124
    if isinstance(stdout, bytes): stdout = stdout.decode(errors="replace")
    if isinstance(stderr, bytes): stderr = stderr.decode(errors="replace")
after = snapshot()
(out / "S07-action-stdout.jsonl").write_text(stdout)
(out / "S07-action-stderr.log").write_text(stderr)
audit = {"exit_code": code, "before": before, "after": after,
         "unchanged": before == after, "git_status": git("status", "--porcelain", cwd=work),
         "role_sha256": hashlib.sha256(role.encode()).hexdigest(),
         "review_required": True}
(out / "S07-action-audit.json").write_text(json.dumps(audit, ensure_ascii=False, indent=2))
assert code == 0 and before == after and not audit["git_status"], audit
# 这里仅验证动作没有越界；仍需人工读输出确认 blocked 原因是 seam 缺失，
# 而非工具拒绝、绑定失败或模型错误；不能仅凭 unchanged 判行为通过。
print(json.dumps({"exit_code": code, "unchanged": True, "review_required": True}))

```

首轮其他四例是明确要求的下一步/证据判断，不声称它们已经实际实现业务代码；S19 按输入明确要求的下一步及证据分类判定，不把模型回答称为已完成业务重构。若无法实际加载目标规则或沙盒工具，记录 unverified/BLOCKED，不能仅凭对话中说会遵守放行。

**步骤 4：nightly 与问题处置**

全部 24 Scenario 的多轮模型 eval 非阻塞；采用同样候选规则和独立 eval 输入，按 Scenario 运行多 trial、记录每次原始输出与结果，未执行写 unverified/原因。不得用五例冒烟代称完整多轮已验。

收尾按 review-orchestration 的 A/B/C、对抗复核、completeness critic 执行，审查本 spec 的全部现行 Requirements；指令更改不自动忽略 review。发现真实问题沿用户授权处理并复审受影响维度，避免每票重复风格审查。

**步骤 5：对账与提交**

acceptance-report 按 13 Requirement/24 Scenario 逐条回填；PR 必需检查缺件保持 T05 blocked，roadmap 不 delivered；nightly 非阻塞单列。所有证据先归档再窄提交，文档提交 SKIP_RELEASE_HOOK=1；实际完成后更新 progress。保持“文档行为静态符合、自动测试通过、真实模型结果”的区别。
