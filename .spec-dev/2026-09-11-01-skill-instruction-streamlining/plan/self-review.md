# 实施计划自检

日期：2026-09-11。主线程完成；没有派子代理自审计划，没有执行生成任务。

## 结论

计划编写与静态自检完成。15 个任务为 T00—T14；19 项整改和 30 项保留约束以本特性已批准 spec 为依据，43 个 Scenario 全部进入实施/验收追溯。所有执行状态仍为 pending。

## 五查结果

| 维度 | 结果与边界 |
|---|---|
| Spec 覆盖 | 19 项均有实施归属，S01—S43 均有结构/契约及适用模型或交付验证；T13 固定 6 个 PR 模型冒烟并列出参数化子例，其他完整评测不静默省略 |
| 完整内容 | 12 个修改票内嵌完整产品/测试 payload；T00、T13、T14 有完整绑定、取证、合并和清理程序。产品技能中的模板变量及负面示例是被编辑内容，不是计划未填项 |
| 类型与调用 | 66 段 Python 程序经 ast.parse，22 份拟修改 JavaScript 经 node --input-type=module --check，均 exit 0；仅语法校验，未执行嵌入程序或测试 |
| 导航和文件 | plan-index CLI exit 0；任务 ID、文件和依赖一致；65 处新增真实 Markdown 引用在预计目录树中可达，行内代码及代码围栏中的示例不当真实文件链接 |
| 依赖最小性 | 依赖覆盖真实文件版本、公共 helper、验收及交付顺序；相关回归只消费本票与真实前置票，不隐含依赖尚未执行的独立票；普通串行 v1，无组或并发声明 |

写入前置验证：在内存按任务顺序比较每份 payload 的原内容与前置票产出，未发现错误基线或相互覆盖。任务程序写前检查源 SHA；不匹配时保留差异，不强行套用。

## 实际检查

```sh
rtk proxy node scripts/validate-output.mjs plan-index .spec-dev/2026-09-11-01-skill-instruction-streamlining/plan
```

返回 `ok: true`、`schema: plan-index`，exit 0。

此外实际运行了只生成本次计划的 Python 标准库程序：在内存组织拟修改文本、核对来源版本、检查新链接、解析全部嵌入 Python 程序，并通过 Node stdin 对拟修改 JS 做语法检查。最终检查 exit 0；没有将候选 payload 写入产品位置。

自检过程中修正了：

- 模板中的行内 ADR 链接曾被误识别为实际文件链接，第一次链接检查因此 exit 1；已调整搬迁和测试观察逻辑，保留代码示例原义，最终 65 处真实新链接检查无缺失。
- 自检扫描器曾将程序字符串里的 payload 示例视作真实标记，已限定为整行标记；该修正仅影响本次自检程序。
- 相关回归取消按全部较小编号取票，改为真实前置，避免引入未声明依赖。
- 清理核对原始资源实体并逐项保存结果；失败在存活来源保存 blocked；不以路径前缀或经过时间作为所有权和停止证明。
- 模型批次按明确归档回执定位，判读文件不覆盖；S39 使用该案例真实线程 ID 续接；缺第二轮或 JSONL 不完整不能记调用完整。

## 未运行的工作

- 未创建实施 worktree，未运行 T00—T14。
- 未执行新旧业务/文档契约测试、模型冒烟、完整模型评测或实际验收。
- 未运行未来任务的安装式校验、上游更新或资源清理。
- 未读取旧 `.spec-dev/` 或旧 `.worktrees/` 内容作研究/夹具。
- 本机 `codex exec --help`、`codex exec resume --help` 与官方非交互说明只用于核对待执行命令形态，不证明认证、模型、能力或行为验收通过。

## 交付记录说明

`plan-files.json` 保存计划交接时各文件的内容摘要，服务本轮作者修订核对；不是第二份运行状态。执行后 progress.yaml 正常变化不应拿初始摘要判违规，执行状态只读 progress.yaml。

本轮只提交新计划文件。Spec 激活已单独提交为 `a1feebf6`；计划仍待用户明确开始执行的指令。两个排除技能及上游更新逻辑不变；来源中既有旧档案删除不归本次所有。
