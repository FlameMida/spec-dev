### 任务 T14：最终验证、真实合并与资源收尾

任务类型：delivery
覆盖 Scenario：S30、S43 及全部保留约束的交付边界。

**文件**：
- 读取：全部任务状态、本次验收报告和真实证据。
- 修改：本次 spec 的 sync_commit、本次 progress、验收/执行记录。
- 清理：仅 T00/本计划登记并证实归属的隔离和临时资源。
- 不修改两个排除技能及更新器，不恢复/暂存旧档案删除，不推送或自动发版。

**接口**：
- 消费：T00 的实际来源与资源绑定、T01—T13 completed 记录、实际验证证据。
- 产出：真实目标分支交付、资源处置、sync_commit 和最终 completed/current=null。
- 本票不在实施票连续循环中提前执行；先完成独立收尾审查、反驳、critic 与 T13 对账。

**步骤 1：最终全量验证与失败归属**

在已绑定实施 worktree 执行。全量 Node 套件和包检查在仅含当前产品的受控副本中运行；
其中旧夹具已在 T01 改成合成资料，排除技能的原测试即使产生运行时文件也只影响副本。
原始源文件不被 normalize/重置。该方式不缩减 Node 套件；具体实际文件清单与退出码必须入报告。

```````python
from pathlib import Path
import hashlib,json,os,subprocess,uuid,re,shlex
FEATURE=".spec-dev/2026-09-11-01-skill-instruction-streamlining"
ROOT=Path.cwd().resolve()
STATE=ROOT/FEATURE/"plan/progress.yaml"
def git(*args, cwd=None, check=True):
    r=subprocess.run(["rtk","proxy","git",*args],cwd=cwd or ROOT,capture_output=True,text=True)
    if check and r.returncode:raise RuntimeError(r.stdout+r.stderr)
    return r.stdout.strip()
def read_state(): return json.loads(STATE.read_text())
def binding():
    rows=[n[len("binding:"): ] for n in read_state()["notes"] if n.startswith("binding:")]
    if not rows:raise RuntimeError("T00 has not recorded a binding")
    return json.loads(rows[-1])
def bound():
    b=binding()
    if ROOT!=Path(b["worktree"]).resolve():raise RuntimeError("wrong task cwd")
    if git("branch","--show-current")!=b["branch"]:raise RuntimeError("wrong task branch")
    return b
def doc_commit(paths,message):
    staged=git("diff","--cached","--name-only")
    if staged:raise RuntimeError("unexpected staged files: "+staged)
    git("add","--",*paths)
    r=subprocess.run(["rtk","proxy","git","diff","--cached","--check"],cwd=ROOT)
    if r.returncode:raise RuntimeError("staged whitespace check failed")
    r=subprocess.run(["rtk","proxy","env","SKIP_CODEX_PACKAGE_HOOK=1","SKIP_RELEASE_HOOK=1",
                      "git","commit","-m",message],cwd=ROOT)
    if r.returncode:raise RuntimeError("commit failed; preserve staged files")
    return git("rev-parse","HEAD")
def checkpoint(task,status,commit=None,tests=None,note=None,extra_paths=()):
    state=read_state()
    state["tasks"][task]["status"]=status
    if commit is not None:state["tasks"][task]["commit"]=commit
    if tests is not None:state["tasks"][task]["tests"]=tests
    state["current"]=task if status in ("in_progress","blocked") else None
    if note:state["notes"].append(note)
    temp=STATE.with_name(STATE.name+"."+str(uuid.uuid4())+".tmp")
    with temp.open("x") as out:
        out.write(json.dumps(state,ensure_ascii=False,indent=2)+"\n");out.flush();os.fsync(out.fileno())
    os.replace(temp,STATE)
    return doc_commit([str(STATE.relative_to(ROOT)),*extra_paths],"docs(progress): "+task+" "+status)
def start(task,deps):
    bound();state=read_state()
    if any(state["tasks"][d]["status"]!="completed" for d in deps):raise RuntimeError("dependency incomplete")
    if state["tasks"][task]["status"]=="completed":raise RuntimeError("already completed: resume next ready task")
    if state["tasks"][task]["status"]=="blocked":raise RuntimeError("resolve recorded blocker before resume")
    if state["tasks"][task]["status"]!="in_progress":checkpoint(task,"in_progress")
def payload(task,name):
    text=(ROOT/FEATURE/"plan/tasks"/(task+".md")).read_text()
    fence="~"*16
    prefix="<!-- payload:"+name+" -->\n"+fence+"text\n"
    suffix="\n"+fence+"\n<!-- /payload:"+name+" -->"
    if text.count(prefix)!=1:raise RuntimeError("missing/duplicate payload "+name)
    rest=text.split(prefix,1)[1]
    if suffix not in rest:raise RuntimeError("payload terminator missing")
    return rest.split(suffix,1)[0]
def apply_rows(task,rows):
    bound();prepared=[]
    for row in rows:
        rel=row["path"]
        if rel.startswith(("skills/anysearch/","skills/sequential-thinking/")) or rel in (
            "scripts/update-vendored-skill.mjs","scripts/tests/update-vendored.test.mjs"):
            raise RuntimeError("excluded write: "+rel)
        target=ROOT/rel
        if target.resolve()!=target or not target.is_relative_to(ROOT) or ".." in Path(rel).parts:
            raise RuntimeError("noncanonical write: "+rel)
        content=payload(task,row["payload"])
        after=hashlib.sha256(content.encode()).hexdigest()
        current=hashlib.sha256(target.read_bytes()).hexdigest() if target.exists() else None
        if current==after:continue
        if current!=row["before"]:raise RuntimeError("unexpected source content: "+rel)
        prepared.append((target,content))
    for target,content in prepared:
        target.parent.mkdir(parents=True,exist_ok=True)
        temp=target.with_name(target.name+"."+str(uuid.uuid4())+".tmp")
        with temp.open("x") as out:
            out.write(content);out.flush();os.fsync(out.fileno())
        os.replace(temp,target)
def capture(task,label,argv,expected=0,cwd=None):
    b=bound();run=uuid.uuid4().hex
    base=ROOT/FEATURE/"execution/serial"/task/label/run
    base.mkdir(parents=True)
    actual_cwd=Path(cwd).resolve() if cwd else ROOT
    r=subprocess.run(argv,cwd=actual_cwd,capture_output=True)
    for stream,data in [("stdout",r.stdout),("stderr",r.stderr)]:(base/(stream+".log")).write_bytes(data)
    record={"argv":argv,"cwd":str(actual_cwd),"exit_code":r.returncode,"base_commit":git("rev-parse","HEAD"),
            "stdout_sha256":hashlib.sha256(r.stdout).hexdigest(),"stderr_sha256":hashlib.sha256(r.stderr).hexdigest(),
            "kind":"actual-command-result","expected_exit":expected}
    (base/"record.json").write_text(json.dumps(record,ensure_ascii=False,indent=2)+"\n")
    print(r.stdout.decode(errors="replace"));print(r.stderr.decode(errors="replace"))
    print("RECORD",str(base.relative_to(ROOT))+"/record.json")
    if r.returncode!=expected:
        checkpoint(task,"blocked",tests="fail",note=task+" unexpected exit at "+str(base.relative_to(ROOT)),
                   extra_paths=[str(base.relative_to(ROOT))])
        raise RuntimeError("unexpected exit; actual record saved, preserve working changes")
    return base

import tempfile,shutil
start("T14",["T13"]);b=bound()
# All required acceptance facts must already be independently reconciled in T13.
report=ROOT/FEATURE/"acceptance/acceptance-report.md"
if not report.exists():raise RuntimeError("missing acceptance report")
copy=Path(tempfile.mkdtemp(prefix="skill-streamlining-full-suite-"))
state=read_state();state["resources"].append("validation-copy: "+str(copy)+" —— retain until test evidence archived; remove only this owned copy")
state["notes"].append("T14 validation copy "+str(copy))
state["notes"].append("owned-resource:"+json.dumps({"path":str(copy),"identity":[copy.stat().st_dev,copy.stat().st_ino]}))
temp=STATE.with_name(STATE.name+"."+uuid.uuid4().hex+".tmp")
temp.write_text(json.dumps(state,ensure_ascii=False,indent=2)+"\n");os.replace(temp,STATE)
doc_commit([str(STATE.relative_to(ROOT))],"docs(progress): register full-suite copy")
paths=[p for p in git("ls-files","-z").split("\0") if p]
for rel in paths:
    if rel.startswith((".spec-dev/",".worktrees/")):continue
    src=ROOT/rel
    if src.is_symlink():raise RuntimeError("inspect declared source symlink before copying: "+rel)
    dst=copy/rel;dst.parent.mkdir(parents=True,exist_ok=True);dst.write_bytes(src.read_bytes())
git("init","-q",cwd=copy)
git("-c","core.hooksPath=/dev/null","add",".",cwd=copy)
git("-c","user.name=Validation Fixture","-c","user.email=validation@example.invalid",
    "-c","core.hooksPath=/dev/null","commit","-qm","candidate source snapshot",cwd=copy)
files=[str(p.relative_to(copy)) for p in sorted((copy/"scripts/tests").glob("*.test.mjs"))]
if not files:raise RuntimeError("no tests")
capture("T14","full-node-suite",["rtk","proxy","node","--test",*files],cwd=copy)
capture("T14","plugin-package",["rtk","proxy","node","scripts/check-plugin.mjs"],cwd=copy)
capture("T14","metadata",["rtk","proxy","node","scripts/check-openai-sync.mjs"],cwd=copy)
capture("T14","official-package",["rtk","proxy","node","scripts/check-plugin.mjs","--codex-validate"],cwd=copy)
capture("T14","skill-format",["rtk","proxy","node","scripts/validate-skills.mjs"],cwd=copy)
# Do not let installation-style validation write into the source tree.
# --codex-validate/validate-skills run only in this owned copy;
# missing authentication/dependencies or requested network access must be reported, not marked PASS.
print("FULL_SUITE_COPY",str(copy))
print("Raw logs are in this feature's execution/serial/T14; keep the copy until reconciliation.")
```````
任一失败先归属；本次引入的回归修复后复跑。验证副本中环境/调用失败、零测试、SKIP 不写 PASS。
需要对比来源版本时另建相同受控副本，不在带用户改动的来源目录跑写入性测试。
额外安装式检查若无法运行，报告具体未验证项；不能把仅运行基础包检查称为官方安装验证通过。

**执行中补齐（2026-09-11）**：最终全量初跑 252/253，session-explain 的 inject 测试依赖源仓库已有 spec。按 S43 将其改为独立合成 Git 仓库和已跟踪 spec，保留相同公开 CLI 断言；不读取旧档案、不修改 guardrail 产品。原失败保留，定向复跑及新受控副本全量复跑另记。

**步骤 2：测试退役与取代核对**

本次没有删除行为需求，测试优先迁移到新引用/合成夹具，不依据“旧文件位置断言失败”删除保护。
禁止读取旧 spec 寻找孤儿测试；无法完成的历史退役核对明确不执行，不声称不存在孤儿。
本 spec supersedes 为空，本次不做旧档案取代回写；它不证明历史不存在相交。

**步骤 3：保存真实来源合并**

用户已确认 T00 的六文件版本回退；随后追加 CHANGELOG 汇总并已在来源独立提交。该提交已整合进实施分支，剩余五个版本文件按 acceptance/source-delivery.json 的精确哈希保留，不暂存。原始审查基线不改变。
只有来源分支/HEAD、该状态摘要和不相交条件仍成立时执行安全快进；任何新变化或冲突保留现场，不强行合并。

```````python
from pathlib import Path
import hashlib,json,os,subprocess,uuid,re,shlex
FEATURE=".spec-dev/2026-09-11-01-skill-instruction-streamlining"
ROOT=Path.cwd().resolve()
STATE=ROOT/FEATURE/"plan/progress.yaml"
def git(*args, cwd=None, check=True):
    r=subprocess.run(["rtk","proxy","git",*args],cwd=cwd or ROOT,capture_output=True,text=True)
    if check and r.returncode:raise RuntimeError(r.stdout+r.stderr)
    return r.stdout.strip()
def read_state(): return json.loads(STATE.read_text())
def binding():
    rows=[n[len("binding:"): ] for n in read_state()["notes"] if n.startswith("binding:")]
    if not rows:raise RuntimeError("T00 has not recorded a binding")
    return json.loads(rows[-1])
def bound():
    b=binding()
    if ROOT!=Path(b["worktree"]).resolve():raise RuntimeError("wrong task cwd")
    if git("branch","--show-current")!=b["branch"]:raise RuntimeError("wrong task branch")
    return b
def doc_commit(paths,message):
    staged=git("diff","--cached","--name-only")
    if staged:raise RuntimeError("unexpected staged files: "+staged)
    git("add","--",*paths)
    r=subprocess.run(["rtk","proxy","git","diff","--cached","--check"],cwd=ROOT)
    if r.returncode:raise RuntimeError("staged whitespace check failed")
    r=subprocess.run(["rtk","proxy","env","SKIP_CODEX_PACKAGE_HOOK=1","SKIP_RELEASE_HOOK=1",
                      "git","commit","-m",message],cwd=ROOT)
    if r.returncode:raise RuntimeError("commit failed; preserve staged files")
    return git("rev-parse","HEAD")
def checkpoint(task,status,commit=None,tests=None,note=None,extra_paths=()):
    state=read_state()
    state["tasks"][task]["status"]=status
    if commit is not None:state["tasks"][task]["commit"]=commit
    if tests is not None:state["tasks"][task]["tests"]=tests
    state["current"]=task if status in ("in_progress","blocked") else None
    if note:state["notes"].append(note)
    temp=STATE.with_name(STATE.name+"."+str(uuid.uuid4())+".tmp")
    with temp.open("x") as out:
        out.write(json.dumps(state,ensure_ascii=False,indent=2)+"\n");out.flush();os.fsync(out.fileno())
    os.replace(temp,STATE)
    return doc_commit([str(STATE.relative_to(ROOT)),*extra_paths],"docs(progress): "+task+" "+status)
def start(task,deps):
    bound();state=read_state()
    if any(state["tasks"][d]["status"]!="completed" for d in deps):raise RuntimeError("dependency incomplete")
    if state["tasks"][task]["status"]=="completed":raise RuntimeError("already completed: resume next ready task")
    if state["tasks"][task]["status"]=="blocked":raise RuntimeError("resolve recorded blocker before resume")
    if state["tasks"][task]["status"]!="in_progress":checkpoint(task,"in_progress")
def payload(task,name):
    text=(ROOT/FEATURE/"plan/tasks"/(task+".md")).read_text()
    fence="~"*16
    prefix="<!-- payload:"+name+" -->\n"+fence+"text\n"
    suffix="\n"+fence+"\n<!-- /payload:"+name+" -->"
    if text.count(prefix)!=1:raise RuntimeError("missing/duplicate payload "+name)
    rest=text.split(prefix,1)[1]
    if suffix not in rest:raise RuntimeError("payload terminator missing")
    return rest.split(suffix,1)[0]
def apply_rows(task,rows):
    bound();prepared=[]
    for row in rows:
        rel=row["path"]
        if rel.startswith(("skills/anysearch/","skills/sequential-thinking/")) or rel in (
            "scripts/update-vendored-skill.mjs","scripts/tests/update-vendored.test.mjs"):
            raise RuntimeError("excluded write: "+rel)
        target=ROOT/rel
        if target.resolve()!=target or not target.is_relative_to(ROOT) or ".." in Path(rel).parts:
            raise RuntimeError("noncanonical write: "+rel)
        content=payload(task,row["payload"])
        after=hashlib.sha256(content.encode()).hexdigest()
        current=hashlib.sha256(target.read_bytes()).hexdigest() if target.exists() else None
        if current==after:continue
        if current!=row["before"]:raise RuntimeError("unexpected source content: "+rel)
        prepared.append((target,content))
    for target,content in prepared:
        target.parent.mkdir(parents=True,exist_ok=True)
        temp=target.with_name(target.name+"."+str(uuid.uuid4())+".tmp")
        with temp.open("x") as out:
            out.write(content);out.flush();os.fsync(out.fileno())
        os.replace(temp,target)
def capture(task,label,argv,expected=0,cwd=None):
    b=bound();run=uuid.uuid4().hex
    base=ROOT/FEATURE/"execution/serial"/task/label/run
    base.mkdir(parents=True)
    actual_cwd=Path(cwd).resolve() if cwd else ROOT
    r=subprocess.run(argv,cwd=actual_cwd,capture_output=True)
    for stream,data in [("stdout",r.stdout),("stderr",r.stderr)]:(base/(stream+".log")).write_bytes(data)
    record={"argv":argv,"cwd":str(actual_cwd),"exit_code":r.returncode,"base_commit":git("rev-parse","HEAD"),
            "stdout_sha256":hashlib.sha256(r.stdout).hexdigest(),"stderr_sha256":hashlib.sha256(r.stderr).hexdigest(),
            "kind":"actual-command-result","expected_exit":expected}
    (base/"record.json").write_text(json.dumps(record,ensure_ascii=False,indent=2)+"\n")
    print(r.stdout.decode(errors="replace"));print(r.stderr.decode(errors="replace"))
    print("RECORD",str(base.relative_to(ROOT))+"/record.json")
    if r.returncode!=expected:
        checkpoint(task,"blocked",tests="fail",note=task+" unexpected exit at "+str(base.relative_to(ROOT)),
                   extra_paths=[str(base.relative_to(ROOT))])
        raise RuntimeError("unexpected exit; actual record saved, preserve working changes")
    return base

import shutil
b=bound()
delivery=json.loads((ROOT/FEATURE/"acceptance/source-delivery.json").read_text())
if not b.get("owned") or not b.get("source") or not b.get("source_anchor"):
    checkpoint("T14","blocked",note="Inherited isolation: hand off to original mechanism; require actual delivery receipt before merge/cleanup/sync")
    raise RuntimeError("original isolation mechanism must provide actual merge evidence")
# Archive all current-feature evidence and state before merging; unknown files are never staged.
allowed=[FEATURE+"/execution/serial/T14",FEATURE+"/acceptance"]
dirty=git("status","--porcelain","--untracked-files=all")
if dirty:
    # Only known evidence may remain at this boundary; product changes require revalidation.
    names=git("diff","--name-only").splitlines()+git("ls-files","--others","--exclude-standard").splitlines()
    if any(not any(n==a or n.startswith(a+"/") for a in allowed) for n in names):
        raise RuntimeError("unexplained dirty files before merge")
    doc_commit([a for a in allowed if (ROOT/a).exists()],"test(T14): archive final validation evidence")
if git("status","--porcelain","--untracked-files=all"):raise RuntimeError("implementation worktree not clean")
source=Path(b["source"]).resolve()
if git("branch","--show-current",cwd=source)!=b["source_branch"]:raise RuntimeError("source branch changed")
if git("rev-parse","HEAD",cwd=source)!=delivery["authorized_source_head"]:raise RuntimeError("source HEAD differs from the explicitly authorized changelog update")
if git("diff","--cached","--name-only",cwd=source):raise RuntimeError("source has staged work not owned by this task")
source_status=subprocess.check_output(["git","status","--porcelain=v1","-z","--untracked-files=all",
 "--",".",":(exclude)"+FEATURE],cwd=source)
if hashlib.sha256(source_status).hexdigest()!=delivery["source_status_sha256"]:
    raise RuntimeError("source's unrelated changes changed since T00; preserve and resolve ownership")
changed=set(git("diff","--name-only",b["source_anchor"],"HEAD").splitlines())
records=subprocess.check_output(["git","status","--porcelain=v1","-z","--untracked-files=all"],cwd=source).decode().split("\0")
# The user confirmed the five remaining version rollback files; CHANGELOG was separately authorized and committed.
dirty_names=[]
for record in records:
    if not record:continue
    name=record[3:]
    if not record.startswith(" M ") or name not in delivery["preserved_files"]:raise RuntimeError("unexpected source dirt")
    if hashlib.sha256((source/name).read_bytes()).hexdigest()!=delivery["preserved_files"][name]:raise RuntimeError("source rollback content changed")
    dirty_names.append(name)
if set(dirty_names)!=set(delivery["preserved_files"]):raise RuntimeError("source rollback set changed")
if changed.intersection(dirty_names):raise RuntimeError("merge overlaps user-owned version rollback")
tip=git("rev-parse","HEAD")
git("merge","--ff-only",b["branch"],cwd=source)
if git("rev-parse","HEAD",cwd=source)!=tip:raise RuntimeError("unexpected merged HEAD")
after=subprocess.check_output(["git","status","--porcelain=v1","-z","--untracked-files=all",
 "--",".",":(exclude)"+FEATURE],cwd=source)
if hashlib.sha256(after).hexdigest()!=delivery["source_status_sha256"]:raise RuntimeError("unrelated state changed after merge")
for rel,digest in b["protected"].items():
    if hashlib.sha256((source/rel).read_bytes()).hexdigest()!=digest:raise RuntimeError("excluded source changed: "+rel)
ROOT=source;STATE=ROOT/FEATURE/"plan/progress.yaml"
state=read_state();state["notes"].append("T14 actual fast-forward merge "+tip+" into "+str(source)+" "+b["source_branch"])
temp=STATE.with_name(STATE.name+"."+uuid.uuid4().hex+".tmp")
temp.write_text(json.dumps(state,ensure_ascii=False,indent=2)+"\n");os.replace(temp,STATE)
doc_commit([str(STATE.relative_to(ROOT))],"docs(progress): preserve actual merge receipt")
print("DELIVERY_WORKDIR",str(source))
```````
**步骤 4：清理已归档且无活跃写者的自有资源**

下一片段 cwd 必须改为实际输出的 DELIVERY_WORKDIR。
执行前逐一确认模型与测试进程已停止、原始资料及审计结果已归档；不能仅以时间过去或目录名前缀证明资源可删。
部分清理失败时在存活来源工作区保存 T14 blocked、剩余台账和实际结果，再恢复未完成步骤。
继承的隔离由原机制交付，不套用本票拥有资源的删除路径。

**步骤 5：锚定并保存最终状态**

清理确认完成后运行下面完整代码。实现/合并提交、锚定提交与最终状态提交分开，不能自引用 SHA。

```````python
from pathlib import Path
import hashlib,json,os,subprocess,uuid,re,shlex
FEATURE=".spec-dev/2026-09-11-01-skill-instruction-streamlining"
ROOT=Path.cwd().resolve()
STATE=ROOT/FEATURE/"plan/progress.yaml"
def git(*args, cwd=None, check=True):
    r=subprocess.run(["rtk","proxy","git",*args],cwd=cwd or ROOT,capture_output=True,text=True)
    if check and r.returncode:raise RuntimeError(r.stdout+r.stderr)
    return r.stdout.strip()
def read_state(): return json.loads(STATE.read_text())
def binding():
    rows=[n[len("binding:"): ] for n in read_state()["notes"] if n.startswith("binding:")]
    if not rows:raise RuntimeError("T00 has not recorded a binding")
    return json.loads(rows[-1])
def bound():
    b=binding()
    if ROOT!=Path(b["worktree"]).resolve():raise RuntimeError("wrong task cwd")
    if git("branch","--show-current")!=b["branch"]:raise RuntimeError("wrong task branch")
    return b
def doc_commit(paths,message):
    staged=git("diff","--cached","--name-only")
    if staged:raise RuntimeError("unexpected staged files: "+staged)
    git("add","--",*paths)
    r=subprocess.run(["rtk","proxy","git","diff","--cached","--check"],cwd=ROOT)
    if r.returncode:raise RuntimeError("staged whitespace check failed")
    r=subprocess.run(["rtk","proxy","env","SKIP_CODEX_PACKAGE_HOOK=1","SKIP_RELEASE_HOOK=1",
                      "git","commit","-m",message],cwd=ROOT)
    if r.returncode:raise RuntimeError("commit failed; preserve staged files")
    return git("rev-parse","HEAD")
def checkpoint(task,status,commit=None,tests=None,note=None,extra_paths=()):
    state=read_state()
    state["tasks"][task]["status"]=status
    if commit is not None:state["tasks"][task]["commit"]=commit
    if tests is not None:state["tasks"][task]["tests"]=tests
    state["current"]=task if status in ("in_progress","blocked") else None
    if note:state["notes"].append(note)
    temp=STATE.with_name(STATE.name+"."+str(uuid.uuid4())+".tmp")
    with temp.open("x") as out:
        out.write(json.dumps(state,ensure_ascii=False,indent=2)+"\n");out.flush();os.fsync(out.fileno())
    os.replace(temp,STATE)
    return doc_commit([str(STATE.relative_to(ROOT)),*extra_paths],"docs(progress): "+task+" "+status)
def start(task,deps):
    bound();state=read_state()
    if any(state["tasks"][d]["status"]!="completed" for d in deps):raise RuntimeError("dependency incomplete")
    if state["tasks"][task]["status"]=="completed":raise RuntimeError("already completed: resume next ready task")
    if state["tasks"][task]["status"]=="blocked":raise RuntimeError("resolve recorded blocker before resume")
    if state["tasks"][task]["status"]!="in_progress":checkpoint(task,"in_progress")
def payload(task,name):
    text=(ROOT/FEATURE/"plan/tasks"/(task+".md")).read_text()
    fence="~"*16
    prefix="<!-- payload:"+name+" -->\n"+fence+"text\n"
    suffix="\n"+fence+"\n<!-- /payload:"+name+" -->"
    if text.count(prefix)!=1:raise RuntimeError("missing/duplicate payload "+name)
    rest=text.split(prefix,1)[1]
    if suffix not in rest:raise RuntimeError("payload terminator missing")
    return rest.split(suffix,1)[0]
def apply_rows(task,rows):
    bound();prepared=[]
    for row in rows:
        rel=row["path"]
        if rel.startswith(("skills/anysearch/","skills/sequential-thinking/")) or rel in (
            "scripts/update-vendored-skill.mjs","scripts/tests/update-vendored.test.mjs"):
            raise RuntimeError("excluded write: "+rel)
        target=ROOT/rel
        if target.resolve()!=target or not target.is_relative_to(ROOT) or ".." in Path(rel).parts:
            raise RuntimeError("noncanonical write: "+rel)
        content=payload(task,row["payload"])
        after=hashlib.sha256(content.encode()).hexdigest()
        current=hashlib.sha256(target.read_bytes()).hexdigest() if target.exists() else None
        if current==after:continue
        if current!=row["before"]:raise RuntimeError("unexpected source content: "+rel)
        prepared.append((target,content))
    for target,content in prepared:
        target.parent.mkdir(parents=True,exist_ok=True)
        temp=target.with_name(target.name+"."+str(uuid.uuid4())+".tmp")
        with temp.open("x") as out:
            out.write(content);out.flush();os.fsync(out.fileno())
        os.replace(temp,target)
def capture(task,label,argv,expected=0,cwd=None):
    b=bound();run=uuid.uuid4().hex
    base=ROOT/FEATURE/"execution/serial"/task/label/run
    base.mkdir(parents=True)
    actual_cwd=Path(cwd).resolve() if cwd else ROOT
    r=subprocess.run(argv,cwd=actual_cwd,capture_output=True)
    for stream,data in [("stdout",r.stdout),("stderr",r.stderr)]:(base/(stream+".log")).write_bytes(data)
    record={"argv":argv,"cwd":str(actual_cwd),"exit_code":r.returncode,"base_commit":git("rev-parse","HEAD"),
            "stdout_sha256":hashlib.sha256(r.stdout).hexdigest(),"stderr_sha256":hashlib.sha256(r.stderr).hexdigest(),
            "kind":"actual-command-result","expected_exit":expected}
    (base/"record.json").write_text(json.dumps(record,ensure_ascii=False,indent=2)+"\n")
    print(r.stdout.decode(errors="replace"));print(r.stderr.decode(errors="replace"))
    print("RECORD",str(base.relative_to(ROOT))+"/record.json")
    if r.returncode!=expected:
        checkpoint(task,"blocked",tests="fail",note=task+" unexpected exit at "+str(base.relative_to(ROOT)),
                   extra_paths=[str(base.relative_to(ROOT))])
        raise RuntimeError("unexpected exit; actual record saved, preserve working changes")
    return base

try:
    import shutil
    b=binding()
    if ROOT!=Path(b["source"]).resolve():raise RuntimeError("cleanup must run at verified source")
    if git("branch","--show-current")!=b["source_branch"]:raise RuntimeError("source branch mismatch")
    def retire(prefix,note):
        state=read_state();state["resources"]=[r for r in state["resources"] if not r.startswith(prefix)]
        state["notes"].append(note)
        temp=STATE.with_name(STATE.name+"."+uuid.uuid4().hex+".tmp")
        temp.write_text(json.dumps(state,ensure_ascii=False,indent=2)+"\n");os.replace(temp,STATE)
        doc_commit([str(STATE.relative_to(ROOT))],"docs(progress): record one resource disposition")
    receipts=[n for n in read_state()["notes"] if n.startswith("T14 actual fast-forward merge ")]
    if not receipts:raise RuntimeError("actual merge receipt missing")
    merged=receipts[-1].split()[4]
    if git("diff","--name-only",merged,"HEAD","--",".",":(exclude).spec-dev",":(exclude).worktrees"):
        raise RuntimeError("source product tree changed after validation")
    worktree=Path(b["worktree"])
    if not b["owned"]:raise RuntimeError("inherited resource must be handed off")
    if worktree.exists():
        identity=[worktree.stat().st_dev,worktree.stat().st_ino]
        if identity!=b["worktree_identity"] or worktree.resolve()!=worktree:raise RuntimeError("worktree ownership changed")
        if git("status","--porcelain",cwd=worktree):raise RuntimeError("worktree still contains unaccepted work")
        result=subprocess.run(["git","merge-base","--is-ancestor",b["branch"],"HEAD"],cwd=ROOT)
        if result.returncode:raise RuntimeError("worktree tip is not merged")
        git("worktree","remove",str(worktree))
    retire("worktree: "+str(worktree)+" —— ","T14 owned worktree removed or verified absent after actual merge")
    if subprocess.run(["git","show-ref","--verify","--quiet","refs/heads/"+b["branch"]],cwd=ROOT).returncode==0:
        git("branch","-d",b["branch"])
    retire("branch: "+b["branch"]+" —— ","T14 implementation branch removed or verified absent")
    owned={}
    for note in read_state()["notes"]:
        if note.startswith("owned-resource:"):
            item=json.loads(note[len("owned-resource:"):]);owned[item["path"]]=item["identity"]
    for entry in list(read_state()["resources"]):
        if entry.startswith(("model-fixtures: ","validation-copy: ")):
            label=entry.split(": ",1)[0];path_text=entry.split(": ",1)[1].split(" —— ",1)[0]
            target=Path(path_text)
            if path_text not in owned:raise RuntimeError("missing original ownership receipt: "+path_text)
            if target.exists():
                if target.resolve()!=target or [target.stat().st_dev,target.stat().st_ino]!=owned[path_text]:
                    raise RuntimeError("resource entity changed: "+path_text)
                # Main thread must first verify owner processes stopped and evidence archive complete.
                shutil.rmtree(target)
            retire(entry,"T14 removed or verified absent owned archived resource "+path_text)
    report=ROOT/FEATURE/"acceptance/acceptance-report.md"
    report_text=report.read_text()
    report_text=report_text.replace("实现与必需验收完成；实际本地合并及最终验证仍由 T14 执行。", "实现、必需验收、最终验证、本地合并及自有临时资源清理均已完成。")
    report_text=report_text.replace("模型/测试临时目录仅在实际合并及身份校验后清理；", "模型/测试临时目录已在实际合并及身份校验后清理；")
    report.write_text(report_text)
    doc_commit([str(report.relative_to(ROOT))],"docs(acceptance): record actual local delivery and cleanup")
    sync=git("rev-parse","HEAD")
    spec=ROOT/FEATURE/"spec/skill-instruction-streamlining-design.md"
    content=spec.read_text()
    updated,count=re.subn(r"(?m)^  sync_commit: .*$","  sync_commit: "+sync,content,count=1)
    if count!=1:raise RuntimeError("sync_commit field missing or ambiguous")
    temp=spec.with_name(spec.name+"."+uuid.uuid4().hex+".tmp")
    temp.write_text(updated);os.replace(temp,spec)
    doc_commit([str(spec.relative_to(ROOT))],"docs(spec): anchor verified delivery "+sync[:8])
    # Source code tree is unchanged by the progress/spec-only commits; record the actual current commit.
    final=git("rev-parse","HEAD")
    checkpoint("T14","completed",commit=final,tests="pass",note="T14: actual merge, owned cleanup and sync anchor completed; see acceptance for model verification boundaries")
    print("FINAL_COMMIT",git("rev-parse","HEAD"))

except Exception as error:
    try:
        source_path=locals().get('b',{}).get('source')
        if source_path and ROOT==Path(source_path).resolve():
            checkpoint('T14','blocked',note='T14 cleanup/anchor interrupted: '+str(error))
    except Exception as record_error:
        print('Could not save blocked checkpoint:',record_error)
    raise
```````
完成后复核：本次来源只有已确认的五个版本回退文件仍未提交；排除文件内容未变；
新特性内没有未保存进度，实际本地交付已完成。输出真实提交、验收边界和保留资源。
未合并、未归档、未清理或未锚定不得标 completed；无权限或来源未知时保留等待状态，不编造成功。
