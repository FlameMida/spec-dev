# 设计审查记录

- 日期：2026-09-11。
- 待审稿：[skill-instruction-streamlining-design.md](skill-instruction-streamlining-design.md)。
- 已批准输入：本会话最后展示的完整设计，用户回复 `ok`；范围为 19 项整改，保留 B01—B30，排除 AnySearch、sequential-thinking 及其更新逻辑。DDD 独立和被引用均采用统一 spec-dev 文档规范。
- 仓库基准：`179d76a7`。
- 待审稿 SHA-256：`3a7bf057e76a77ce889e22af85f420ce895d89caa459c60f0f726b836630c898`。
- 本记录仅证明设计文档自检及独立审查，不是实施或验收通过记录。

## 主线程自检

实际执行 `rtk proxy python3 -` 的 Python 标准库检查，退出码 0：

| 检查 | 实际结果 |
|---|---|
| 审查条目 | 19 项，A02—A07、A09—A11、A13—A19、C01—C03 |
| 规范条目 | 19 项对应 Requirement，另有 1 项共用引用/排除边界支持要求 |
| 保留约束 | B01—B30 全部出现，无缺号 |
| 场景 | S01—S43 连续且唯一，均含 GIVEN / WHEN / THEN |
| 规范结构 | 每条 Requirement 包含 SHALL 和对应 Scenario |
| 来源链接 | 21 个链接均映射到实际存在的允许范围文件 |
| 完整性 | 无未完成占位词，代码围栏闭合 |
| 验收归属 | PR 结构/回归、6 个固定模型冒烟与完整模型评测分开，参数化子例不静默缩减 |
| 排除完整性 | 30 个已跟踪排除文件的路径及内容摘要与写入前一致 |
| 无关改动 | 排除本次新特性目录后的 Git 状态摘要与写入前一致 |

排除文件集合：两个排除技能目录的已跟踪文件、`scripts/update-vendored-skill.mjs` 和 `scripts/tests/update-vendored.test.mjs`。

- 排除文件清单及内容哈希的集合摘要：`3716ead70b90c24bd48eb8b41a804d83e78c376fd45d1e543b8cf31b31112871`。
- 无关 Git 状态摘要：`4ec4cd7aeba7ff6b638e79d2d2cb05fd2f1fc2dbca3153eae260af08cf4a67da`。
- 摘要用于比较本轮操作前后是否变化，不证明这些文件的业务正确性。

## 独立审查

实际派发：`spec_streamlining_review`，`fork_turns: none`，由独立上下文审查者执行；仅允许读取本次新 spec 及当前产品文件，禁止读取既有 `.spec-dev/`、`.worktrees/` 内容。

审查回执结论：**Approved**。

审查者未发现会导致实施计划出错的实质问题，并回报已核对以下内容：

- 19 项均有对应 Requirement 和可观察 Scenario；B01—B30 已登记。
- MODIFIED / ADDED 分类与本次变化相符。
- 分层保留单点权威、有效入口和专项动作前读取；角色、锁、进度和输出契约明确。
- A19 区分恢复既定契约、新语义与冲突，未把事实判定扩大为审批或守卫授权。
- C03 覆盖独立/被引用、保存授权复用、多域、无内容不建及禁止自动迁移。
- PR 冒烟、完整模型评测和未执行状态分开，未把静态结果当模型效果。
- 仅批准设计与审查，计划和实施交接边界清楚。

实际定向核对范围：RA、共享探索规则、quick-fix、DDD、计划、验收、测试策略和探索角色文件。没有逐条重证 B01—B30 的全部下游实现；未读取旧 active spec/ADR，因此不宣称不存在历史契约冲突。审查者未写文件、提交或运行测试。

## 提交与检查边界

本次只提交此目录内的 spec 及本审查记录。工作区已有旧档案删除不归本任务，不暂存或恢复。

仓库 `pre-commit` 会触发安装式插件校验和技能校验，`post-commit` 会自动发版。纯设计提交使用仓库提供的 `SKIP_CODEX_PACKAGE_HOOK=1`、`SKIP_RELEASE_HOOK=1` 开关，避免读取被排除档案、安装副作用及版本/tag 改动；另行执行本次文档检查与暂存差异检查。被跳过的包校验不记 PASS。

全部 43 个模型/运行场景仍为待执行要求，当前没有生产代码或技能规则改动。下一步是用户对该 spec 的整体 review；取得开始编写计划的明确同意后才交接 writing-plans。
