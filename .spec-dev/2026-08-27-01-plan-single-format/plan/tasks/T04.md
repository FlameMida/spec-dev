### 任务 4:guardrail snippet 双语四行随动

**文件**:
- 修改:`guardrail/templates/CLAUDE.md.snippet`(:9 英文、:30 中文)
- 修改:`guardrail/templates/AGENTS.md.snippet`(:9 英文、:30 中文)

**接口**:
- 消费:T01 形态术语
- 产出:snippet 产物位置文本(安装器注入用户仓库的发现指引);T07 验收 rg 范围覆盖

**步骤 1:写失败断言(临时 rg,红)**

```bash
rg -c 'plan/ directory \(index' guardrail/templates/ || echo RED
```
预期:RED(两文件均无新措辞)。

**步骤 2:确认失败**

运行上述命令,输出 RED 即红。

**步骤 3:实现(双语各两处,两文件对称)**

英文行(两文件 :9)定位串 `` `plan/<feature>-plan.md` is the executable plan `` 所在 bullet 整句改写为:

```markdown
- **plan**: `plan/` directory (`index.md` + `tasks/` + `progress.yaml`) is the executable plan — legacy single-file plans (`plan/<feature>-plan.md`) are read as-is. `plan/index.md` anchors the task nav table and global constraints.
```

中文行(两文件 :30)定位句 `plan/<特性>-plan.md 是可执行计划` 所在 bullet 改写为:

```markdown
- **plan**:`plan/` 目录(`index.md` + `tasks/` + `progress.yaml`)是可执行计划——存量单文件(`plan/<特性>-plan.md`)按原样读取。`plan/index.md` 承载任务导航表与全局约束。
```

注意保留该 bullet 原有的其余部分(docs/ 迁移指引句),只改形态描述;两文件改动逐字对称。

**步骤 4:确认通过**

```bash
rg -c 'plan/ directory \(index' guardrail/templates/ && rg -c '存量单文件' guardrail/templates/
```
预期:两文件各命中 ≥1;`bash -n` 无关(纯 markdown),跑 `node scripts/validate-skills.mjs` 确认无破坏。

**步骤 5:提交**

```bash
git add guardrail/templates/
git commit -m "feat(T04): guardrail snippet 双语产物位置改分文件形态（存量单文件按原样读取）"
```
