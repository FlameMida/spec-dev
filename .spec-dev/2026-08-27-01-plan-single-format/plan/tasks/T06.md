### 任务 6:evals 两件(路径断言改分文件 + 存量可执行用例补录)

**文件**:
- 修改:`skills/writing-plans/evals/evals.json`(用例 wp-* 的 expected_output 产物路径)
- 修改:`skills/executing-plans/evals/evals.json`(新增用例 `ep-legacy-single-file-executable`)

**接口**:
- 消费:T02 读宽容语义、T01 形态术语
- 产出:eval 承载(spec ADDED「存量计划兼容读取」的 Scenario 断言锚);T07 验收核对

**步骤 1:写失败断言(红)**

```bash
rg -c 'ep-legacy-single-file-executable' skills/executing-plans/evals/evals.json || echo RED
```
预期:RED(新用例不存在)。

**步骤 2:确认失败**

输出 RED。

**步骤 3:实现**

**(a) writing-plans/evals/evals.json**:全部 expected_output 中的 `.spec-dev/YYYY-MM-DD-NN-<feature>/plan/<feature>-plan.md` 路径改为 `.spec-dev/YYYY-MM-DD-NN-<feature>/plan/`(index.md + tasks/ + progress.yaml);涉及"保存至/落盘"语义的措辞同步(如「计划保存至 …/plan/ 分文件三件套」)。

**(b) executing-plans/evals/evals.json** cases 追加(spec Scenario「旧单文件计划可执行」直译,GIVEN→prompt 构造、WHEN→触发、THEN→断言):

```json
{
  "id": "ep-legacy-single-file-executable",
  "prompt": "执行 .spec-dev/2026-08-26-01-major-upgrade/plan/major-upgrade-plan.md（存量单文件计划，目录下无 tasks/ 子目录）",
  "expected_output": "格式嗅探走单文件分支：按原样一次性读取计划全文与同特性目录 spec 执行；不提示迁移、不生成 tasks/ 或 progress.yaml；恢复用复选框判读（与 git log 的 feat(TN) 对照，不一致以提交为准并报告）；资源登记就地编辑其最终任务内嵌台账行"
}
```

改完跑 JSON 合法性:`node -e "JSON.parse(require('fs').readFileSync('skills/executing-plans/evals/evals.json','utf8')); JSON.parse(require('fs').readFileSync('skills/writing-plans/evals/evals.json','utf8'))"`。

**步骤 4:确认通过**

```bash
rg -c 'ep-legacy-single-file-executable' skills/executing-plans/evals/evals.json && rg -c 'plan/<feature>-plan\.md' skills/writing-plans/evals/evals.json || echo "旧路径清零"
```
预期:第一条 ≥1;第二条零命中(输出"旧路径清零")。

**步骤 5:提交**

```bash
git add skills/writing-plans/evals/ skills/executing-plans/evals/
git commit -m "feat(T06): evals 随动——writing-plans 用例产物路径改分文件；executing-plans 补存量单文件可执行用例（读宽容行为锚）"
```
