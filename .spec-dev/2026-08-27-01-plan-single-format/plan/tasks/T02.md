### 任务 2:executing-plans 渐进默认 + 读宽容(reference 并入后删除)

**文件**:
- 修改:`skills/executing-plans/SKILL.md`(:4 description、:35 读取句、:40 恢复入口、:59 勾选句、:61 资源登记句、渐进纪律并入)
- 修改:`skills/executing-plans/agents/openai.yaml`(description 同步)
- 删除:`skills/executing-plans/references/progressive-execution.md`(内容并入 SKILL.md)
- 修改:`scripts/tests/plan-single-format.test.mjs`(追加断言)

**接口**:
- 消费:T01 的形态术语(`plan/` 三件套、任务文件语义)
- 产出:文本锚——`按原样读取`读分支(存量单文件)、渐进加载为新计划默认;`progressive-execution.md` 删除;T03/T06 消费

**步骤 1:写失败测试(追加到 plan-single-format.test.mjs)**

```js
const ep = readFileSync(path.join(repoRoot, "skills/executing-plans/SKILL.md"), "utf8");

test("Scenario: 新计划走渐进分支(默认)且 reference 已并入", () => {
  assert.isFalse(existsSync(path.join(repoRoot, "skills/executing-plans/references/progressive-execution.md")), "reference 应已删除");
  assert.doesNotMatch(ep, /progressive-execution/, "SKILL.md 不应再引用该 reference");
  assert.match(ep, /渐进加载/, "渐进加载纪律应并入本体");
  assert.match(ep, /ready 任务/, "resume 规程应并入本体");
});

test("Scenario: 旧单文件计划可执行(读宽容句在位)", () => {
  assert.match(ep, /按原样读取|原样读取执行/, "读分支应在位");
  assert.match(ep, /格式嗅探|tasks\/.*存在/, "格式嗅探分流应在位");
  assert.match(ep, /冻结/, "单文件侧冻结语义应声明");
});

test("openai.yaml 与 SKILL description 同步含分文件入口", () => {
  const yaml = readFileSync(path.join(repoRoot, "skills/executing-plans/agents/openai.yaml"), "utf8");
  assert.match(yaml, /plan\/|tasks\//, "openai.yaml 应含分文件形态入口");
});
```

**步骤 2:运行测试确认失败**

运行:`node --test scripts/tests/plan-single-format.test.mjs`
预期:新增 3 用例红(reference 存在、冻结语义缺失、openai.yaml 无分文件入口)。

**步骤 3:实现**

**(a) description(:4)**:`plan/*-plan.md under the feature directory in .spec-dev/` 改为 `plan/ split-file layout (index.md + tasks/ + progress.yaml), legacy single-file plans read as-is, under the feature directory in .spec-dev/`;中文侧同步(`.spec-dev/` 特性目录下 plan/ 的分文件结构(index+tasks+progress);存量单文件计划按原样读取)。

**(b) :35 读取句**改为:`读取计划(格式嗅探):plan/tasks/ 子目录存在 → 分文件形态(全部新生成计划的唯一形态),渐进加载——启动只读 index + progress + spec,执行 TN 时只读 tasks/TN.md 与导航表中依赖行的「产出接口」列,不提前读无关后继任务正文;不存在 → 存量单文件形态,按 plan/*-plan.md 原样一次性读取计划全文与同特性目录 spec(旧命名 YYYY-MM-DD-<feature> 目录按原样读取)。该读分支为冻结侧——后续流程演进不再为其新增条款,仅维持既有语义。`

**(c) 并入渐进加载与恢复纪律**:progressive-execution.md 的「渐进加载纪律」(任务完成条件六项)与「恢复执行 resume」(一致性校验→ready 任务续跑→不重跑已完成测试)两节内容,作为「阶段 2」之后的内嵌段落并入 SKILL.md(原「单文件形态的轻量恢复(对照)」节保留,标题改为「存量单文件的轻量恢复(兼容分支)」)。

**(d) :59 勾选句**括注改为:`(分文件形态原子更新 progress.yaml 并随任务提交——唯一状态源;存量单文件计划沿用其复选框)`。

**(e) :61 资源登记句**括注改为:`(登记进 progress.yaml 的 resources 键——writing-plans 的资源台账规范定义点;存量单文件计划就地编辑其最终任务内嵌台账行,该侧冻结)`。

**(f) 删除文件**:`git rm skills/executing-plans/references/progressive-execution.md`

**(g) openai.yaml**:`short_description` 与 SKILL description 同步改写(与 (a) 同语义,保持 interface+policy 键结构不变)。

**步骤 4:运行测试确认通过**

运行:`node --test scripts/tests/plan-single-format.test.mjs && node scripts/validate-skills.mjs && node scripts/check-openai-sync.mjs`
预期:7/7 PASS + 双校验通过。

**步骤 5:提交**

```bash
git add skills/executing-plans/ scripts/tests/plan-single-format.test.mjs
git commit -m "feat(T02): executing-plans 渐进加载升默认+读宽容冻结侧——progressive-execution 并入本体后删除、description 与 openai.yaml 同步分文件入口"
```
