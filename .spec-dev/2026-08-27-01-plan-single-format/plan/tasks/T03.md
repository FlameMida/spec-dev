### 任务 3:外围指针五处随动

**文件**:
- 修改:`skills/requirement-analysis/SKILL.md:184`(上游路径预期)
- 修改:`skills/quick-fix/SKILL.md:91`(台账指针)
- 修改:`skills/acceptance-qa/SKILL.md:89`(台账指针)
- 修改:`skills/executing-plans/SKILL.md`(阶段 3 资源登记总则句的"行格式以 writing-plans 最终任务模板的资源台账定义为准"指针)
- 修改:`skills/executing-plans/references/review-orchestration.md:54`("计划文件路径"→"计划入口")
- 修改:`scripts/tests/resource-ledger-split.test.mjs`(断言随动)

**接口**:
- 消费:T01 的台账定义点锚(progress.yaml resources 键结构节)、T02 的读宽容措辞
- 产出:五处指针新措辞;resource-ledger-split 断言与 T01/T02 措辞对齐(测试绿)

**步骤 1:写失败断言(修改 resource-ledger-split.test.mjs)**

第三个用例(防回归断言)不变;前两个用例替换为收敛后语义:

```js
test("writing-plans 资源预登记锚定 progress.yaml resources(唯一形态)", () => {
  const wp = read("skills/writing-plans/SKILL.md");
  assert.match(wp, /resources[^\n]*键[^\n]*预登记|预登记[^\n]*progress\.yaml[^\n]*resources/, "预登记应锚定 progress.yaml resources");
  assert.match(wp, /progress\.yaml[^\n]*resources[^\n]*定义点|定义点[^\n]*progress\.yaml/, "台账规范定义点应锚定 progress.yaml 键结构节");
});

test("executing-plans 登记落点为 progress.yaml resources(存量单文件兼容句在位)", () => {
  const ep = read("skills/executing-plans/SKILL.md");
  assert.match(ep, /登记进 progress\.yaml 的 resources 键/);
  assert.match(ep, /存量单文件[^\n]*台账行/, "存量兼容句应在位");
});

test("外围指针不悬空(quick-fix/acceptance-qa 不再定位到最终任务模板)", () => {
  assert.doesNotMatch(read("skills/quick-fix/SKILL.md"), /最终任务模板的资源台账定义/);
  assert.doesNotMatch(read("skills/acceptance-qa/SKILL.md"), /writing-plans 最终任务模板/);
  assert.match(read("skills/quick-fix/SKILL.md"), /writing-plans 的资源台账定义/);
});
```

**步骤 2:运行测试确认失败**

运行:`node --test scripts/tests/resource-ledger-split.test.mjs`
预期:FAIL(旧断言失配/新断言未满足)。

**步骤 3:实现(五处指针改写)**

**(a) requirement-analysis/SKILL.md:184** 定位串 `spec 与后续 writing-plans 的计划(同目录 `plan/<feature>-plan.md`)共用这一个特性目录` 改为:
`spec 与后续 writing-plans 的计划(同目录 `plan/` 分文件结构:index.md + tasks/ + progress.yaml)共用这一个特性目录`

**(b) quick-fix/SKILL.md:91** 定位串 `台账纪律细则以 writing-plans 最终任务模板的资源台账定义为准` 改为:
`台账纪律细则以 writing-plans 的资源台账定义为准(progress.yaml 的 resources 键)`

**(c) acceptance-qa/SKILL.md:89** 定位串 `行格式以 writing-plans 的资源台账定义为准` 保持、其前的 `写入计划最终任务的资源台账` 改为 `写入计划的资源台账`,并将该句尾括注补 `(progress.yaml 的 resources 键;存量单文件计划为其最终任务内嵌台账行)`。

**(d) executing-plans/SKILL.md** 阶段 3「资源登记」总则句中 `行格式以 writing-plans 最终任务模板的资源台账定义为准` 改为 `行格式以 writing-plans 的资源台账定义为准(progress.yaml 的 resources 键;存量单文件计划沿用其内嵌台账行)`。

**(e) review-orchestration.md:54** `计划文件路径(供对照验收标准)` 改为 `计划入口(index.md 或存量单文件;供对照验收标准)`。

**步骤 4:运行测试确认通过**

运行:`node --test scripts/tests/resource-ledger-split.test.mjs && node --test scripts/tests/plan-single-format.test.mjs && node scripts/validate-skills.mjs`
预期:3/3 + 7/7 PASS + 校验通过。

**步骤 5:提交**

```bash
git add skills/ scripts/tests/resource-ledger-split.test.mjs
git commit -m "feat(T03): 外围指针五处随动——台账定义点指向 progress.yaml resources(quick-fix/acceptance-qa/executing-plans),requirement-analysis 上游路径与 review-orchestration 计划入口措辞,断言联动"
```
