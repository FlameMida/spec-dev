### 任务 8:合并与清理

**资源台账**(承载于 `plan/progress.yaml` 的 `resources` 键,清理步骤遍历该清单;本任务文件不嵌复选框台账):

已预登记:`worktree: .worktrees/plan-2026-08-27-01-plan-single-format —— git worktree remove .worktrees/plan-2026-08-27-01-plan-single-format && git branch -d plan/2026-08-27-01-plan-single-format`

台账总则:清理只遍历本台账、台账外一律不动(可疑残留只报告不删);共享缓存默认保留;台账限定持久资源,worktree 内构建产物随 worktree 删除自然回收、不入账。

**步骤 1:全量验证(安全网)与归属裁决**

在 worktree 内运行:

```bash
node --test scripts/tests/*.test.mjs
bash scripts/tests/visual-path.test.sh
node scripts/validate-skills.mjs
node scripts/check-openai-sync.mjs
node scripts/check-plugin.mjs
node scripts/validate-output.mjs agent-plugin-1.0.0 plugin.json
```

- 全绿 → 进入步骤 2。
- 失败在相关测试范围内 → 修复复跑。
- 失败在范围外 → 归属裁决:主工作区源分支(main)检出复跑;既有失败报用户裁决;本次引入的回归修复后继续。

**步骤 2:测试退役检查**

扫描「相关测试范围」内测试找孤儿(测试名对不上任何 active spec 现行 Scenario 且对应 Requirement 已 REMOVED/被 Superseded 标注/所属 spec superseded,双条件缺一不可)。本计划的测试为新增(docs 断言与断言随动),对应 spec 均为现行——预期"无孤儿测试",声明后跳过;`resource-ledger-split.test.mjs` 的被改断言对应 major-upgrade 被取代三条 Requirement——该 spec 仍 active 且标注为 Superseded-pending(非 Superseded),不构成退役条件,不删。

**步骤 3:取代回写(spec `supersedes` 非空:major-upgrade-design.md,部分取代)**

按 spec「取代与共存」节执行:
- `.spec-dev/2026-08-26-01-major-upgrade/spec/major-upgrade-design.md` 三条 Requirement 标题下各插入一行标注(形制见 spec-template「取代标注形制」节):
  - 「Requirement: plan 分文件形态(阈值门控)」标题下:`> **Superseded (2026-08-27)** — by .spec-dev/2026-08-27-01-plan-single-format/spec/plan-single-format-design.md#plan-单一形态;原文保留仅作历史参考。`
  - 「Requirement: 渐进执行与断点恢复」标题下:`> **Superseded (2026-08-27)** — by .spec-dev/2026-08-27-01-plan-single-format/spec/plan-single-format-design.md#渐进执行与断点恢复;原文保留仅作历史参考。`
  - 「Requirement: 资源登记纪律(按计划形态分流)(改了什么:登记与模板载体由"计划文件台账行"扩展为随计划形态分流,创建即登记语义不变)」标题下:`> **Superseded (2026-08-27)** — by .spec-dev/2026-08-27-01-plan-single-format/spec/plan-single-format-design.md#资源登记纪律;原文保留仅作历史参考。`
- H1 标题下的 `Superseded-pending (2026-08-27)` 行移除(三 Requirement 均已标注)。
- 分面共存的 spec(supersede-lifecycle/test-scoping 等)零回写;提交命中其 covers 时按双声明规则同步或 `Spec-Guard: off` trailer 放行。
- major-upgrade spec 保持 active(其余 Requirement 未被取代)。

**步骤 4:合并回来源分支**

```bash
cd "$(dirname "$(git rev-parse --git-common-dir)")"   # 回到主工作区
git merge plan/2026-08-27-01-plan-single-format
```

合并冲突、或主工作区有未提交改动 → 停下向计划作者确认,不强行合并。

**步骤 5:清理(按 progress.yaml 的 resources 清单逐条执行)**

执行已预登记的 worktree 清理命令并勾选(progress.yaml 置 completed);命令失败 → 保留未勾选并报告;资源已不存在 → 勾选并注明。

**步骤 6:sync_commit 锚定**

```bash
SYNC=$(git rev-parse HEAD)   # 合并完成后的主工作区 HEAD
# 把 spec frontmatter 的 sync_commit: null 更新为 $SYNC
git add .spec-dev/2026-08-27-01-plan-single-format/spec/plan-single-format-design.md
git commit -m "chore(spec): sync_commit 锚定 ${SYNC:0:7}"
```

**步骤 7:progress.yaml 归档收口**

合并后把 `plan/progress.yaml` 的 `current` 置 null、全部任务 completed、notes 补一行"交付完成",随步骤 6 同一提交或紧随提交——它是执行档案,随特性目录归档。
