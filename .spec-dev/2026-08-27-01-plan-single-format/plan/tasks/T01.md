### 任务 1:writing-plans 唯一形态重组(主体改写 + progressive-plan-format 并入后删除)

**文件**:
- 修改:`skills/writing-plans/SKILL.md`(约 10 处改写,详见步骤 3)
- 删除:`skills/writing-plans/references/progressive-plan-format.md`(内容并入 SKILL.md)
- 创建:`scripts/tests/plan-single-format.test.mjs`

**接口**:
- 消费:`skills/writing-plans/references/progressive-plan-format.md` 现文(45 行:结构节/导航表规则/progress.yaml 键结构/生成规则)
- 产出:文本锚——`plan/` 目录唯一形态表述、「**步骤 N:**」标题式(无复选框)、资源台账规范定义点(progress.yaml resources 键结构节);T03/T04/T05 消费这些锚的精确措辞

**步骤 1:写失败测试**

创建 `scripts/tests/plan-single-format.test.mjs`:

```js
import { test } from "node:test";
import assert from "node:assert/strict";
import { readFileSync, existsSync } from "node:fs";
import path from "node:path";
import { fileURLToPath } from "node:url";

const repoRoot = path.resolve(path.dirname(fileURLToPath(import.meta.url)), "../..");
const wp = readFileSync(path.join(repoRoot, "skills/writing-plans/SKILL.md"), "utf8");

test("Scenario: 小计划也产分文件——writing-plans 无按规模分流判定", () => {
  // 注意:验收矩阵表头的"阈值/预期""阈值数字"是合法用法,正则只锁门控条款措辞
  assert.doesNotMatch(wp, /形态分流|阈值门控|任务数\s*>?\s*8|正文预估[^。]*25KB|低于阈值维持/, "不应存在阈值门控条款");
  assert.doesNotMatch(wp, /单文件形态[^\n]*默认|默认[^\n]*单文件形态/, "不应有单文件默认表述");
  assert.match(wp, /plan\/index\.md/, "应声明 index.md");
  assert.match(wp, /plan\/progress\.yaml/, "应声明 progress.yaml");
});

test("Scenario: 阈值条款不复存在 + reference 已并入本体", () => {
  assert.isFalse(existsSync(path.join(repoRoot, "skills/writing-plans/references/progressive-plan-format.md")), "reference 应已删除");
  assert.doesNotMatch(wp, /progressive-plan-format/, "SKILL.md 不应再引用该 reference");
  assert.match(wp, /任务导航表/, "导航表规则应并入本体");
  assert.match(wp, /format_version/, "progress.yaml 键结构应并入本体");
});

test("Scenario: 任务文件无复选框(步骤标题式)", () => {
  assert.match(wp, /「?\*\*步骤 N:\*\*」?标题式/, "应声明步骤标题式");
  assert.doesNotMatch(wp, /步骤用复选框|用复选框（`- \[ \]`）语法跟踪/, "不应保留复选框跟踪语义");
});

test("Scenario: 资源台账规范定义点在 progress.yaml 键结构节", () => {
  assert.match(wp, /resources[^\n]*键[^\n]*台账|资源台账[^\n]*progress\.yaml[^\n]*resources/, "台账定义点应锚定 progress.yaml resources");
});
```

**步骤 2:运行测试确认失败**

运行:`node --test scripts/tests/plan-single-format.test.mjs`
预期:FAIL(4 个用例中至少 3 个红——现状存在阈值条款、reference 存在、复选框语义在)。

**步骤 3:实现(逐处改写 SKILL.md + 并入 + 删除 reference)**

以下改写按定位串执行(行号以 v8.0.3 为参考,以定位串为准):

**(a) 「计划保存至」段(:22 附近)整段替换为:**

```markdown
**计划保存至**:spec 所在特性目录的 `plan/` 目录——`plan/index.md` + `plan/tasks/TNN.md` + `plan/progress.yaml` 三件套(唯一生成形态,结构见下文「plan 目录结构」节);特性目录由 requirement-analysis 在写 spec 时按同日序号命名规则创建;无 spec 输入的独立触发则自建特性目录。用户对计划位置的偏好优先于此默认值。
```

**(b) 「形态分流(阈值门控)」段(:24 附近)整段替换为:**

```markdown
**唯一分文件形态**:每份计划(不论任务数多少)都产出 `plan/` 三件套;progress.yaml 是唯一状态源,任务文件与 index 内不使用复选框跟踪。存量单文件计划(`plan/*-plan.md`)由 executing-plans 按原样读取执行(读宽容),本 skill 不再生成该形态。
```

**(c) 删除「大型计划的分组导航」条款(:68 附近整段)**——单文件文档内组织手段,由 index 导航表取代。

**(d) 「计划文档头部」节(:70 起)重组为「plan 目录结构 + index.md 头部」两节**——原头部模板保留但其引导行中「步骤用复选框(`- [ ]`)语法跟踪」改为「任务状态由 `plan/progress.yaml` 跟踪(唯一状态源;任务文件步骤用「**步骤 N:**」标题式、不含复选框)」,并在头部模板代码块后追加「index.md 头部之后是任务导航表(四列:任务/依赖/消费接口/产出接口;任务 ID 形如 `T\d\d` 与 tasks/ 文件名一一对应、依赖只引用表内 ID、禁止环、接口列写精确签名)」。

**(e) 并入「plan 目录结构」节**(插在「文件结构先行」节之前),内容取自 progressive-plan-format.md 的「结构」节逐字迁移:

```markdown
## plan 目录结构

```
.spec-dev/<特性目录>/plan/
├── index.md        # 头部 + 全局约束 + 相关测试范围 + 设计原则块 + 任务导航表;不复制任务正文
├── tasks/T00.md …  # 每任务一文件,正文结构与下文任务模板逐字一致(文件块/接口块/TDD 五步)
└── progress.yaml   # 唯一运行时状态
```
```

**(f) 并入「progress.yaml 键结构」节**(插在「任务结构」节之后),逐字迁移 progressive-plan-format.md 的 yaml 块与状态枚举/写入纪律说明,并在该节末尾加一句:`resources` 键即**资源台账规范定义点**(预登记与执行期追加的载体,最终任务清理遍历此清单)。

**(g) 任务模板去复选框**:「任务 0」「任务 N」「验收任务」「最终任务」四个模板中所有 `- [ ] **步骤` 改为 `**步骤`;「计划文档头部」引导行的复选框语义已在 (d) 处理。

**(h) 最终任务「资源台账」小节(:211 附近)整段替换为:**

```markdown
**资源台账**(承载于 `plan/progress.yaml` 的 `resources` 键:生成时预登记 worktree 行,执行中创建即追加;行格式 `<类型>: <标识> —— <清理命令>`);下方清理步骤遍历该清单。
```

**(i) 牢记节(:287 附近)该行替换为:**

```markdown
- 任务步骤会创建持久资源(容器、测试库/表、临时目录、后台服务)时,写计划时就在 progress.yaml 的初始 `resources` 键预登记对应行——资源不允许只活在对话里
```

**(j) 「执行交接」话术(:303 附近)**中 `plan/<feature>-plan.md` 改为 `plan/`(index.md)。

**(k) Self-Review 追加第 4 查**(取自 progressive-plan-format 生成规则第 4 条):「分文件形态加一查:导航表接口列与任务文件接口块逐条一致」。

**(l) 删除文件**:`git rm skills/writing-plans/references/progressive-plan-format.md`

**步骤 4:运行测试确认通过**

运行:`node --test scripts/tests/plan-single-format.test.mjs && node scripts/validate-skills.mjs`
预期:4/4 PASS + 校验通过。

**步骤 5:提交**

```bash
git add skills/writing-plans/ scripts/tests/plan-single-format.test.mjs
git commit -m "feat(T01): writing-plans 唯一分文件形态——阈值门控删除、progressive-plan-format 并入本体后删除、任务模板去复选框、台账定义点锚定 progress.yaml resources"
```
