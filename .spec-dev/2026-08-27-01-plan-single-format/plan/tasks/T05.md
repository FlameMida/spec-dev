### 任务 5:README 双语随动

**文件**:
- 修改:`README.md`(:33 管线图、:162 流程详述、:168 命令示例)
- 修改:`README.zh-CN.md`(对应三处)

**接口**:
- 消费:T01 形态术语
- 产出:README 面向用户的产物形态描述;T07 验收 rg 范围覆盖

**步骤 1:写失败断言(红)**

```bash
rg -n 'plan/<feature>-plan\.md' README.md README.zh-CN.md
```
预期:命中 ≥3(现状管线图/详述/示例均为单文件形态)——即为红。

**步骤 2:确认失败**

上述命令输出命中行。

**步骤 3:实现(双语各三处)**

**(a) 管线图(:33 附近)**:`writing-plans (plan → plan/<feature>-plan.md in the same feature dir)` 改为 `writing-plans (plan → plan/ split files: index + tasks/ + progress.yaml)`;中文对应行同步。

**(b) 流程详述(:162 附近)**:`(the later plan lands in `plan/<feature>-plan.md` of the same directory)` 改为 `(the later plan lands in the same directory's `plan/` as index.md + tasks/ + progress.yaml)`;中文对应句同步(`后续计划落同目录 plan/ 分文件结构`)。

**(c) 命令示例(:168 附近)**:保留存量示例并在其后补一行分文件示例:

```markdown
/executing-plans 执行 .spec-dev/2026-08-27-01-plan-single-format/plan/index.md
```

中文侧对称补 `/executing-plans 执行 …/plan/index.md`。

**步骤 4:确认通过**

```bash
rg -n 'plan/<feature>-plan\.md' README.md README.zh-CN.md && echo "存量示例保留(合法)" ; rg -c 'tasks/ \+ progress|plan/index' README.md README.zh-CN.md
```
预期:存量示例仅存于"按原样读取"语境或示例行;新形态描述两文件各 ≥2 处;`node scripts/check-openai-sync.mjs` 通过(README 不在校验面,跑一遍作回归)。

**步骤 5:提交**

```bash
git add README.md README.zh-CN.md
git commit -m "feat(T05): README 双语产物形态改分文件三件套+命令示例补 index.md 入口"
```
