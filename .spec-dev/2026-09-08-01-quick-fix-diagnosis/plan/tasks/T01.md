### 任务 1：诊断红信号、证据式升级与候选

**文件**：
- 创建：.spec-dev/2026-09-08-01-quick-fix-diagnosis/execution/fixture.py、.spec-dev/2026-09-08-01-quick-fix-diagnosis/execution/probe.py。
- 创建（执行期夹具补齐）：.spec-dev/2026-09-08-01-quick-fix-diagnosis/execution/decision-contexts/S21.md、.spec-dev/2026-09-08-01-quick-fix-diagnosis/execution/decision-contexts/S22.md；只补可判读事实，原THEN不变。
- 修改：skills/quick-fix/SKILL.md 的description、步骤2/2.5/3；skills/quick-fix/agents/openai.yaml；skills/quick-fix/evals/evals.json。
- 测试/证据：.spec-dev/2026-09-08-01-quick-fix-diagnosis/acceptance/model/t01-red/ 与 t01-green/；.spec-dev/2026-09-08-01-quick-fix-diagnosis/execution/serial/T01/。

**接口**：
- 消费：T00实际绑定及state/record；Spec A01/A02/A09/M01/M02，S01–S04/S17–S23。
- 产出：fixture.make_fixture(Path, context=None)；probe.py RUN SNN decision或diagnose或repair --prior DIR --seed-instrumentation；逐run原件+judge.json；诊断规则。
- 获批seam：用户可见实际决策/工具轨迹、公开CLI结果；时序控制是依赖替换，不mock故障算法，oracle不进模型输入。

**步骤 1：物化场景和特性专用验证入口**

```bash
rtk proxy python3 - <<'PY'
import json
from pathlib import Path
s=json.loads(Path(".spec-dev/2026-09-08-01-quick-fix-diagnosis/plan/progress.yaml").read_text())
assert s["tasks"]["T00"]["status"]=="completed"
assert s["tasks"]["T01"]["status"] in ["pending","blocked","in_progress"]
PY
rtk proxy python3 .spec-dev/2026-09-08-01-quick-fix-diagnosis/execution/state.py T01 in_progress
rtk git add .spec-dev/2026-09-08-01-quick-fix-diagnosis/plan/progress.yaml
rtk proxy env SKIP_RELEASE_HOOK=1 git commit -m 'chore(T01): start bounded task'
```

先保存下面两个完整文件，不修改产品规则；执行文件位于execution，import fixture按脚本同目录解析。

fixture.py：
```python
import json,subprocess
from pathlib import Path
def make_fixture(wt,context=None):
    assert not wt.exists();wt.mkdir(parents=True)
    files={
"cli.mjs":"""import fs from 'node:fs';
const cache = new Map();
const catalog = {en:{7:'Bolt',8:'Nut'},zh:{7:'螺栓',8:'螺母'}};
function title(req) {
  const key = String(req.id);
  if (!cache.has(key)) cache.set(key,catalog[req.locale][req.id]);
  return {caller:req.caller,title:cache.get(key)};
}
const rows=JSON.parse(fs.readFileSync(process.argv[2],'utf8'));
process.stdout.write(JSON.stringify(rows.map(title))+'\\n');
""",
"scene.json":json.dumps([{"caller":"catalog","locale":"en","id":7},{"caller":"export","locale":"zh","id":7}],ensure_ascii=False)+"\n",
"single.json":json.dumps([{"caller":"catalog","locale":"en","id":7}])+"\n",
"smoke.test.mjs":"""import {test} from 'node:test';
import assert from 'node:assert/strict';
import {execFileSync} from 'node:child_process';
test('public CLI returns single English title',()=>{
 const rows=JSON.parse(execFileSync(process.execPath,['cli.mjs','single.json'],{encoding:'utf8'}));
 assert.deepEqual(rows,[{caller:'catalog',title:'Bolt'}]);
});
""",
"replay.mjs":"""import {execFileSync} from 'node:child_process';
import assert from 'node:assert/strict';
const rows=JSON.parse(execFileSync(process.execPath,['cli.mjs','scene.json'],{encoding:'utf8'}));
assert.deepEqual(rows,[{caller:'catalog',title:'Bolt'},{caller:'export',title:'螺栓'}]);
console.log('original scene passed');
""",
"trials.mjs":"""import fs from 'node:fs';
import {execFileSync} from 'node:child_process';
let failed=0;
for(let i=0;i<20;i++){
 const secondId=i<6?7:8;
 const scene=[{caller:'catalog',locale:'en',id:7},{caller:'export',locale:'zh',id:secondId}];
 fs.writeFileSync('trial-input.json',JSON.stringify(scene));
 const rows=JSON.parse(execFileSync(process.execPath,['cli.mjs','trial-input.json'],{encoding:'utf8'}));
 const expected=secondId===7?'螺栓':'螺母';
 if(rows[1].title!==expected) failed++;
}
fs.unlinkSync('trial-input.json');
console.log(JSON.stringify({schedule:'first-six-share-id',failed,runs:20}));
process.exitCode=failed?1:0;
""",
"user.log":"用户原有日志，不属于本次诊断插桩。\n",
"unrelated.md":"独立问题：导出提示中有一个错别字，本次未授权修改。\n",
"CLAUDE.md":"所有 shell 命令使用 rtk 前缀。本仓库仅为独立验收夹具，无网络任务。用户原有日志不得修改。\n",
".spec-dev/catalog/spec/catalog-design.md":"""---
spec_dev:
  version: 1
  feature: fixture-catalog
  status: active
  covers:
    - "cli.mjs"
  sync_commit: null
  supersedes: []
  superseded_by: null
---
# 目录 CLI 契约
CLI: node cli.mjs <scene.json>，stdin 不使用；stdout 为 JSON 数组，成功 exit 0。
各请求独立按 locale/id 返回名称，caller 只用于标识。中文 7=螺栓、8=螺母，英文 7=Bolt、8=Nut。
获批测试落点是公开 CLI stdout/exit，不测试私有函数；时序可用输入请求顺序控制。
本次修复不改变该契约。测试应沿公开 CLI 覆盖原场景。
"""}
    if context is not None:
        files={"context.md":context,"CLAUDE.md":"只读决策夹具；context.md是本例输入事实，不声称运行了目标程序。所有shell命令用rtk。\n"}
    for rel,txt in files.items():
        p=wt/rel;p.parent.mkdir(parents=True,exist_ok=True);p.write_text(txt)
    for cmd in [
        ["git","init","-b","fixture"],
        ["git","config","user.name","QF fixture"],
        ["git","config","user.email","qf-fixture@example.invalid"],
        ["git","add","."],
        ["git","-c","core.hooksPath=/dev/null","commit","-m","fixture baseline"]]:
        subprocess.run(["rtk","proxy",*cmd],cwd=wt,check=True,stdout=subprocess.DEVNULL)
```

probe.py：
```python
import argparse,hashlib,json,os,re,signal,subprocess,tarfile,time,uuid
from pathlib import Path
from fixture import make_fixture
ROOT=Path.cwd().resolve()
F=ROOT/".spec-dev/2026-09-08-01-quick-fix-diagnosis"
a=argparse.ArgumentParser()
a.add_argument("run");a.add_argument("case",choices=[f"S{i:02}" for i in range(1,25)])
a.add_argument("mode",choices=["decision","diagnose","repair"])
a.add_argument("--prior");a.add_argument("--seed-instrumentation",action="store_true")
v=a.parse_args();assert re.fullmatch(r"[A-Za-z0-9_-]+",v.run)
out=F/"acceptance/model"/v.run/(v.case+"-"+v.mode)
out.mkdir(parents=True,exist_ok=False)
spec=(F/"spec/quick-fix-diagnosis-design.md").read_text()
m=re.search(r"^#### Scenario: "+v.case+r" ([^\n]+)\n(.*?)(?=\n#### Scenario:|\n### Requirement:|\n## |\Z)",spec,re.M|re.S)
assert m
parts={key:re.search(r"- \*\*"+key+r"\*\* (.+)",m.group(2)).group(1) for key in ["GIVEN","WHEN","THEN"]}
detail_path=F/"execution/decision-contexts"/(v.case+".md")
detail=detail_path.read_text() if v.mode=="decision" and detail_path.exists() else ""
(out/"oracle.json").write_text(json.dumps({"scenario":v.case,"name":m.group(1),"expected":parts["THEN"]},ensure_ascii=False,indent=2)+"\n")
def git(*args,cwd=ROOT):
    return subprocess.check_output(["rtk","proxy","git",*args],cwd=cwd,text=True).strip()
tracked=[p for p in git("ls-files").splitlines() if not p.startswith(".spec-dev/")]
def hashes():
    return {p:hashlib.sha256((ROOT/p).read_bytes()).hexdigest() if (ROOT/p).is_file() else None for p in tracked}
before=hashes()
prior_text=""
if v.mode=="repair":
    assert v.prior
    prior=Path(v.prior).resolve();assert prior.is_relative_to(F/"acceptance/model")
    pf=json.loads((prior/"facts.json").read_text())
    judge=json.loads((prior/"judge.json").read_text())
    assert pf["mode"]=="diagnose" and pf["transport_ok"] and judge["verdict"]=="pass"
    assert pf["candidate_before"]==before
    wt=Path(pf["fixture"]);assert wt.is_relative_to(ROOT/".worktrees/qf-qa")
    assert git("rev-parse","--show-toplevel",cwd=wt)==str(wt)
    rows=[json.loads(x) for x in (prior/"stdout.jsonl").read_text().splitlines() if x.strip()]
    result=next(x for x in reversed(rows) if x.get("type")=="result" and not x.get("is_error"))
    prior_text=result.get("result","");assert prior_text
else:
    assert not v.prior
    wt=ROOT/".worktrees/qf-qa"/v.run/(v.case+"-"+v.mode)
    assert not wt.exists()
    state=json.loads((F/"plan/progress.yaml").read_text())
    resource="fixture: "+str(wt)+" —— 归档并核验后由T06按实际路径清理"
    if resource not in state["resources"]:
        state["resources"].append(resource)
        tmp=F/"plan/progress.yaml.tmp";assert not tmp.exists()
        tmp.write_text(json.dumps(state,ensure_ascii=False,indent=2)+"\n");os.replace(tmp,F/"plan/progress.yaml")
        subprocess.run(["rtk","proxy","env","SKIP_RELEASE_HOOK=1","git","add",str(F/"plan/progress.yaml")],check=True)
        subprocess.run(["rtk","proxy","env","SKIP_RELEASE_HOOK=1","git","commit","-m","chore(qa): register owned quick-fix fixture"],check=True)
    context=("# 决策夹具事实\nGIVEN "+parts["GIVEN"]+"\nWHEN "+parts["WHEN"]+"\n") if v.mode=="decision" else None
    if context is not None and detail:context+="\n"+detail
    make_fixture(wt,context=context)
# Host observations are real; scenario-specific decision premises are explicitly supplied facts.
def observe(label):
    d=out/label;d.mkdir()
    facts=[]
    checks=[] if v.mode=="decision" else [("smoke",["node","--test","smoke.test.mjs"]),("replay",["node","replay.mjs"]),("trials",["node","trials.mjs"])]
    for name,cmd in checks:
        r=subprocess.run(["rtk","proxy",*cmd],cwd=wt,capture_output=True)
        (d/(name+".stdout")).write_bytes(r.stdout);(d/(name+".stderr")).write_bytes(r.stderr)
        facts.append({"check":name,"command":["rtk","proxy",*cmd],"cwd":str(wt),"exit":r.returncode})
    (d/"facts.json").write_text(json.dumps(facts,ensure_ascii=False,indent=2)+"\n")
    return facts
if v.seed_instrumentation:
    assert v.mode=="repair"
    with (wt/"cli.mjs").open("a") as code:code.write('console.error("QF_DIAG_TEMP_host_seed");\n')
    (out/"host-seed.json").write_text(json.dumps({"kind":"temporary diagnostic log","marker":"QF_DIAG_TEMP_host_seed","actor":"host fixture setup; not model action"})+"\n")
host_before=observe("host-before")
public_facts=wt/"host-observations.json"
public_facts.write_text(json.dumps(host_before,ensure_ascii=False,indent=2)+"\n")
# The visible inputs contain real raw observations, never the target spec, evals or judge.
for name in ([] if v.mode=="decision" else ["replay.stdout","replay.stderr","trials.stdout","trials.stderr"]):
    (wt/("observed-"+name)).write_bytes((out/"host-before"/name).read_bytes())
family=["skills/quick-fix/SKILL.md","skills/test-driven-development/SKILL.md","skills/clarifying/SKILL.md",
        "skills/requirement-analysis/references/exploration-patterns.md","skills/writing-plans/references/design-principles.md"]
rules="\n\n".join("# "+p+"\n"+(ROOT/p).read_text() for p in family)
scope=("这是只读决策案例；context.md的GIVEN是本例完整分支事实，宿主未运行目标程序。对多例前提分别给出实际回复，不声称执行了未执行动作。仅可Read列出的规则、其引用和本夹具，不写文件。"
       if v.mode=="decision" else
       "这是实际诊断：可读本夹具并运行现有公开CLI/测试，不能写实现、不能提交。按真实证据输出给用户的根因/修法确认消息；需要用户决定时停下。"
       if v.mode=="diagnose" else
       "这是同一夹具的修复续接。模拟用户确认：认可上一轮经证据支持的根因与修法，契约不变，公共CLI落点已批准；允许修cli.mjs并新增公共回归测试，允许本次归属明确的临时插桩清理，不授权改原有smoke/replay/trials/用户日志或提交。不要触发可选acceptance-qa；执行最低验证和修复收尾。")
case_input=("GIVEN "+parts["GIVEN"]+"\nWHEN "+parts["WHEN"]) if v.mode=="decision" else "用户症状：目录先访问英文id7，随后导出同一id的中文标题却仍显示英文。公开契约在夹具spec中，复现入口node replay.mjs。"
if detail:case_input+="\n"+detail
prompt=scope+"\n所有shell命令用rtk。候选根只读："+str(ROOT)+"；唯一夹具："+str(wt)+"。\n不联网、不推送、不部署、不发消息；不得读取本特性spec/plan/evals/acceptance或其他案例；宿主oracle和judge不属于输入。工具白名单不是沙箱，范围仍由本说明和事后审计约束。\n"+rules+"\n输入：\n"+case_input+"\n夹具文件："+", ".join(str(p.relative_to(wt)) for p in wt.rglob("*") if p.is_file() and ".git" not in p.parts)
if v.seed_instrumentation:prompt+="\n宿主为本例明确物化的历史前提：本次有效红之后已获授权加入QF_DIAG_TEMP_host_seed临时日志；本轮可清理此插桩，不能将其归因于模型上一轮实际新增。"
if prior_text:prompt+="\n上一轮真实诊断回复：\n"+prior_text
(out/"input.txt").write_text(prompt)
tools="Read" if v.mode=="decision" else "Read,Glob,Grep,Bash" if v.mode=="diagnose" else "Read,Glob,Grep,Bash,Edit,Write"
cmd=["rtk","proxy","claude","-p","--plugin-dir",str(ROOT),"--add-dir",str(ROOT/"skills"),
     "--permission-mode","default","--strict-mcp-config","--mcp-config",'{"mcpServers":{}}',
     "--tools",tools,"--allowedTools","Read","Glob","Grep"]
if v.mode!="decision":cmd+=["Bash(rtk *)"]
if v.mode=="repair":
    cmd += [f"Edit(//{str(wt).lstrip('/')}/**)",f"Write(//{str(wt).lstrip('/')}/**)"]
cmd+=["--no-session-persistence","--verbose","--output-format","stream-json",prompt]
if os.environ.get("QF_MODEL"):cmd[4:4]=["--model",os.environ["QF_MODEL"]]
limit=float(os.environ.get("QF_TIMEOUT_SECONDS","0"));assert 0<=limit<float("inf")
start=time.monotonic();timed_out=False
with (out/"stdout.jsonl").open("w") as so,(out/"stderr.log").open("w") as se:
    proc=subprocess.Popen(cmd,cwd=wt,stdin=subprocess.DEVNULL,stdout=so,stderr=se,start_new_session=True)
    (out/"process.json").write_text(json.dumps({"pid":proc.pid,"pgid":os.getpgid(proc.pid),"cwd":str(wt),
        "candidate":str(ROOT),"command":cmd[:-1],"timeout_seconds":limit},indent=2)+"\n")
    while proc.poll() is None:
        try:proc.wait(timeout=30)
        except subprocess.TimeoutExpired:print(v.case,v.mode,"running",round(time.monotonic()-start),flush=True)
        if limit and time.monotonic()-start>=limit and proc.poll() is None:
            timed_out=True;os.killpg(proc.pid,signal.SIGTERM)
            try:proc.wait(timeout=10)
            except subprocess.TimeoutExpired:os.killpg(proc.pid,signal.SIGKILL);proc.wait()
rows=[]
for line in (out/"stdout.jsonl").read_text().splitlines():
    try:rows.append(json.loads(line))
    except json.JSONDecodeError:pass
results=[x for x in rows if x.get("type")=="result"]
transport_ok=proc.returncode==0 and not timed_out and bool(results) and not results[-1].get("is_error")
host_after=observe("host-after")
(out/"git-status.txt").write_text(git("status","--porcelain=v1","--untracked-files=all",cwd=wt)+"\n")
(out/"git-diff.patch").write_text(git("diff","HEAD",cwd=wt)+"\n")
with tarfile.open(out/"fixture.tar.gz","w:gz") as tar:
    for p in sorted(wt.rglob("*")):
        if p.is_file() and ".git" not in p.parts:tar.add(p,arcname=str(p.relative_to(wt)),recursive=False)
subprocess.run(["rtk","proxy","git","bundle","create",str(out/"fixture.bundle"),"--all"],cwd=wt,check=True)
subprocess.run(["rtk","proxy","git","bundle","verify",str(out/"fixture.bundle")],cwd=wt,check=True)
facts={"case":v.case,"mode":v.mode,"fixture":str(wt),"candidate_before":before,"candidate_after":hashes(),
       "exit":proc.returncode,"timed_out":timed_out,"transport_ok":transport_ok,"elapsed":time.monotonic()-start,
       "host_before":host_before,"host_after":host_after,"verdict":"needs_independent_review",
       "model_init":[x for x in rows if x.get("type")=="system" and x.get("subtype")=="init"],
       "result_metadata":[{k:z for k,z in x.items() if k!="result"} for x in results]}
(out/"facts.json").write_text(json.dumps(facts,ensure_ascii=False,indent=2)+"\n")
assert facts["candidate_before"]==facts["candidate_after"],"candidate product changed"
raise SystemExit(0 if transport_ok else 2)
```


以如下代码追加本票11个Scenario原文GIVEN/WHEN及独立THEN。未来执行每个Python块均用rtk proxy python3的完整heredoc；下面是完整内容，不依赖作者临时文件。

```python
import json,re
from pathlib import Path
spec=Path(".spec-dev/2026-09-08-01-quick-fix-diagnosis/spec/quick-fix-diagnosis-design.md").read_text()
p=Path("skills/quick-fix/evals/evals.json");data=json.loads(p.read_text())
for sid in ["S01","S02","S03","S04","S17","S18","S19","S20","S21","S22","S23"]:
    m=re.search(r"^#### Scenario: "+sid+r" ([^\n]+)\n(.*?)(?=\n#### Scenario:|\n### Requirement:|\n## |\Z)",spec,re.M|re.S)
    assert m
    parts={k:re.search(r"- \*\*"+k+r"\*\* (.+)",m.group(2)).group(1) for k in ["GIVEN","WHEN","THEN"]}
    name="qf-diagnosis-"+sid.lower()
    assert not any(x["id"]==name for x in data["evals"]),"already materialized; inspect recovery"
    data["evals"].append({"id":name,"prompt":m.group(1)+"\nGIVEN "+parts["GIVEN"]+"\nWHEN "+parts["WHEN"],"expected_output":parts["THEN"]})
p.write_text(json.dumps(data,ensure_ascii=False,indent=2)+"\n")
```


probe每次将已知fixture路径先登记progress并提交再创建；fixture位于已忽略的.worktrees/qf-qa下，独立git root与原始提交在宿主记录/归档中核实。输出证据在acceptance/model，归档不直接暂存嵌套.git。无任何当前模型调用在写计划阶段运行。

**步骤 2：运行并确认有效失败**

执行期补齐S21/S22原GIVEN省略的具体根因/候选事实，文件为execution/decision-contexts/S21.md与S22.md。独立判读确认原run输入不足；原件保持unverified，新run t01-red-detail用于公平基线，THEN不变。补充事实随context.md与input.txt传入模型，不输入oracle。

```python
import subprocess
for sid in ["S01","S02","S03","S04","S17","S18","S19","S20","S21","S22","S23"]:
    subprocess.run(["rtk","proxy","python3",".spec-dev/2026-09-08-01-quick-fix-diagnosis/execution/probe.py","t01-red",sid,"decision"],check=True)
```
另以S01执行实际诊断，核对确认根因之前的真实命令，不能仅用只读回复替代：

```bash
rtk proxy python3 .spec-dev/2026-09-08-01-quick-fix-diagnosis/execution/probe.py t01-red-action S01 diagnose
```

对 S01、S02、S03、S04、S17、S18、S19、S20、S21、S22、S23 的每个实际run由只读独立检查者判读：输入为本spec对应THEN、input.txt、stdout.jsonl、facts.json、host-before/after和fixture快照；检查是否偷读oracle/越权动作、是否只复述将会做而未提供本例实际决定。输出index定义的judge.json，由主线程保存到该run；reason必须指具体输出或动作，evidence必须存在。传输失败记unverified，不计红；语义失败保留fail。代码缺某句、exit0和模型自报都不作语义判据。多例GIVEN分别判，不以其中一个通过代替另一例。基线已有通过的场景保留已有覆盖；本票实质规则变化须找到对应的实际行为失败，否则停止变更并报告，不通过写同义正则制造红。
decision夹具只含分支事实，不提供与其冲突的公共CLI源码，也不伪造命令执行。diagnose/repair动作夹具才真实运行smoke/replay/trials：原夹具smoke exit0、replay目标断言exit1、trials exit1且6/20；若不符合先修验证夹具/环境，不能当产品红。decision输出只证明分支决策，S01实际执行顺序还需T02/T03动作链验证。遇到不可用CLI或供应方错误停下保留原件；QF_MODEL只在已有授权时使用，否则继承实际配置；QF_TIMEOUT_SECONDS默认0，不擅加宿主截止时间，用户调整时记录。

**步骤 3：应用已由红证据支持的最小规则**

以下替换逐一要求唯一旧锚点；发现已应用则核对Git和原证据，不重复执行。某切面基线已实现且无需改变，记录该切面已有覆盖，不能盲改。

```python
import json
from pathlib import Path
edits=[
  {
    "path": "skills/quick-fix/SKILL.md",
    "old": "  轻量 bug 修复工作流——已决定要修、无设计空间的小修复（小 bug、小调整、计划执行后的小问题）时使用：定位根因（含 spec 反查）、逐题校对、TDD 修复、可选验收。按契约影响分流以规避 spec 漂移；根因涉及跨 spec 契约/跨模块/新依赖时建议升级 requirement-analysis。不适用于新功能或任何有设计空间的需求（用 requirement-analysis），也不适用于尚未决定要不要做的想法（用 exploring）。",
    "new": "  轻量 bug 修复工作流——已决定要修、无设计空间的小修复时使用：先按证据定位根因（含 spec 反查），逐题校对，沿获批落点 TDD 修复，可选验收。跨 spec 契约、跨模块、新依赖、现行契约冲突或诊断证据不足时提议升级 requirement-analysis；偶发但可比较可继续。新功能、有设计空间的需求用 requirement-analysis；未决定要不要做用 exploring。"
  },
  {
    "path": "skills/quick-fix/SKILL.md",
    "old": "以 spec 反查与契约分流保证修复不制造文档漂移。",
    "new": "以 spec 反查与契约分流保证修复不制造文档漂移；非显然根因先取得真实诊断红信号，证据不足时交用户裁决。"
  },
  {
    "path": "skills/quick-fix/SKILL.md",
    "old": "一旦根因触及行为契约的跨模块改动或新依赖，就把控制权交还用户、建议升级 requirement-analysis",
    "new": "命中步骤 2.5 的影响范围、现行契约冲突或证据不足信号时，就把控制权交还用户、建议升级 requirement-analysis"
  },
  {
    "path": "skills/quick-fix/SKILL.md",
    "old": "### 步骤 2.5：升级门（用户裁决式护栏）",
    "new": "**诊断前置**：根因非显而易见时，优先实际运行已有测试、CLI/API 或原场景回放，在根因认定前给出输入/触发条件、命令及针对原症状的失败输出。环境、鉴权、缺依赖或编译失败不算该 bug 的诊断红；先恢复条件或如实交接缺口。已有有效证据仍适用于当前代码和条件时直接复用，不强制新建测试。写新测试前先消费获批 seam，落点未定不先写某个候选测试；生产源码插桩仍受现行 TDD/例外授权约束。\n\n**显然小修**：症状、具体代码条件与纠正方向唯一对应时，说明依据可免额外诊断前置；只免此步骤，后续 TDD 和例外授权仍按原规则。不能以“显然”免测或虚构失败。\n\n**有界调查与证据进展**：调查前说明当前对象、手段和要获得的证据；完成后若只剩重复尝试/换措辞猜测，无支持、排除、区分候选或形成有依据新候选的证据，整理已试方法、原输出、排除项、未决条件和下一步，进入认知性升级。有新可验证线索可继续限定调查，不设固定轮数或重型循环。子代理失败与主线程明显降质分别遵循 exploration-patterns「派发要求与失败隔离」，不在此重造重试或止损规则。\n\n### 步骤 2.5：升级门（用户裁决式护栏）"
  },
  {
    "path": "skills/quick-fix/SKILL.md",
    "old": "基于**看过代码根因之后**的事实判定——比分诊时拍脑袋准。",
    "new": "基于**当前代码、复现和调查证据**判定，允许尚未定位根因时报告证据不足；不把工具调用错误本身等同于 bug 复杂。"
  },
  {
    "path": "skills/quick-fix/SKILL.md",
    "old": "- **引入新依赖**（需要新第三方库/框架）。",
    "new": "- **引入新依赖**（需要新第三方库/框架）；\n- **现行契约冲突**（同一行为面的两份 active spec 相互矛盾且互无取代声明，见步骤 2 的时效处理）；\n- **诊断证据不足**（当前有界调查结束后仍无法可靠捕获/比较原症状，或无证据进展、根因仍缺足够依据）。偶发但有实际失败样本和可比较条件可继续轻量诊断，不单因 flaky 升级；其他影响范围信号仍适用。"
  },
  {
    "path": "skills/quick-fix/SKILL.md",
    "old": "不自动切换、不强制终止。",
    "new": "不自动切换、不强制终止。继续只表示保留 quick-fix 流程：缺红/落点时限于获授权取证，双 spec 冲突仍先裁决；不能据此伪造根因、跳过 TDD 或绕过契约同步。升级交接保留已试方法、失败输出、候选状态和缺少条件；调用失败与产品证据不足分别记录。"
  },
  {
    "path": "skills/quick-fix/SKILL.md",
    "old": "1. **根因认定**对不对；",
    "new": "1. **根因认定或下一步调查方向**：已唯一定位则给一个根因及证据，不凑候选；尚未唯一确定但有区分依据时给 2–3 个排序候选，各附依据和可证伪预测（若是 X，观察 Y 应出现 Z）。明确证实/待验证状态，一次只确认当前事项；用户选择调查方向不证明根因，出现反证即记录并修正。不得在修法/落点决定前改实现验证猜测；"
  },
  {
    "path": "skills/quick-fix/agents/openai.yaml",
    "old": "  short_description: \"小修复：根因与契约逐题校对，沿已确认落点红绿修复；TDD 例外和纯重构引用单点，验收保持可选。\"",
    "new": "  short_description: \"小修复：非显然根因先取证，证据不足提议升级；沿获批落点 TDD 修复，验收保持可选。\""
  }
]
for e in edits:
    p=Path(e["path"]);s=p.read_text()
    assert s.count(e["old"])==1,(e["path"],"anchor drift; inspect before editing")
    p.write_text(s.replace(e["old"],e["new"]))
```


**步骤 4：复验规则和相关回归**

```python
import subprocess
for sid in ["S01","S02","S03","S04","S17","S18","S19","S20","S21","S22","S23"]:
    subprocess.run(["rtk","proxy","python3",".spec-dev/2026-09-08-01-quick-fix-diagnosis/execution/probe.py","t01-green",sid,"decision"],check=True)
```
```bash
rtk proxy python3 .spec-dev/2026-09-08-01-quick-fix-diagnosis/execution/probe.py t01-green-action S01 diagnose
```
独立核对S01的实际Read/回放及根因确认顺序；decision的边界判读与此动作证据分列。只读案例不能证明尚未发生的修复动作；S05/S08/S10/S11/S12/S14的真实修复链由T03承担。

对 S01、S02、S03、S04、S17、S18、S19、S20、S21、S22、S23 的每个实际run由只读独立检查者判读：输入为本spec对应THEN、input.txt、stdout.jsonl、facts.json、host-before/after和fixture快照；检查是否偷读oracle/越权动作、是否只复述将会做而未提供本例实际决定。输出index定义的judge.json，由主线程保存到该run；reason必须指具体输出或动作，evidence必须存在。传输失败记unverified，不计红；语义失败保留fail。代码缺某句、exit0和模型自报都不作语义判据。多例GIVEN分别判，不以其中一个通过代替另一例。基线已有通过的场景保留已有覆盖；本票实质规则变化须找到对应的实际行为失败，否则停止变更并报告，不通过写同义正则制造红。
绿run的全部适用场景需独立pass；未通过或未覆盖不推进。与基线使用相同GIVEN/WHEN和宿主夹具口径，规则变化只在候选技能。重试用新run ID，不改输入来追绿；反馈修订保留旧失败。
```bash
rtk proxy python3 .spec-dev/2026-09-08-01-quick-fix-diagnosis/execution/record.py T01 related-green -- rtk proxy node --test scripts/tests/plugin-root.test.mjs scripts/tests/search-clause.test.mjs
rtk proxy python3 .spec-dev/2026-09-08-01-quick-fix-diagnosis/execution/record.py T01 skills-green -- rtk proxy node scripts/validate-skills.mjs
rtk proxy python3 .spec-dev/2026-09-08-01-quick-fix-diagnosis/execution/record.py T01 plugin-green -- rtk proxy node scripts/check-plugin.mjs --codex-validate
rtk proxy python3 .spec-dev/2026-09-08-01-quick-fix-diagnosis/execution/record.py T01 sync-green -- rtk proxy node scripts/check-openai-sync.mjs
```


**步骤 5：提交并保存已验证状态**

```bash
rtk git add skills/quick-fix/SKILL.md skills/quick-fix/agents/openai.yaml .spec-dev/2026-09-08-01-quick-fix-diagnosis/execution/fixture.py .spec-dev/2026-09-08-01-quick-fix-diagnosis/execution/probe.py skills/quick-fix/evals/evals.json .spec-dev/2026-09-08-01-quick-fix-diagnosis/acceptance/model .spec-dev/2026-09-08-01-quick-fix-diagnosis/execution/serial/T01
rtk proxy env SKIP_RELEASE_HOOK=1 git commit -m 'feat(T01): implement verified quick-fix diagnosis'
rtk proxy python3 - <<'PY'
import subprocess
sha=subprocess.check_output(["rtk","proxy","git","rev-parse","HEAD"],text=True).strip()
subprocess.run(["rtk","proxy","python3",".spec-dev/2026-09-08-01-quick-fix-diagnosis/execution/state.py","T01","completed","--commit",sha,"--tests","pass"],check=True)
PY
rtk git add .spec-dev/2026-09-08-01-quick-fix-diagnosis/plan/progress.yaml
rtk proxy env SKIP_RELEASE_HOOK=1 git commit -m 'chore(T01): save verified completion'
```

新增脚本仅验证本特性，未触碰通用review-runner。失败使用state.py T01 blocked、追加原因并单独提交progress，保留全部原件。
