### 任务 2：真实故障落点与修复对象边界

**文件**：
- 修改：skills/quick-fix/SKILL.md 步骤1/5a；skills/quick-fix/agents/openai.yaml；skills/quick-fix/evals/evals.json。
- 测试/证据：.spec-dev/2026-09-08-01-quick-fix-diagnosis/acceptance/model/t02-red/、t02-green/、t02-action/；.spec-dev/2026-09-08-01-quick-fix-diagnosis/execution/serial/T02/。

**接口**：
- 消费：probe/state/record；Spec A03/A08，S05–S07/S15–S16；T01诊断确认协议。
- 产出：获批CLI实际诊断记录；seam与对象规则；S05–S07/S15–S16证据。
- 公开seam为node cli.mjs scene.json的stdout/exit；不测试内部title函数。S07唯一提取与未决候选两例分别验证；S15恢复输入携带已有决定，不宣称实际触发了平台压缩。

**步骤 1：追加五个场景**
```bash
rtk proxy python3 - <<'PY'
import json
from pathlib import Path
s=json.loads(Path(".spec-dev/2026-09-08-01-quick-fix-diagnosis/plan/progress.yaml").read_text())
assert s["tasks"]["T01"]["status"]=="completed"
assert s["tasks"]["T02"]["status"] in ["pending","blocked","in_progress"]
PY
rtk proxy python3 .spec-dev/2026-09-08-01-quick-fix-diagnosis/execution/state.py T02 in_progress
rtk git add .spec-dev/2026-09-08-01-quick-fix-diagnosis/plan/progress.yaml
rtk proxy env SKIP_RELEASE_HOOK=1 git commit -m 'chore(T02): start bounded task'
```

```python
import json,re
from pathlib import Path
spec=Path(".spec-dev/2026-09-08-01-quick-fix-diagnosis/spec/quick-fix-diagnosis-design.md").read_text()
p=Path("skills/quick-fix/evals/evals.json");data=json.loads(p.read_text())
for sid in ["S05","S06","S07","S15","S16"]:
    m=re.search(r"^#### Scenario: "+sid+r" ([^\n]+)\n(.*?)(?=\n#### Scenario:|\n### Requirement:|\n## |\Z)",spec,re.M|re.S)
    assert m
    parts={k:re.search(r"- \*\*"+k+r"\*\* (.+)",m.group(2)).group(1) for k in ["GIVEN","WHEN","THEN"]}
    name="qf-diagnosis-"+sid.lower()
    assert not any(x["id"]==name for x in data["evals"]),"already materialized; inspect recovery"
    data["evals"].append({"id":name,"prompt":m.group(1)+"\nGIVEN "+parts["GIVEN"]+"\nWHEN "+parts["WHEN"],"expected_output":parts["THEN"]})
p.write_text(json.dumps(data,ensure_ascii=False,indent=2)+"\n")
```


**步骤 2：确认旧行为的实际缺口**
```python
import subprocess
for sid in ["S05","S06","S07","S15","S16"]:
    subprocess.run(["rtk","proxy","python3",".spec-dev/2026-09-08-01-quick-fix-diagnosis/execution/probe.py","t02-red",sid,"decision"],check=True)
```

对 S05、S06、S07、S15、S16 的每个实际run由只读独立检查者判读：输入为本spec对应THEN、input.txt、stdout.jsonl、facts.json、host-before/after和fixture快照；检查是否偷读oracle/越权动作、是否只复述将会做而未提供本例实际决定。输出index定义的judge.json，由主线程保存到该run；reason必须指具体输出或动作，evidence必须存在。传输失败记unverified，不计红；语义失败保留fail。代码缺某句、exit0和模型自报都不作语义判据。多例GIVEN分别判，不以其中一个通过代替另一例。基线已有通过的场景保留已有覆盖；本票实质规则变化须找到对应的实际行为失败，否则停止变更并报告，不通过写同义正则制造红。

**步骤 3：扩充原位规则**
```python
import json
from pathlib import Path
edits=[
  {
    "path": "skills/quick-fix/SKILL.md",
    "old": "### 步骤 2：定位根因 + spec 反查",
    "new": "**修复对象边界**：以同一用户症状及经证据确认的根因链为对象。多文件、多调用方、跨轮次或上下文压缩不自动拆对象；恢复沿已有证据和决定继续，跨模块修法仍走升级门。发现无关根因的旁支只记录症状、位置与后续入口；用户明确扩大范围时更新对象记录和路由，不默认顺手修。\n\n### 步骤 2：定位根因 + spec 反查"
  },
  {
    "path": "skills/quick-fix/SKILL.md",
    "old": "- **同步 spec**：",
    "new": "- **真实故障对应检查**：确认获批公共 seam 覆盖真实输入、调用顺序及多调用方触发模式，不能仅以孤立纯函数绿代替真实故障。无 seam 标签但批准接口与 Scenario 唯一对应时记录来源直接消费；多候选或冲突仍并入原确认，决定前不写候选测试。只有实际调用/观察证据表明公共边界无法覆盖目标行为时，才说明具体结构障碍，沿原澄清/升级处置；共享判据见 writing-plans/references/design-principles.md。不因架构发现自动重构、制造私有接口或无测试交付。5b 同样适用。\n- **同步 spec**："
  },
  {
    "path": "skills/quick-fix/agents/openai.yaml",
    "old": "  short_description: \"小修复：非显然根因先取证，证据不足提议升级；沿获批落点 TDD 修复，验收保持可选。\"",
    "new": "  short_description: \"小修复：证据式诊断与升级，按同根因链收敛范围；沿获批公共落点覆盖真实故障，TDD 与可选验收沿原规则。\""
  }
]
for e in edits:
    p=Path(e["path"]);s=p.read_text()
    assert s.count(e["old"])==1,(e["path"],"anchor drift; inspect before editing")
    p.write_text(s.replace(e["old"],e["new"]))
```


**步骤 4：真实决策与诊断动作复验**
```python
import subprocess
for sid in ["S05","S06","S07","S15","S16"]:
    subprocess.run(["rtk","proxy","python3",".spec-dev/2026-09-08-01-quick-fix-diagnosis/execution/probe.py","t02-green",sid,"decision"],check=True)
```

```bash
rtk proxy python3 .spec-dev/2026-09-08-01-quick-fix-diagnosis/execution/probe.py t02-action S05 diagnose
```

对 S05、S06、S07、S15、S16 的每个实际run由只读独立检查者判读：输入为本spec对应THEN、input.txt、stdout.jsonl、facts.json、host-before/after和fixture快照；检查是否偷读oracle/越权动作、是否只复述将会做而未提供本例实际决定。输出index定义的judge.json，由主线程保存到该run；reason必须指具体输出或动作，evidence必须存在。传输失败记unverified，不计红；语义失败保留fail。代码缺某句、exit0和模型自报都不作语义判据。多例GIVEN分别判，不以其中一个通过代替另一例。基线已有通过的场景保留已有覆盖；本票实质规则变化须找到对应的实际行为失败，否则停止变更并报告，不通过写同义正则制造红。
实际diagnose需看见模型Read公开契约与源码、真实运行公开回放，并形成有证据的根因/修法确认；不能仅依赖宿主提前跑过回放。此段写权限不开放，只检查诊断，不以其通过声称已修复；有权限/工具障碍如实unverified。原始完整端到端repair留T03。S06证据需指出实际观察障碍，不能因“没有测试”造架构发现。
```bash
rtk proxy python3 .spec-dev/2026-09-08-01-quick-fix-diagnosis/execution/record.py T02 related-green -- rtk proxy node --test scripts/tests/plugin-root.test.mjs scripts/tests/search-clause.test.mjs
rtk proxy python3 .spec-dev/2026-09-08-01-quick-fix-diagnosis/execution/record.py T02 skills-green -- rtk proxy node scripts/validate-skills.mjs
rtk proxy python3 .spec-dev/2026-09-08-01-quick-fix-diagnosis/execution/record.py T02 plugin-green -- rtk proxy node scripts/check-plugin.mjs --codex-validate
rtk proxy python3 .spec-dev/2026-09-08-01-quick-fix-diagnosis/execution/record.py T02 sync-green -- rtk proxy node scripts/check-openai-sync.mjs
```


**步骤 5：提交**
```bash
rtk git add skills/quick-fix/SKILL.md skills/quick-fix/agents/openai.yaml skills/quick-fix/evals/evals.json .spec-dev/2026-09-08-01-quick-fix-diagnosis/acceptance/model .spec-dev/2026-09-08-01-quick-fix-diagnosis/execution/serial/T02
rtk proxy env SKIP_RELEASE_HOOK=1 git commit -m 'feat(T02): implement verified quick-fix seam and scope'
rtk proxy python3 - <<'PY'
import subprocess
sha=subprocess.check_output(["rtk","proxy","git","rev-parse","HEAD"],text=True).strip()
subprocess.run(["rtk","proxy","python3",".spec-dev/2026-09-08-01-quick-fix-diagnosis/execution/state.py","T02","completed","--commit",sha,"--tests","pass"],check=True)
PY
rtk git add .spec-dev/2026-09-08-01-quick-fix-diagnosis/plan/progress.yaml
rtk proxy env SKIP_RELEASE_HOOK=1 git commit -m 'chore(T02): save verified completion'
```
