### 任务 6：全矩阵验收与独立审查

**文件**：
- 创建：本特性execution/qa.mjs、model.py（完整代码见下文及后续块）、acceptance/下真实回执/报告。
- 修改：本spec实际交付记录、plan/progress.yaml；必要的产品修复返回责任票并重新验证，不能降低THEN。
- 资源：实际实施工作区 `.worktrees/status-overview-qa/acceptance-RUN`、model-RUN；创建前通过state.py登记并单独提交。

**接口**：
- 消费：T05完整候选与所有T01—T05红绿证据；26Scenario、既有审查/acceptance-qa技能。
- 产出：S01—S26逐项独立判定、完整性覆盖、全库验证、真实模型三例及资源/源码冻结证据；全部必需项通过才允许T07。

进入T06先保存当前任务检查点（前序必须completed）：
```bash
rtk proxy python3 .spec-dev/2026-09-10-01-status-overview/execution/state.py T06 in_progress
rtk git add .spec-dev/2026-09-10-01-status-overview/plan/progress.yaml
rtk proxy env SKIP_RELEASE_HOOK=1 git commit -m 'chore(T06): start status acceptance'
```

**步骤 1：冻结候选与全量安全网**

先持有T00绑定、前序completed且无产品未提交改动。执行器按review-orchestration实际编排正常规模A/B/C/S审查及completeness；派发读spec现行条款，源码只读；问题成立回责任票修复并留下原失败。不要让审查者只看模型自报或只复述计划。模型、预算沿用户/宿主配置，不指定历史模型名字。

```bash
rtk proxy python3 .spec-dev/2026-09-10-01-status-overview/execution/record.py T06 full node --test scripts/tests/*.test.mjs
rtk proxy python3 .spec-dev/2026-09-10-01-status-overview/execution/record.py T06 plugin node scripts/check-plugin.mjs --codex-validate
rtk proxy python3 .spec-dev/2026-09-10-01-status-overview/execution/record.py T06 skills node scripts/validate-skills.mjs
rtk proxy python3 .spec-dev/2026-09-10-01-status-overview/execution/record.py T06 sync node scripts/check-openai-sync.mjs
```

全量测试前停止所有写证据/夹具的后台进程，避免对worktree不变性测试产生竞争；同候选全量通过后，不无理由重复。S01—S25机器CLI与公开collectStatus查询；S26模型动作独立判读；nightly多trial单列not_run。

**步骤 2：真实机器矩阵**

将下方完整代码保存本特性execution/qa.mjs。调用前给本次RUN登记fixture资源、将T06置in_progress并提交progress；RUN每次唯一，不复用失败目录。qa只写自建测试数据及证据，产品查询仍只读；tar只归档自建无凭据夹具。临时Git身份仅夹具有效，禁改用户Git配置。

```javascript

import * as fs from 'node:fs/promises';
import path from 'node:path';
import assert from 'node:assert/strict';
import {execFileSync,spawnSync} from 'node:child_process';
import {createHash} from 'node:crypto';
import {collectStatus,renderStatus} from '../../../scripts/lib/status.mjs';
const ROOT=process.cwd(),F=path.resolve('.spec-dev/2026-09-10-01-status-overview'),run=process.argv[2];
assert.match(run??'',/^[a-zA-Z0-9_-]+$/);
const scope=path.join(ROOT,'.worktrees/status-overview-qa','acceptance-'+run),out=path.join(F,'acceptance','machine',run),entry=path.join(ROOT,'scripts/status.mjs');
await fs.mkdir(scope,{recursive:false});await fs.mkdir(out,{recursive:true});
const git=(cwd,...args)=>execFileSync('git',args,{cwd,encoding:'utf8',stdio:['ignore','pipe','pipe'],env:{...process.env,GIT_OPTIONAL_LOCKS:'0'}}).trim();
const write=async(root,p,s)=>{const file=path.join(root,p);await fs.mkdir(path.dirname(file),{recursive:true});await fs.writeFile(file,s);};
const meta=status=>'---\nspec_dev:\n  feature: F\n  status: '+status+'\n---\n';
const nav=ids=>'| 任务 | 依赖 | 消费接口 | 产出接口 |\n|---|---|---|---|\n'+ids.map((id,i)=>`| ${id} title | ${i?ids[i-1]:'—'} | a | b |`).join('\n');
async function plan(root,options={}){
 const key=options.key??'F',ids=options.ids??['T00','T01'];await write(root,`.spec-dev/${key}/spec/f-design.md`,meta(options.specStatus??'active'));
 await write(root,`.spec-dev/${key}/plan/index.md`,nav(ids));for(const id of ids)await write(root,`.spec-dev/${key}/plan/tasks/${id}.md`,'forbidden task body');
 const state={format_version:1,current:null,tasks:Object.fromEntries(ids.map((id,i)=>[id,{status:i===0?'completed':'pending'}])),resources:[],notes:[],...options.state};
 await write(root,`.spec-dev/${key}/plan/progress.yaml`,options.text??JSON.stringify(state));
}
async function roadmap(root,status='pending',target='.spec-dev/F/'){
 await write(root,'.spec-dev/roadmaps/p.md','---\nspec_dev_roadmap:\n  project: P\n  status: active\n---\n## 子项目\n| # | 子项目 | 范围 | 依赖 | 状态 | 特性目录 |\n|---|---|---|---|---|---|\n| 1 | F | scope | — | '+status+' | '+target+' |');
}
function call(root,args=['--json']){
 const p=spawnSync(process.execPath,[entry,...args],{cwd:root,encoding:'utf8'});let data=null;try{data=JSON.parse(p.stdout);}catch{}
 return {exit:p.status,stdout:p.stdout,stderr:p.stderr,data};
}
const first=r=>r.data.features[0].sources[0];
const second=async(root,parent,detached=false)=>{const target=path.join(parent,detached?'detached':'other tree');git(root,'worktree','add',...(detached?['--detach']:['-b','other']),target);return target;};
async function digest(root){const rows={};async function walk(p){for(const n of(await fs.readdir(p)).sort()){const f=path.join(p,n),s=await fs.lstat(f);if(s.isDirectory())await walk(f);else if(s.isFile())rows[path.relative(root,f)]=createHash('sha256').update(await fs.readFile(f)).digest('hex');else if(s.isSymbolicLink())rows[path.relative(root,f)]='link:'+await fs.readlink(f);}}await walk(root);return rows;}
async function makeV2(root){
 const a='a'.repeat(40),b='b'.repeat(40),ids=['T00','T01','T02','T03','T04'];
 const index='| 任务 | 依赖 | 消费接口 | 产出接口 |\n|---|---|---|---|\n| T00 | — | a | b |\n| T01 | T00 | a | b |\n| T02 | T01 | a | b |\n| T03 | T01-T02 | a | b |\n| T04 | T03 | a | b |\n```json spec-dev-integration\n'+JSON.stringify({protocol_version:1,task_roles:{T00:'isolation',T04:'delivery'},groups:{G01:{members:['T01','T02'],verify:'T03',reason:'atomic behavior'}}})+'\n```';
 const state={format_version:2,current:'T02',tasks:{T00:{status:'completed',commit:a},T01:{status:'awaiting_verification',tests:'pending_group',implementation_commit:b,commit:null,evidence_paths:['e']},T02:{status:'blocked'},T03:{status:'pending'},T04:{status:'pending'}},resources:[],notes:[],integration:{owner:'o',worktree:'/fixture',branch:'b',base_commit:a,validated_commit:a,active_group:'G01',groups:{G01:{status:'blocked',base_commit:a,checkpoint_commit:b,validated_commit:null,evidence_paths:[]}}}};

 await write(root,'.spec-dev/F/spec/f-design.md',meta('active'));await write(root,'.spec-dev/F/plan/index.md',index);await write(root,'.spec-dev/F/plan/progress.yaml',JSON.stringify(state));for(const id of ids)await write(root,'.spec-dev/F/plan/tasks/'+id+'.md','task body');
}
const checks={
 S01:async(root,parent,save)=>{await plan(root);await fs.mkdir(path.join(root,'sub dir'));const r=call(path.join(root,'sub dir'));save(r);assert.equal(r.exit,0);assert.equal(r.data.repository.worktree,root);assert.equal(call(parent,['--repo',root,'--json']).exit,0);},
 S02:async(root,parent,save)=>{for(const args of [['--bad'],['--repo'],['--repo',parent],['--json','--json']]){const r=call(root,args);save(r);assert.equal(r.exit,2);}assert.equal(call(parent,['--help']).exit,0);const bare=path.join(parent,'bare.git');await fs.mkdir(bare);git(bare,'init','--bare','-q');assert.equal(call(root,['--repo',bare]).exit,2);},
 S03:async(root,parent,save)=>{await plan(root);await plan(await second(root,parent));await plan(await second(root,parent,true));const r=call(root);save(r);assert.equal(r.exit,0);assert.equal(r.data.worktrees.length,3);assert.equal(r.data.worktrees.filter(w=>w.detached).length,1);assert.equal(r.data.summary.source_records,3);},
 S04:async(root,parent,save)=>{await plan(root);const other=await second(root,parent);await fs.rename(other,other+'-moved');const r=call(root);save(r);assert.equal(r.exit,1);assert.equal(r.data.features.length,1);},
 S05:async(root,parent,save)=>{await plan(root);await write(root,'.spec-dev/F/acceptance/x/.spec-dev/Fake/plan/progress.yaml','{}');await write(root,'.spec-dev/F/execution/plan/progress.yaml','{}');const r=call(root);save(r);assert.equal(r.exit,0);assert.equal(r.data.features.length,1);},
 S06:async(root,parent,save)=>{await write(root,'docs/2020-01-01-old/plan/a-plan.md','### 任务 0：x\n- [x] done');await fs.mkdir(path.join(root,'.spec-dev'));await fs.symlink(parent,path.join(root,'.spec-dev/external'));const r=call(root);save(r);assert.equal(r.exit,1);assert.ok(r.data.diagnostics.some(d=>d.code==='symlink_skipped'));assert.equal(r.data.features[0].key,'docs/2020-01-01-old');},
 S07:async(root,parent,save)=>{await write(root,'.spec-dev/F/spec/f-design.md',meta('active'));await roadmap(root);const r=call(root);save(r);assert.equal(r.exit,0);assert.equal(first(r).plan,null);assert.equal(first(r).specs[0].status,'active');assert.equal(r.data.roadmaps[0].sources[0].rows[0].status,'pending');},
 S08:async(root,parent,save)=>{await write(root,'.spec-dev/F/spec/f-design.md',meta('delivered'));await roadmap(root,'pending','.spec-dev/Missing/');const r=call(root);save(r);assert.equal(r.exit,1);assert.equal(first(r).specs[0].status,'delivered');assert.ok(r.data.diagnostics.some(d=>d.code==='missing'));},
 S09:async(root,parent,save)=>{await plan(root);const other=await second(root,parent);await plan(other,{text:'format_version: 1\ncurrent: null\ntasks:\n  T00: {status: completed}\n  T01: {status: pending}\nresources: []\nnotes: []'});const r=call(root);save(r);assert.equal(r.exit,0);assert.equal(r.data.features[0].divergence.different,false);},
 S10:async(root,parent,save)=>{await plan(root,{state:{execution:{mode:'parallel'},tasks:{T00:{status:'in_progress'},T01:{status:'in_progress'}}}});const r=call(root);save(r);assert.equal(r.exit,0);assert.equal(first(r).plan.counts.in_progress,2);assert.equal(first(r).plan.current,null);},
 S11:async(root,parent,save)=>{await makeV2(root);const r=call(root);save(r);assert.equal(r.exit,0);const c=first(r).plan.counts;assert.equal(c.total,5);assert.equal(c.completed,1);assert.equal(c.awaiting_verification,1);assert.equal(c.blocked,1);},
 S12:async(root,parent,save)=>{await write(root,'.spec-dev/Old/plan/a-plan.md','### 任务 0：x\n- [x] done\n```md\n### 任务 99\n- [x] fake\n```\n### Task 1: y\n- [ ] open');const r=call(root);save(r);assert.equal(r.exit,0);assert.equal(first(r).plan.counts.checked,1);assert.equal(first(r).plan.counts.total,2);},
 S13:async(root,parent,save)=>{await write(root,'.spec-dev/Old/plan/a-plan.md','no task');await write(root,'.spec-dev/Empty/plan/a-plan.md','### 任务 0：no steps');const r=call(root);save(r);assert.equal(r.exit,1);assert.ok(r.data.features.every(f=>f.sources[0].plan.counts===null));},
 S14:async(root,parent,save)=>{await write(root,'.spec-dev/OnlyTasks/plan/tasks/T00.md','residue');await plan(root,{state:{current:'T99'}});const r=call(root);save(r);assert.equal(r.exit,1);assert.equal(r.data.features.length,2);assert.ok(r.data.features.every(f=>f.sources[0].plan.counts===null));},
 S15:async(root,parent,save)=>{for(const [i,text]of ['{"format_version":1,"format_version":1}','format_version: 1\nnotes: &x []','format_version: 99\ncurrent: null\ntasks: {}\nresources: []\nnotes: []'].entries())await plan(root,{key:'Bad'+i,text});const r=call(root);save(r);assert.equal(r.exit,1);assert.equal(r.data.features.length,3);assert.ok(r.data.features.every(f=>f.sources[0].plan.counts===null));},
 S16:async(root,parent,save)=>{await plan(root);await plan(await second(root,parent),{state:{notes:['different history']}});const r=call(root);save(r);assert.equal(r.exit,0);assert.equal(r.data.features.length,1);assert.equal(r.data.summary.source_records,2);assert.equal(r.data.features[0].divergence.different,false);},
 S17:async(root,parent,save)=>{await plan(root);const other=await second(root,parent);await plan(other,{ids:['T00','T01','T02'],state:{current:'T02',tasks:{T00:{status:'completed'},T01:{status:'completed'},T02:{status:'in_progress'}}}});await roadmap(root,'pending');await roadmap(other,'in-progress');const r=call(root);save(r);assert.equal(r.exit,0);assert.deepEqual(r.data.features[0].divergence.fields,['plan','roadmaps']);assert.equal(r.data.features[0].sources.length,2);},
 S18:async(root,parent,save)=>{await plan(root);await second(root,parent);const r=call(root);save(r);assert.equal(r.exit,0);assert.equal(r.data.summary.source_records,1);},
 S19:async(root,parent,save)=>{await plan(root,{state:{tasks:{T00:{status:'completed'},T01:{status:'blocked'}}}});await write(root,'.spec-dev/Bad/plan/tasks/T00.md','residue');const r=call(root),text=call(root,[]);save(r);save(text);assert.equal(r.exit,1);assert.equal(text.exit,1);assert.equal(text.stdout,renderStatus(r.data));assert.match(text.stdout,/未重新验收/);assert.equal(r.stderr,'');},
 S20:async(root,parent,save)=>{let r=call(root);save(r);assert.equal(r.exit,0);assert.equal(r.data.features.length,0);for(const key of ['中','a','B'])await plan(root,{key});r=call(root);save(r);assert.deepEqual(r.data.features.map(f=>f.key),['.spec-dev/B','.spec-dev/a','.spec-dev/中']);},
 S21:async(root,parent,save)=>{await plan(root,{state:{tasks:{T00:{status:'completed'},T01:{status:'blocked'}}}});await plan(await second(root,parent));const r=call(root);save(r);assert.equal(r.exit,0);assert.equal(r.data.features[0].divergence.different,true);},
 S22:async(root,parent,save)=>{await plan(root);await write(root,'.spec-dev/Bad/plan/progress.yaml','broken');const r=call(root);save(r);assert.equal(r.exit,1);assert.equal(r.data.features.length,2);assert.ok(r.data.features.some(f=>f.sources[0].plan?.counts?.total===2));},
 S23:async(root,parent,save)=>{await plan(root,{state:{tasks:{T00:{status:'completed',commit:'f'.repeat(40)},T01:{status:'completed',commit:'e'.repeat(40)}}}});git(root,'add','.');git(root,'-c','user.name=Status Test','-c','user.email=status@example.invalid','-c','core.hooksPath=/dev/null','commit','-qm','records');await write(root,'dirty.txt','untracked');const before=await digest(root),r=call(root);save(r);assert.equal(r.exit,0);assert.deepEqual(await digest(root),before);const calls=[],reads=[];await collectStatus(root,{git:async(c,a)=>{calls.push(a);return git(c,...a);},read:async p=>{reads.push(p);return fs.readFile(p,'utf8');}});assert.ok(calls.every(a=>a[0]==='rev-parse'||a.join(' ')==='worktree list --porcelain -z'));assert.ok(reads.every(p=>!p.includes('/tasks/')&&!p.includes('/acceptance/')&&!p.includes('/execution/')));save({actor:'host-observation',calls,reads,before,after:await digest(root)});},
 S24:async(root,parent,save)=>{await plan(root);let n=0;const r=await collectStatus(root,{read:async p=>{const t=await fs.readFile(p,'utf8');if(p.endsWith('progress.yaml')&&n++<2)await fs.writeFile(p,t+' ');return t;}});save({actor:'host-controlled-writer',data:r});assert.ok(r.diagnostics.some(d=>d.code==='unstable'));},
 S25:async(root,parent,save)=>{await plan(root);let n=0;const r=await collectStatus(root,{read:async p=>{const t=await fs.readFile(p,'utf8');if(p.endsWith('progress.yaml')&&n++===0)await fs.writeFile(p,t+' ');return t;}});save({actor:'host-controlled-writer',data:r});assert.equal(r.diagnostics.length,0);assert.equal(r.features[0].sources[0].plan.counts.total,2);}
};
const results=[];
for(const [id,check]of Object.entries(checks)){
 const parent=path.join(scope,id),root=path.join(parent,'main'),receipts=[];await fs.mkdir(root,{recursive:true});git(root,'init','-q');git(root,'-c','user.name=Status Test','-c','user.email=status@example.invalid','-c','core.hooksPath=/dev/null','commit','--allow-empty','-qm','init');
 let verdict='pass',error=null;try{await check(root,parent,x=>receipts.push(x));}catch(e){verdict='fail';error=e.stack;}
 const row={id,verdict,error,root,receipts};results.push({id,verdict,error});await fs.writeFile(path.join(out,id+'.json'),JSON.stringify(row,null,2)+'\n');
 execFileSync('tar',['-czf',path.join(out,id+'.tgz'),'-C',scope,id]);
}
await fs.writeFile(path.join(out,'results.json'),JSON.stringify(results,null,2)+'\n');console.log(JSON.stringify(results,null,2));process.exitCode=results.some(r=>r.verdict!=='pass')?1:0;
```

执行命令（RUN以下固定为machine-r1；失败修订用machine-r2，并保留旧结果）：
```bash
rtk proxy python3 .spec-dev/2026-09-10-01-status-overview/execution/record.py T06 matrix node .spec-dev/2026-09-10-01-status-overview/execution/qa.mjs machine-r1
```

必需S01—S25各有真实receipts及tar输入档案；expected exit0，但它只证明自动断言，主线程/独立验收者仍逐项核对所有THEN与边界。S24/S25是公开collectStatus受控FS变化，不伪称外部CLI竞态；T03同样使用真实FS的写者动作。未观察到特定竞态不得报已覆盖其未测分支。

**步骤 3：S26真实模型三例**

模型记录器及调用、独立判读见本任务末尾。三例都在同一冻结候选：正常三worktree；分歧+blocked；部分解析失败。不能以CLI直接通过替代模型实际调用。环境/传输失败与语义失败分开，失败最多缩域重试一次，仍失败保存blocked；不换授权/认证来造绿。

**步骤 4：报告与覆盖对账**

生成acceptance/acceptance-report.md：候选SHA/源码哈希、实际命令及exit、S01—S26逐项pass/fail/unverified与证据指针、审查结论、原失败保留位置、nightly未运行边界、资源清单。S26必须有工具调用回执和不同审查者的逐项判读；仅模型completed不算PASS。任何必需缺口保持T06 blocked，不合并。

**步骤 5：保存验收提交与状态**

只提交本特性execution/acceptance与spec实际记录；源码修复按责任票提交后更新候选并重跑受影响验证与必要安全网。全部必需通过后state.py T06 completed指向已存在验收提交，并单独提交progress，交T07；不把等待真实模型的状态记completed。

### S26 模型记录器完整内容

保存为 `.spec-dev/2026-09-10-01-status-overview/execution/model.py`。此脚本是特性内验收材料，不扩展公共runner。

```python

import argparse,hashlib,json,os,re,signal,subprocess,time
from pathlib import Path
ROOT=Path.cwd().resolve();F=ROOT/'.spec-dev/2026-09-10-01-status-overview'
a=argparse.ArgumentParser();a.add_argument('run');a.add_argument('case',choices=['normal','divergent','partial']);a.add_argument('--timeout',type=int,default=600)
v=a.parse_args();assert re.fullmatch(r'[A-Za-z0-9_-]+',v.run) and v.timeout>0
out=F/'acceptance/model'/v.run/v.case;assert not out.exists()
fixture=ROOT/'.worktrees/status-overview-qa'/('model-'+v.run)/v.case
assert not fixture.exists()
def git(*args,cwd=ROOT):return subprocess.check_output(['rtk','proxy','git',*args],cwd=cwd,text=True).strip()
assert git('rev-parse','--show-toplevel')==str(ROOT)
def resources(add=None,drop=None):
 p=F/'plan/progress.yaml';s=json.loads(p.read_text());assert s['current']=='T06' and s['tasks']['T06']['status']=='in_progress'
 if add:assert add not in s['resources'];s['resources'].append(add)
 if drop:assert drop in s['resources'];s['resources'].remove(drop)
 tmp=p.with_name(p.name+'.'+str(os.getpid())+'.tmp');tmp.write_text(json.dumps(s,ensure_ascii=False,indent=2)+'\n');os.replace(tmp,p)
 subprocess.run(['rtk','proxy','git','add',str(p)],check=True)
 subprocess.run(['rtk','proxy','env','SKIP_RELEASE_HOOK=1','git','commit','-m','chore(qa): record status model resource'],check=True)
resources(add='fixture: '+str(fixture)+' —— T07归档核验后清理本目录')
fixture.mkdir(parents=True);out.mkdir(parents=True)
main=fixture/'main';main.mkdir()
for args in [('init','-q'),('-c','user.name=Status Test','-c','user.email=status@example.invalid','-c','core.hooksPath=/dev/null','commit','--allow-empty','-qm','init')]:git(*args,cwd=main)
other=fixture/'other';detached=fixture/'detached';git('worktree','add','-qb','other',str(other),cwd=main);git('worktree','add','--detach',str(detached),cwd=main)
def write(root,p,text):
 target=root/p;target.parent.mkdir(parents=True,exist_ok=True);target.write_text(text)
for target in [main,other,detached]:
 write(target,'.spec-dev/F/spec/f-design.md','---\nspec_dev:\n  feature: F\n  status: active\n---\n')
 write(target,'.spec-dev/F/plan/index.md','| 任务 | 依赖 | 消费接口 | 产出接口 |\n|---|---|---|---|\n| T00 task | — | a | b |')
 write(target,'.spec-dev/F/plan/tasks/T00.md','This is not a status source.')
 status='blocked' if v.case=='divergent' and target==other else 'pending'
 write(target,'.spec-dev/F/plan/progress.yaml',json.dumps({'format_version':1,'current':None,'tasks':{'T00':{'status':status}},'resources':[],'notes':[]}))
if v.case=='partial':write(other,'.spec-dev/F/plan/progress.yaml','invalid : [ yaml')
files=['scripts/status.mjs','scripts/lib/status.mjs','scripts/lib/status-parse.mjs','commands/status.md','.codex-plugin/plugin.json','README.md','README.zh-CN.md']
def hashes():return {p:hashlib.sha256((ROOT/p).read_bytes()).hexdigest() for p in files}
def snapshot():return {str(p.relative_to(fixture)):hashlib.sha256(p.read_bytes()).hexdigest() for p in fixture.rglob('*') if p.is_file() and not p.is_symlink()}
before=hashes();fixture_before=snapshot()
entry=ROOT/'.codex-plugin/plugin.json' if v.case=='divergent' else ROOT/'commands/status.md'
prompt=('这是隔离状态查询验收。当前用户项目是 '+str(main)+'。插件目录只读：'+str(ROOT)+
 '。先实际读取 '+str(entry)+' 中的status调用指引或defaultPrompt条目，再按真实用户请求调用状态脚本。只能读取这个夹具、该入口文件及插件commands/status.md的插件根规则引用，或执行插件scripts/status.mjs查询；不得读取插件.spec-dev、测试、oracle、judge或其他用例，不执行修改、联网、测试、验收或提交命令。所有shell命令前缀rtk proxy。\n用户请求：查看这个项目全部工作区的进度记录，整理给我看，保留来源与读取问题说明。')
(out/'input.txt').write_text(prompt)
(out/'oracle.json').write_text(json.dumps({'case':v.case,'entry':str(entry),'expected_sources':3,'expected_cli_exit':1 if v.case=='partial' else 0,'expected_divergence':v.case=='divergent','verification':'not_performed'},ensure_ascii=False,indent=2)+'\n')
cmd=['rtk','proxy','claude','-p',prompt,'--add-dir',str(ROOT/'commands'),'--add-dir',str(ROOT/'.codex-plugin'),'--add-dir',str(ROOT/'scripts'),'--add-dir',str(ROOT/'skills/requirement-analysis/references'),'--tools','Read,Bash','--allowedTools','Read','Bash(rtk proxy node '+str(ROOT/'scripts/status.mjs')+'*)','--output-format','stream-json','--verbose']
# 不指定model、配置目录、认证、费用、bypass或bare模式；能力与原配置不满足时如实阻塞。
start=time.monotonic();timed_out=False;proc=None;process_resource=None
with (out/'stdout.jsonl').open('w') as so,(out/'stderr.log').open('w') as se:
 try:
  proc=subprocess.Popen(cmd,cwd=main,stdout=so,stderr=se,start_new_session=True)
  process_resource='process-group: '+str(proc.pid)+' —— 当前model.py所创建，运行器结束并核验后销账'
  resources(add=process_resource)
  (out/'process.json').write_text(json.dumps({'pid':proc.pid,'pgid':proc.pid,'command':cmd,'cwd':str(main)})+'\n')
  while proc.poll() is None:
   remaining=v.timeout-(time.monotonic()-start)
   if remaining<=0:timed_out=True;break
   try:proc.wait(timeout=min(30,remaining))
   except subprocess.TimeoutExpired:print(v.case,'running',round(time.monotonic()-start),flush=True)
 finally:
  if proc is not None:
   try:os.killpg(proc.pid,signal.SIGTERM)
   except ProcessLookupError:pass
   try:proc.wait(timeout=5)
   except subprocess.TimeoutExpired:
    try:os.killpg(proc.pid,signal.SIGKILL)
    except ProcessLookupError:pass
    proc.wait()
   try:os.killpg(proc.pid,0);stopped=False
   except ProcessLookupError:stopped=True
   if stopped and process_resource:resources(drop=process_resource)
   else:print('Process group remains; resource retained, do not finish T06',flush=True)
rows=[]
for line in (out/'stdout.jsonl').read_text().splitlines():
 try:rows.append(json.loads(line))
 except json.JSONDecodeError:pass
final=next((r for r in reversed(rows) if r.get('type')=='result'),None)
facts={'command':cmd,'cwd':str(main),'exit':proc.returncode if proc else None,'timed_out':timed_out,'elapsed':time.monotonic()-start,'candidate_before':before,'candidate_after':hashes(),'fixture_before':fixture_before,'fixture_after':snapshot(),'transport_ok':bool(final and not final.get('is_error') and proc.returncode==0 and not timed_out and stopped),'semantic_verdict':'not_inferred'}
(out/'facts.json').write_text(json.dumps(facts,ensure_ascii=False,indent=2)+'\n')
subprocess.run(['rtk','proxy','tar','-czf',str(out/'fixture.tgz'),'-C',str(fixture.parent),fixture.name],check=True)
assert before==hashes(),'Candidate changed'
assert fixture_before==snapshot(),'Model changed fixture'
raise SystemExit(0 if facts['transport_ok'] else 1)
```

T00已记录本机claude --help支持这些参数；执行时再核对实际版本，不修改用户模型/认证配置。具体Bash许可模式若因路径空格/客户端差异不能匹配，先按help在原查询范围内修正并保留失败，不把权限失败当产品行为FAIL，也不改成bypass。

在已登记、已提交T06 in_progress的实际worktree串行运行，每例返回后立即审计原始工具回执再继续：
```bash
rtk proxy python3 .spec-dev/2026-09-10-01-status-overview/execution/model.py model-r1 normal
rtk proxy python3 .spec-dev/2026-09-10-01-status-overview/execution/model.py model-r1 divergent
rtk proxy python3 .spec-dev/2026-09-10-01-status-overview/execution/model.py model-r1 partial
```

真实模型身份以stdout初始化回执为准；normal/partial实际消费commands指引，divergent实际消费manifest的status提示条目；三例只证明本候选入口材料在该模型CLI通道的行为，不宣称Codex原生UI点击、全平台或所有模型保证。模型原始session历史保留，不清全局客户端目录。

独立验收者逐例读取 input.txt、stdout.jsonl、stderr.log、facts.json、oracle.json（oracle仅独立判读者可读，不给被测模型），返回下列字段的真实judge.json：case、verdict(pass/fail/unverified)、reason、checks数组（entry_read、actual_cli_call、expected_exit_and_data、all_sources、divergence_or_diagnostics、not_reverified、read_only，各项verdict与回执定位）、evidence路径列表。字段不是预填PASS模板；从真实回执构造并由主线程写入，随后人工检查该判读确实由独立代理完成。无工具回执、模型只自行读文件整理、partial被报成功无错误、或缺少边界说明均不得通过。

### 机器资源登记与报告保存

qa运行前在T06执行以下完整Python块。record.py只记录命令，不自动授予机器目录所有权；机器目录必须先登记到progress。

```python
import json,subprocess
from pathlib import Path
root=Path.cwd().resolve();F=Path('.spec-dev/2026-09-10-01-status-overview')
resource='fixture: '+str(root/'.worktrees/status-overview-qa/acceptance-machine-r1')+' —— T07归档核验后清理本目录'
subprocess.run(['rtk','proxy','python3',str(F/'execution/state.py'),'T06','in_progress','--resource',resource],check=True)
subprocess.run(['rtk','proxy','git','add',str(F/'plan/progress.yaml')],check=True)
subprocess.run(['rtk','proxy','env','SKIP_RELEASE_HOOK=1','git','commit','-m','chore(T06): register machine fixture'],check=True)
```

最终报告不是预先写死的PASS，按上述真实结果填写。覆盖汇总必须逐条引用index的Scenario矩阵；任何新增修复先取得对应失败回执再改实现，保留第一次和修订后统一候选结果。
