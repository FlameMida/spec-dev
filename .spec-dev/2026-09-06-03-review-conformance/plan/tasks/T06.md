### 任务 6：本地交付、清理与锚定

> 最终任务，只在 T05 必需验收、审查处置和对账完成后进入。未完成这一步不能把 roadmap 标 delivered。

**文件**：本 spec、acceptance/acceptance-report.md、acceptance/delivery.json、plan/progress.yaml、roadmap；来源分支与隔离 worktree。无产品新增。

**接口**：
- 消费：28 Scenario 对照、PR 五组真实证据、独立审查和交付对账。
- 产出：实际合并、资源清理、sync_commit 与 roadmap delivered。

**步骤 1：最终全量与测试退役核对**

在 worktree 执行 `rtk proxy node --test scripts/tests/*.test.mjs` 和既有 skill/plugin/openai/diff 检查，归档实际退出码与计数。若 T05 后完全没有产品变化且已完成同一候选的最终全量，可引用其确切 HEAD/哈希证据，不为形式重复跑；任何修复或未决担忧需复跑相关与最终验证。

范围外失败到来源分支核实是否既有，来源有未知改动先确定所有权，不能覆盖。新引入回归修复后重验；既有失败按用户裁决，不静默放行。测试退役只看相关范围内采用 Scenario 命名且对应契约确已 REMOVED/Superseded 的双条件候选；本项无取代，预期无孤儿测试，若有候选先列证据并征询，不自动删除。

**步骤 2：取代与交付树准备**

本 spec `supersedes: []`，声明无取代回写，不改旧 spec 的状态或 Accepted ADR。把审查/验收/12 Requirement 对账、实际候选产品文件哈希、原始 base 和工作区资源状态先归档并提交；保留 T06 in_progress，不提前 sync_commit。确保所有必须保留的模型输入/输出已离开将删除的夹具目录，嵌套 .git 没有进入暂存。

**步骤 3：核实来源并本地合并**

```bash
rtk proxy git -C /Users/maverick/feature-dev status --short
rtk proxy git -C /Users/maverick/feature-dev branch --show-current
rtk proxy git -C /Users/maverick/feature-dev merge --ff-only plan/2026-09-06-03-review-conformance
```

仅在实际来源与 T00 记录一致且未知改动已处理时运行合并。不能 ff-only 时先诊断分支变动，按既有冲突/契约纪律处理，不强推、不 reset。记录实际合并 HEAD、合并前验收树与合并后产品哈希；一致性未证实不能交付。此计划本地出口，无 push/PR 操作。

**步骤 4：按台账清理**

在来源主工作区执行。先确认夹具无活跃模型/子代理进程，所有证据已归档；只清理 progress.resources 的具体路径。fixtures 是本票创建的独立仓库目录，无额外 Git worktree，随特性 worktree 回收；不要删除共享缓存。

```bash
rtk proxy git -C /Users/maverick/feature-dev worktree remove /Users/maverick/feature-dev/.worktrees/plan-2026-09-06-03-review-conformance
rtk proxy git -C /Users/maverick/feature-dev branch -d plan/2026-09-06-03-review-conformance
```

若未跟踪夹具导致 remove 拒绝，不用 --force：先核对登记路径/证据/存活进程，再仅删除登记的 fixtures 目录，重试普通 remove。在主工作区且上述核对完成后，精确清理代码为：

``````python
from pathlib import Path
import shutil
source=Path('/Users/maverick/feature-dev')
work=source/'.worktrees/plan-2026-09-06-03-review-conformance'
fixture=work/'.spec-dev/2026-09-06-03-review-conformance/acceptance/model-smoke/fixtures'
archive=source/'.spec-dev/2026-09-06-03-review-conformance/acceptance/model-smoke/artifacts'
assert archive.is_dir(), '归档证据尚不可从来源主工作区读取'
assert not fixture.is_symlink()
assert fixture.resolve().is_relative_to(work.resolve())
if fixture.exists(): shutil.rmtree(fixture)
``````

清理失败保留资源行并说明；已不存在记事实，不当作新成功动作。

**步骤 5：锚定与完成状态提交**

在主工作区把真实合并 SHA 写入 spec.sync_commit（指向锚定文档提交之前的实际交付提交，不自引用）；progress T06 completed，resources 各行记录已清理/已不存在，notes 追加对账和实际合并证据。roadmap #4 标 delivered，胶囊追加交付摘要、验证边界和 #5 plan-decomposition 的衔接注意事项。

只暂存本特性 spec/progress/acceptance 和 roadmap，`SKIP_RELEASE_HOOK=1` 提交 `docs(T06): 锚定审查符合性交付并回写路线图`。最终检查工作区、worktree 列表和提交记录，报告本地提交与未 push、必需检查结果和 nightly 边界。下一项 #5 仍走独立需求设计，不自动扩大本计划。
