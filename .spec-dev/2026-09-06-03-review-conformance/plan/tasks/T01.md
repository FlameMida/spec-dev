### 任务 1：共享模块判据与上游消费

**文件**：
- 修改：`skills/requirement-analysis/SKILL.md`
- 修改：`skills/requirement-analysis/agents/openai.yaml`
- 修改：`skills/requirement-analysis/evals/evals.json`
- 修改：`skills/requirement-analysis/references/spec-reviewer-prompt.md`
- 修改：`skills/writing-plans/SKILL.md`
- 修改：`skills/writing-plans/agents/openai.yaml`
- 修改：`skills/writing-plans/evals/evals.json`
- 修改：`skills/writing-plans/references/design-principles.md`

**接口**：
- 消费：已核实 worktree、原始 base_commit、相关基线。
- 产出：design-principles 模块判据；RA/计划/spec-reviewer 指针消费；S08-S10/S27。
- 测试落点：spec 测试策略的 CLI exit/JSON 与候选规则→决定/报告；依赖替换无。完整 Scenario 输入/独立预期内嵌于下方 eval，禁止向受测模型泄漏 expected_output。

**步骤 1：写失败测试**

先增加以下行为 eval，保留原条目。每个 GIVEN/WHEN/THEN 来自已批准 spec，不能改预期迁就模型。

``````python
from pathlib import Path
import json
batches = [('skills/writing-plans/evals/evals.json', [{'id': 'review-conformance-s08', 'prompt': 'S08 模块删除判据比较两个模块\nGIVEN 一个模块只转发调用，另一个集中处理多个调用方共同的业务规则\nWHEN 使用模块删除判据', 'expected_output': '比较假想移除后的复杂性去向及实际用途；不实际删除模块或保护测试，不仅凭转发形态要求整改。'}, {'id': 'review-conformance-s09', 'prompt': 'S09 一个 adapter 与已批准 seam\nGIVEN 计划已批准一个公共 seam，当前只有一个生产 adapter\nWHEN 使用 adapter 计数评价抽象', 'expected_output': '不为凑两个实现创建 fake、不据此取消已批准 seam；只有真实需求和具体复杂性证据才能支持改进建议。'}, {'id': 'review-conformance-s10', 'prompt': 'S10 多 adapter 仍暴露内部结构\nGIVEN 两个真实 adapter 的接口向消费者暴露各自内部参数，调用方需做分支判断\nWHEN 使用接口三问', 'expected_output': '检查是否能减少方法、简化参数、隐藏内部复杂性，并引用实际边界；adapter 数量不自动证明抽象合理。'}]), ('skills/requirement-analysis/evals/evals.json', [{'id': 'review-conformance-s27', 'prompt': 'S27 上游消费共享判据\nGIVEN RA 比较方案、writing-plans 编写头部、spec reviewer 检查设计可实施性\nWHEN 各入口读取设计判据', 'expected_output': '通过同一 design-principles 指针使用新增判据，保留八原则与原审查职责，不复制三套判据或新增独立批准门。'}])]
for name, cases in batches:
    p=Path(name); data=json.loads(p.read_text())
    seen={case["id"] for case in data["evals"]}
    assert not seen.intersection(case["id"] for case in cases), "already added: inspect progress"
    data["evals"].extend(cases)
    p.write_text(json.dumps(data, ensure_ascii=False, indent=2)+"\n")
``````
**步骤 2：运行确认失败**

下面是独立真值的真实模型决策探针。运行时工具调用每次最多等待 60 秒并持续报告进度；模型输出由执行者对照固定 expected_output 判定，退出 0 不是 PASS。旧规则缺定义可记录静态差异，但不能冒充行为红；至少核实本票行为目标的有效失败再改规则，若全部已通过则如实报告并重审最小变更，不伪造红。

``````python
import hashlib, json, subprocess
from pathlib import Path
root=Path.cwd().resolve()
out=root/".spec-dev/2026-09-06-03-review-conformance/execution/serial/T01"
out.mkdir(parents=True,exist_ok=True)
phase="red"  # 首轮 red，改后另存 green；重试需新目录，禁止覆盖
files=['skills/writing-plans/references/design-principles.md', 'skills/requirement-analysis/SKILL.md', 'skills/writing-plans/SKILL.md']
roles="\n\n".join("# "+p+"\n"+(root/p).read_text() for p in files)
snap={p:hashlib.sha256((root/p).read_bytes()).hexdigest() for p in files}
cases=[{'id': 'review-conformance-s08', 'prompt': 'S08 模块删除判据比较两个模块\nGIVEN 一个模块只转发调用，另一个集中处理多个调用方共同的业务规则\nWHEN 使用模块删除判据', 'expected_output': '比较假想移除后的复杂性去向及实际用途；不实际删除模块或保护测试，不仅凭转发形态要求整改。'}, {'id': 'review-conformance-s09', 'prompt': 'S09 一个 adapter 与已批准 seam\nGIVEN 计划已批准一个公共 seam，当前只有一个生产 adapter\nWHEN 使用 adapter 计数评价抽象', 'expected_output': '不为凑两个实现创建 fake、不据此取消已批准 seam；只有真实需求和具体复杂性证据才能支持改进建议。'}, {'id': 'review-conformance-s10', 'prompt': 'S10 多 adapter 仍暴露内部结构\nGIVEN 两个真实 adapter 的接口向消费者暴露各自内部参数，调用方需做分支判断\nWHEN 使用接口三问', 'expected_output': '检查是否能减少方法、简化参数、隐藏内部复杂性，并引用实际边界；adapter 数量不自动证明抽象合理。'}, {'id': 'review-conformance-s27', 'prompt': 'S27 上游消费共享判据\nGIVEN RA 比较方案、writing-plans 编写头部、spec reviewer 检查设计可实施性\nWHEN 各入口读取设计判据', 'expected_output': '通过同一 design-principles 指针使用新增判据，保留八原则与原审查职责，不复制三套判据或新增独立批准门。'}]
cases=[c for c in cases if c["id"] in ['review-conformance-s09']]
for c in cases:
    prefix=out/(c["id"]+"-"+phase)
    assert not prefix.with_suffix(".jsonl").exists(), prefix
    prompt="请按所附当前规则处理输入，只输出下一步决定与依据；不实现。\n"+roles+"\n任务：\n"+c["prompt"]
    prefix.with_suffix(".input.txt").write_text(prompt)
    cmd=["rtk","proxy","claude","-p","--plugin-dir",str(root),"--model","haiku","--effort","low","--tools","","--no-session-persistence","--verbose","--output-format","stream-json",prompt]
    try:
        r=subprocess.run(cmd,cwd=root,stdin=subprocess.DEVNULL,capture_output=True,text=True,timeout=180)
        code,stdout,stderr=r.returncode,r.stdout,r.stderr
    except subprocess.TimeoutExpired as e:
        code=124; stdout=e.stdout or b""; stderr=e.stderr or b""
        if isinstance(stdout,bytes): stdout=stdout.decode(errors="replace")
        if isinstance(stderr,bytes): stderr=stderr.decode(errors="replace")
    prefix.with_suffix(".jsonl").write_text(stdout)
    prefix.with_suffix(".stderr.log").write_text(stderr)
    prefix.with_suffix(".meta.json").write_text(json.dumps({"command":cmd[:-1],"cwd":str(root),"hashes":snap,"exit":code,"expected":c["expected_output"],"status":"needs_review"},ensure_ascii=False,indent=2))
    print(c["id"],code,flush=True)
    if code: raise SystemExit("调用故障，检查证据；不是有效行为红")
``````
**步骤 3：写最小实现**

以下替换只写本票文件；唯一锚点不成立先查当前 HEAD/进度，不重复执行。

``````python
from pathlib import Path
changes = []
changes.append(('skills/writing-plans/references/design-principles.md', r'''# 设计原则（方案取舍与计划编写共同遵循）

> **阅读时机**：requirement-analysis 阶段 4（方案对比）与阶段 5（设计展示）、writing-plans 编写计划头部时加载；executing-plans 执行者经计划头部「设计原则」块间接获得。

八条原则（方案评价维度与任务分解检查项）：

1. **默认不保留向后兼容性**。删除过时路径，而不是添加兼容层、回退垫片或仅用于兼容性的数据迁移。常规的前向模式迁移仍然是必需的。仅保留产品和部署规则中明确记录的当前合同。
2. **选择能完全满足当前需求的最简单实现**。避免推测性的抽象、配置和间接层。
3. **分层构建系统**。从能够端到端工作的最小版本开始，然后基于运行中的产品逐步添加各项能力。
4. **绝不用未完成的复杂性换取可工作的产品**。
5. **保持组件模块化，关注点清晰分离**。
6. **优先使用成熟且维护良好的库**，前提是它们能降低整体复杂性或提高可靠性；没有充分理由不重新实现通用功能。
7. **优先使用项目中已有的依赖**；判断某依赖缺少某能力之前，先查阅其文档和类型定义。
8. **做出长期性的架构决策**。不接受打算日后替换的权宜之计。

## spec-dev 语境注解

- 第 1 条管**实现层**：不写兼容层/垫片/仅兼容性迁移。在 spec-dev 里，active spec 就是"明确记录的当前合同"——删除旧行为路径的前提是经取代分流**显式取代**旧 spec（supersede 机制），而非绕开它；读取历史落盘产物（如旧格式 plan）属于读历史数据，不是兼容垫片。
- 第 1 条与阶段 1「契约姿态判定」的关系：姿态判定的默认值不变（旧 active spec 默认硬约束），本原则要求的是**取代要走正门、实现不留垫片**。
- 第 6/7 条的既有呼应：收尾审查维度 C「优先使用项目已有工具与模式」；ADR 三判据承载第 8 条的"长期决策"沉淀。
''', r'''# 设计原则（方案取舍与计划编写共同遵循）

> **阅读时机**：requirement-analysis 阶段 4（方案对比）与阶段 5（设计展示）、writing-plans 编写计划头部时加载；executing-plans 执行者经计划头部「设计原则」块间接获得。

八条原则（方案评价维度与任务分解检查项）：

1. **默认不保留向后兼容性**。删除过时路径，而不是添加兼容层、回退垫片或仅用于兼容性的数据迁移。常规的前向模式迁移仍然是必需的。仅保留产品和部署规则中明确记录的当前合同。
2. **选择能完全满足当前需求的最简单实现**。避免推测性的抽象、配置和间接层。
3. **分层构建系统**。从能够端到端工作的最小版本开始，然后基于运行中的产品逐步添加各项能力。
4. **绝不用未完成的复杂性换取可工作的产品**。
5. **保持组件模块化，关注点清晰分离**。
6. **优先使用成熟且维护良好的库**，前提是它们能降低整体复杂性或提高可靠性；没有充分理由不重新实现通用功能。
7. **优先使用项目中已有的依赖**；判断某依赖缺少某能力之前，先查阅其文档和类型定义。
8. **做出长期性的架构决策**。不接受打算日后替换的权宜之计。

## spec-dev 语境注解

- 第 1 条管**实现层**：不写兼容层/垫片/仅兼容性迁移。在 spec-dev 里，active spec 就是"明确记录的当前合同"——删除旧行为路径的前提是经取代分流**显式取代**旧 spec（supersede 机制），而非绕开它；读取历史落盘产物（如旧格式 plan）属于读历史数据，不是兼容垫片。
- 第 1 条与阶段 1「契约姿态判定」的关系：姿态判定的默认值不变（旧 active spec 默认硬约束），本原则要求的是**取代要走正门、实现不留垫片**。
- 第 6/7 条的既有呼应：收尾审查维度 C「优先使用项目已有工具与模式」；ADR 三判据承载第 8 条的"长期决策"沉淀。

## 模块判据（按实际问题使用）

本节是设计、计划、spec 审查与代码审查共用的判据定义点。八条原则保持；下列启发式不自动成为缺陷、架构整改或新增批准门。引用方提供当前需求、获批 seam 和证据，不复制定义。

- **模块删除判据**：假想移除模块，复杂性会消失，还是重新分散到多个调用方？前者提示可能是无价值间接层，后者说明模块集中承担职责；结合真实用途判断，纯转发也可能承担有效契约。只做推演，不实际删除模块或保护测试。
- **真实 adapter 计数**：列出现实中变化的实现和用途，检查抽象是否服务真实需求。两个实现不自动证明抽象合理，一个实现也不否决已批准 seam；不为凑数制造 fake、投机第二实现或私有测试接口。
- **接口收敛三问**：能否减少方法、简化参数、隐藏更多内部复杂性？回答须关联实际消费者和现行需求，不凭方法数量制造违规。
- **结构摩擦五问**：理解同一概念是否跨过多模块？接口是否与内部实现同样复杂？测试是否只覆盖提取的纯函数而遗漏公共调用行为？耦合是否泄漏到消费者？公共接口是否难以测试？检查本次变更新增或加剧的具体摩擦，保留公共行为覆盖与获批 seam；不要求逐私有函数测试。

**消费边界**：设计/计划可据证据改进方案，审查只据实际影响和既有授权处理；更优雅的设想记重构候选，缺陷走正常复核。纯重构仍由 TDD 的收尾规则承接，最终全量保持原计划时机。
'''))
changes.append(('skills/requirement-analysis/SKILL.md', r'''（对照 writing-plans/references/design-principles.md 八条——尤其''', r'''（对照 writing-plans/references/design-principles.md 八条及「模块判据」——尤其'''))
changes.append(('skills/writing-plans/SKILL.md', r'''任务分解与方案形态遵循 [design-principles.md](references/design-principles.md) 八条设计原则——分解时逐条对照，违反即重划。''', r'''任务分解与方案形态遵循 [design-principles.md](references/design-principles.md) 八条设计原则；同时按实际问题使用其「模块判据」。原则违反时重划；启发式须有证据，不为凑 adapter 数量新建抽象，不否决已批准 seam。计划头部保留八原则声明并引用该判据定义点。'''))
changes.append(('skills/requirement-analysis/references/spec-reviewer-prompt.md', r'''# Spec 审查子代理提示词模板（阶段 7 对抗验证）

**用途**：验证 spec 完整、一致、可直接进入实施计划编写。

**派发时机**：spec 已写入特性目录的 `spec/<feature>-design.md` 且 inline 自检完成之后。

**派发方式**：Claude Code 用 `Agent`（subagent_type: general-purpose）；Codex 用 `spawn_agent` 且**不继承主会话上下文**（`fork_turns: "none"`；参数的新旧版本兼容见 [codex-compat.md](codex-compat.md)）——审查者不应继承主会话的立场与上下文。

```
你是 spec 文档审查者。请独立验证这份 spec 是否完整、可直接用于编写实施计划。

**待审查 spec**：[SPEC_FILE_PATH]
（如需了解项目背景，可阅读项目规范文件与 spec 中引用的代码路径）

## 审查维度

| 维度 | 检查内容 |
|------|----------|
| 完整性 | TODO、占位符、"TBD"、未写完的节；每条 Requirement 是否至少有一个 Scenario |
| 一致性 | 内部矛盾、互相冲突的需求、架构与功能描述不匹配 |
| 清晰度 | 歧义大到足以让人做错东西的需求表述 |
| 可测性 | Requirement 是否可观察（没读过代码的测试者能否判定通过与否）；是否一条一个 SHALL/MUST；Scenario 是否真正检验需求（复述一遍不算）；边缘与错误场景有没有命名的 Scenario，还是只有 happy path |
| 差量正确性 | （spec 使用 ADDED/MODIFIED/REMOVED 三节时）分类是否与既有行为对得上——把真实变更标成 ADDED 会留下两条竞争需求，把新行为标成 MODIFIED 则无物可替换 |
| 外部一致性 | 对照 spec「取代与共存」节与 `.spec-dev/` 下既有 active spec/ADR：有无未声明的行为冲突（新 Requirement 与既有 active 的现行 Requirement 矛盾、但取代与共存节未提及该 spec）；完全取代时新 spec covers 是否接管旧 covers 中仍存在的路径（真空差集列为 issue）；部分取代清单中的 Requirement 标题是否与旧 spec 实文对得上 |
| 范围 | 意图能否一句话说清；是否聚焦到单个实施计划能承载——不横跨多个独立子系统、没有"一半任务可独立交付"的过大迹象；正文是否出现"第一阶段/Phase 1/先做 X 再做 Y"式的阶段化结构（伪聚焦——把多个实施周期塞进一份 spec，应拆成 roadmap 子项目、spec 只留第一阶段） |
| YAGNI | 没人要求的功能、过度设计 |

## 校准规则

**只报告会在实施计划编写阶段造成真实问题的发现。**
缺失的章节、内部矛盾、可以两种方式解读的需求——这些是问题。
措辞润色、风格偏好、"某节比其他节写得简略"——这些不是。

除非存在会导致计划出错的严重缺口，否则给出 Approved。

## 输出格式

## Spec 审查

**结论**：Approved | Issues Found

**问题清单（如有）**：
- [第 X 节]：[具体问题] —— [为什么会影响实施计划]

**建议（仅供参考，不阻塞通过）**：
- [改进建议]
```

**主线程处置**：审查回报的问题逐条裁决——成立则修 spec，不成立则记录不采纳理由；**只要 spec 有过修改（含 inline 自检的修改），必须让用户重新 review**。
''', r'''# Spec 审查子代理提示词模板（阶段 7 对抗验证）

**用途**：验证 spec 完整、一致、可直接进入实施计划编写。

**派发时机**：spec 已写入特性目录的 `spec/<feature>-design.md` 且 inline 自检完成之后。

**派发方式**：Claude Code 用 `Agent`（subagent_type: general-purpose）；Codex 用 `spawn_agent` 且**不继承主会话上下文**（`fork_turns: "none"`；参数的新旧版本兼容见 [codex-compat.md](codex-compat.md)）——审查者不应继承主会话的立场与上下文。

```
你是 spec 文档审查者。请独立验证这份 spec 是否完整、可直接用于编写实施计划。

**待审查 spec**：[SPEC_FILE_PATH]
（如需了解项目背景，可阅读项目规范文件与 spec 中引用的代码路径）

## 审查维度

| 维度 | 检查内容 |
|------|----------|
| 完整性 | TODO、占位符、"TBD"、未写完的节；每条 Requirement 是否至少有一个 Scenario |
| 一致性 | 内部矛盾、互相冲突的需求、架构与功能描述不匹配 |
| 清晰度 | 歧义大到足以让人做错东西的需求表述 |
| 可测性 | Requirement 是否可观察（没读过代码的测试者能否判定通过与否）；是否一条一个 SHALL/MUST；Scenario 是否真正检验需求（复述一遍不算）；边缘与错误场景有没有命名的 Scenario，还是只有 happy path |
| 差量正确性 | （spec 使用 ADDED/MODIFIED/REMOVED 三节时）分类是否与既有行为对得上——把真实变更标成 ADDED 会留下两条竞争需求，把新行为标成 MODIFIED 则无物可替换 |
| 外部一致性 | 对照 spec「取代与共存」节与 `.spec-dev/` 下既有 active spec/ADR：有无未声明的行为冲突（新 Requirement 与既有 active 的现行 Requirement 矛盾、但取代与共存节未提及该 spec）；完全取代时新 spec covers 是否接管旧 covers 中仍存在的路径（真空差集列为 issue）；部分取代清单中的 Requirement 标题是否与旧 spec 实文对得上 |
| 范围 | 意图能否一句话说清；是否聚焦到单个实施计划能承载——不横跨多个独立子系统、没有"一半任务可独立交付"的过大迹象；正文是否出现"第一阶段/Phase 1/先做 X 再做 Y"式的阶段化结构（伪聚焦——把多个实施周期塞进一份 spec，应拆成 roadmap 子项目、spec 只留第一阶段） |
| YAGNI | 没人要求的功能、过度设计 |

## 校准规则

**只报告会在实施计划编写阶段造成真实问题的发现。**
缺失的章节、内部矛盾、可以两种方式解读的需求——这些是问题。
措辞润色、风格偏好、"某节比其他节写得简略"——这些不是。

除非存在会导致计划出错的严重缺口，否则给出 Approved。

## 输出格式

## Spec 审查

**结论**：Approved | Issues Found

**问题清单（如有）**：
- [第 X 节]：[具体问题] —— [为什么会影响实施计划]

**建议（仅供参考，不阻塞通过）**：
- [改进建议]
```

**主线程处置**：审查回报的问题逐条裁决——成立则修 spec，不成立则记录不采纳理由；**只要 spec 有过修改（含 inline 自检的修改），必须让用户重新 review**。

## 共享设计判据

审查方案形态时读取 writing-plans 的 [design-principles.md](../../writing-plans/references/design-principles.md)「模块判据」，结合当前需求与已批准 seam 使用。保持本模板的真实缺口校准：接口可以更简单或只有一个 adapter 本身不足以否决设计；不复制判据，不新增批准门。
'''))
changes.append(('skills/requirement-analysis/agents/openai.yaml', r'''  short_description: "需求设计：探索、逐题澄清、方案与完整设计批准；公共测试落点随设计声明，spec 经独立审查后交接计划。"''', r'''  short_description: "需求设计：探索、逐题澄清、方案与完整设计批准；消费共享模块判据，公共测试落点随设计声明，spec 经独立审查后交接计划。"'''))
changes.append(('skills/writing-plans/agents/openai.yaml', r'''  short_description: "把获批 spec 写成 plan 三件套：精确任务接口与 seam 来源、TDD 五步、适用快检和验收闭环；progress 唯一记状态。"''', r'''  short_description: "把获批 spec 写成 plan 三件套：共享设计判据、精确任务接口与 seam 来源、TDD 五步、适用快检和验收闭环；progress 唯一记状态。"'''))
for name, old, new in changes:
    p = Path(name); source = p.read_text()
    assert source.count(old) == 1, (name, "replacement anchor drift")
    p.write_text(source.replace(old, new, 1))
``````
**步骤 4：运行确认通过**

重跑步骤 2 的同一探针，只把 `phase="red"` 改成 `phase="green"`，输入/期望/模型绑定不改；保留两轮原件。按 spec 对照逐项填写 verdict/reason/evidence，模型未验证不能记本票行为 PASS。T05 再补真实工具动作与全局语境。

```bash
rtk proxy node scripts/validate-skills.mjs
rtk proxy node scripts/check-plugin.mjs
rtk proxy node --test scripts/tests/plugin-root.test.mjs scripts/tests/plan-single-format.test.mjs scripts/tests/search-clause.test.mjs
rtk git diff --check
```

**步骤 5：提交**

只暂存本票产品文件、本 spec 的实施记录、本票证据与 progress；先检查漂移，再按 index 共存纪律提交，预期钩子全部通过。使用下面精确文件清单；eval 已在步骤 1 修改，一并暂存。

```bash
rtk git add skills/requirement-analysis/SKILL.md skills/requirement-analysis/agents/openai.yaml skills/requirement-analysis/evals/evals.json skills/requirement-analysis/references/spec-reviewer-prompt.md skills/writing-plans/SKILL.md skills/writing-plans/agents/openai.yaml skills/writing-plans/evals/evals.json skills/writing-plans/references/design-principles.md
rtk git add .spec-dev/2026-09-06-03-review-conformance/spec/review-conformance-design.md .spec-dev/2026-09-06-03-review-conformance/execution/serial/T01 .spec-dev/2026-09-06-03-review-conformance/plan/progress.yaml
rtk proxy node guardrail/check-spec-drift.mjs --staged
```

提交消息主旨为 `feat(T01): 共享模块判据与上游消费`。以文件传入 Spec 指针和逐项共存说明，按实际命中的旧 spec 写 trailer；若无未知契约偏差，可用已批准的单次守卫豁免，不跳过 pre-commit。读取发版钩子后的最终 HEAD，再原子更新本票 completed/commit/tests；未通过必需验证保持未完成。
