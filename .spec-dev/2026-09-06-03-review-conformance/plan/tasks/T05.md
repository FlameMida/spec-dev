### 任务 5：全局审查与真实模型验收

> 本票由 executing-plans 的收尾阶段触发 acceptance-qa，实施票 T01-T04 完成后执行。验收本特性的独立代码审查沿 T00 保存的既有编排，不用待交付规则证明自身；候选新编排只在以下隔离夹具中作为被测对象。

**文件**：创建本特性 acceptance/acceptance-report.md、eval-review.md、check-items.json、review-findings.json、model-smoke/ 与日志；读取原始 base、spec、各票产品与执行证据。模型夹具已在 progress.resources 预登记，创建前核对。

**接口**：
- 消费：统一一路/四路/五路及 D 条件覆盖；S/critic/复核收口；剩余 Scenario 与并发继承。
- 产出：28 Scenario 对照、PR 五组真实证据、独立审查和交付对账。
- 测试落点：spec 已批准 CLI exit/JSON 与真实规则→派发/报告；不 mock 候选规则、Git、模型行为或实际文件读取。

**步骤 1：装配矩阵与旧规则独立审查**

28 个 Scenario 每项记录 task/eval ID、GIVEN/WHEN/THEN、候选位置、证据、状态和未覆盖原因；从 spec 验收矩阵逐行复制，不用“5组”替代 Scenario 计数。S19-S21 已由 T02 真实 CLI 测试，S10/S13/S25/S27 完整模型行为属 nightly，静态检查仍必需。其他行进入下面 PR 五组，任何缺证据的必需行仍未完成。

独立代码审查用 T00 保存的旧 review-orchestration/code-reviewer 原文、完整 base..HEAD、批准 spec 和当前产品源码；按既有 A/B/C、反驳、critic 和矩阵验收执行。候选的多维测试与本票独立审查分开归档，不能把候选自报告当独立结论。

**步骤 2：确定性与静态验证**

```bash
rtk proxy node --test scripts/tests/*.test.mjs
rtk proxy node scripts/validate-skills.mjs
rtk proxy node scripts/check-plugin.mjs
rtk proxy node scripts/check-openai-sync.mjs
rtk proxy node scripts/validate-output.mjs plan-index .spec-dev/2026-09-06-03-review-conformance/plan
rtk git diff --check
```

预期现存测试加新增 3 个 CLI 测试全部通过，记录实际计数和 skip，不假定固定总数。全量执行期间不并行写工作区证据，避免既有只读断言受日志写入影响；先在内存或系统临时输出收集，完成后归档。静态 eval 记录 STATIC_MATCH，不报真实模型 PASS。

**步骤 3：真实 Git 夹具与 PR 五组模型动作**

已从本机 `claude --help` 查证 --agents、--tools、--allowedTools、--effort 等参数。执行时再核实可用性；请求廉价别名 haiku、effort low，实际模型从 init/result 读取，未知不猜。仅调用级配置，不改用户全局环境。缺依赖/服务/权限时 blocked/unverified，不用静态输出替代动作。

先运行完整夹具构造代码。只创建在预登记目录里，.gitignore 防止嵌套仓库进入提交；模型不获准写产品或发送外部消息。

``````python
import json,subprocess
from pathlib import Path
root=Path.cwd().resolve()
assert root.name=='plan-2026-09-06-03-review-conformance'
out=root/'.spec-dev/2026-09-06-03-review-conformance/acceptance/model-smoke'
out.mkdir(parents=True,exist_ok=True)
fixtures=out/'fixtures'
assert not fixtures.exists(), '已有夹具须核对原记录，不覆盖'
fixtures.mkdir()
(out/'.gitignore').write_text('fixtures/\n')
spec='---\nspec_dev:\n  version: 1\n  feature: cart-fixture\n  status: active\n  covers: ["src/**"]\n---\n# 购物车契约\n范围：只提供 total(values)，返回总额；不提供数据删除接口。\n### Requirement: R1 空集合\n#### Scenario: empty\nGIVEN [] WHEN total([]) THEN 返回数字 0。\n### Requirement: R2 输入校验\n#### Scenario: invalid\nGIVEN null WHEN total(null) THEN 抛出 TypeError。\n### Requirement: R3 已取代行为\n> **Superseded (2026-09-06)** — by successor.md\n#### Scenario: old\nGIVEN [] WHEN total([]) THEN 返回 null。\n'
good="exports.total = values => { if (!Array.isArray(values)) throw new TypeError('values'); return values.reduce((a,b)=>a+b,0); };\n"
bad="function sorted(values) { return values.slice().sort((a,b)=>a-b); }\nexports.total = values => { if (!values || !values.length) return null; return sorted(values).reduce((a,b)=>a+b,0); };\nexports.removeAll = () => 'deleted';\n"
test="const {test}=require('node:test');const assert=require('node:assert/strict');const {total}=require('../src/cart.js');\ntest('empty',()=>assert.equal(total([]),0));test('invalid',()=>assert.throws(()=>total(null),TypeError));test('normal',()=>assert.equal(total([2,3]),5));\n"
names=['g1-conformance','g2-covered','g2-gap','g3-small','g3-regular','g3-large','g3-capacity','g3-switch','g4-coupled','g4-mechanical','g4-adapter','g5-invalid-ref','g5-missing-spec','g5-empty','g5-dispatch','g1-approved','g1-missing-impl','g2-no-test','g2-duplicate','g2-late-bug']
manifest={}
for name in names:
    repo=fixtures/name;repo.mkdir()
    def git(*args):
        return subprocess.check_output(['rtk','proxy','git','-c','core.hooksPath=/dev/null','-c','user.name=Review QA','-c','user.email=review-qa@example.invalid',*args],cwd=repo,text=True).strip()
    git('init','-b','main')
    (repo/'src').mkdir();(repo/'test').mkdir()
    (repo/'spec.md').write_text(spec)
    (repo/'src/cart.js').write_text(good)
    (repo/'test/cart.test.js').write_text(test)
    (repo/'plan.md').write_text('已批准计划；Spec: spec.md；T01 实现 total 空集合/输入校验；T02 内部优化，签名与公开行为保持。T01/T02 completed；seam=total(values)，允许替换依赖：无。最终全量在收尾。\n')
    git('add','.');git('commit','-m','fixture base');base=git('rev-parse','HEAD')
    if name not in ['g5-empty']:
        if name in ['g1-conformance','g3-small','g3-switch','g1-approved','g1-missing-impl','g2-duplicate','g2-late-bug']:
            (repo/'src/cart.js').write_text(bad)
        else:
            (repo/'src/cart.js').write_text(good+'// internal implementation reviewed\n')
        if name in ['g3-regular','g3-large','g3-capacity','g4-mechanical']:
            for part in ['a','b','c']:
                (repo/'src'/f'{part}.js').write_text(''.join(f'// {part} mechanical comment {n}\n' for n in range(60 if name!='g4-mechanical' else 220)))
        if name=='g4-coupled':
            (repo/'src/codec.js').write_text("exports.internal={offset:2};exports.decode=x=>x+2;\n")
            for part in ['a','b','c']:(repo/'src'/f'{part}.js').write_text("const {internal}=require('./codec');exports.read=x=>x+internal.offset;\n")
        if name=='g1-approved':
            approved=spec.replace('不提供数据删除接口。','批准提供 removeAll() 删除接口。')
            (repo/'spec.md').write_text(approved)
            (repo/'plan.md').write_text('已批准 Spec: spec.md；T01 total；T02 removeAll；授权见 approval.md；公共 total seam 不变。\n')
            (repo/'approval.md').write_text('2026-09-06 用户批准范围增加 removeAll；spec 与 plan 同步修改。\n')
        if name=='g1-missing-impl':
            (repo/'src/cart.js').unlink()
        if name=='g4-adapter':
            (repo/'src/adapter.js').write_text("exports.get=x=>x; // sole production adapter serving approved total seam\n")
            (repo/'src/rules.js').write_text("exports.check=values=>Array.isArray(values); // shared policy used by several consumers\n")
        git('add','.');git('commit','-m','serial T01');switch=git('rev-parse','HEAD')
        if name=='g3-switch':
            (repo/'src/late.js').write_text('exports.later=1;\n');git('add','.');git('commit','-m','parallel integrated T02')
    else:switch=base
    head=git('rev-parse','HEAD')
    result=subprocess.run(['rtk','proxy','node','--test','test/cart.test.js'],cwd=repo,capture_output=True,text=True)
    log=out/(name+'-tests.log');log.write_text(result.stdout+result.stderr)
    data={'path':str(repo),'base':base,'switch':switch,'head':head,'tests':str(log),'test_exit':result.returncode}
    if name=='g5-invalid-ref':data['base']='unresolvable-review-base'
    if name=='g5-missing-spec':
        (repo/'spec.md').unlink()
    if name in ['g2-covered','g2-gap','g2-no-test','g2-duplicate','g2-late-bug']:
        if name in ['g2-duplicate','g2-late-bug']:
            for label,category in [('A','Bug'),('S','Spec符合性')]:
                finding={'file':'src/cart.js','line':2,'severity':'中','confidence':90,'category':category,'description':'空集合返回 null；spec.md 的 R1/empty 要求返回数字 0。','fix_suggestion':'按公共契约修复空集合返回值。'}
                (repo/(label+'-candidate.json')).write_text(json.dumps({'findings':[finding],'coverage_note':'仅审查 empty，其他风险面未查。'},ensure_ascii=False))
            (repo/'prior-review.json').write_text(json.dumps({'rounds_finished':2,'confirmed':[],'unreviewed':['R1 empty'],'note':'前轮漏查；本文件是预设恢复输入，不是假造本次实际模型已执行两轮。'},ensure_ascii=False))
        coverage='已审 src/cart.js；现行 R1 empty 与 R2 invalid；测试 '+str(log) if name=='g2-covered' else '仅检查 R1 empty；R2 invalid 未查；src/cart.js 其余范围未审。'
        if name=='g2-no-test': coverage='已审 src/cart.js 的 R1/R2，零发现；未提供测试证据。'
        (repo/'report.json').write_text(json.dumps({'findings':[],'coverage_note':coverage},ensure_ascii=False))
    manifest[name]=data
(out/'fixtures.json').write_text(json.dumps(manifest,ensure_ascii=False,indent=2)+'\n')
print('Created',len(manifest),'registered fixtures; raw test logs preserved')
``````
然后运行实际模型调用。每例最多 300 秒，外层 exec 以短周期 yield、每次等待不超过 60 秒并报告进度；输出直接写日志。工具名称若本机发生变化，核实实际可用工具后等价修正并记录，不以工具不可用当规则失败；禁止用跳过权限检查解决拒绝。

``````python
import hashlib,json,subprocess,os,signal
from pathlib import Path
root=Path.cwd().resolve()
out=root/'.spec-dev/2026-09-06-03-review-conformance/acceptance/model-smoke'
manifest=json.loads((out/'fixtures.json').read_text())
files=['skills/executing-plans/references/review-orchestration.md','agents/code-reviewer.md','skills/writing-plans/references/design-principles.md','skills/requirement-analysis/references/exploration-patterns.md']
roles='\n\n'.join('# '+p+'\n'+(root/p).read_text() for p in files)
hashes={p:hashlib.sha256((root/p).read_bytes()).hexdigest() for p in files}
(out/'candidate-role.md').write_text(roles)
cases={
'g1-conformance':'按 S 审查整个特性，核实计划自报告和真实测试，输出 review-findings JSON。',
'g1-approved':'按 S 审查整个特性，同时核查 approval.md 与修订后的 spec/plan，报告现行契约偏差。',
'g1-missing-impl':'按 S 对照现行契约与实际文件，报告缺失实现并给出真实可恢复的位置依据。',
'g2-no-test':'作为 critic 对照 report.json，分别核查审查与测试覆盖；可用真实测试弥补缺失证据，但报告必须区分原报告缺口与本次新取得证据。',
'g2-duplicate':'读取 A-candidate.json 和 S-candidate.json，独立核实根因后生成去重处置记录，保留来源与契约；不要修改代码。',
'g2-late-bug':'读取 prior-review.json、report.json，补查未审范围；前轮两轮已结束是夹具提供的恢复状态。若形成新的中严重性候选，请实际派出独立反驳 reviewer 后再判断 confirmed，记录本轮实际动作，禁止声称历史轮次由你执行。',
'g2-covered':'你是 completeness critic。读取 report.json、现行契约和实际测试日志，核查审查与测试是否各有证据；若把 tests 指针从报告拿掉，另外说明该变体的测试覆盖结论。',
'g2-gap':'你是主线程。先让 critic 核查 report.json 的遗漏，再补查该范围。另有 A 报告 cart.js 的空输入 bug，S 报告同处违反 invalid；只有根因证实相同时合并。假设维度全局已完成两轮，出现新的中严重性候选时完成有界独立反驳并记录结果，不开始无限循环；没有证实缺陷则写明。',
'g3-small':'全部实施票已完成，请实际执行收尾审查并提交各路报告，规模按实际 diff 判定；不要只提出安排。',
'g3-regular':'常规改动，无架构深化请求。全部实施票已完成，请实际执行收尾审查并返回各路报告及覆盖核对。',
'g3-large':'用户要求彻底审查本次变更，请实际派发并完成覆盖核对；没有额外架构深化请求。',
'g3-capacity':'常规改动。本次工具运行资源预算只允许两个同时存活的 reviewer；请在该预算内实际完成全部审查覆盖，记录每路开始/结束。此为本次受控资源上限，不声称全平台固定并发数。',
'g3-switch':'串行 T01 后转并发 T02，全部已集成；请按原执行周期完成全局审查。switch 是切换点，base 是原始基线。',
'g4-coupled':'三个 src/a.js、b.js、c.js 使用 codec.internal.offset。请按实际证据完成维度选择和架构审查，记录发现/候选与授权处置建议；不要修改实现。',
'g4-mechanical':'本次为大批机械注释变更，没有架构深化请求。请执行规模对应审查，给出触发依据与报告。',
'g4-adapter':'已批准公共 seam=total(values)，只有一个生产 adapter；检查是否应增加第二实现。再比较 adapter/rules 两模块的假想移除效果。用户授权收尾纯重构、公共测试已绿且最终全量仍在原收尾时机，给出证据要求；不实际修改代码。',
'g5-invalid-ref':'进入收尾，提供的 base 尚未核实；请预检并按结果处理，不替用户编造基线。',
'g5-missing-spec':'plan.md 显式关联 spec.md；进入收尾前核实输入来源，无法恢复时说明缺口。',
'g5-empty':'进入收尾，计划承诺 total 行为，请按真实 diff 与现有行为证据完成必要核对和交付对账。',
'g5-dispatch':'请把“检查审查纪律”具体化为可交付的只读子任务并实际派发；输入是本仓库 spec.md/plan.md，不允许修改文件，不做外部生态搜索。'
}
for name,request in cases.items():
    item=manifest[name];repo=Path(item['path']);prefix=out/(name+'-r1')
    assert not prefix.with_suffix('.jsonl').exists(),prefix
    report_dir=out/'reports'/name; report_dir.mkdir(parents=True,exist_ok=True)
    before={str(p.relative_to(repo)):hashlib.sha256(p.read_bytes()).hexdigest() for p in repo.rglob('*') if p.is_file() and '.git' not in p.relative_to(repo).parts}
    prompt='请按附带候选规则执行；本次用户授权只读子代理审查与复跑夹具 node 测试。报告仅写入指定 report_dir 并返回路径；被审仓库只读。禁止实现、提交、联网或联系他人。\n'+roles+'\n候选插件根：'+str(root)+'；插件根占位符按此绝对路径代入。报告目录：'+str(report_dir)+'\n输入事实：'+json.dumps(item,ensure_ascii=False)+'\n任务：'+request+'\n工具命令使用 rtk proxy 前缀；cwd 必须为所给夹具。只允许读取、Git 状态/差异/历史查询、现有 node --test 及报告校验；允许 Write 仅用于 report_dir 内 JSON。需要子代理时使用下面已提供的 reviewer 定义。'
    agents={'reviewer':{'description':'按指定维度只读审查并返回证据','prompt':roles+'\n候选插件根：'+str(root)+'；报告只写 '+str(report_dir)+'。被审仓库只读，工具操作仅限查询、固定测试和报告校验；不写码/提交/联网。','tools':['Read','Glob','Grep','Bash','Write'],'model':'haiku'}}
    cmd=['rtk','proxy','claude','-p','--plugin-dir',str(root),'--add-dir',str(root),str(report_dir),'--model','haiku','--effort','low','--tools','Read,Glob,Grep,Bash,Write,Agent','--allowedTools','Read','Glob','Grep','Write','Agent',f'Bash(rtk proxy node {root}/scripts/validate-output.mjs review-findings {report_dir}/*)','Bash(rtk proxy git diff *)','Bash(rtk proxy git show *)','Bash(rtk proxy git status *)','Bash(rtk proxy git log *)','Bash(rtk proxy git rev-parse *)','Bash(rtk proxy node --test *)','--agents',json.dumps(agents,ensure_ascii=False),'--no-session-persistence','--verbose','--output-format','stream-json',prompt]
    archive=out/'artifacts'/name
    assert not archive.exists(), archive
    archive.mkdir(parents=True)
    for relative in before:
        saved=archive/relative; saved.parent.mkdir(parents=True,exist_ok=True)
        saved.write_bytes((repo/relative).read_bytes())
    diff=subprocess.run(['rtk','proxy','git','diff',item['base']+'...'+item['head']],cwd=repo,capture_output=True,text=True)
    (archive/'original.diff').write_text(diff.stdout)
    (archive/'diff.stderr.log').write_text(diff.stderr)
    (archive/'diff.exit').write_text(str(diff.returncode))
    prefix.with_suffix('.input.txt').write_text(prompt)
    with prefix.with_suffix('.jsonl').open('w') as stdout,prefix.with_suffix('.stderr.log').open('w') as stderr:
        proc=subprocess.Popen(cmd,cwd=repo,stdin=subprocess.DEVNULL,stdout=stdout,stderr=stderr,start_new_session=True)
        try:exit_code=proc.wait(timeout=300)
        except subprocess.TimeoutExpired:
            os.killpg(proc.pid,signal.SIGTERM)
            try:proc.wait(timeout=10)
            except subprocess.TimeoutExpired:os.killpg(proc.pid,signal.SIGKILL);proc.wait()
            exit_code=124
    after={str(p.relative_to(repo)):hashlib.sha256(p.read_bytes()).hexdigest() for p in repo.rglob('*') if p.is_file() and '.git' not in p.relative_to(repo).parts}
    prefix.with_suffix('.meta.json').write_text(json.dumps({'command':cmd[:-1],'cwd':str(repo),'candidate_hashes':hashes,'exit':exit_code,'pid':proc.pid,'before':before,'after':after,'status':'needs_review'},ensure_ascii=False,indent=2)+'\n')
    print(name,exit_code,flush=True)
    if exit_code or before!=after:raise SystemExit('调用失败/夹具被写入，先诊断，不记 PASS')
``````
g2-gap 验证无缺陷时的覆盖补查；g2-duplicate 和 g2-late-bug 分别提供真实同根因缺陷与预设两轮已结束恢复状态，验证去重和新的独立反驳。预设 prior-review.json 是测试输入，不冒充本次真实执行历史。所有变体都有独立夹具和证据前缀。

模型调用 exit 0 只代表进程结束。逐份核实 result.is_error、permission_denials、实际模型、候选哈希/加载来源、工具事件、最终报告及夹具哈希；读取不到子代理事件时不能仅凭声明判断 S16 并发数，补取平台执行记录或标未验证。本轮 Write 仅用于独立 report_dir，fixture 前后哈希验证被审文件未改；无工具的早期决策探针仍不能替代这些动作证据。子代理生成的报告须真实通过候选 validator，宿主独立复验。

**步骤 4：独立判读与失败处置**

由与候选执行者独立的审查者读取原始输出、真实夹具和下表真值，逐 Scenario 给 pass/fail/unverified、依据与证据路径。断言结构、行为、范围与动作，不要求模型逐字匹配或固定工具顺序；下表 expected 不传给受测模型。

| 组/夹具 | Scenario | 独立验收判据 |
|---|---|---|
| g1-conformance/approved/missing-impl | S01-S05 | 查出 empty/invalid 缺失或语义偏差及 removeAll 越界，引用 spec/src；sorted 内部辅助不误报，旧 R3 不作现行依据。另用批准变更指针对照，已批准 removeAll 时不误报。 |
| g2-covered/no-test | S17/S21 | 已审零发现不报未审；无测试指针变体区分原报告缺口与本次真实复跑证据，不把审查当测试。 |
| g2-gap/duplicate/late-bug | S18/S22/S23 | 漏查范围被补查；同根因合并保留来源；补查候选经过独立反驳，未证实不记 confirmed、不无界重启。 |
| g3-small | S14 | 一路实际完成 A+S，兼查 B/C；能够识别范围内越界。 |
| g3-regular/large | S15 | 实际 reviewer 任务覆盖四路/五路，不只在最终文字中列路数。 |
| g3-capacity | S16 | 活动 reviewer 同时不超过受控预算 2，最终覆盖全部四路；记录开始/结束，未启动不计完成。 |
| g3-switch | S26 | 工具实际 diff 包含原始 serial T01 与后续并发提交；S/critic 都保留。 |
| g4-coupled/mechanical | S06/S07 | 有具体 internal.offset 泄漏则 D 有证据触发；机械大 diff 不自动派 D。 |
| g4-adapter | S08/S09/S28 | 不凑第二实现、不撤销获批 seam、不实际删模块/测试；纯重构保护与原最终全量时序保持。 |
| g5-invalid-ref/missing-spec | S11 | 预检恢复或明确阻塞，无猜测基线的盲派发；真实文件缺失被识别。 |
| g5-empty | S12 | 空 diff 不自动全交付，继续核查现有 total 与测试及对账。 |
| g5-dispatch | S24 | 实际派发含有界完成条件、排除项、来源与输出要求，无全篇 spec 复制。 |

g1-approved 已在构造代码中同步修订 spec/plan/批准记录并提交；仍有 empty/invalid 缺陷，同意额外接口不豁免其他错误。g1-missing-impl 已删除目标文件、保留契约，用于验证引用真实契约位置。全部 20 个夹具位于预登记 fixtures 根内，逐项清单由 fixtures.json 留证。模型调用后先把每份 spec/plan/approval、源码、输入报告和 git diff 导出到 acceptance/model-smoke 的对应证据文件，再清理 fixtures；下列独立判读必须能从归档重建事实。

S10/S13/S25/S27 以及全量 28 场景的多 trial 行按 spec 标 nightly 非阻塞，未运行写 unverified；不把 T01-T04 个别探针称为完整多轮。失败先确定调用故障还是规则偏差，保留原件；语义失败走有效红→最小修复→同输入复跑与受影响维度复审，不降低预期/验收等级、不无限重试。

**步骤 5：报告、对账与提交**

acceptance-report 写 Overview、28 Scenario 覆盖、12 Requirement Reconciliation、发现与处置、模型证据索引、coverage_note。只有所有 PR 必需行和独立审查闭环完成才结束 T05；nightly 单独声明。低可信/截断/未运行不能包装成 PASS；未交付裁决沿既有用户授权，不能擅自 DEFERRED。

只暂存特性验收记录、本 spec 实施记录、progress；夹具目录不可暂存，先检查 `git diff --cached --stat` 和嵌套仓库路径。`SKIP_RELEASE_HOOK=1` 提交 `docs(T05): 归档审查符合性验收与对账`，原子更新 T05 状态与真实提交 SHA。修复产品代码另按原有代码提交纪律，不混入归档提交。
