# T11：统一 critic 触发与配置拒绝

**文件**：修改 `scripts/lib/review_store.py`、`scripts/tests/controlled-review.test.mjs`。全部自然语言同步在 T12/T13。

**验证阻塞补齐 R06**：全量中断恢复用例反复失败，独立窄诊断证实 `review_process.py` 的信号0探测 EPERM 可中断 finally。补充该文件的保守终止证明：只有 leader 已实际退出且成功、完整可解析的 `ps` 全表证明该PGID无活跃成员时才结束stop；活跃/未知状态、查询失败或不可解析仍blocked。不假定原失败现场仅有僵尸，不改旧run/finished、不放宽strict snapshot。公共CLI通过OS故障注入验证无成员/僵尸正例与活跃/坏格式/查询失败反例；原R06及全量仍须实际通过。

**接口**：init 接受 small/regular 的 on-findings/always；large 默认 always，显式 on-findings 拒绝。tasks/status 保留高/中候选触发 critic，不以 confirmed 计数替代候选触发。S17/S18。

## 步骤 1：完整公共测试

```js
test('S18 large cannot silently override required critic',t=>{
 const f=fixture(t,{tier:'large',critic:'on-findings',expectInitError:true});
 assert.notEqual(f.init.code,0);
 assert.match(JSON.stringify(f.init.data),/large.*critic|critic.*large/);
});
```
复用现有 fakeClient：small/regular 零高/中候选不派反驳和 critic；regular always、large 默认即使零候选也派 critic；regular 候选全部被否仍派 critic；AS coverage gap 即使零 confirmed 仍 incomplete。

## 步骤 2：确认红

`rtk proxy node --test --test-name-pattern='S3.2|S3.5|S3.7|S18|coverage' scripts/tests/controlled-review.test.mjs`
预期 large+on-findings 被旧 init 接受而失败。检查选中的实际测试数非零。

## 步骤 3：最小实现

initialize 在有效值检查后增加：
```python
if tier == 'large' and critic != 'always':
    raise ValueError('large requires critic=always')
```
tasks() 依据所有已核验高/中候选及配置决定 critic，不只看 confirmed；首轮无候选结束反驳循环，但仍进入适用 critic 与覆盖检查。不要改动 1/2/5 路、D 触发、独立 actor 身份、轮数或预算。

## 步骤 4：确认绿

运行上述选择命令后运行 `rtk proxy node --test scripts/tests/controlled-review.test.mjs`。输出保持测试退出码、review_result、completed/incomplete 分开；controller completed 不是产品全绿。

## 步骤 5：提交

提交 `fix(T11): require critic for large reviews`；`TID=T11` 保存真实状态。禁止为新结果改成仅匹配中文句子的测试来取代真实 CLI 分支。
