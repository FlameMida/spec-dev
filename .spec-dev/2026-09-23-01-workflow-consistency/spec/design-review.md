## Spec 审查

**结论**：Approved

审查对象：[workflow-consistency-design.md](workflow-consistency-design.md)，2026-09-23，提交 `ba864a6` 对应版本；文件 SHA-256：`02eb60633fd50cb4f0de8a1c7c3feb07ec9add29e5dd8df9456cd7bc6b8a6688`。

14 条 Requirement 均有可判定 Scenario，共 S01—S27；未发现 TODO、占位章节、相互竞争的状态来源或会使实施计划必须重新裁决需求的缺口。设计可以用于编写实施计划；本结论不激活 draft spec，也不授权实施。

**问题清单**：

- 无阻塞问题。

**建议（仅供参考，不阻塞通过）**：

- 编写计划时将 S03 的工作区、index 和历史入口拆成独立测试输入，特别保留“工作区声明已扩大而 index 仍旧版”的反例，避免同一测试夹具掩盖版本视图差异。这是落实 A01—A03 的测试分解，不要求增加产品能力。
- S21—S24 的计划步骤宜明确标出“复制成功但检查点尚未保存”和“实际 squash 后原分支/worktree 已删”两个恢复窗口，并继续运行现有票级 ancestry 拒绝用例。字段名称与命令可以按第 388 行授权在计划中定稿。

### 关键核对结果

| 核查面 | 结论与依据 |
|---|---|
| 任务范围与版本 | A01—A03、第 380—390 行区分静态计划、运行绑定、历史关联和执行证据；编辑读取文件系统，staged 读取 index，历史逐提交/逐 ref 读取 Git 版本。S02 排除同批扩写自授权，S04—S06 覆盖过期、越界、多契约和缺历史材料。精确字段尚待计划定稿不妨碍需求边界成立。 |
| 当前任务恢复 | M01/M02 与 S07—S10 明确先处理当前 in_progress/blocked、允许无完成 SHA、提交后补检查点、旧单文件状态字符与正文版本分离；活动组仍保留锁、claim、证据和未知协议拒绝，未以普通串行恢复替代组协议。 |
| final 与修复闭环 | M03 的 F → 审查 → 验收/对账 → D 顺序无依赖环；无验收票和未完成组均有判据。M04/M05 保留历史 F 结果，修复后补验并更新复审/验收，影响不能可靠划定时回到全量。第 394—410 行流程图已覆盖 final 回归和目标差异，不把旧 completed 当作新候选的放行证据。 |
| 审查与验收 | M06 独立计算反驳与 critic，保留固定引用和不同 actor 的实际回执；零候选与覆盖完整分开。M07 对外部回执检查来源与原件，standard/deep 的审计条件一致。M05 保留受控 run 的快照拒绝与新 run 边界。 |
| 忽略证据与交付 | M08 保留原件字节、cwd、失败 attempt 和哈希；转存冲突/中断保留原区。M09 区分票级接收、来源历史与实际目标，真实 squash 使用持久来源锚及内容映射，不放宽票级 ancestry；PR open、后续受保护文档回写、未完成清理均不能提前 completed/delivered。 |
| 图文精简与测试边界 | M10/M11 保留行为红绿、纯重构保护、已授权例外、未知契约影响和角色授权边界。每幅图仍有文本依据；静态文档/CLI 检查与 manual 模型行为证据分开，未将结构检查当作模型效果。 |
| 范围与模块判据 | 绑定、验证、审查、证据转存和交付共同处理一个执行闭环；没有阶段化延期。复用现有 plan/progress、守卫和公共 CLI，不新增竞争状态服务、编译快照或私有测试接口；没有仅因单一 adapter 或未指定内部函数而产生否决理由。 |

### 外部一致性与差量核对

本次逐条核对取代表中的真实 Requirement 标题和其现行状态；这些是部分取代，没有完全取代 spec 的 covers 真空差集。ADDED 部分为新增任务范围、绑定和历史关联；MODIFIED 部分明确改变已有执行/审查/交付语义，未把新旧竞争规则同时留作现行要求。

| 当前来源 | 实际核对的行为交集及承接 |
|---|---|
| workflow-slimming | R3 收尾审查路数与证据、R4 本地三时点测试、R5 本地三 lane、R6 验收复用执行证据、R7 执行证据留存；取代表使用的完整标题与原文一致，M03—M08 承接仍有效的范围、manual、路数和本地证据约束，不重新要求历史清除。 |
| plan-decomposition | M01 plan 单一形态（增加组声明与类型化步骤）、M02 渐进执行与断点恢复（扩展组检查点）、A08 集成组恢复核对事实、A09 协议能力检查先于写入；新设计保留三件套、组待验/原子完成和能力拒绝，最终交付映射不改变活动组协议。 |
| concurrent-execution | 特性级双交付通道、既有收尾审查全量继承；M03—M05/M09 承接全局审查、真实合并、发布授权、保护分支、PR ready/awaiting_merge 和 sync_commit 边界。已被取代的旧恢复/计划条款只作历史。 |
| plan-single-format | 存量计划兼容读取；M02 明确取代“冻结读侧不再扩展”的部分，仍保持旧格式、复选框和不生成第二份 progress，不恢复单文件计划生成。 |
| test-scoping | 最终任务归属裁决、executing-plans 消费声明语义；全量及归属比较改至 F/补验，既有失败仍需真实来源证据及用户裁决。 |
| supersede-lifecycle | 装机侧把 superseded 重写为生命周期终态；A02 第 163 行完整保留后继指针、禁止依据旧行为开展新工作、covers 接管和冻结 sync_commit。其激活 pending、交付回写、ADR 裁决期生效与消费者过滤继续有效。 |
| controlled-review | 快照引用和独立复核；M06/M07 承接引用真实性、独立身份、适用回执与覆盖缺口。未取代的“有界运行及恢复”继续适用，M05 未允许跨 run 拼报告或修改快照后续跑。 |
| skill-instruction-streamlining | A17 精简劝诫而保持测试纪律、A18 验收遵循矩阵和计划指定时机、A19 证据驱动的契约影响分流，以及按需加载/单点定义切面；M10/M11 未删除已有授权、未知分歧及到期验证边界。 |
| tdd-seam | 公共落点、真实行为覆盖及 TDD 例外统一定义；M10 保留纯文案与技能行为的区分，纯重构按后继 plan-decomposition 的现行规则消费。旧已取代的收尾重构条款不重新生效。 |
| resource-ledger | 清理只遍历台账、共享缓存默认保留、quick-fix 收尾清单式清理；M08/M09 的转存和本地历史锚不授权清理台账外资源。 |
| triage-routing、quick-fix-diagnosis、exploring-clarifying | 核对分诊确认/零落盘、证据式升级门、用户裁决不可自代及公开摘要边界；M11 的图文精简没有授权自动选出口、把未知当契约不变或扩大探索实施权限。 |
| status-overview | R05 分文件计划记录读取、R06 旧单文件复选框记录、R07 格式与缺失诊断、R11 只读与验证边界；第 388—390 行要求投影识别合法扩展且不写状态，不把状态概览变为交付事实校验。 |
| portability-hygiene | 插件根/导航依赖和单点纪律；新守卫只分发必要判据，不复制完整插件校验器/schema，图文移动仍要求权威可达。 |

ADR-0001/0002/0005/0006/0007、0009、0010 按当前 Accepted 决定核对。ADR-0008 已标 `Superseded by ADR-0010`，仅用于核对承接，未作为新的现行约束；ADR-0004 同样为历史。ADR-0010 保留集成组独立任务/提交、待验、同组依赖、唯一验证出口、主线程锁与独占、原子完成、原始历史及并发授权等有效决定，明确替换原始证据提交与最终交付校验方式。其与 spec 不同的生效时点符合 ADR-0001/0002。

### coverage_note

- 已独立阅读全文、27 个 Scenario、验收矩阵和更新后的流程图；静态检查 Requirement/Scenario 对应、占位符、ADR 链接、取代标题和当前状态。
- 读取当前 `guardrail/check-spec-drift.mjs` 的入口/变更视图、`scripts/lib/integration-plan.mjs` 的运行事实/证据/ancestry、`scripts/lib/review_store.py` 的快照及外部回执消费，以及相关 CLI/状态投影符号，作为计划可落地性的边界核对；未把旧 plan、acceptance 或历史测试回执当作当前实现通过证据。
- 本次是静态设计审查，未实现代码、未运行产品测试/模型任务、未验证真实远端 push/PR/合并行为，也未对未相交子系统作完整审计。公开入口夹具、真实 Git squash/转存与模型行为仍按本 spec 的后续计划和验收要求执行。
- 唯一写入为本报告；未修改待审 spec、ADR、源码或用户原有 `hooks/hooks.json` 改动。
