### 任务 3：四个 SKILL.md 载入即声明 + 命令写法

**文件**：
- 修改：`skills/requirement-analysis/SKILL.md:8`（之后插入声明行）
- 修改：`skills/writing-plans/SKILL.md:8`（之后插入声明行）
- 修改：`skills/executing-plans/SKILL.md:8`（之后插入声明行）、`:93`（命令双引号 + 契约校验 gist）
- 修改：`skills/acceptance-qa/SKILL.md:8`（之后插入声明行）、`:78`（命令双引号）、`:80-81`（删除降级说明句）、`:117`（契约校验变体 gist）
- 测试：`scripts/tests/plugin-root.test.mjs`（追加 2 个 test）

**接口**：
- 消费：T02 节标题 `插件根解析`；全局约束「声明行字面量」
- 产出：四个 SKILL.md 各含逐字一致的声明行（第 10 行）；executing-plans 含 `node "${CLAUDE_PLUGIN_ROOT}/scripts/validate-output.mjs" review-findings <file>` 与契约校验 gist；acceptance-qa 含 `node "${CLAUDE_PLUGIN_ROOT}/skills/acceptance-qa/scripts/detect-env.mjs"`、不含"先定位插件安装目录"、:117 为 `校验失败发回补全一次，再失败标记 unverified（acceptance-qa 变体；通用规则见 exploration-patterns「输出契约与校验」）`

**步骤 1：写失败测试**

在 `scripts/tests/plugin-root.test.mjs` 的 `export {` 行之前追加：

```js
const DECL = "> **插件根**：`${CLAUDE_PLUGIN_ROOT}`——本 skill 正文与其 references 中的插件根命令以此为准；若上式仍为变量字面量（平台未替换），按 requirement-analysis 的 references/exploration-patterns.md「插件根解析」序列推导。";

test("Scenario: 四个 skill 都有声明行", () => {
  const withDecl = ["requirement-analysis", "writing-plans", "executing-plans", "acceptance-qa"];
  for (const s of withDecl) assert.ok(read(`skills/${s}/SKILL.md`).includes(DECL), `${s} 缺声明行或措辞不一致`);
  for (const s of readdirSync(path.join(repoRoot, "skills"))) {
    if (withDecl.includes(s) || VENDORED.includes(`skills/${s}`)) continue;
    if (!existsSync(path.join(repoRoot, "skills", s, "SKILL.md"))) continue;
    assert.ok(!read(`skills/${s}/SKILL.md`).includes("**插件根**："), `${s} 正文无插件根命令，不应有声明行`);
  }
});

test("Scenario: 变量未替换时按序列推导——SKILL 内命令为双引号写法且契约 gist 带指针", () => {
  const ep = read("skills/executing-plans/SKILL.md");
  const aq = read("skills/acceptance-qa/SKILL.md");
  assert.ok(ep.includes('node "${CLAUDE_PLUGIN_ROOT}/scripts/validate-output.mjs" review-findings <file>'));
  assert.ok(aq.includes('node "${CLAUDE_PLUGIN_ROOT}/skills/acceptance-qa/scripts/detect-env.mjs"'));
  assert.ok(!aq.includes("先定位插件安装目录"), "acceptance-qa 不应再自带降级说明");
  for (const [f, t] of [["executing-plans", ep], ["acceptance-qa", aq]]) {
    for (const line of t.split("\n")) if (line.includes("补全一次")) assert.ok(line.includes("exploration-patterns"), `${f}: 契约校验句缺指针 → ${line.trim().slice(0, 80)}`);
  }
});
```

**步骤 2：运行测试确认失败**

运行：`node --test scripts/tests/plugin-root.test.mjs`
预期：6 个 test 中新增 2 个 FAIL（四个 SKILL.md 均无声明行；executing-plans:93 / acceptance-qa:78 为无引号写法，acceptance-qa:81 仍含降级说明）。

**步骤 3：写最小实现**

(a) 四个 SKILL.md 插入声明行——在以 `> 语言协议：` 开头的行之后插入一个空行与声明行（用脚本保证四处逐字一致）：

```bash
python3 - <<'PY'
DECL = '> **插件根**：`${CLAUDE_PLUGIN_ROOT}`——本 skill 正文与其 references 中的插件根命令以此为准；若上式仍为变量字面量（平台未替换），按 requirement-analysis 的 references/exploration-patterns.md「插件根解析」序列推导。'
for p in ["skills/requirement-analysis/SKILL.md", "skills/writing-plans/SKILL.md", "skills/executing-plans/SKILL.md", "skills/acceptance-qa/SKILL.md"]:
    lines = open(p, encoding="utf-8").read().split("\n")
    assert DECL not in lines, p
    i = next(k for k, l in enumerate(lines) if l.startswith("> 语言协议："))
    lines[i + 1:i + 1] = ["", DECL]
    open(p, "w", encoding="utf-8").write("\n".join(lines))
    print("declared", p)
PY
```

(b) `skills/executing-plans/SKILL.md` 第 93 行（插入声明行后为第 95 行）整行替换：

原文（精确匹配）：

```
- **契约校验**：每份报告落盘后 `node ${CLAUDE_PLUGIN_ROOT}/scripts/validate-output.mjs review-findings <file>`，失败退回补全一次，再失败主线程接管该维度
```

替换为：

```
- **契约校验**：每份报告落盘后 `node "${CLAUDE_PLUGIN_ROOT}/scripts/validate-output.mjs" review-findings <file>`；校验失败发回补全一次，再失败主线程接管（定义见 exploration-patterns「输出契约与校验」）
```

(c) `skills/acceptance-qa/SKILL.md`：

- 第 78 行 `node ${CLAUDE_PLUGIN_ROOT}/skills/acceptance-qa/scripts/detect-env.mjs [--cwd <project>]` → `node "${CLAUDE_PLUGIN_ROOT}/skills/acceptance-qa/scripts/detect-env.mjs" [--cwd <project>]`
- 删除第 81 行 `（\`${CLAUDE_PLUGIN_ROOT}\` 不可用时，先定位插件安装目录再以其为根解析路径。）` 及其后的一个空行（使代码块结束的 ```` ``` ```` 与下一段 `输出 JSON 含 ...` 之间只保留一个空行）
- 第 117 行 `落盘 acceptance-check-items 契约 JSON → validate-output.mjs 校验 → 失败按 errors 补全一次` → `落盘 acceptance-check-items 契约 JSON → validate-output.mjs 校验 → 校验失败发回补全一次，再失败标记 unverified（acceptance-qa 变体；通用规则见 exploration-patterns「输出契约与校验」）`

**步骤 4：运行测试确认通过**

运行：`node --test scripts/tests/plugin-root.test.mjs && node --test scripts/tests/search-clause.test.mjs scripts/tests/plan-single-format.test.mjs scripts/tests/resource-ledger-split.test.mjs scripts/tests/numbering-docs.test.mjs && node scripts/validate-skills.mjs`
预期：plugin-root 6/6 PASS；四个既有 docs 测试全 PASS（特征串未动）；validate-skills 13 个 skill 全 valid。

**步骤 5：提交**

```bash
git add skills/requirement-analysis/SKILL.md skills/writing-plans/SKILL.md skills/executing-plans/SKILL.md skills/acceptance-qa/SKILL.md scripts/tests/plugin-root.test.mjs .spec-dev/2026-09-03-02-portability-hygiene/plan/progress.yaml
SKIP_OPENAI_SYNC_CHECK=1 git commit -m "fix(T03): 四个 skill 载入即声明插件根；executing-plans/acceptance-qa 插件根命令改双引号写法、契约校验句改 gist + 指针、删各处降级复述

Spec-Guard: off facet-coexist with major-upgrade/plan-single-format/supersede-lifecycle/resource-ledger/test-scoping"
```
