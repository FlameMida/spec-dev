### 任务 4：visual 根自建 .gitignore + visual-preview 写法

**文件**：
- 修改：`skills/visual-preview/scripts/start-server.sh:167-168`（SESSION_DIR 分支之后、DRY_RUN 块之前：新增 `GITIGNORE_ROOTS` 与 `ensure_visual_gitignore`）、`:171-172`（DRY_RUN 块内打印 `GITIGNORE=`）、`:182`（mkdir 之后逐根自建）
- 修改：`skills/visual-preview/SKILL.md:36-38`（启动命令改 `${CLAUDE_SKILL_DIR}` + 注释）、`:43`（隔离提醒改脚本自建）、`:71`（停止命令改 `${CLAUDE_SKILL_DIR}`）
- 测试：`scripts/tests/visual-path.test.sh`（追加 1 个场景）、`scripts/tests/plugin-root.test.mjs`（追加 1 个 test）

**接口**：
- 消费：start-server.sh 现文（:152-167 SESSION_DIR 三分支、:168-173 DRY_RUN 块含 `exit 0`、:182 `mkdir -p "${SESSION_DIR}/content" "$STATE_DIR"`）
- 产出：bash 数组 `GITIGNORE_ROOTS`（FEATURE_DIR 分支 = `${FEATURE_DIR}/visual` 与 `${FEATURE_DIR%/*}/visual`；PROJECT_DIR 分支 = `${PROJECT_DIR}/.spec-dev/visual`；/tmp 分支为空）；函数 `ensure_visual_gitignore <root>`（`mkdir -p` 后不存在则写入 `*`，失败只 warn）；dry-run 输出行 `GITIGNORE=<root>/.gitignore`；SKILL.md 含 `脚本自建`、`"${CLAUDE_SKILL_DIR}/scripts/start-server.sh"`，不含 `<skill-base-directory>` 与 `加入 \`.gitignore\``

**步骤 1：写失败测试**

`scripts/tests/visual-path.test.sh`：在 `rm -rf /tmp/vp-rel` 与 `echo PASS` 之间插入：

```bash
# Scenario: dry-run 打印 GITIGNORE 且不落盘（特性上下文两根、回退一根）
rm -rf /tmp/vp-proj
out=$(bash "$SCRIPT" --project-dir /tmp/vp-proj --feature-dir /tmp/vp-proj/.spec-dev/2026-08-26-01-demo --dry-run 2>&1)
echo "$out" | grep -q "GITIGNORE=/tmp/vp-proj/.spec-dev/2026-08-26-01-demo/visual/.gitignore" || { echo "FAIL: 特性 visual 根 .gitignore 未打印, got: $out"; exit 1; }
echo "$out" | grep -q "GITIGNORE=/tmp/vp-proj/.spec-dev/visual/.gitignore" || { echo "FAIL: 项目 visual 根 .gitignore 未打印, got: $out"; exit 1; }
[ ! -e /tmp/vp-proj/.spec-dev/visual/.gitignore ] || { echo "FAIL: dry-run 不应落盘"; exit 1; }
out=$(bash "$SCRIPT" --project-dir /tmp/vp-proj --dry-run 2>&1)
[ "$(echo "$out" | grep -c '^GITIGNORE=')" = "1" ] || { echo "FAIL: 回退分支应只打印一个 visual 根, got: $out"; exit 1; }
```

`scripts/tests/plugin-root.test.mjs` 在 `export {` 行之前追加：

```js
test("Scenario: 提醒句更新", () => {
  const vp = read("skills/visual-preview/SKILL.md");
  assert.doesNotMatch(vp, /加入 `\.gitignore`/, "不应再要求用户手动加 gitignore");
  assert.ok(vp.includes("脚本自建"), "应说明脚本自建 .gitignore");
  assert.ok(vp.includes("不要忽略整个 `.spec-dev/`"), "应保留不要忽略整个 .spec-dev 提醒");
  assert.ok(!vp.includes("<skill-base-directory>"), "自造占位符应消失");
  assert.ok(vp.includes('bash "${CLAUDE_SKILL_DIR}/scripts/start-server.sh"'), "启动命令应用官方 CLAUDE_SKILL_DIR");
  assert.ok(vp.includes('bash "${CLAUDE_SKILL_DIR}/scripts/stop-server.sh"'), "停止命令应用官方 CLAUDE_SKILL_DIR");
  assert.ok(vp.includes("保留供日后查看"), "回看设计应保留");
});
```

**步骤 2：运行测试确认失败**

运行：`bash scripts/tests/visual-path.test.sh; node --test scripts/tests/plugin-root.test.mjs`
预期：visual-path 输出 `FAIL: 特性 visual 根 .gitignore 未打印` 并退出 1；plugin-root 7 个 test 中「提醒句更新」FAIL。

**步骤 3：写最小实现**

(a) `skills/visual-preview/scripts/start-server.sh`——在 SESSION_DIR 三分支的结束 `fi`（第 167 行，紧接 `SESSION_DIR="/tmp/brainstorm-${SESSION_ID}"` 之后的那个 `fi`）之后插入：

```bash

# visual 根（会话目录的父目录）自建 .gitignore（内容 *）：项目内会话文件不入 git，
# 不要求用户改仓库 .gitignore；/tmp 会话无需处理。
GITIGNORE_ROOTS=()
if [[ -n "${FEATURE_DIR:-}" ]]; then
  GITIGNORE_ROOTS+=("${FEATURE_DIR}/visual" "${FEATURE_DIR%/*}/visual")
elif [[ -n "$PROJECT_DIR" ]]; then
  GITIGNORE_ROOTS+=("${PROJECT_DIR}/.spec-dev/visual")
fi

ensure_visual_gitignore() {
  local root="$1"
  if ! mkdir -p "$root" 2>/dev/null; then
    echo "warn: cannot create ${root}; skip .gitignore" >&2
    return 0
  fi
  [[ -e "${root}/.gitignore" ]] && return 0
  printf '*\n' > "${root}/.gitignore" 2>/dev/null || echo "warn: cannot write ${root}/.gitignore" >&2
  return 0
}
```

(b) DRY_RUN 块：把

```bash
  [[ -n "${BRAINSTORM_TOKEN_FILE:-}" ]] && echo "TOKEN_FILE=${BRAINSTORM_TOKEN_FILE}"
  exit 0
```

替换为

```bash
  [[ -n "${BRAINSTORM_TOKEN_FILE:-}" ]] && echo "TOKEN_FILE=${BRAINSTORM_TOKEN_FILE}"
  for root in ${GITIGNORE_ROOTS[@]+"${GITIGNORE_ROOTS[@]}"}; do echo "GITIGNORE=${root}/.gitignore"; done
  exit 0
```

(c) 把 `mkdir -p "${SESSION_DIR}/content" "$STATE_DIR"` 替换为

```bash
mkdir -p "${SESSION_DIR}/content" "$STATE_DIR"
for root in ${GITIGNORE_ROOTS[@]+"${GITIGNORE_ROOTS[@]}"}; do ensure_visual_gitignore "$root"; done
```

（`${arr[@]+"${arr[@]}"}` 写法在 bash 3.2 + `set -u` 下对空数组安全。）

(d) `skills/visual-preview/SKILL.md`：

- 第 36-38 行代码块：

原文：

```
# 用户同意后再启动。--open 自动打开浏览器；--project-dir 使 mockup 持久化并支持同端口重启。
# 可选 --theme-css <file>：注入项目的 design tokens/CSS 变量，让 mockup 用项目自己的配色。
bash <skill-base-directory>/scripts/start-server.sh --project-dir /path/to/project --open
```

替换为：

```
# 用户同意后再启动。--open 自动打开浏览器；--project-dir 使 mockup 持久化并支持同端口重启。
# 可选 --theme-css <file>：注入项目的 design tokens/CSS 变量，让 mockup 用项目自己的配色。
# ${CLAUDE_SKILL_DIR} 由平台替换为本 skill 目录；未替换时取 skill base directory。
bash "${CLAUDE_SKILL_DIR}/scripts/start-server.sh" --project-dir /path/to/project --open
```

- 第 43 行：把 `；提醒用户把 \`.spec-dev/visual/\` 与 \`.spec-dev/*/visual/\` 加入 \`.gitignore\`（如尚未加入）。**不要忽略整个 \`.spec-dev/\`**` 替换为 `；两个 visual 根（\`.spec-dev/visual/\` 与 \`<feature-dir>/visual/\`）的 \`.gitignore\`（内容 \`*\`）由脚本自建，无需修改仓库 \`.gitignore\`。**不要忽略整个 \`.spec-dev/\`**`
- 第 71 行：`bash <skill-base-directory>/scripts/stop-server.sh $SESSION_DIR` → `bash "${CLAUDE_SKILL_DIR}/scripts/stop-server.sh" "$SESSION_DIR"`

**步骤 4：运行测试确认通过**

运行：`bash scripts/tests/visual-path.test.sh && node --test scripts/tests/plugin-root.test.mjs && bash -n skills/visual-preview/scripts/start-server.sh`
预期：visual-path `PASS`；plugin-root 7/7 PASS；`bash -n` 无语法错误。

**步骤 5：提交**

```bash
git add skills/visual-preview/scripts/start-server.sh skills/visual-preview/SKILL.md scripts/tests/visual-path.test.sh scripts/tests/plugin-root.test.mjs .spec-dev/2026-09-03-02-portability-hygiene/plan/progress.yaml
SKIP_OPENAI_SYNC_CHECK=1 git commit -m "feat(T04): visual 根由脚本自建 .gitignore（dry-run 打印 GITIGNORE= 不落盘）；visual-preview 命令改官方 CLAUDE_SKILL_DIR、隔离提醒改脚本自建

Spec-Guard: off facet-coexist with major-upgrade"
```
