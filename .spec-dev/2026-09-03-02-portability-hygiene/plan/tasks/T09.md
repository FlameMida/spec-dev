### 任务 9：验收（acceptance-qa）

> 本任务由 executing-plans 收尾审查阶段触发 acceptance-qa 按下表执行，
> 不参与逐任务连续执行；报告与证据落盘特性目录 `acceptance/` 子目录。
> 临时目录统一用 `/tmp/ph-accept-<PID>/`，验收结束即 `rm -rf`（即建即删，不入台账）。

| Scenario / 检查项 | 维度 | 执行方式 | 目标 | 阈值/预期 | 验收证据 |
|-------------------|------|---------|------|----------|---------|
| 用户项目 cwd 下校验命令可执行（bug 修复实证） | integration | 验收任务 (D) | `mkdir -p /tmp/ph-accept-$$/proj && cp -R <worktree>/.spec-dev/2026-08-27-01-plan-single-format/plan /tmp/ph-accept-$$/proj/plan && cd /tmp/ph-accept-$$/proj`；按 writing-plans 指令以插件根（本 worktree 绝对路径）执行 `node "<worktree>/scripts/validate-output.mjs" plan-index ./plan`；对照 HEAD 095eb40 写法 `node scripts/validate-output.mjs plan-index ./plan` | 前者 exit 0 且 stdout 含 `"ok": true`；后者 exit 1 且 stderr 含 `Cannot find module` | 两次命令的完整输出与退出码 |
| Claude Code 加载后声明行为绝对路径（机制验证） | integration | 验收任务 (X，AI 自主验收) | 本会话触发 acceptance-qa skill 时，截取已安装插件（8.1.0 缓存）正文中 `${CLAUDE_PLUGIN_ROOT}` 出现处（acceptance-qa:78 `detect-env.mjs` 命令行）是否已被替换为 `/` 起始绝对路径 | 已替换 → 机制成立（声明行本身待本版本发布并重装后复验，报告注明）；未替换 → 标 fail 并记录 | 加载文本片段 + `ls <该路径>/scripts/validate-output.mjs` 存在性 |
| 会话文件被忽略 / 已有 .gitignore 不覆盖 | integration | 验收任务 (D) | `/tmp/ph-accept-$$/repo`：`git init -q && mkdir -p .spec-dev/2026-09-03-02-demo`；先运行一次 `bash <worktree>/skills/visual-preview/scripts/start-server.sh --project-dir "$PWD" --feature-dir "$PWD/.spec-dev/2026-09-03-02-demo" --background` 取返回 JSON 的 `state_dir` 推出 SESSION_DIR，`bash <worktree>/skills/visual-preview/scripts/stop-server.sh <SESSION_DIR>`；然后 `printf 'keep-me\n' > .spec-dev/visual/.gitignore` 后再启动一次并停止；第三次：新建 `/tmp/ph-accept-$$/ro` 仓库，`mkdir -p .spec-dev/2026-09-03-02-demo && chmod 555 .spec-dev`（特性目录可写、`.spec-dev/visual` 不可建）后以 `--project-dir` + `--feature-dir` 启动并停止，再 `chmod 755 .spec-dev` 清理 | 第一次后：`git check-ignore -q <SESSION_DIR>/state/server-instance-id` 退出 0，`git status --short` 输出中不含 `visual`，仓库根 `.gitignore` 不存在或未被脚本创建；第二次后：`cat .spec-dev/visual/.gitignore` 仍为 `keep-me`；两个 visual 根都有 `.gitignore`；第三次：stderr 含 `warn: cannot create <repo>/.spec-dev/visual; skip .gitignore`，stdout 仍为 `server-started` JSON，特性根 `.gitignore` 已生成（R8 写入失败只警告） | 命令输出 |
| validate-skills / check-openai-sync / check-plugin / node --test / visual-path 全绿 | integration | 验收任务 (D) | worktree 内依次运行 `node scripts/validate-skills.mjs`、`node scripts/check-openai-sync.mjs`、`node scripts/check-plugin.mjs`、`node --test scripts/tests/*.test.mjs`、`bash scripts/tests/visual-path.test.sh` | 五条命令退出码全 0；node --test 报告 12 个文件全 pass（51 既有 + 本计划新增 18 + 5） | 命令退出码与摘要行 |
| living docs 零残留 | integration | 验收任务 (D) | 两条扫描：① `rg -n '<插件根>|<skill-base-directory>|先定位插件安装目录|先找插件安装目录|主进程接管' skills agents commands scripts/schemas README.md README.zh-CN.md --glob '!skills/anysearch/**' --glob '!skills/sequential-thinking/**'`；② `rg -n 'node scripts/validate-output\.mjs' skills agents commands --glob '!skills/anysearch/**' --glob '!skills/sequential-thinking/**'`（裸路径规则范围 = skills/agents/commands；`scripts/schemas/README.md:44` 的仓库开发期命令属范围外） | 两条均零命中（rg 退出 1） | rg 输出 |
| evals 人工走查 | docs | 验收任务 (D) | 逐条核对 requirement-analysis / writing-plans / executing-plans / acceptance-qa / quick-fix 的 `evals/evals.json` 用例，判断本次改动（声明行、gist 化、映射表指针化、例外清单引用、visual 自建 gitignore）是否使任何 expected_output 失效 | 全部用例仍可按现行文本满足；若某条描述陈旧（如 qf-small-bug-triggers 的"三条 glob"既有陈旧）只记录不修 | 走查清单（用例 id → 结论） |
