### 任务 6：失败隔离 / 契约校验复述收敛为 gist + 指针

**文件**：
- 修改：`skills/requirement-analysis/SKILL.md`（阶段 2「每个子代理必须给定」段末句——插入声明行后约第 135 行）
- 修改：`skills/quick-fix/SKILL.md:38`（根因定位括注）
- 修改：`skills/requirement-analysis/references/codex-compat.md:54`
- 修改：`skills/executing-plans/references/review-orchestration.md:21`、`:58`
- 修改：`scripts/schemas/README.md:14`
- 修改：`skills/acceptance-qa/references/ai-acceptance.md:38`（有意变体标注）
- 修改：`agents/code-explorer.md:116`、`agents/code-reviewer.md:147`（契约输出段的"补全一次"句补指针）
- 测试：`scripts/tests/plugin-root.test.mjs`（追加 3 个 test）

**接口**：
- 消费：全局约束两条 gist 字面量；T02 canonical（exploration-patterns:89 失败隔离、「输出契约与校验」节）
- 产出：失败隔离 gist `失败先缩小范围重试 1 次，再失败主线程接管（定义见 exploration-patterns「派发要求与失败隔离」）` 出现于 RA / quick-fix / codex-compat / review-orchestration 四处且逐字一致；契约校验 gist `校验失败发回补全一次，再失败主线程接管（定义见 exploration-patterns「输出契约与校验」）` 出现于 review-orchestration:21 / executing-plans:93（T03 已就位）/ schemas/README:14 三处且逐字一致；"主进程接管"全仓零命中；每处"补全一次"同行含 `exploration-patterns`

**步骤 1：写失败测试**

`scripts/tests/plugin-root.test.mjs` 在 `export {` 行之前追加：

```js
const GIST_FI = "失败先缩小范围重试 1 次，再失败主线程接管（定义见 exploration-patterns「派发要求与失败隔离」）";
const GIST_CV = "校验失败发回补全一次，再失败主线程接管（定义见 exploration-patterns「输出契约与校验」）";

test("Scenario: 四处 gist 与 canonical 字面一致", () => {
  for (const f of ["skills/requirement-analysis/SKILL.md", "skills/quick-fix/SKILL.md", "skills/requirement-analysis/references/codex-compat.md", "skills/executing-plans/references/review-orchestration.md"]) {
    assert.ok(read(f).includes(GIST_FI), `${f} 缺失败隔离 gist 或措辞不一致`);
  }
  assert.equal(count(read(EP), "缩小该主题范围重试 1 次"), 1, "canonical 只在 exploration-patterns 出现一次");
  for (const f of [...mdFiles(), "scripts/schemas/README.md"]) assert.ok(!read(f).includes("主进程接管"), `${f} 仍有措辞分化"主进程接管"`);
});

test("Scenario: 契约校验完整陈述唯一（引用侧）", () => {
  for (const f of ["skills/executing-plans/SKILL.md", "skills/executing-plans/references/review-orchestration.md", "scripts/schemas/README.md"]) {
    assert.ok(read(f).includes(GIST_CV), `${f} 缺契约校验 gist 或措辞不一致`);
  }
  for (const f of [...mdFiles(), "scripts/schemas/README.md"]) {
    if (f === EP) continue;
    for (const line of read(f).split("\n")) {
      if (line.includes("补全一次")) assert.ok(line.includes("exploration-patterns"), `${f}: "补全一次"同行缺指针 → ${line.trim().slice(0, 100)}`);
    }
  }
});

test("Scenario: 有意变体被标注", () => {
  const ai = read("skills/acceptance-qa/references/ai-acceptance.md");
  assert.match(ai, /再失败将缺失项标记 unverified[^\n]*acceptance-qa 有意变体[^\n]*exploration-patterns/, "ai-acceptance 应保留 unverified 结局并标注为变体、指向定义点");
});
```

**步骤 2：运行测试确认失败**

运行：`node --test scripts/tests/plugin-root.test.mjs`
预期：12 个 test 中三个新 test FAIL（四处失败隔离句措辞各异且 codex-compat:54 含"主进程接管"；review-orchestration:21 / schemas/README:14 / ai-acceptance:38 / code-explorer:116 / code-reviewer:147 的"补全一次"行无 exploration-patterns 指针）。

**步骤 3：写最小实现**

逐处替换（原文 → 新文）：

1. `skills/requirement-analysis/SKILL.md` 阶段 2 段：`失败的子代理先缩小范围重试 1 次，再失败由主线程接管。` → `失败先缩小范围重试 1 次，再失败主线程接管（定义见 exploration-patterns「派发要求与失败隔离」）。`
2. `skills/quick-fix/SKILL.md:38`：`根因不明朗时可派 1 个 \`code-explorer\` 子代理只读追踪（失败则缩小范围重试 1 次，再失败主线程接管）。` → `根因不明朗时可派 1 个 \`code-explorer\` 子代理只读追踪；失败先缩小范围重试 1 次，再失败主线程接管（定义见 exploration-patterns「派发要求与失败隔离」）。`
3. `skills/requirement-analysis/references/codex-compat.md:54`：`- 子代理失败后先缩小任务范围重试 1 次，仍失败由主进程接管` → `- 失败先缩小范围重试 1 次，再失败主线程接管（定义见 exploration-patterns「派发要求与失败隔离」）`
4. `skills/executing-plans/references/review-orchestration.md:21`：`  #   失败 → 把 errors 清单发回该子代理补全一次；再失败 → 主线程接管该维度` → `  #   校验失败发回补全一次，再失败主线程接管（定义见 exploration-patterns「输出契约与校验」）；接管以维度为单位`
5. `skills/executing-plans/references/review-orchestration.md:58`：`某维度子代理失败 → 缩小该维度范围重试 1 次 → 仍失败则主线程接管该维度，其余维度流水线不受影响。` → `失败先缩小范围重试 1 次，再失败主线程接管（定义见 exploration-patterns「派发要求与失败隔离」）；接管以维度为单位，其余维度流水线不受影响。`
6. `scripts/schemas/README.md:14`：`退出码 1——把 \`errors\` 清单发回子代理补全一次；再失败由主进程接管` → `退出码 1——校验失败发回补全一次，再失败主线程接管（定义见 exploration-patterns「输出契约与校验」）`
7. `skills/acceptance-qa/references/ai-acceptance.md:38`：`→ 校验失败：按 errors 清单补全一次；再失败将缺失项标记 unverified 并在报告说明` → `→ 校验失败：按 errors 清单补全一次；再失败将缺失项标记 unverified 并在报告说明（acceptance-qa 有意变体——不由主线程接管；通用规则见 requirement-analysis 的 references/exploration-patterns.md「输出契约与校验」）`
8. `agents/code-explorer.md:116` 与 `agents/code-reviewer.md:147`：把句末 `校验失败将被退回补全一次：` 替换为 `校验失败将被退回补全一次（处置规则见 requirement-analysis 的 references/exploration-patterns.md「输出契约与校验」）：`

**步骤 4：运行测试确认通过**

运行：`node --test scripts/tests/plugin-root.test.mjs && node --test scripts/tests/search-clause.test.mjs scripts/tests/numbering-docs.test.mjs scripts/tests/resource-ledger-split.test.mjs`
预期：plugin-root 12/12 PASS；三个既有 docs 测试 PASS。

**步骤 5：提交**

```bash
git add skills/requirement-analysis/SKILL.md skills/quick-fix/SKILL.md skills/requirement-analysis/references/codex-compat.md skills/executing-plans/references/review-orchestration.md scripts/schemas/README.md skills/acceptance-qa/references/ai-acceptance.md agents/code-explorer.md agents/code-reviewer.md scripts/tests/plugin-root.test.mjs .spec-dev/2026-09-03-02-portability-hygiene/plan/progress.yaml
SKIP_OPENAI_SYNC_CHECK=1 git commit -m "refactor(T06): 失败隔离与契约校验复述收敛为同措辞 gist + 指针（canonical 留 exploration-patterns），ai-acceptance 变体标注，消除\"主进程\"措辞分化

Spec-Guard: off facet-coexist with major-upgrade/plan-single-format/supersede-lifecycle/resource-ledger"
```
