### 任务 5：references / agents / commands / schemas 引用写法统一

**文件**：
- 修改：`skills/executing-plans/references/review-orchestration.md:20`（命令双引号）、`:74`（Codex 降级句去裸路径表述）
- 修改：`skills/acceptance-qa/references/ai-acceptance.md:37`（命令双引号）
- 修改：`skills/requirement-analysis/references/codex-compat.md:61`（`<插件根>` → 变量双引号写法；降级说明改占位符语义）
- 修改：`scripts/schemas/README.md:8`（命令双引号）、`:16`（降级说明改指针）
- 修改：`agents/code-explorer.md:148`（命令双引号 + 回退句）
- 修改：`agents/external-resource-explorer.md:31`（行内注释改回退句）
- 修改：`commands/doctor.md:5`（命令双引号 + 回退句）
- 测试：`scripts/tests/plugin-root.test.mjs`（追加 2 个全局 test）

**接口**：
- 消费：T02 节标题；全局约束「回退句」`未替换时按插件根解析序列推导（定义见 requirement-analysis 的 references/exploration-patterns.md）`
- 产出：skills/ agents/ commands/ 全部 .md（排除 vendored）零裸路径命令、零自造占位符、变量命令位一律双引号；三个 agent/command 文件含回退句；"先定位插件安装目录再以其为根解析路径"与"先找插件安装目录"全仓零命中

**步骤 1：写失败测试**

`scripts/tests/plugin-root.test.mjs` 在 `export {` 行之前追加：

```js
const BARE = /(^|[\s`(])(node|bash|python3?|bun)\s+(scripts|skills|agents|guardrail)\//;
const UNQUOTED = /(node|bash|python3?|bun)\s+\$\{CLAUDE_(PLUGIN_ROOT|SKILL_DIR)\}/;
const PLACEHOLDER = /<插件根>|<plugin-root>|<skill-base-directory>/;

test("Scenario: 零裸路径与零自造占位符", () => {
  for (const f of [...mdFiles(), "scripts/schemas/README.md"]) {
    const text = read(f);
    for (const line of text.split("\n")) {
      if (line.includes("scripts/spec-dev/") || line.includes("update-vendored-skill")) continue; // 目标仓库路径 / 仓库开发期维护提示
      assert.ok(!BARE.test(line), `${f}: 裸路径命令 → ${line.trim().slice(0, 100)}`);
      assert.ok(!UNQUOTED.test(line), `${f}: 变量未加双引号 → ${line.trim().slice(0, 100)}`);
    }
    assert.ok(!PLACEHOLDER.test(text), `${f}: 自造占位符`);
  }
});

test("Scenario: 降级说明不再各处复述", () => {
  for (const f of [...mdFiles(), "scripts/schemas/README.md"]) {
    const t = read(f);
    assert.ok(!t.includes("先定位插件安装目录再以其为根解析路径"), `${f} 仍有降级复述`);
    assert.ok(!t.includes("先找插件安装目录"), `${f} 仍有降级复述`);
  }
  for (const f of ["agents/code-explorer.md", "agents/external-resource-explorer.md", "commands/doctor.md"]) {
    assert.ok(read(f).includes("未替换时按插件根解析序列推导"), `${f} 缺固定回退句`);
  }
});
```

**步骤 2：运行测试确认失败**

运行：`node --test scripts/tests/plugin-root.test.mjs`
预期：9 个 test 中两个新 test FAIL（review-orchestration:20 / ai-acceptance:37 / code-explorer:148 / doctor.md:5 / schemas/README:8 变量未加引号；codex-compat:61 含 `<插件根>` 与"先找插件安装目录"；schemas/README:16 与 external-resource-explorer:31 含"先定位插件安装目录再以其为根解析路径"；三个 agent/command 无回退句）。

**步骤 3：写最小实现**

逐处替换（原文 → 新文）：

1. `skills/executing-plans/references/review-orchestration.md:20`
   `  #   node ${CLAUDE_PLUGIN_ROOT}/scripts/validate-output.mjs review-findings <file>` → `  #   node "${CLAUDE_PLUGIN_ROOT}/scripts/validate-output.mjs" review-findings <file>`
2. `skills/executing-plans/references/review-orchestration.md:74`
   `- 契约校验命令相同（校验器随插件分发在 \`scripts/validate-output.mjs\`）` → `- 契约校验命令相同（校验器位于插件根下的 \`scripts/validate-output.mjs\`；Codex 下 \`${CLAUDE_PLUGIN_ROOT}\` 是占位符，按 exploration-patterns「插件根解析」序列代入绝对路径）`
3. `skills/acceptance-qa/references/ai-acceptance.md:37`
   `→ node ${CLAUDE_PLUGIN_ROOT}/scripts/validate-output.mjs acceptance-check-items <file>` → `→ node "${CLAUDE_PLUGIN_ROOT}/scripts/validate-output.mjs" acceptance-check-items <file>`
4. `skills/requirement-analysis/references/codex-compat.md:61`：把 `\`python3 <插件根>/skills/anysearch/scripts/anysearch_cli.py search "查询词" --max_results 5\`` 替换为 `\`python3 "${CLAUDE_PLUGIN_ROOT}/skills/anysearch/scripts/anysearch_cli.py" search "查询词" --max_results 5\``；把 `；插件根定位不到时先找插件安装目录再以其为根解析路径）` 替换为 `；Codex 下 \`${CLAUDE_PLUGIN_ROOT}\` 是占位符，按 [exploration-patterns.md](exploration-patterns.md)「插件根解析」序列代入绝对路径）`
5. `scripts/schemas/README.md:8`
   `node ${CLAUDE_PLUGIN_ROOT}/scripts/validate-output.mjs <schema-name> <json-file>` → `node "${CLAUDE_PLUGIN_ROOT}/scripts/validate-output.mjs" <schema-name> <json-file>`
6. `scripts/schemas/README.md:16`
   原文：`> \`${CLAUDE_PLUGIN_ROOT}\` 指向插件安装根目录；该变量不可用时，先定位插件安装目录再以其为根解析路径（Codex 端同理，skill 加载时以 skill base directory 推导插件根）。`
   新文：`> \`${CLAUDE_PLUGIN_ROOT}\` 指向插件安装根目录（插件根）；写法约定与未替换时的解析序列见 \`skills/requirement-analysis/references/exploration-patterns.md\`「插件根解析」节（唯一定义点）。`
7. `agents/code-explorer.md:148`
   原文：`AnySearch（插件内嵌 skill 自带 CLI：\`python3 ${CLAUDE_PLUGIN_ROOT}/skills/anysearch/scripts/anysearch_cli.py search "查询词"\`，python 缺依赖换同目录 \`anysearch_cli.js\`），再降级 \`WebSearch\`。`
   新文：`AnySearch（插件内嵌 skill 自带 CLI：\`python3 "${CLAUDE_PLUGIN_ROOT}/skills/anysearch/scripts/anysearch_cli.py" search "查询词"\`，python 缺依赖换同目录 \`anysearch_cli.js\`；\`${CLAUDE_PLUGIN_ROOT}\` 未替换时按插件根解析序列推导（定义见 requirement-analysis 的 references/exploration-patterns.md）），再降级 \`WebSearch\`。`
8. `agents/external-resource-explorer.md:31`
   `CLI="${CLAUDE_PLUGIN_ROOT}/skills/anysearch/scripts/anysearch_cli.py"  # 变量不可用时,先定位插件安装目录再以其为根解析路径` → `CLI="${CLAUDE_PLUGIN_ROOT}/skills/anysearch/scripts/anysearch_cli.py"  # 未替换时按插件根解析序列推导（定义见 requirement-analysis 的 references/exploration-patterns.md）`
9. `commands/doctor.md:5`
   `运行 \`node ${CLAUDE_PLUGIN_ROOT}/scripts/doctor.mjs\` 并把输出原样呈现给用户，` → `运行 \`node "${CLAUDE_PLUGIN_ROOT}/scripts/doctor.mjs"\`（\`${CLAUDE_PLUGIN_ROOT}\` 未替换时按插件根解析序列推导——定义见 requirement-analysis 的 references/exploration-patterns.md）并把输出原样呈现给用户，`

**步骤 4：运行测试确认通过**

运行：`node --test scripts/tests/plugin-root.test.mjs && grep -rn '<插件根>\|先定位插件安装目录\|先找插件安装目录' skills agents commands scripts/schemas/README.md | grep -v skills/anysearch; echo "grep exit=$?"`
预期：plugin-root 9/9 PASS；grep 零命中（exit=1）。

**步骤 5：提交**

```bash
git add skills/executing-plans/references/review-orchestration.md skills/acceptance-qa/references/ai-acceptance.md skills/requirement-analysis/references/codex-compat.md scripts/schemas/README.md agents/code-explorer.md agents/external-resource-explorer.md commands/doctor.md scripts/tests/plugin-root.test.mjs .spec-dev/2026-09-03-02-portability-hygiene/plan/progress.yaml
git commit -m "fix(T05): references/agents/commands/schemas 插件根引用统一为双引号变量写法，去自造占位符与各处降级复述，agent/命令改固定回退句

Spec-Guard: off facet-coexist with major-upgrade/plan-single-format/supersede-lifecycle"
```
