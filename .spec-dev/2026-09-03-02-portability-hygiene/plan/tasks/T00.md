### 任务 0：建立隔离工作区

**文件**：
- 创建：`.worktrees/plan-2026-09-03-02-portability-hygiene/`（git worktree，不入库）
- 修改：`plan/progress.yaml`（`current` 置 T00、T00 置 completed 后随本任务提交）

**接口**：
- 消费：无
- 产出：worktree `.worktrees/plan-2026-09-03-02-portability-hygiene`，分支 `plan/2026-09-03-02-portability-hygiene`（T01-T10 全部在该 worktree 内执行）

**步骤 1：检测已有隔离**

运行：`git rev-parse --git-dir` 与 `git rev-parse --git-common-dir`
两者不同、且 `git rev-parse --show-superproject-working-tree` 无输出（排除 submodule）
→ 已在隔离工作区，跳过本任务。

**步骤 2：建立 worktree**

有原生 worktree 工具（如 EnterWorktree）或 using-git-worktrees skill 时优先使用（Codex 无原生 worktree 工具，直接走下面的手工路径）；否则手工降级：
确认 `.worktrees/` 已被忽略（`git check-ignore -q .worktrees`，本仓库 `.gitignore` 已含 `/.worktrees/`），然后

```bash
git worktree add .worktrees/plan-2026-09-03-02-portability-hygiene -b plan/2026-09-03-02-portability-hygiene
cd .worktrees/plan-2026-09-03-02-portability-hygiene
```

**步骤 3：安装依赖并验证基线**

本仓库无 package 依赖（纯 markdown + 零依赖 node 脚本），跳过安装。按计划头部「相关测试范围」运行基线验证：

```bash
node --test scripts/tests/*.test.mjs
bash scripts/tests/visual-path.test.sh
node scripts/validate-skills.mjs
node scripts/check-openai-sync.mjs
node scripts/check-plugin.mjs
```

预期：11 个测试文件 51 个 test 全 pass、visual-path `PASS`、三个校验各输出 passed 且退出码 0。基线失败 → 停下报告，先问再继续。

**步骤 4：登记状态并提交**

progress.yaml：`current` 置 `T00`，`T00` 置 `{ status: completed, commit: <本提交 sha>, tests: pass }`（sha 在提交后回填并 amend，或按仓库惯例记录短 sha）。

```bash
git add .spec-dev/2026-09-03-02-portability-hygiene/plan/progress.yaml
git commit -m "chore(T00): 隔离工作区建立（portability-hygiene，基线 51/51 + visual-path + 三校验绿）"
```
