### T08：全矩阵验收与独立对账

**文件**：
- 创建本特性 `acceptance/` 中的脚本、报告、原始日志与结构化判读；夹具工作区由 resources 预登记，不能写其他特性。
- 读取全部七票产物与 spec 的 S01—S33。

**接口**：
- 消费：T01—T07 产品、原始 base_commit、33 Scenario 与执行证据。
- 产出：逐 Scenario 的真实状态、自动回归、模型只读/可写/受控三类证据和独立审查/完整性对账。

本票由 executing-plans 收尾阶段调用 acceptance-qa，沿当前共享多维审查、S/critic 与模型边界，不在逐任务实施循环提前触发。只读验收脚本写入是计划明确的证据动作；若需修产品，回归所属实施票按TDD修复并复审，不用验收任务越界改功能。

**步骤 1：冻结来源并执行机器回归**

记录候选 HEAD、全部产品文件 SHA-256、实际运行命令与退出码；停止所有会写证据的进程后再跑含不可变工作区断言的测试。必须有非零测试数量、0 fail/0 skip，不能用静态检查或fixtures存在替代运行。

``````bash
rtk proxy node scripts/validate-skills.mjs
rtk proxy node scripts/check-plugin.mjs --codex-validate
rtk proxy node --test scripts/tests/plan-index.test.mjs scripts/tests/integration-plan.test.mjs scripts/tests/plan-state.test.mjs scripts/tests/plan-single-format.test.mjs scripts/tests/parallel-plan.test.mjs scripts/tests/parallel-integration.test.mjs
rtk git diff --check
``````

**步骤 2：建立有界模型验收输入与执行脚本**

先在 progress.resources 预登记 acceptance/action-fixtures、model-smoke、model-actions 与本票临时进程，随后执行下列完整脚本生成代码。它们不读取或回写全局设置，不固定模型别名，实际模型/权限从 init/result 核实。只读每例300秒，可写每段1800秒；当前调用超时只能记未通过，增加预算先按既有授权核对，不能无限重试。命令每30秒报告状态，用户可中断。



``````python
from pathlib import Path
files = {
'.spec-dev/2026-09-07-01-plan-decomposition/acceptance/run-decisions.py': r'''import hashlib,json,os,signal,subprocess,time
from pathlib import Path
root=Path.cwd().resolve()
feature=root/'.spec-dev/2026-09-07-01-plan-decomposition'
out=feature/'acceptance/model-smoke'
out.mkdir(parents=True,exist_ok=True)
run=os.environ.get('PD_QA_RUN','r1')
assert run.replace('-','').replace('_','').isalnum()
files=['skills/writing-plans/SKILL.md','skills/writing-plans/references/design-principles.md','skills/executing-plans/SKILL.md','skills/executing-plans/references/integration-groups.md','skills/executing-plans-parallel/SKILL.md','skills/test-driven-development/SKILL.md','skills/test-strategy/SKILL.md','skills/requirement-analysis/SKILL.md','skills/requirement-analysis/references/exploration-patterns.md','skills/requirement-analysis/assets/spec-template.md','skills/requirement-analysis/assets/roadmap-template.md']
# Each case only receives relevant rule families; independent expectations remain outside prompts.
family={
'planning':files[:2],
'execution':[files[i] for i in [2,3,4,5,6]],
'requirement':[files[i] for i in [0,1,7,8,9,10]],
}
fixture_root=feature/'acceptance/decision-fixtures'/run
fixture_root.mkdir(parents=True,exist_ok=False)
sources={
 'with-tests/format.mjs':'export const format = s => s.trim().toUpperCase();\n',
 'with-tests/format.test.mjs':'import {test} from "node:test"; import assert from "node:assert/strict"; import {format} from "./format.mjs"; test("public format trims and uppercases",()=>assert.equal(format(" hi "),"HI"));\n',
 'without-tests/format.mjs':'export const format = s => s.trim().toUpperCase();\n',
 'actors.mjs':'export async function submitByUser(id,approve,save){if(!await approve(id))return;return save(id);}\nexport async function retryInBackground(id,load,save){const saved=await load(id);return saved??save(id);}\n',
 'source-report.md':'# Acceptance at fixture checkpoint\nActual model execution: unverified; local structural checks only.\n',
 'capsule.md':'# Fixture capsule\nSummary says model PASS; source: source-report.md. Original requirement: preserve user and background retry behavior.\n'}
for rel,text in sources.items():
 p=fixture_root/rel;p.parent.mkdir(parents=True,exist_ok=True);p.write_text(text)
context={
 'S22':'请实际Read这两个夹具。宿主已穷举目录：with-tests含format.mjs和format.test.mjs；without-tests只含format.mjs、没有相邻测试。以真实读到的公共入口/测试为依据输出standard定向探索报告，不以目录缺测试创造私有seam。根目录：'+str(fixture_root),
 'S23':'实际参与者来源代码为 '+str(fixture_root/'actors.mjs')+'；请读取后输出对应spec片段及命名重试Scenario，不给后台交互授权能力。',
 'S33':'请读取 '+str(fixture_root/'capsule.md')+' 及它引用的同目录source-report.md，按实际来源输出修正胶囊与时点边界。'}
selected={}
for name in ['writing-plans','executing-plans','executing-plans-parallel','test-driven-development','test-strategy','requirement-analysis']:
    data=json.loads((root/'skills'/name/'evals/evals.json').read_text())
    for c in data['evals']:
        if c['id'].startswith('plan-decomposition-'):
            sid=c['id'].split('-')[-1].upper();selected.setdefault(sid,c)
# S03/S04 are deterministic CLI scenarios; they also get a bounded model interpretation input.
spec=(feature/'spec/plan-decomposition-design.md').read_text()
import re
for sid in [f'S{i:02}' for i in range(1,34)]:
    if sid not in selected:
        m=re.search(r'^#### Scenario: '+sid+r' ([^\n]+)\n(.*?)(?=\n#### Scenario:|\n### Requirement:|\n## |\Z)',spec,re.M|re.S)
        parts={k:re.search(r'- \*\*'+k+r'\*\* (.*)',m.group(2)).group(1) for k in ['GIVEN','WHEN','THEN']}
        selected[sid]={'id':'plan-decomposition-'+sid.lower(),'prompt':m.group(1)+'\nGIVEN '+parts['GIVEN']+'\nWHEN '+parts['WHEN'],'expected_output':parts['THEN']}
# Deterministic case selection is explicit; unknown case IDs fail rather than run zero tests.
only=os.environ.get('PD_QA_CASE')
assert only is None or only in selected,only
for sid,c in sorted(selected.items()):
    if only and sid!=only:continue
    n=int(sid[1:]);kind='planning' if n in [1,2,19,20,26,27,32] else 'requirement' if n in [21,22,23,24,25,33] else 'execution'
    dest=out/run/sid;dest.mkdir(parents=True,exist_ok=False)
    roles='\n\n'.join('# '+p+'\n'+(root/p).read_text() for p in family[kind])
    artifact=('计划类输入须直接输出可审阅的计划稿：任务导航、每票类型/文件/接口/命令/预期及依赖理由；不能仅回答将如何做。S27按GIVEN列出全部任务类型的完整示例。' if kind=='planning' else '需求类输入须直接输出对应spec片段、命名Scenario、约束归属/来源与2至3个检查点后的一个整体确认；仅在输入支持时包含拒绝项。' if kind=='requirement' else '')
    prompt=artifact+'本次是有界的只读规则决策验证。下面GIVEN是测试夹具事实，不声称已在真实用户项目执行。按候选规则返回可执行的下一步与状态，指出不能执行的条件；不写文件、不联网、不读取验收预期或其他案例。候选插件根：'+str(root)+'\n'+roles+'\n输入：\n'+c['prompt']+'\n'+context.get(sid,'')
    (dest/'input.txt').write_text(prompt)
    hashes={p:hashlib.sha256((root/p).read_bytes()).hexdigest() for p in family[kind]}
    cmd=['rtk','proxy','claude','-p','--plugin-dir',str(root),'--strict-mcp-config','--mcp-config','{"mcpServers":{}}','--tools','Read','--allowedTools','Read','--no-session-persistence','--verbose','--output-format','stream-json',prompt]
    start=time.monotonic();timed_out=False
    with (dest/'stdout.jsonl').open('w') as stdout,(dest/'stderr.log').open('w') as stderr:
        proc=subprocess.Popen(cmd,cwd=root,stdin=subprocess.DEVNULL,stdout=stdout,stderr=stderr,start_new_session=True)
        while proc.poll() is None:
            try:proc.wait(timeout=30)
            except subprocess.TimeoutExpired:print(sid,'running',round(time.monotonic()-start),flush=True)
            if time.monotonic()-start>=300 and proc.poll() is None:
                timed_out=True;os.killpg(proc.pid,signal.SIGTERM)
                try:proc.wait(timeout=10)
                except subprocess.TimeoutExpired:os.killpg(proc.pid,signal.SIGKILL);proc.wait()
                break
    facts={'case':sid,'command':cmd[:-1],'cwd':str(root),'exit':proc.returncode,'timed_out':timed_out,'elapsed':time.monotonic()-start,'candidate_hashes':hashes,'expected_output':c['expected_output'],'verdict':'needs_independent_review','mode':'real_model_readonly','model_config':'inherit; inspect actual init/result'}
    (dest/'facts.json').write_text(json.dumps(facts,ensure_ascii=False,indent=2)+'\n')
''',
'.spec-dev/2026-09-07-01-plan-decomposition/acceptance/create-action-fixture.py': r'''import hashlib,json,subprocess,os
from pathlib import Path
root=Path.cwd().resolve();feature=root/'.spec-dev/2026-09-07-01-plan-decomposition'
base=feature/'acceptance/action-fixtures';base.mkdir(parents=True,exist_ok=True)
run=os.environ.get('PD_ACTION_RUN','r1');assert run.replace('-','').replace('_','').isalnum()
outer=base/run;outer.mkdir(exist_ok=False)
def git(cwd,*args):return subprocess.check_output(['rtk','proxy','git','-C',str(cwd),*args],text=True).strip()
main=outer/'main';main.mkdir();git(main,'init','-q');git(main,'config','user.name','Fixture');git(main,'config','user.email','fixture@example.invalid');git(main,'config','core.hooksPath','/dev/null')
(main/'src').mkdir();(main/'test').mkdir()
(main/'src/core.mjs').write_text('export const trim = s => s.trim();\n')
(main/'src/a.mjs').write_text('import {trim} from "./core.mjs"; export const a = s => trim(s).toUpperCase();\n')
(main/'src/b.mjs').write_text('import {trim} from "./core.mjs"; export const b = s => trim(s).toLowerCase();\n')
(main/'test/public.test.mjs').write_text('import {test} from "node:test"; import assert from "node:assert/strict"; import {a} from "../src/a.mjs"; import {b} from "../src/b.mjs"; test("public behavior preserved",()=>{assert.equal(a("  hi  "),"HI");assert.equal(b("  HI  "),"hi");});\n')
git(main,'add','.');git(main,'commit','-qm','fixture baseline')
wt=outer/'work';git(main,'worktree','add','-qb','group-action',str(wt))
f=wt/'.spec-dev/migration';plan=f/'plan';(plan/'tasks').mkdir(parents=True)
decl={'protocol_version':1,'task_roles':{'T00':'isolation','T04':'delivery'},'groups':{'G01':{'members':['T01','T02'],'verify':'T03','reason':'renaming shared export breaks the second consumer until migrated'}}}
rows=[('T00',''),('T01','T00'),('T02','T01'),('T03','T01,T02'),('T04','T03')]
index='# migration plan\n\n| 任务 | 依赖 | 消费接口 | 产出接口 |\n|---|---|---|---|\n'+'\n'.join('| '+id+' | '+deps+' | public a/b | public a/b |' for id,deps in rows)+'\n\n```json spec-dev-integration\n'+json.dumps(decl,indent=2)+'\n```\n'
(plan/'index.md').write_text(index)
(f/'spec.md').write_text('# Approved fixture contract\nRename internal trim export to normalize across both consumers. Public a/b behavior is unchanged. Use G01; T01 changes core and a, T02 changes b; T03 runs the complete public test and confirms no trim import/export remains. User approved this exact plan and execution within this fixture only. No remote, deployment or external messages.\n')
contents={
'T00':'Isolation and baseline already established by the fixture host; do not claim you ran historical steps.',
'T01':'任务类型：integration-member\nFiles: src/core.mjs, src/a.mjs. Rename trim export/import/use to normalize. Do not change b yet. Run node --test test/public.test.mjs and preserve the expected unresolved trim import in b as temporary group failure. Save implementation, self-check and waiting evidence checkpoint. No public behavior change; group baseline protects it.',
'T02':'任务类型：integration-member\nFiles: src/b.mjs. Rename trim import/use to normalize. Run node --test test/public.test.mjs. Save implementation, self-check and waiting evidence checkpoint; do not complete the group.',
'T03':'任务类型：group-verification\nNo business edits. Run node --test test/public.test.mjs and verify src contains no trim export/import; complete all members and verifier only after both checks pass and evidence is persisted.',
'T04':'Delivery is outside this fixture action authorization; stop before this task.'}
for id,body in contents.items():(plan/'tasks'/f'{id}.md').write_text('# '+id+'\n'+body+'\n')
git(wt,'add','.');git(wt,'commit','-qm','fixture contract')
b=git(wt,'rev-parse','HEAD')
state={'format_version':2,'current':None,'tasks':{id:{'status':'pending'} for id,_ in rows},'resources':[],'notes':['Fixture host built isolation and baseline; model must not claim host actions.'],'integration':{'owner':'fixture-host','worktree':str(wt.resolve()),'branch':'group-action','base_commit':b,'validated_commit':b,'active_group':None,'groups':{'G01':{'status':'pending','base_commit':None,'checkpoint_commit':None,'validated_commit':None,'evidence_paths':[]}}}}
state['tasks']['T00']={'status':'completed','commit':b,'tests':'pass'}
(plan/'progress.yaml').write_text(json.dumps(state,indent=2)+'\n');git(wt,'add','.');git(wt,'commit','-qm','fixture state')
r=subprocess.run(['rtk','proxy','node','--test','test/public.test.mjs'],cwd=wt,capture_output=True,text=True)
(outer/'baseline.stdout').write_text(r.stdout);(outer/'baseline.stderr').write_text(r.stderr);assert r.returncode==0
manifest={'worktree':str(wt.resolve()),'plan':str(plan.resolve()),'feature':str(f.resolve()),'baseline':b,'fixture_host_complete':True,'lock_held':False,'candidate':str(root),'baseline_exit':r.returncode}
(outer/'manifest.json').write_text(json.dumps(manifest,indent=2)+'\n')
print(outer/'manifest.json')
''',
'.spec-dev/2026-09-07-01-plan-decomposition/acceptance/run-action.py': r'''import hashlib,json,os,signal,subprocess,time
from pathlib import Path
root=Path.cwd().resolve();f=root/'.spec-dev/2026-09-07-01-plan-decomposition'
run=os.environ.get('PD_ACTION_RUN','r1');step=os.environ.get('PD_ACTION_STEP','first')
assert step in ['plan','first','resume','verify','repair']
outer=f/'acceptance/action-fixtures'/run;manifest=json.loads((outer/'manifest.json').read_text());wt=Path(manifest['worktree'])
dest=f/'acceptance/model-actions'/run/step;dest.mkdir(parents=True,exist_ok=False)
files=['skills/executing-plans/SKILL.md','skills/executing-plans/references/integration-groups.md','skills/writing-plans/SKILL.md','skills/test-driven-development/SKILL.md']
roles='\n\n'.join('# '+p+'\n'+(root/p).read_text() for p in files)
request={'plan':'只编写计划，不实施。根据本夹具 .spec-dev/migration/spec.md 和真实 src/test，从零生成 .spec-dev/generated/plan/index.md、tasks及progress.yaml。不要读取已有migration/plan，避免复制宿主答案。完整代码、各票验证及组协议必须可执行，保存后运行候选plan-index并报告结果；不要替用户宣称执行通过。', 'first':'实际取得本夹具特性锁，激活G01，完成T01到awaiting_verification并保存检查点后停止；不要执行T02，不要宣称组完成。主动暂停时按协议保存并释放自己的锁。', 'resume':'上次会话已结束，实际恢复同一G01，只完成T02到awaiting_verification并保存检查点后停止；不得重做T01，不能把两票直接标completed。主动暂停保存并释放自己的锁。','verify':'恢复原G01，实际完成T03组验证和原子完成检查点，然后停止；不执行T04，不合并或清理夹具。', 'repair':'夹具宿主在两成员待验后故意注入src/core.mjs的错误并实际运行T03得到失败，原件见execution/groups/G01/T03/host-failure.json；这不是模型自己造成或测试的。用户授权按原公共a/b合同修复，T01负责core，T02消费T01。按候选协议先标T01修复中及依赖闭包T02 blocked，保存检查点；只修复core错误，保留历史证据，核验T02既有改名不重复应用并补局部检查，逐票恢复待验；再实际运行全部组验证及原子完成。保持T04未执行，不改测试预期或公共行为。'}[step]
prompt='本次用户授权仅在给定独立夹具内执行以下范围。候选插件目录只读，绝不修改宿主源代码、其他夹具、真实用户数据；不联网、不推送、不部署、不发外部消息。所有shell命令用rtk前缀。候选插件根：'+str(root)+'\n'+roles+'\n输入事实：'+json.dumps(manifest)+'\n任务：'+request+'\n使用候选绝对路径scripts/validate-output.mjs验证夹具；权限或能力不足如实阻塞，不换规则造通过。'
(dest/'input.txt').write_text(prompt)
cmd=['rtk','proxy','claude','-p','--plugin-dir',str(root),'--add-dir',str(root/'skills'),str(root/'scripts'),'--strict-mcp-config','--mcp-config','{"mcpServers":{}}','--tools','Read,Glob,Grep,Bash,Edit,Write','--allowedTools','Read','Glob','Grep',f'Edit(//{str(wt).lstrip("/")}/**)',f'Write(//{str(wt).lstrip("/")}/**)',f'Bash(rtk proxy git *)',f'Bash(rtk git *)',f'Bash(rtk proxy node *)',f'Bash(rtk proxy python3 *)','--no-session-persistence','--verbose','--output-format','stream-json',prompt]
# Tool allowance is not a security sandbox: scope is bounded by task instructions and post-run audit.
tracked=subprocess.check_output(['rtk','proxy','git','ls-files','-z'],cwd=root).decode().split('\0')
product=[p for p in tracked if p and not p.startswith('.spec-dev/') and (root/p).is_file()]
before={p:hashlib.sha256((root/p).read_bytes()).hexdigest() for p in product};start=time.monotonic();timed_out=False
with (dest/'stdout.jsonl').open('w') as stdout,(dest/'stderr.log').open('w') as stderr:
 proc=subprocess.Popen(cmd,cwd=wt,stdin=subprocess.DEVNULL,stdout=stdout,stderr=stderr,start_new_session=True)
 while proc.poll() is None:
  try:proc.wait(timeout=30)
  except subprocess.TimeoutExpired:print(step,'running',round(time.monotonic()-start),flush=True)
  if time.monotonic()-start>=1800 and proc.poll() is None:
   timed_out=True;os.killpg(proc.pid,signal.SIGTERM)
   try:proc.wait(timeout=10)
   except subprocess.TimeoutExpired:os.killpg(proc.pid,signal.SIGKILL);proc.wait()
   break
state=json.loads((wt/'.spec-dev/migration/plan/progress.yaml').read_text())
after={p:hashlib.sha256((root/p).read_bytes()).hexdigest() for p in product if (root/p).is_file()}
(dest/'facts.json').write_text(json.dumps({'command':cmd[:-1],'exit':proc.returncode,'timed_out':timed_out,'state':state,'candidate_before':before,'candidate_after':after,'elapsed':time.monotonic()-start,'generated_plan':str(wt/'.spec-dev/generated/plan') if step=='plan' else None,'mode':'real_model_write_action','verdict':'needs_independent_review'},ensure_ascii=False,indent=2)+'\n')
assert before==after,'candidate source modified by model'
''',
'.spec-dev/2026-09-07-01-plan-decomposition/acceptance/check-lock.py': r'''import hashlib,json,multiprocessing,os,subprocess,time
from pathlib import Path
root=Path.cwd().resolve();f=root/'.spec-dev/2026-09-07-01-plan-decomposition';run=os.environ.get('PD_ACTION_RUN','r1')
manifest=json.loads((f/'acceptance/action-fixtures'/run/'manifest.json').read_text());wt=Path(manifest['worktree']);plan=Path(manifest['plan'])
common=Path(subprocess.check_output(['rtk','proxy','git','-C',str(wt),'rev-parse','--git-common-dir'],text=True).strip())
if not common.is_absolute():common=(wt/common).resolve()
key=hashlib.sha256(b'.spec-dev/migration').hexdigest();lock=common/'spec-dev-locks'/key

def contender(lockname,label,ready,release,queue):
 p=Path(lockname)
 try:p.mkdir(parents=False)
 except FileExistsError:queue.put(label+':blocked');return
 (p/'owner.json').write_text(json.dumps({'owner':label,'feature_key':'.spec-dev/migration'}));queue.put(label+':acquired');ready.set()
 release.wait(20)
 assert json.loads((p/'owner.json').read_text())['owner']==label
 (p/'owner.json').unlink();p.rmdir();queue.put(label+':released')

if __name__=='__main__':
 assert not lock.exists(),'do not steal a lock from a model run'
 lock.parent.mkdir(exist_ok=True)
 before=(plan/'progress.yaml').read_bytes();ctx=multiprocessing.get_context('spawn');q=ctx.Queue();ready=ctx.Event();release=ctx.Event()
 a=ctx.Process(target=contender,args=(str(lock),'A',ready,release,q));a.start();assert ready.wait(10)
 b=ctx.Process(target=contender,args=(str(lock),'B',ctx.Event(),release,q));b.start();b.join(10);assert b.exitcode==0
 first=[q.get(timeout=10),q.get(timeout=10)];assert set(first)=={'A:acquired','B:blocked'}
 assert (plan/'progress.yaml').read_bytes()==before
 release.set();a.join(10);assert a.exitcode==0;events=first+[q.get(timeout=10)]
 assert not lock.exists()
 out=f/'acceptance/lock-replay.json';assert not out.exists();out.write_text(json.dumps({'events':events,'progress_unchanged':True,'kind':'controlled_real_process_lock_mechanism','limitation':'not proof of autonomous model locking','common_dir':str(common)},indent=2)+'\n')
''',
'.spec-dev/2026-09-07-01-plan-decomposition/acceptance/inject-failure.py': r'''from pathlib import Path
import hashlib,json,subprocess,os
root=Path.cwd().resolve();f=root/'.spec-dev/2026-09-07-01-plan-decomposition'
outer=f/'acceptance/action-fixtures/repair';manifest=json.loads((outer/'manifest.json').read_text());wt=Path(manifest['worktree'])
plan=wt/'.spec-dev/migration/plan';state=json.loads((plan/'progress.yaml').read_text())
assert state['integration']['active_group']=='G01'
assert all(state['tasks'][id]['status']=='awaiting_verification' for id in ['T01','T02'])
def git(*args):return subprocess.check_output(['rtk','proxy','git','-C',str(wt),*args],text=True).strip()
assert not git('status','--porcelain'),'preserve dirty state; do not inject'
common=Path(git('rev-parse','--git-common-dir'))
if not common.is_absolute():common=(wt/common).resolve()
lock=common/'spec-dev-locks'/hashlib.sha256(b'.spec-dev/migration').hexdigest()
lock.parent.mkdir(exist_ok=True);lock.mkdir() # exclusive; existing owner is never stolen
owner='fixture-failure-host';(lock/'owner.json').write_text(json.dumps({'owner':owner,'pid':os.getpid(),'feature_key':'.spec-dev/migration'}))
try:
 core=wt/'src/core.mjs';old=core.read_text();assert 'normalize' in old and '.trim()' in old
 core.write_text('export const normalize = s => s.trim()+"!";\n')
 git('add','src/core.mjs');git('commit','-qm','fixture host injected contract failure')
 commit=git('rev-parse','HEAD');prefix='execution/groups/G01/T03/'
 out=wt/'.spec-dev/migration'/prefix;out.mkdir(parents=True,exist_ok=True)
 cmd=['rtk','proxy','node','--test','test/public.test.mjs']
 r=subprocess.run(cmd,cwd=wt,capture_output=True);assert r.returncode!=0,'fault did not fail'
 (out/'host.stdout').write_bytes(r.stdout);(out/'host.stderr').write_bytes(r.stderr)
 listing=subprocess.check_output(['rtk','proxy','git','-C',str(wt),'ls-tree','-r','-z',commit]).decode()
 entries=[line for line in listing.split('\0') if line and line.split('\t',1)[1]!='.spec-dev/migration/plan/progress.yaml' and not line.split('\t',1)[1].startswith('.spec-dev/migration/execution/')]
 record={'command':cmd,'cwd':str(wt.resolve()),'exit_code':r.returncode,'commit':commit,'tree':hashlib.sha256('\0'.join(entries).encode()).hexdigest(),'stdout':prefix+'host.stdout','stderr':prefix+'host.stderr','stdout_sha256':hashlib.sha256(r.stdout).hexdigest(),'stderr_sha256':hashlib.sha256(r.stderr).hexdigest()}
 (out/'host-failure.json').write_text(json.dumps(record,indent=2)+'\n')
 state['tasks']['T03']={'status':'blocked','tests':'fail','evidence_paths':[prefix+'host-failure.json']}
 state['integration']['groups']['G01'].update(status='blocked',checkpoint_commit=commit)
 state['integration']['owner']=owner;state['current']=None
 state['notes'].append('Fixture host injected known T01 interface fault after waiting checkpoints; logs are host evidence. Baseline unchanged. Repair model must preserve original member history and validate dependency closure.')
 tmp=plan/'progress.yaml.tmp';tmp.write_text(json.dumps(state,indent=2)+'\n');os.replace(tmp,plan/'progress.yaml')
 git('add','.spec-dev/migration');git('commit','-qm','fixture failure evidence checkpoint')
finally:
 assert json.loads((lock/'owner.json').read_text())['owner']==owner
 (lock/'owner.json').unlink();lock.rmdir()
print('controlled fault saved; model repair not yet run')
'''
}
for name,text in files.items():
 p=Path(name);assert not p.exists(),name;p.parent.mkdir(parents=True,exist_ok=True);p.write_text(text)
``````

**步骤 3：运行真实模型只读决定与三段可写恢复**

只读33例覆盖五组核心主题，输入为GIVEN/WHEN、候选规则及所需真实文件夹具，THEN留在facts供独立判读；不把受控输入事实说成真实项目操作。每次新run目录，保留失败、超时和模型不一致。随后三段可写任务在同一真实Git夹具分会话执行；每段退出后先独立检查状态、实际diff与日志，再允许下一段，不能只看CLI退出0。first预期T01待验、resume预期T01/T02待验且未重复改T01、verify预期全组completed且T04未执行。

``````bash
rtk proxy python3 .spec-dev/2026-09-07-01-plan-decomposition/acceptance/run-decisions.py
rtk proxy python3 .spec-dev/2026-09-07-01-plan-decomposition/acceptance/create-action-fixture.py
rtk proxy env PD_ACTION_STEP=first python3 .spec-dev/2026-09-07-01-plan-decomposition/acceptance/run-action.py
rtk proxy env PD_ACTION_STEP=resume python3 .spec-dev/2026-09-07-01-plan-decomposition/acceptance/run-action.py
rtk proxy env PD_ACTION_STEP=verify python3 .spec-dev/2026-09-07-01-plan-decomposition/acceptance/run-action.py
``````

命令按步骤顺序单独执行，中间执行上述状态审阅门；不要把五行当作无条件连续shell脚本。组共享common-dir锁和其owner事件、真实测试命令/日志、代码/状态各提交都须在trace和Git中可查。工具白名单不是沙箱，核查模型没有写候选源或外部目录；越界记录失败并停止，不修改原日志来隐藏。

**步骤 4：补齐受控故障与产物检查，独立判读**

真实模型只读决定不能替代下面的真实状态检查。运行 T03 的 CLI/Git用例保留输出，再用三段可写夹具的原始证据逐项核对：

| Scenario / 检查项 | 维度 | Lane | 预期/证据 |
|---|---|---|---|
| S01/S02/S19/S20/S26/S27/S32 | 计划产物 | PR | 模型决定之外，审阅只读模型实际输出的完整计划稿，分别覆盖expand/group/前置重构/混合类型；另用可写生成组计划通过plan-index，不能只口述会做 |
| S03/S04/S17/S18/S29 | CLI结构/拒绝 | PR | T01/T02实际Node用例与exit，旧路径不回归，未知能力不执行 |
| S05/S07/S08/S11 | 状态推进 | PR | 三段真实可写trace、progress/Git、plan-state输出；组外不解锁、统一完成 |
| S06/S12/S13 | 失败修复 | PR | T03失败夹具CLI证据 + 真实模型处理决定；必需证据包括真实故障日志与下述修复会话；只读回答不能替代依赖闭包复核/重验动作 |
| S09/S10 | 排他/投影 | PR | 实际不同进程争同一common-dir锁的受控演练；核验失败方未写progress，配合模型只读决定，分开记账 |
| S14/S15/S16/S28 | 中断/恢复 | PR | T03真实Git中断窗口 + 三段新会话恢复；只运行正常暂停不声称证明强制kill的全部窗口 |
| S21/S22/S23/S24/S25/S33 | 需求/交互 | PR | 模型决定及实际spec/plan产物，actor、constraint、拒绝解读、gist来源可追溯 |
| S30/S31 | TDD边界 | PR | T06红绿探针 + 模型只读/可写日志，纯重构保护与行为红分开 |
| 所有33Scenario及元数据 | 静态一致 | PR | 逐项eval覆盖/引用/covers/openai/README检查，标静态 |
| 随机中断/竞争及核心模型3trial | 完整eval | nightly | 非阻塞；未执行标not_run，不降格伪装PR通过 |


实际计划产物使用另一个独立夹具，避免生成新计划改变三段恢复夹具的业务树：

```bash
rtk proxy env PD_ACTION_RUN=planning python3 .spec-dev/2026-09-07-01-plan-decomposition/acceptance/create-action-fixture.py
rtk proxy env PD_ACTION_RUN=planning PD_ACTION_STEP=plan python3 .spec-dev/2026-09-07-01-plan-decomposition/acceptance/run-action.py
rtk proxy node scripts/validate-output.mjs plan-index .spec-dev/2026-09-07-01-plan-decomposition/acceptance/action-fixtures/planning/work/.spec-dev/generated/plan
rtk proxy python3 .spec-dev/2026-09-07-01-plan-decomposition/acceptance/check-lock.py
```

真实故障修复使用第三个独立夹具 `repair`。先分别运行 first/resume 并逐段核验待验状态，再由宿主注入可归属的 T01 公共行为错误、实际保存 T03 失败；最后交模型按依赖闭包恢复。注入不是产品操作，也不记作模型动作；修复必须从真实trace核验，不用预填通过。

```bash
rtk proxy env PD_ACTION_RUN=repair python3 .spec-dev/2026-09-07-01-plan-decomposition/acceptance/create-action-fixture.py
rtk proxy env PD_ACTION_RUN=repair PD_ACTION_STEP=first python3 .spec-dev/2026-09-07-01-plan-decomposition/acceptance/run-action.py
rtk proxy env PD_ACTION_RUN=repair PD_ACTION_STEP=resume python3 .spec-dev/2026-09-07-01-plan-decomposition/acceptance/run-action.py
rtk proxy python3 .spec-dev/2026-09-07-01-plan-decomposition/acceptance/inject-failure.py
rtk proxy env PD_ACTION_RUN=repair PD_ACTION_STEP=repair python3 .spec-dev/2026-09-07-01-plan-decomposition/acceptance/run-action.py
```

每段间独立核验；S06/S12 的失败不得移动基线，S13 必须见到修复中/依赖blocked检查点、旧T02操作复用、局部补验与全部组验证，不满足则T08不完成。宿主注入前后与修复后的code/state SHA、日志都保留。

计划模型不得读取宿主现成 migration/plan；从原始 trace 核查实际读取边界，发生答案污染则该生成证据无效、用新夹具重跑。S22/S23/S33 必须从trace核验实际读取所附文件并引用真实源；缺少实际读取不能只凭模型自述通过。把只读 S21—S25/S33 的结构化设计输出连同原件作为需求产物审阅，不将其说成已实施功能。锁脚本是两个真实进程竞争同一 common-dir 锁的受控机制演练；模型自己是否按协议持锁，仍由三段动作 trace 独立核对，二者不能互相冒充。

未完成必需矩阵项不能完成T08。使用只读独立审查者逐Scenario填写verdict/reason/evidence，重核引用的真实原件；模型自报告或token/退出码不足以证明动作完成。缺工具、超时、零测试分别记录BLOCKED/not_run，不改阈值制造绿。已有受控review-runner只用于代码审查，不能宣称其机器保证覆盖本次原生模型调用。

**步骤 5：保存验收与审查对账**

产出 acceptance-report.md、scenario-results.json、requirements-reconciliation.md，区分机器/真实模型决定/真实可写/受控窗口/nightly。所有必需项完成、收尾多维审查及复核无未处置高/中问题后，保存T08状态检查点。未实现或未验证行为标明原因，不提前写DELIVERED。按index记录本票提交与完整证据，不push。
