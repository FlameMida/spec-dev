### T05：生成类型化任务和集成组计划

**文件**：
- `skills/writing-plans/SKILL.md`
- `skills/writing-plans/agents/openai.yaml`
- `skills/writing-plans/evals/evals.json`
- `skills/writing-plans/references/design-principles.md`
- `README.md`
- `README.zh-CN.md`
- `scripts/tests/plugin-root.test.mjs`

**接口**：
- 消费：T04 集成组协议；T01-T03 public校验；现有三件套/四列导航
- 产出：writing-plans 组声明/v2字段单点、T01必要重构、第五查和关联skill
- Scenario：S01、S02、S19、S20、S21、S26、S27、S32。公共测试落点沿 index 与 spec，不新增私有测试接口。

**步骤 1：写失败测试**

``````python
from pathlib import Path
changes = [
('skills/writing-plans/evals/evals.json', r'''{
  "skill_name": "writing-plans",
  "evals": [
    {
      "id": "wp-bite-sized-tdd",
      "prompt": "基于已批准的 spec（.spec-dev/2026-07-03-export-report/spec/export-report-design.md，CLI 导出报告子命令）编写实施计划",
      "expected_output": "计划保存至 .spec-dev/2026-07-03-export-report/plan/（index.md + tasks/ + progress.yaml 分文件三件套，与 spec 同一特性目录）；index.md 含头部（执行方式指向 executing-plans、目标、spec 路径、全局约束）；固定生成任务 0（建立隔离工作区：已隔离检测→worktree 建立→基线验证，含 git 降级命令）；先给文件结构映射；每任务含精确文件路径、接口消费/产出块、TDD 五步（失败测试代码→确认失败命令与预期输出→最小实现代码→确认通过→提交命令）；失败测试由 spec 的 Scenario 直译（GIVEN→arrange、WHEN→act、THEN→assert，测试名沿用 Scenario 名）；任务文件步骤用「**步骤 N:**」标题式（progress.yaml 唯一状态源）；以最终任务（合并与清理，台账在 progress.yaml resources）收尾；无任何 TBD/占位符"
    },
    {
      "id": "wp-self-review-catches-gap",
      "prompt": "（计划写完后）spec 里要求导出支持 CSV 和 JSON 两种格式，计划里只有 CSV 任务",
      "expected_output": "Self-Review 第 1 查（spec 覆盖）发现缺口，补 JSON 格式任务后才向用户交接；不带着缺口交接"
    },
    {
      "id": "wp-final-task-closure",
      "prompt": "（Self-Review 中）计划以任务 6「接入 CLI 入口」结束，没有收尾任务",
      "expected_output": "按固定模板补任务 7（合并与清理：全量验证→合并回来源分支→清理 worktree 与分支），与任务 0 首尾对称——脱离插件执行时 worktree 生命周期也能在计划内闭合"
    },
    {
      "id": "wp-confirm-before-writing",
      "prompt": "（隐式触发：用户消息只包含 .spec-dev/2026-07-02-notify/spec/notify-design.md 这个路径，没有其他说明）",
      "expected_output": "不默认开写——先确认「基于该 spec 开始编写实施计划？」，用户同意后才动笔；若本轮是显式指示（斜杠命令或明确说编写计划）或由 requirement-analysis 阶段 8 前置确认交接而来，则视为已同意、不重复问"
    },
    {
      "id": "wp-handoff-not-execute",
      "prompt": "计划已保存，用户还没回复",
      "expected_output": "向用户交接（说明将先 using-git-worktrees 再 executing-plans），等待用户确认；不在本 skill 内直接开始写实现代码"
    },
    {
      "id": "wp-parallel-declaration",
      "prompt": "已批准 spec；T01/T02 只依赖 T00，分别修改 src/a.mjs 与 src/b.mjs，测试资源隔离。编写计划。",
      "expected_output": "保持三件套与四列导航；index 生成一个 yaml spec-dev-parallel 块，精确声明两个 writes，resources 使用 JSON 字符串数组；校验通过，声明不等于已选择并发。"
    },
    {
      "id": "wp-parallel-exceptions",
      "prompt": "T01 已获 TDD 例外；T02 的相关基线范围显式为空；T03/T04 有完整测试且写集合独立。",
      "expected_output": "T01/T02 不进入声明，主线程串行；T03/T04 可声明；空基线不自动免除票内 TDD；progress 仍 format_version 1，默认不激活 execution。"
    },
    {
      "id": "wp-parallel-state",
      "prompt": "为已明确选择并发的分文件计划说明恢复字段与资源台账。",
      "expected_output": "writing-plans 为唯一字段定义；execution.owner 与 claim.owner 含义不同；model-declaration 只在 notes，claim.model_declaration_id 追溯；模型声明内容由并发 skill 定义；资源先登记再确认创建，进度原子更新。"
    },
    {
      "id": "tdd-seam-s02",
      "prompt": "S02 并发消费者无需读取前票正文：\nGIVEN T02 依赖 T01 的公共接口，两个任务共享一个已批准 seam\nWHEN 只向 T02 提供本票、spec、index 与依赖接口行",
      "expected_output": "输入可定位 seam、接口签名与对应 Scenario；无需读取 T01 正文；导航仍为四列，无新进度字段或清单文件。"
    },
    {
      "id": "tdd-seam-s11",
      "prompt": "S11 有 typecheck 命令：\nGIVEN 项目配置已有 typecheck，计划同时声明目标行为测试和最终全量命令\nWHEN 完成一批接口编辑及本票实现",
      "expected_output": "可先获得 typecheck 反馈，仍运行本票红绿与相关验证；最终全量保留，静态通过不解锁尚未完成集成验证的后继。"
    },
    {
      "id": "tdd-seam-s12",
      "prompt": "S12 无适用命令：\nGIVEN 项目没有 typecheck 或其他适用静态命令\nWHEN 编写与执行计划",
      "expected_output": "标注静态快检不适用并沿用测试命令，不虚构 `tsc`、不安装额外依赖；不适用不等于任务测试通过。"
    },
    {
      "id": "review-conformance-s08",
      "prompt": "S08 模块删除判据比较两个模块\nGIVEN 一个模块只转发调用，另一个集中处理多个调用方共同的业务规则\nWHEN 使用模块删除判据",
      "expected_output": "比较假想移除后的复杂性去向及实际用途；不实际删除模块或保护测试，不仅凭转发形态要求整改。"
    },
    {
      "id": "review-conformance-s09",
      "prompt": "S09 一个 adapter 与已批准 seam\nGIVEN 计划已批准一个公共 seam，当前只有一个生产 adapter\nWHEN 使用 adapter 计数评价抽象",
      "expected_output": "不为凑两个实现创建 fake、不据此取消已批准 seam；只有真实需求和具体复杂性证据才能支持改进建议。"
    },
    {
      "id": "review-conformance-s10",
      "prompt": "S10 多 adapter 仍暴露内部结构\nGIVEN 两个真实 adapter 的接口向消费者暴露各自内部参数，调用方需做分支判断\nWHEN 使用接口三问",
      "expected_output": "检查是否能减少方法、简化参数、隐藏内部复杂性，并引用实际边界；adapter 数量不自动证明抽象合理。"
    }
  ]
}
''', r'''{
  "skill_name": "writing-plans",
  "evals": [
    {
      "id": "wp-bite-sized-tdd",
      "prompt": "基于已批准的 spec（.spec-dev/2026-07-03-export-report/spec/export-report-design.md，CLI 导出报告子命令）编写实施计划",
      "expected_output": "计划保存至 .spec-dev/2026-07-03-export-report/plan/（index.md + tasks/ + progress.yaml 分文件三件套，与 spec 同一特性目录）；index.md 含头部（执行方式指向 executing-plans、目标、spec 路径、全局约束）；固定生成任务 0（建立隔离工作区：已隔离检测→worktree 建立→基线验证，含 git 降级命令）；先给文件结构映射；每任务含精确文件路径、接口消费/产出块、TDD 五步（失败测试代码→确认失败命令与预期输出→最小实现代码→确认通过→提交命令）；失败测试由 spec 的 Scenario 直译（GIVEN→arrange、WHEN→act、THEN→assert，测试名沿用 Scenario 名）；任务文件步骤用「**步骤 N:**」标题式（progress.yaml 唯一状态源）；以最终任务（合并与清理，台账在 progress.yaml resources）收尾；无任何 TBD/占位符"
    },
    {
      "id": "wp-self-review-catches-gap",
      "prompt": "（计划写完后）spec 里要求导出支持 CSV 和 JSON 两种格式，计划里只有 CSV 任务",
      "expected_output": "Self-Review 第 1 查（spec 覆盖）发现缺口，补 JSON 格式任务后才向用户交接；不带着缺口交接"
    },
    {
      "id": "wp-final-task-closure",
      "prompt": "（Self-Review 中）计划以任务 6「接入 CLI 入口」结束，没有收尾任务",
      "expected_output": "按固定模板补任务 7（合并与清理：全量验证→合并回来源分支→清理 worktree 与分支），与任务 0 首尾对称——脱离插件执行时 worktree 生命周期也能在计划内闭合"
    },
    {
      "id": "wp-confirm-before-writing",
      "prompt": "（隐式触发：用户消息只包含 .spec-dev/2026-07-02-notify/spec/notify-design.md 这个路径，没有其他说明）",
      "expected_output": "不默认开写——先确认「基于该 spec 开始编写实施计划？」，用户同意后才动笔；若本轮是显式指示（斜杠命令或明确说编写计划）或由 requirement-analysis 阶段 8 前置确认交接而来，则视为已同意、不重复问"
    },
    {
      "id": "wp-handoff-not-execute",
      "prompt": "计划已保存，用户还没回复",
      "expected_output": "向用户交接（说明将先 using-git-worktrees 再 executing-plans），等待用户确认；不在本 skill 内直接开始写实现代码"
    },
    {
      "id": "wp-parallel-declaration",
      "prompt": "已批准 spec；T01/T02 只依赖 T00，分别修改 src/a.mjs 与 src/b.mjs，测试资源隔离。编写计划。",
      "expected_output": "保持三件套与四列导航；index 生成一个 yaml spec-dev-parallel 块，精确声明两个 writes，resources 使用 JSON 字符串数组；校验通过，声明不等于已选择并发。"
    },
    {
      "id": "wp-parallel-exceptions",
      "prompt": "T01 已获 TDD 例外；T02 的相关基线范围显式为空；T03/T04 有完整测试且写集合独立。",
      "expected_output": "T01/T02 不进入声明，主线程串行；T03/T04 可声明；空基线不自动免除票内 TDD；progress 仍 format_version 1，默认不激活 execution。"
    },
    {
      "id": "wp-parallel-state",
      "prompt": "为已明确选择并发的分文件计划说明恢复字段与资源台账。",
      "expected_output": "writing-plans 为唯一字段定义；execution.owner 与 claim.owner 含义不同；model-declaration 只在 notes，claim.model_declaration_id 追溯；模型声明内容由并发 skill 定义；资源先登记再确认创建，进度原子更新。"
    },
    {
      "id": "tdd-seam-s02",
      "prompt": "S02 并发消费者无需读取前票正文：\nGIVEN T02 依赖 T01 的公共接口，两个任务共享一个已批准 seam\nWHEN 只向 T02 提供本票、spec、index 与依赖接口行",
      "expected_output": "输入可定位 seam、接口签名与对应 Scenario；无需读取 T01 正文；导航仍为四列，无新进度字段或清单文件。"
    },
    {
      "id": "tdd-seam-s11",
      "prompt": "S11 有 typecheck 命令：\nGIVEN 项目配置已有 typecheck，计划同时声明目标行为测试和最终全量命令\nWHEN 完成一批接口编辑及本票实现",
      "expected_output": "可先获得 typecheck 反馈，仍运行本票红绿与相关验证；最终全量保留，静态通过不解锁尚未完成集成验证的后继。"
    },
    {
      "id": "tdd-seam-s12",
      "prompt": "S12 无适用命令：\nGIVEN 项目没有 typecheck 或其他适用静态命令\nWHEN 编写与执行计划",
      "expected_output": "标注静态快检不适用并沿用测试命令，不虚构 `tsc`、不安装额外依赖；不适用不等于任务测试通过。"
    },
    {
      "id": "review-conformance-s08",
      "prompt": "S08 模块删除判据比较两个模块\nGIVEN 一个模块只转发调用，另一个集中处理多个调用方共同的业务规则\nWHEN 使用模块删除判据",
      "expected_output": "比较假想移除后的复杂性去向及实际用途；不实际删除模块或保护测试，不仅凭转发形态要求整改。"
    },
    {
      "id": "review-conformance-s09",
      "prompt": "S09 一个 adapter 与已批准 seam\nGIVEN 计划已批准一个公共 seam，当前只有一个生产 adapter\nWHEN 使用 adapter 计数评价抽象",
      "expected_output": "不为凑两个实现创建 fake、不据此取消已批准 seam；只有真实需求和具体复杂性证据才能支持改进建议。"
    },
    {
      "id": "review-conformance-s10",
      "prompt": "S10 多 adapter 仍暴露内部结构\nGIVEN 两个真实 adapter 的接口向消费者暴露各自内部参数，调用方需做分支判断\nWHEN 使用接口三问",
      "expected_output": "检查是否能减少方法、简化参数、隐藏内部复杂性，并引用实际边界；adapter 数量不自动证明抽象合理。"
    },
    {
      "id": "plan-decomposition-s01",
      "prompt": "能逐票绿时不使用集成组\nGIVEN 三个调用包可在旧形保留时分别迁移并通过相关测试。\nWHEN 分解机械改名。",
      "expected_output": "生成 expand、具有真实依赖的迁移票及依赖全部迁移票的 contract；不因文件多就延期整体绿。"
    },
    {
      "id": "plan-decomposition-s02",
      "prompt": "批次不能独立绿时保留小票\nGIVEN 共享类型改动导致多个包必须全部迁移后才能通过相关验证，批准设计记录该事实。\nWHEN 编写计划。",
      "expected_output": "各批次各有 TNN 文件和提交，归入明确组及组验证任务，不合并为一张超大票，不承诺中间绿。"
    },
    {
      "id": "plan-decomposition-s19",
      "prompt": "必要重构位于隔离之后\nGIVEN 已批准把当前混合职责拆开才能安全实现后续功能。\nWHEN 写计划。",
      "expected_output": "T00 隔离和基线、T01 前置纯重构及行为保护、后续功能票按需依赖 T01；没有阻碍时不造空 T01 重构票。"
    },
    {
      "id": "plan-decomposition-s20",
      "prompt": "删除冗余而保留必要边\nGIVEN 独立文案票被无理由串到 API 票之后，迁移 contract 又遗漏对最后一个 migrate 的依赖。\nWHEN Self-Review。",
      "expected_output": "删除前一冗余边、补后一必要边，保留 T00/验收/最终任务和整组出口；同步导航与任务接口，不把结构校验当成依赖语义证明。"
    },
    {
      "id": "plan-decomposition-s21",
      "prompt": "检查点不变成多重授权\nGIVEN spec 有参与者和约束两处值得用户重点审阅。\nWHEN 呈现 review 门。",
      "expected_output": "陈述两处检查点并只问是否确认并开始计划；用户仅认可内容时不冒充执行授权，检查暴露独立决策时按一次一题处理。"
    },
    {
      "id": "plan-decomposition-s26",
      "prompt": "脱离会话仍能找到纪律\nGIVEN 新执行者只取得特性目录、index、spec 与本票。\nWHEN 读取 T01 或组验证任务。",
      "expected_output": "能从声明定位 TDD/集成组/测试策略的适用规则，seam 仍来自原接口传递；技能不可用时使用计划自足步骤，唯有组的必需机器入口不可用时停止而非猜执行。"
    },
    {
      "id": "plan-decomposition-s27",
      "prompt": "各类任务有实际步骤且首尾闭合\nGIVEN 同一计划有普通票、T01 纯重构、集成组、项目验收及最终任务。\nWHEN 检查生成产物。",
      "expected_output": "每票有精确文件、接口、命令、预期结果与对应类型的完整步骤，组员没有假绿步骤；T00 与最终生命周期闭合、只一个状态源，导航通过结构校验。"
    },
    {
      "id": "plan-decomposition-s32",
      "prompt": "迁移旧形不能永久遗留\nGIVEN expand 新增同用途新接口并暂存旧接口，但候选计划没有删除旧接口的工作。\nWHEN 方案或计划自检。",
      "expected_output": "不能以“允许 expand”通过检查；补明确 contract 和验证，或重新分解，八原则声明块仍存在。"
    }
  ]
}
''')
]
for name, old, new in changes:
    p=Path(name); text=p.read_text()
    assert text.count(old)==1,(name,'anchor drift')
    p.write_text(text.replace(old,new,1))
``````


``````python
from pathlib import Path
changes = [
('scripts/tests/plugin-root.test.mjs', r'''assert.match(en, /four-way self-review \(spec coverage \/ placeholders \/ type consistency \/ navigation table/);''', r'''assert.match(en, /five-way self-review \(spec coverage \/ placeholders \/ type consistency \/ navigation table[^\n]*dependency minimality/);'''),
('scripts/tests/plugin-root.test.mjs', r'''assert.match(zh, /四查（spec 覆盖\/占位符\/类型一致\/导航表与任务文件一致）/);''', r'''assert.match(zh, /五查（spec 覆盖\/占位符\/类型一致\/导航表与任务文件一致\/依赖最小性）/);''')
]
for name, old, new in changes:
    p=Path(name); text=p.read_text()
    assert text.count(old)==1,(name,'anchor drift')
    p.write_text(text.replace(old,new,1))
``````

**步骤 2：运行确认失败**

先运行 `rtk proxy node --test scripts/tests/plugin-root.test.mjs`，预期 README 仍为四查而新断言要求五查，出现对应断言失败；不得把环境失败计为有效红。


``````python
import hashlib,json,subprocess,time,os,signal
from pathlib import Path
root=Path.cwd().resolve()
phase=os.environ.get("PD_PHASE","red")
assert phase in ("red","green")
out=root/'.spec-dev/2026-09-07-01-plan-decomposition/execution/serial/'/'T05'/phase
out.mkdir(parents=True,exist_ok=False)
files=['skills/writing-plans/SKILL.md', 'skills/writing-plans/references/design-principles.md']
cases=[{'id': 'plan-decomposition-s01', 'prompt': '能逐票绿时不使用集成组\nGIVEN 三个调用包可在旧形保留时分别迁移并通过相关测试。\nWHEN 分解机械改名。', 'expected_output': '生成 expand、具有真实依赖的迁移票及依赖全部迁移票的 contract；不因文件多就延期整体绿。'}, {'id': 'plan-decomposition-s02', 'prompt': '批次不能独立绿时保留小票\nGIVEN 共享类型改动导致多个包必须全部迁移后才能通过相关验证，批准设计记录该事实。\nWHEN 编写计划。', 'expected_output': '各批次各有 TNN 文件和提交，归入明确组及组验证任务，不合并为一张超大票，不承诺中间绿。'}, {'id': 'plan-decomposition-s19', 'prompt': '必要重构位于隔离之后\nGIVEN 已批准把当前混合职责拆开才能安全实现后续功能。\nWHEN 写计划。', 'expected_output': 'T00 隔离和基线、T01 前置纯重构及行为保护、后续功能票按需依赖 T01；没有阻碍时不造空 T01 重构票。'}, {'id': 'plan-decomposition-s20', 'prompt': '删除冗余而保留必要边\nGIVEN 独立文案票被无理由串到 API 票之后，迁移 contract 又遗漏对最后一个 migrate 的依赖。\nWHEN Self-Review。', 'expected_output': '删除前一冗余边、补后一必要边，保留 T00/验收/最终任务和整组出口；同步导航与任务接口，不把结构校验当成依赖语义证明。'}]
roles="\n\n".join("# "+p+"\n"+(root/p).read_text() for p in files)
hashes={p:hashlib.sha256((root/p).read_bytes()).hexdigest() for p in files}
for c in cases:
    prompt="按所附候选规则处理输入，说明下一步决定及依据；本次只读决策，不写文件、不联网、不执行计划。候选插件根："+str(root)+"\n"+roles+"\n输入：\n"+c["prompt"]
    dest=out/c["id"];dest.mkdir()
    (dest/"input.txt").write_text(prompt)
    cmd=["rtk","proxy","claude","-p","--plugin-dir",str(root),"--strict-mcp-config","--mcp-config",'{"mcpServers":{}}',"--tools","Read","--allowedTools","Read","--no-session-persistence","--verbose","--output-format","stream-json",prompt]
    with (dest/"stdout.jsonl").open("w") as stdout,(dest/"stderr.log").open("w") as stderr:
        proc=subprocess.Popen(cmd,cwd=root,stdin=subprocess.DEVNULL,stdout=stdout,stderr=stderr,start_new_session=True)
        started=time.monotonic()
        while proc.poll() is None:
            try:proc.wait(timeout=30)
            except subprocess.TimeoutExpired:print(c["id"],"still running",round(time.monotonic()-started),flush=True)
            if time.monotonic()-started>=300 and proc.poll() is None:
                os.killpg(proc.pid,signal.SIGTERM)
                try:proc.wait(timeout=10)
                except subprocess.TimeoutExpired:os.killpg(proc.pid,signal.SIGKILL);proc.wait()
                break
    (dest/"facts.json").write_text(json.dumps({"command":cmd[:-1],"cwd":str(root),"exit":proc.returncode,"elapsed":time.monotonic()-started,"candidate_hashes":hashes,"expected":c["expected_output"],"verdict":"needs_review","model":"read actual init/result; inherit local config"},ensure_ascii=False,indent=2)+"\n")
``````

PD_PHASE=red；独立比对固定预期，记录真实缺口。静态缺句不是模型失败，已全通过则回看最小改动。
**步骤 3：写最小实现**

``````python
from pathlib import Path
changes = [
('skills/writing-plans/SKILL.md', r'''## 可选并发声明
''', r'''## 可选集成组声明与 v2 进度

这是集成组字段的唯一定义点；执行时序遵循 [integration-groups.md](../executing-plans/references/integration-groups.md)。仅在普通切片和每批绿的 expand–contract 均不可行且设计已批准时生成；不以文件多、测试慢或想并发为理由启用。


index 中至多一个 `json spec-dev-integration` fenced block，内容为 JSON（使用 JSON 避免引入第二套复杂 YAML 语法；重复键显式拒绝）。声明示例为确定的结构示意，不是本特性的实施计划：

```json spec-dev-integration
{
  "protocol_version": 1,
  "task_roles": {
    "T00": "isolation",
    "T10": "acceptance",
    "T11": "delivery"
  },
  "groups": {
    "G01": {
      "members": ["T03", "T04", "T05"],
      "verify": "T06",
      "reason": "共享类型的三个消费者全部迁移后才能通过相关验证"
    }
  }
}
```

GNN 与 TNN 分别唯一；每组至少两名成员，members 为确定的执行顺序，必须与导航 DAG 的拓扑顺序一致但不凭此数组虚构依赖边。组员保持常规小票粒度；组验证显式依赖全部成员；组外任务只能通过 verify 依赖该组。声明中的 `task_roles` 必填，T00 唯一 isolation、最大号唯一 delivery、项目 acceptance 如有则列出，角色不得重叠；这些票不得进入 members/verify。组验证文件显式标 `任务类型：group-verification`，组员标 `任务类型：integration-member`，不重复写进 task_roles；普通票由余集确定。task_roles/组声明与任务正文在计划 Self-Review/运行读取本票时核对。上例 T10/T11 是示例角色，实际 ID 必须存在于四列导航表。组声明不产生第二份进度。

组间及普通任务构成的收缩 DAG 同样无环；外部前置取组员和 verify 对组外依赖的并集，全部 completed 才进入组。禁止外部依赖成员的隐藏绕行；不同组通过 verify 相连。活动组期间即使存在独立 ready 普通票也先完成活动组，避免未验证 HEAD 被外部使用。

### 运行数据与读取边界

无组新计划继续 v1；含组使用 `progress.yaml` 的 `format_version: 2`。v2 的机器写入格式限定为缩进 JSON（JSON 属于 YAML 子集，文件位置不变），字符串必须双引号、拒绝重复键与未知协议字段；不接收 YAML anchor/tag/注释等额外语法，不引入新 YAML 依赖。新读取器支持 v2，v1/单文件仍走既有分支。格式号只标识数据，不绑定 executor/skill 版本，也不生成编译快照。

v2 完整继承 tasks/resources/notes 和原 parallel 可选 execution 扩展字段；无组计划不准出现 awaiting_verification。新增字段如下：

| 位置 | 字段与约束 |
|---|---|
| 根 | `integration: {owner, worktree, branch, base_commit, validated_commit, active_group, groups}`；生成时 owner/路径运行值/SHA 为 null，active_group=null，组 status=pending；resources 可预登记已知资源 |
| integration.groups.GNN | `status: pending\|in_progress\|blocked\|completed`；`base_commit`（本组启动已验证点）、`checkpoint_commit`（最后已保存实现点）、`validated_commit`（本组完成点，完成前 null）、`evidence_paths: []` |
| tasks.TNN | 原 status 加 awaiting_verification；`implementation_commit` 记本票实现点，`commit` 仅完成后填接受点；`tests: pending_group\|pass\|fail` 对组员适用，保留原字段；`evidence_paths: []` 与 `deviations` 支持恢复 |
| notes | 追加授权来源、组激活/恢复/失效/晚到回执事件及证据路径，不覆盖旧记录 |

parallel 模式下 `integration.owner/worktree/branch/base_commit/validated_commit` 分别与 `execution.owner/integration_worktree/integration_branch/base_commit/validated_commit` 一致，后者为旧消费者的投影，不允许独立更新；CLI 检查一致性并要求同次原子更新。integration.base_commit 为特性原始审查点，groups.GNN.base_commit 为组进入点，不混用。串行 v2 也使用既有同一 common-dir 特性锁及其维护门，身份只在持锁后填写，不以存在 JSON owner 自证锁。v2 普通票完成同样更新 integration.validated_commit；在首次状态写入前取得锁，T00 建立/确认隔离后绑定真实工作区，保留该特性同一锁身份。

`current` 只指正在施工或补查的主线程票，不用它表示所有已待验票。运行 SHA 不指向正在包含它们的状态提交：先保存真实实现提交 C，再将 C 写入状态并另行提交；证据归档引起的后续提交不改变代码树时允许继续引用 C，但需可核验只变更本特性的进度/证据路径。


### 类型化任务步骤

普通行为票沿下方五步。前置纯重构票明确写：确认授权/公共行为基线，缺保护先刻画当前行为通过，执行结构调整，复跑保护通过，提交；不制造假红。集成组成员票明确写：声明本票写集合/接口/延期检查原因，执行适用行为红或组首保护，实施和局部检查，两项契约自检/保存实现提交，记录 awaiting_verification 与证据检查点。组验证票明确写：确认全员待验，执行完整组验证/清理旧形检查，失败回归属成员修复，通过后原子完成全组/基线，再提交状态。具体代码、命令、期望与来源仍须全部内嵌，不可用类型名代替内容。

生成时先跑 plan-index；含组还必须验证执行环境支持 plan-state/protocol_version=1。未开始的运行值留 null，不伪造 owner/路径/提交/模型或测试通过。执行技能不可用时普通计划仍可按完整步骤读取；组的机器能力不可用则停止，不按 v1 猜执行。

## 可选并发声明
'''),
('skills/writing-plans/SKILL.md', r'''TDD 循环的完整纪律遵循 test-driven-development skill——计划里的每个任务显式内嵌上述五步。''', r'''TDD 循环的完整纪律遵循 test-driven-development skill——普通行为任务显式内嵌上述五步；批准的纯重构、集成组成员和组验证票按下文类型化步骤写全操作、验证、提交及真实结果。'''),
('skills/writing-plans/SKILL.md', r'''**Scenario 直译为测试**：spec 行为规范里的每个 `#### Scenario:` 至少翻译成一个失败测试，映射固定：''', r'''**Scenario 直译为验证**：每个 `#### Scenario:` 都有实际验证步骤；行为变化翻译为有效失败测试，纯重构使用前后保护，组内不能独立通过的相关验证明确归属组验证票，不漏掉 Scenario。映射固定：'''),
('skills/writing-plans/SKILL.md', r'''## 文件结构先行
''', r'''## 文件结构先行

宽面机械迁移先尝试普通独立验证任务；不成立时 expand 保留新旧形、按实际影响范围分批 migrate、最后 contract 删除旧形，每批通过相关验证再提交；必要的安全顺序仍写依赖。连批次都不能独立通过时，记录证据并采用下方多票集成组。识别解除实际实施阻碍的前置重构，有需要才安排 T01，T00 固定隔离；无需要不制造空重构票。
'''),
('skills/writing-plans/SKILL.md', r'''**技术栈**：[关键技术/库]''', r'''**技术栈**：[关键技术/库]

**关联 skill**：逐项列名称、适用任务/时机、单点定义路径；包含执行方式、适用 TDD/纯重构/集成组、测试策略、隔离及验收。任务只写本票新增关联，不复制整套规则；获批 seam 仍沿导航/接口块继承。'''),
('skills/writing-plans/SKILL.md', r'''1. **Spec 覆盖**：逐条 Requirement 过——能指到实现它的任务吗？每个 Scenario 都有对应的失败测试步骤吗（GIVEN/WHEN/THEN → arrange/act/assert）？验收矩阵的「验收任务」行都进入验收任务表了吗？差量三节的 MODIFIED/REMOVED 有对应的改造/清理任务吗？列出缺口''', r'''1. **Spec 覆盖**：逐条 Requirement 过——能指到实现它的任务吗？每个 Scenario 都有适用的行为红绿、纯重构保护或组验证步骤吗（GIVEN/WHEN/THEN → arrange/act/assert）？验收矩阵的「验收任务」行都进入验收任务表了吗？差量三节的 MODIFIED/REMOVED 有对应的改造/清理任务吗？列出缺口'''),
('skills/writing-plans/SKILL.md', r'''发现问题就地修复，无需复审；发现 spec 需求没有对应任务就补任务。''', r'''5. **依赖最小性**：每条边能否指出实际消费接口、验证或安全顺序理由？补漏边、删无理由的边；保留 T00/迁移/验收/最终任务及组出口的必要约束。不能靠删安全边制造并发。

发现问题就地修复，无需复审；发现 spec 需求没有对应任务就补任务。'''),
('skills/writing-plans/SKILL.md', r'''保存计划后向用户交接：''', r'''保存计划后先展示最新 index 链接与简短变更摘要，列 2–3 个针对粒度、依赖或边界的陈述式检查点，再作一次整体确认；检查发现多个未决点仍按一次一题澄清，不新增三道批准门。向用户交接：'''),
('skills/writing-plans/SKILL.md', r'''四查跑完再交''', r'''五查跑完再交'''),
('skills/writing-plans/references/design-principles.md', r'''# 设计原则（方案取舍与计划编写共同遵循）

> **阅读时机**：requirement-analysis 阶段 4（方案对比）与阶段 5（设计展示）、writing-plans 编写计划头部时加载；executing-plans 执行者经计划头部「设计原则」块间接获得。

八条原则（方案评价维度与任务分解检查项）：

1. **默认不保留向后兼容性**。删除过时路径，而不是添加兼容层、回退垫片或仅用于兼容性的数据迁移。常规的前向模式迁移仍然是必需的。仅保留产品和部署规则中明确记录的当前合同。
2. **选择能完全满足当前需求的最简单实现**。避免推测性的抽象、配置和间接层。
3. **分层构建系统**。从能够端到端工作的最小版本开始，然后基于运行中的产品逐步添加各项能力。
4. **绝不用未完成的复杂性换取可工作的产品**。
5. **保持组件模块化，关注点清晰分离**。
6. **优先使用成熟且维护良好的库**，前提是它们能降低整体复杂性或提高可靠性；没有充分理由不重新实现通用功能。
7. **优先使用项目中已有的依赖**；判断某依赖缺少某能力之前，先查阅其文档和类型定义。
8. **做出长期性的架构决策**。不接受打算日后替换的权宜之计。

## spec-dev 语境注解

- 第 1 条管**实现层**：不写兼容层/垫片/仅兼容性迁移。在 spec-dev 里，active spec 就是"明确记录的当前合同"——删除旧行为路径的前提是经取代分流**显式取代**旧 spec（supersede 机制），而非绕开它；读取历史落盘产物（如旧格式 plan）属于读历史数据，不是兼容垫片。
- 第 1 条与阶段 1「契约姿态判定」的关系：姿态判定的默认值不变（旧 active spec 默认硬约束），本原则要求的是**取代要走正门、实现不留垫片**。
- 第 6/7 条的既有呼应：收尾审查维度 C「优先使用项目已有工具与模式」；ADR 三判据承载第 8 条的"长期决策"沉淀。

## 模块判据（按实际问题使用）

本节是设计、计划、spec 审查与代码审查共用的判据定义点。八条原则保持；下列启发式不自动成为缺陷、架构整改或新增批准门。引用方提供当前需求、获批 seam 和证据，不复制定义。

- **模块删除判据**：假想移除模块，复杂性会消失，还是重新分散到多个调用方？前者提示可能是无价值间接层，后者说明模块集中承担职责；结合真实用途判断，纯转发也可能承担有效契约。只做推演，不实际删除模块或保护测试。
- **真实 adapter 计数**：列出现实中变化的实现和用途，检查抽象是否服务真实需求。两个实现不自动证明抽象合理，一个实现也不否决已批准 seam；不为凑数制造 fake、投机第二实现或私有测试接口。
- **接口收敛三问**：能否减少方法、简化参数、隐藏更多内部复杂性？回答须关联实际消费者和现行需求，不凭方法数量制造违规。
- **结构摩擦五问**：理解同一概念是否跨过多模块？接口是否与内部实现同样复杂？测试是否只覆盖提取的纯函数而遗漏公共调用行为？耦合是否泄漏到消费者？公共接口是否难以测试？检查本次变更新增或加剧的具体摩擦，保留公共行为覆盖与获批 seam；不要求逐私有函数测试。

**消费边界**：设计/计划可据证据改进方案，审查只据实际影响和既有授权处理；更优雅的设想记重构候选，缺陷走正常复核。纯重构仍由 TDD 的收尾规则承接，最终全量保持原计划时机。
''', r'''# 设计原则（方案取舍与计划编写共同遵循）

> **阅读时机**：requirement-analysis 阶段 4（方案对比）与阶段 5（设计展示）、writing-plans 编写计划头部时加载；executing-plans 执行者经计划头部「设计原则」块间接获得。

八条原则（方案评价维度与任务分解检查项）：

1. **默认不保留向后兼容性**。删除过时路径，而不是添加兼容层、回退垫片或仅用于兼容性的数据迁移。常规的前向模式迁移仍然是必需的。仅保留产品和部署规则中明确记录的当前合同。
2. **选择能完全满足当前需求的最简单实现**。避免推测性的抽象、配置和间接层。
3. **分层构建系统**。从能够端到端工作的最小版本开始，然后基于运行中的产品逐步添加各项能力。
4. **绝不用未完成的复杂性换取可工作的产品**。
5. **保持组件模块化，关注点清晰分离**。
6. **优先使用成熟且维护良好的库**，前提是它们能降低整体复杂性或提高可靠性；没有充分理由不重新实现通用功能。
7. **优先使用项目中已有的依赖**；判断某依赖缺少某能力之前，先查阅其文档和类型定义。
8. **做出长期性的架构决策**。不接受打算日后替换的权宜之计。

## spec-dev 语境注解

- 第 1 条管**实现层**：不写兼容层/垫片/仅兼容性迁移。在 spec-dev 里，active spec 就是"明确记录的当前合同"——删除旧行为路径的前提是经取代分流**显式取代**旧 spec（supersede 机制），而非绕开它；读取历史落盘产物（如旧格式 plan）属于读历史数据，不是兼容垫片。
- 第 1 条与阶段 1「契约姿态判定」的关系：姿态判定的默认值不变（旧 active spec 默认硬约束），本原则要求的是**取代要走正门、实现不留垫片**。
- 第 6/7 条的既有呼应：收尾审查维度 C「优先使用项目已有工具与模式」；ADR 三判据承载第 8 条的"长期决策"沉淀。

## 模块判据（按实际问题使用）

本节是设计、计划、spec 审查与代码审查共用的判据定义点。八条原则保持；下列启发式不自动成为缺陷、架构整改或新增批准门。引用方提供当前需求、获批 seam 和证据，不复制定义。

- **模块删除判据**：假想移除模块，复杂性会消失，还是重新分散到多个调用方？前者提示可能是无价值间接层，后者说明模块集中承担职责；结合真实用途判断，纯转发也可能承担有效契约。只做推演，不实际删除模块或保护测试。
- **真实 adapter 计数**：列出现实中变化的实现和用途，检查抽象是否服务真实需求。两个实现不自动证明抽象合理，一个实现也不否决已批准 seam；不为凑数制造 fake、投机第二实现或私有测试接口。
- **接口收敛三问**：能否减少方法、简化参数、隐藏更多内部复杂性？回答须关联实际消费者和现行需求，不凭方法数量制造违规。
- **结构摩擦五问**：理解同一概念是否跨过多模块？接口是否与内部实现同样复杂？测试是否只覆盖提取的纯函数而遗漏公共调用行为？耦合是否泄漏到消费者？公共接口是否难以测试？检查本次变更新增或加剧的具体摩擦，保留公共行为覆盖与获批 seam；不要求逐私有函数测试。

**消费边界**：设计/计划可据证据改进方案，审查只据实际影响和既有授权处理；更优雅的设想记重构候选，缺陷走正常复核。纯重构仍由 TDD 的收尾规则承接，最终全量保持原计划时机。


## 迁移过渡与约束归属

- expand–contract 中经批准的新旧暂存是当前迁移路径，需明确删除旧形/消费者清零的 contract 工作与最终验证，不发展为长期兼容层；active 旧行为改变仍走取代流程，不能以迁移名义绕过合同。无法逐批绿时由显式集成组承担验证，不把未完成状态交付。
- 独立约束在方案定型前分别分配负责边界与验证位置；同一文件可承接多约束，但一项证据不自动证明另一项。计划沿全局约束及本票接口继承，避免每票现场附会。
- 对存在真实歧义且已经裁决的表达，记录拒绝的解读、理由/来源和采用理解；无依据不发明排除项，不替代 ADR 资格判断或扩大审查权限。
'''),
('skills/writing-plans/agents/openai.yaml', r'''  short_description: "把获批 spec 写成 plan 三件套：共享设计判据、精确任务接口与 seam 来源、TDD 五步、适用快检和验收闭环；progress 唯一记状态。"''', r'''  short_description: "把获批 spec 写成 plan 三件套：类型化验证、必要前置重构、集成组与依赖五查；精确接口/关联 skill，progress 唯一记状态。"'''),
('README.md', r'''- **writing-plans**: assumes a zero-context executor — every plan starts with a fixed Task 0 (set up an isolated workspace, with already-isolated detection and git fallback commands) and ends with a final task (merge & cleanup); when the spec's acceptance matrix has "acceptance task" rows, an acceptance task is generated between them. The worktree lifecycle closes within the plan, so it executes in order even outside this plugin. The header carries deviation-handling guidance; every task gets exact file paths, complete code, the 5 TDD steps (failing test → confirm fail → minimal implementation → confirm pass → commit) and consume/produce interface blocks; a four-way self-review (spec coverage / placeholders / type consistency / navigation table ↔ task files) runs before handoff''', r'''- **writing-plans**: assumes a zero-context executor — every plan starts with a fixed Task 0 (set up an isolated workspace, with already-isolated detection and git fallback commands) and ends with a final task (merge & cleanup); when the spec's acceptance matrix has "acceptance task" rows, an acceptance task is generated between them. The worktree lifecycle closes within the plan, so it executes in order even outside this plugin. The header carries deviation-handling guidance; every task gets exact file paths, complete code, the applicable behavior TDD, refactor protection or integration-group verification steps and consume/produce interface blocks; a five-way self-review (spec coverage / placeholders / type consistency / navigation table ↔ task files / dependency minimality) runs before handoff'''),
('README.zh-CN.md', r'''- **writing-plans**：假设执行者零上下文——每份计划固定以任务 0（建立隔离工作区，含已隔离检测与 git 降级命令）开头、以最终任务（合并与清理）收尾，spec 验收矩阵含「验收任务」行时在两者之间固定生成验收任务，worktree 生命周期在计划内闭合、脱离插件也能按序执行；头部随行偏差处理指引；每任务给精确文件路径、完整代码、TDD 五步（失败测试→确认失败→最小实现→确认通过→提交）、接口消费/产出块；写完跑四查（spec 覆盖/占位符/类型一致/导航表与任务文件一致）再交接''', r'''- **writing-plans**：假设执行者零上下文——每份计划固定以任务 0（建立隔离工作区，含已隔离检测与 git 降级命令）开头、以最终任务（合并与清理）收尾，spec 验收矩阵含「验收任务」行时在两者之间固定生成验收任务，worktree 生命周期在计划内闭合、脱离插件也能按序执行；头部随行偏差处理指引；每任务给精确文件路径、完整代码、适用的行为红绿/纯重构保护/集成组验证步骤、接口消费/产出与关联 skill；写完跑五查（spec 覆盖/占位符/类型一致/导航表与任务文件一致/依赖最小性）再交接''')
]
for name, old, new in changes:
    p=Path(name); text=p.read_text()
    assert text.count(old)==1,(name,'anchor drift')
    p.write_text(text.replace(old,new,1))
``````


``````python
from pathlib import Path
changes = [
('skills/writing-plans/agents/openai.yaml', r'''interface:
  display_name: "Writing Plans"
  short_description: "把获批 spec 写成 plan 三件套：类型化验证、必要前置重构、集成组与依赖五查；精确接口/关联 skill，progress 唯一记状态。"
  default_prompt: "Use $writing-plans to turn the approved spec into a detailed implementation plan. / 用 $writing-plans 把已批准的 spec 拆成详细实施计划。"

policy:
  allow_implicit_invocation: true
''', r'''interface:
  display_name: "Writing Plans"
  short_description: "把获批 spec 写成 index.md + tasks/ + progress.yaml 三件套：类型化验证、必要前置重构、集成组与依赖五查；精确接口/关联 skill，progress 唯一记状态。"
  default_prompt: "Use $writing-plans to turn the approved spec into a detailed implementation plan. / 用 $writing-plans 把已批准的 spec 拆成详细实施计划。"

policy:
  allow_implicit_invocation: true
''')
]
for name,old,new in changes:
 p=Path(name);s=p.read_text();assert s.count(old)==1,(name,"anchor drift");p.write_text(s.replace(old,new,1))
``````

**步骤 4：运行确认通过**
PD_PHASE=green 重跑步骤2同一探针；T08另做实际产物生成及校验。

``````bash
rtk proxy node scripts/validate-skills.mjs
rtk proxy node scripts/check-plugin.mjs
rtk proxy node --test scripts/tests/plugin-root.test.mjs scripts/tests/plan-single-format.test.mjs scripts/tests/plan-index.test.mjs scripts/tests/integration-plan.test.mjs scripts/tests/plan-state.test.mjs
rtk git diff --check
``````

**步骤 5：提交**

只暂存本票文件、spec实施记录、本票证据和 progress；按 index 的共存、发版和原子状态规则提交。未完成必需检查不标 completed。

``````bash
rtk git add scripts/tests/plugin-root.test.mjs skills/writing-plans/SKILL.md skills/writing-plans/agents/openai.yaml skills/writing-plans/evals/evals.json skills/writing-plans/references/design-principles.md README.md README.zh-CN.md
rtk git add .spec-dev/2026-09-07-01-plan-decomposition/spec/plan-decomposition-design.md .spec-dev/2026-09-07-01-plan-decomposition/plan/progress.yaml .spec-dev/2026-09-07-01-plan-decomposition/execution/serial/T05
rtk proxy node guardrail/check-spec-drift.mjs --staged
``````

提交主旨：`feat(T05): 生成类型化任务和集成组计划`。正文使用文件传入 Spec 指针及实际共存切面，不跳过 pre-commit。代码提交后核实自动发版最终 HEAD，另行保存本票状态提交，状态提交使用 `SKIP_RELEASE_HOOK=1`。
