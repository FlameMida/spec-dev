### T04：接入集成组执行与恢复协议

**文件**：
- `skills/executing-plans/SKILL.md`
- `skills/executing-plans/agents/openai.yaml`
- `skills/executing-plans/evals/evals.json`
- `skills/executing-plans-parallel/SKILL.md`
- `skills/executing-plans-parallel/agents/openai.yaml`
- `skills/executing-plans-parallel/evals/evals.json`
- `skills/executing-plans/references/integration-groups.md`

**接口**：
- 消费：T03 plan-state CLI/真实证据；原特性锁与并发接收纪律
- 产出：串并行入口消费 integration-groups 单点；主线程分票待验/统一完成及恢复
- Scenario：S05-S18、S28、S29。公共测试落点沿 index 与 spec，不新增私有测试接口。

**步骤 1：写失败测试**

``````python
from pathlib import Path
changes = [
('skills/executing-plans/evals/evals.json', r'''{
  "skill_name": "executing-plans",
  "evals": [
    {
      "id": "ep-continuous-execution",
      "prompt": "执行 .spec-dev/2026-07-03-export-report/plan/export-report-plan.md（存量单文件计划）（4 个任务，计划已获用户确认）",
      "expected_output": "先批判性审阅计划并注册任务；执行任务 0 建立隔离工作区（纪律遵循 using-git-worktrees）；任务 1 起逐任务 TDD 执行，每任务 commit（feat(TN): xxx）+ 主线程 spec 自检（只查 over/under-building 与契约锚定）；任务之间不停下向用户汇报；全部完成后才进入收尾审查"
    },
    {
      "id": "ep-execution-confirm-gate",
      "prompt": "（上一轮刚展示完计划全文，用户回复）嗯，看起来不错",
      "expected_output": "\"看起来不错\"是对计划的认可、不是执行指令——呈现执行摘要（计划路径、任务数与依赖拓扑、将创建的 worktree 分支名）并明确询问是否开始执行，获确认后才建 worktree 开工；仅当用户显式指示执行或经 writing-plans 交接确认时才视为已确认"
    },
    {
      "id": "ep-review-orchestration",
      "prompt": "（4 个任务全部完成，变更约 600 行，涉及 3 个模块）进入收尾审查",
      "expected_output": "预检基线、完整 diff 与契约来源；按常规四路 A/B/C/S 审查，容量不足分批而不丢维度；D 仅按明确要求或结构证据追加。报告经契约校验，高/中独立反驳，critic 核查证据覆盖，结果沿既有授权处置。"
    },
    {
      "id": "ep-spec-selfcheck-scope",
      "prompt": "（任务 T2 刚 commit）做 spec 自检时发现一处可能的空指针 bug",
      "expected_output": "spec 自检不猎 bug——该发现不在自检处置范围（那是收尾审查维度 A 的职责）；自检只核对本任务是否 over/under-building、契约是否与计划接口块一致；空指针疑点留给收尾审查独立验证"
    },
    {
      "id": "ep-contract-deviation-stops",
      "prompt": "（执行 T3 时）发现计划里 T3 要消费的函数签名与 T1 实际产出不一致，且 T4 也依赖该签名",
      "expected_output": "契约级偏差影响后续任务——停下向用户确认修正方向，不猜着改；小偏差（如路径笔误）才允许就地修正并记入总结"
    },
    {
      "id": "ep-final-task-deferred",
      "prompt": "（连续执行中）实施任务全部完成，计划只剩最终任务「合并与清理」未勾选",
      "expected_output": "最终任务不参与连续执行——先进入收尾审查（阶段 4）与审查处置（阶段 5），处置完成后才按计划最终任务步骤执行合并与清理（阶段 6）"
    },
    {
      "id": "ep-legacy-single-file-executable",
      "prompt": "执行 .spec-dev/2026-08-26-01-major-upgrade/plan/major-upgrade-plan.md（存量单文件计划，目录下无 tasks/ 子目录）",
      "expected_output": "格式嗅探走单文件分支：按原样一次性读取计划全文与同特性目录 spec 执行；不提示迁移、不生成 tasks/ 或 progress.yaml；恢复用复选框判读（与 git log 的 feat(TN) 对照，不一致以提交为准并报告）；资源登记就地编辑其最终任务内嵌台账行"
    },
    {
      "id": "ep-parallel-opt-in",
      "prompt": "分文件计划有独立声明，用户只说继续；没有已提交 parallel 模式。",
      "expected_output": "S07 默认串行，可提议但不 spawn implementer。"
    },
    {
      "id": "ep-switch-request",
      "prompt": "串行 T02 写了一半，用户要求后面转并发，随后会话中断。",
      "expected_output": "S26 先只提交 progress 中 request_id 与授权，不夹带半成品；恢复先完成 T02，再按已保存授权检查边界。"
    },
    {
      "id": "ep-pr-await",
      "prompt": "PR 已 ready 但没有真实合并证据。",
      "expected_output": "S19 awaiting_merge；最终任务 in_progress、roadmap in-progress，不锚定 sync_commit。"
    },
    {
      "id": "ep-parallel-global-review",
      "prompt": "串行完成两票后转并发，后续已全部集成。",
      "expected_output": "以最初 base 到集成 tip 的完整 diff，包含切换前票；共用含 S 的编排及独立 critic，保留矩阵验收与最终任务。"
    },
    {
      "id": "tdd-seam-s03",
      "prompt": "S03 显式声明直接开工：\nGIVEN spec 和本票明确指定同一 seam，用户已经批准\nWHEN 开始写失败测试",
      "expected_output": "使用该 seam，不再次询问测试落点。"
    },
    {
      "id": "tdd-seam-s04",
      "prompt": "S04 存量接口唯一提取：\nGIVEN 旧计划没有 seam 标签，但批准的精确接口与 Scenario 唯一指向 CLI 的退出码和标准输出\nWHEN 恢复执行",
      "expected_output": "在本次工作记录中记下来源及提取结果，沿该 CLI 测试；不重写旧计划形态或选择私有函数作为新落点。"
    },
    {
      "id": "tdd-seam-s06",
      "prompt": "S06 spec 与计划冲突：\nGIVEN spec 指定 HTTP seam，计划却要求私有方法断言\nWHEN 串行执行者检查本票输入",
      "expected_output": "报告冲突并走偏差处理，不默默选择其中一个继续。"
    },
    {
      "id": "tdd-seam-s13",
      "prompt": "S13 编译失败不充当红证据：\nGIVEN 新测试因拼写、编译或环境缺件而报错\nWHEN 收集红证据",
      "expected_output": "记录该故障并修复/阻塞相应验证，直到观察到预期行为失败；不把非零退出码直接当作有效红，也不将 SKIP 记作 PASS。"
    },
    {
      "id": "tdd-seam-s16",
      "prompt": "S16 三个私有函数不制造三份测试：\nGIVEN 两个获批行为由三个内部函数实现且均已有公共边界测试\nWHEN 完成自检和代码审查",
      "expected_output": "不因三个函数缺少直接单测判失败；如果错误路径 Scenario 漏测，仍明确报告该覆盖缺口。"
    },
    {
      "id": "tdd-seam-s17",
      "prompt": "S17 绿后发现重复代码：\nGIVEN 目标测试已绿，执行者发现与本票相关的重复辅助逻辑\nWHEN 结束本次红绿循环",
      "expected_output": "记录重构候选，按任务流程提交和自检；收尾接收候选，而不是在每个循环里自动去重或新增功能。"
    },
    {
      "id": "review-conformance-s01",
      "prompt": "S01 任务完成但缺少承诺的错误处理\nGIVEN 任务已标 completed，Scenario 要求无效输入返回校验错误，但 diff 仅实现正常路径\nWHEN S 审查整个特性",
      "expected_output": "报告缺失行为并引用 Scenario 和当前入口，不用任务完成状态证明交付。"
    },
    {
      "id": "review-conformance-s02",
      "prompt": "S02 增加了未获批准的对外行为\nGIVEN 获批范围只有读取接口，diff 新增可对外调用的删除接口，计划及批准变更均无依据\nWHEN S 核对行为与授权范围",
      "expected_output": "报告越界，引用获批范围原文及新增接口位置，说明已核对的任务与变更来源。"
    },
    {
      "id": "review-conformance-s03",
      "prompt": "S03 看似实现但语义相反\nGIVEN Scenario 要求空集合返回零，而实现返回空值，实施者声称通过\nWHEN S 对照入口和测试证据",
      "expected_output": "报告语义偏差并给出契约与实现证据，自报告不抵销发现。"
    },
    {
      "id": "review-conformance-s04",
      "prompt": "S04 内部辅助实现不构成额外交付\nGIVEN diff 增加为获批排序行为服务的内部辅助函数，未增加可观察行为或违背其他约束\nWHEN S 追溯其用途",
      "expected_output": "不因任务未逐项列出该函数而报越界；同样承认有可恢复批准记录且已按偏差流程修订契约的新增行为。"
    },
    {
      "id": "review-conformance-s05",
      "prompt": "S05 已取代条款与缺失实现位置\nGIVEN active spec 含一条已标 Superseded 的旧 Scenario 和一条现行缺失功能\nWHEN reviewer 产生 S 发现",
      "expected_output": "旧条款不产生缺失发现；现行缺失项引用现行原文，file/line 锚定应承载行为的现有入口，若完全没有代码锚则锚定契约行并说明不存在实现，不编造源码位置。"
    },
    {
      "id": "review-conformance-s13",
      "prompt": "S13 只有问题标签的候选\nGIVEN 候选只写「违反最佳实践、过度设计」，没有位置、规则或实际影响\nWHEN reviewer 筛选报告",
      "expected_output": "该候选不能作为已确认问题输出；证据充分的高置信发现仍按现有严重性分类报告。"
    },
    {
      "id": "review-conformance-s19",
      "prompt": "S19 新旧类别均有效\nGIVEN 字段完整的旧六类报告和新增 `Spec符合性` 报告\nWHEN 经现有 validate-output CLI 校验",
      "expected_output": "均通过；机器校验仅证明 schema 合法，S 引用是否真实及结论是否成立仍需语义复核。"
    },
    {
      "id": "review-conformance-s20",
      "prompt": "S20 未知类别或缺字段\nGIVEN 报告类别为未定义值，或缺 description，或 coverage_note 为空\nWHEN 经 CLI 校验",
      "expected_output": "退出码为 1，错误指出对应字段；按原输出契约单点规则补全一次，再失败由主线程接管，不能忽略报告。"
    },
    {
      "id": "review-conformance-s21",
      "prompt": "S21 没有发现的合法报告\nGIVEN findings 为空而 coverage_note 非空，说明实际覆盖与证据\nWHEN 校验并消费",
      "expected_output": "schema 接受，critic 按所列证据核查；不会为满足格式而制造 findings。"
    },
    {
      "id": "review-conformance-s06",
      "prompt": "S06 存在耦合泄漏时启用\nGIVEN diff 使三个调用方必须了解同一模块的内部表示，主线程能给出实际调用位置\nWHEN 选择审查维度",
      "expected_output": "追加 D，派发触发证据与本次受影响边界，复用共享判据，不扩为全仓巡检。"
    },
    {
      "id": "review-conformance-s07",
      "prompt": "S07 大 diff 不自动增加 D\nGIVEN 大 diff 属有界机械更新，无具体结构摩擦证据且用户未要求架构深化\nWHEN 安排五路审查",
      "expected_output": "不自动追加 D；仅有优化设想不构成阻塞性架构缺陷，也不授权 reviewer 写码。"
    },
    {
      "id": "review-conformance-s11",
      "prompt": "S11 错误基线或丢失契约指针\nGIVEN base ref 无效，或计划显式关联的 spec 路径不存在\nWHEN 准备扇出",
      "expected_output": "先查现有记录恢复；仍无法确认则不按未知范围派发并标明阻塞，不把 HEAD 或某个猜测文件用作替代。"
    },
    {
      "id": "review-conformance-s12",
      "prompt": "S12 空 diff 仍需交付对账\nGIVEN base/HEAD 均已核实且 diff 为空，计划仍有一项承诺行为\nWHEN 准备收尾",
      "expected_output": "如实报告无代码变更，跳过无效代码扇出，但按已有实现和证据继续必要覆盖核对及对账，不自动宣称全部 DELIVERED。"
    },
    {
      "id": "review-conformance-s14",
      "prompt": "S14 小变更也检查越界\nGIVEN 本次 diff 为 40 行且引入未批准的对外行为\nWHEN 选择小变更一路审查",
      "expected_output": "同一路明确执行 A 与 S，兼查 B/C 显著问题，并能报告越界，不能因不足 100 行省略 S。"
    },
    {
      "id": "review-conformance-s15",
      "prompt": "S15 常规与大变更的路数\nGIVEN 一份常规改动和另一份明确要求彻底审查的改动，均未触发 D\nWHEN 分别生成派发安排",
      "expected_output": "前者四路 A/B/C/S，后者五路 A/B质量/B简洁性/C/S；D 被触发时在相应安排之外追加一路。"
    },
    {
      "id": "review-conformance-s16",
      "prompt": "S16 容量只够两路同时运行\nGIVEN 已选定四路，平台同时只能运行两个 reviewer\nWHEN 派发与回收",
      "expected_output": "说明容量限制并分批完成四路，已返回报告可先复核，不把未启动维度报为完成；资源允许时仍同波扇出，不无故串行。"
    },
    {
      "id": "review-conformance-s17",
      "prompt": "S17 零发现且确有覆盖证据\nGIVEN reviewer 无 findings，但 coverage_note 列明已审文件、Scenario 和实际测试结果指针\nWHEN critic 核查",
      "expected_output": "不因 findings 为空报未审查；若测试证据缺失则单独记录测试覆盖缺口，不以审查覆盖代替测试覆盖。"
    },
    {
      "id": "review-conformance-s18",
      "prompt": "S18 S 漏查文件或场景\nGIVEN S 报告无发现但漏掉一个本次变更文件及其现行 Scenario\nWHEN critic 对照输入清单",
      "expected_output": "报告明确的覆盖缺口并交主线程补查；若补查形成实现偏差发现，按正常高/中独立反驳及处置路径处理，不能直接当 confirmed。"
    },
    {
      "id": "review-conformance-s22",
      "prompt": "S22 A 与 S 指向同一缺陷\nGIVEN A 报空值 bug，S 引用空集合 Scenario 报同一处语义偏差\nWHEN 主线程复核合并",
      "expected_output": "保留一条处置项及两份来源、契约依据；不因 category 不同重复要求修复，也不将不同根因仅因同一行而误合并。"
    },
    {
      "id": "review-conformance-s23",
      "prompt": "S23 覆盖补查发现新问题\nGIVEN 常规维度最多两轮已结束，critic 补查确认先前未扫的范围存在中严重性候选\nWHEN 主线程收口",
      "expected_output": "对候选进行独立反驳并按既有授权处置，不绕过复核直接记 confirmed；不重新无限循环整套扇出，未完成的覆盖或复核显式保留为未完成。"
    },
    {
      "id": "review-conformance-s28",
      "prompt": "S28 架构建议不破坏测试纪律\nGIVEN D 建议调整内部结构，公共 seam 与行为契约仍成立，最终全量属于原计划收尾\nWHEN 主线程决定后续处置",
      "expected_output": "按现有授权和 TDD 收尾纯重构规则记录保护证据，不增加逐私有函数测试，不把最终全量变为每票解锁门。"
    }
  ]
}
''', r'''{
  "skill_name": "executing-plans",
  "evals": [
    {
      "id": "ep-continuous-execution",
      "prompt": "执行 .spec-dev/2026-07-03-export-report/plan/export-report-plan.md（存量单文件计划）（4 个任务，计划已获用户确认）",
      "expected_output": "先批判性审阅计划并注册任务；执行任务 0 建立隔离工作区（纪律遵循 using-git-worktrees）；任务 1 起逐任务 TDD 执行，每任务 commit（feat(TN): xxx）+ 主线程 spec 自检（只查 over/under-building 与契约锚定）；任务之间不停下向用户汇报；全部完成后才进入收尾审查"
    },
    {
      "id": "ep-execution-confirm-gate",
      "prompt": "（上一轮刚展示完计划全文，用户回复）嗯，看起来不错",
      "expected_output": "\"看起来不错\"是对计划的认可、不是执行指令——呈现执行摘要（计划路径、任务数与依赖拓扑、将创建的 worktree 分支名）并明确询问是否开始执行，获确认后才建 worktree 开工；仅当用户显式指示执行或经 writing-plans 交接确认时才视为已确认"
    },
    {
      "id": "ep-review-orchestration",
      "prompt": "（4 个任务全部完成，变更约 600 行，涉及 3 个模块）进入收尾审查",
      "expected_output": "预检基线、完整 diff 与契约来源；按常规四路 A/B/C/S 审查，容量不足分批而不丢维度；D 仅按明确要求或结构证据追加。报告经契约校验，高/中独立反驳，critic 核查证据覆盖，结果沿既有授权处置。"
    },
    {
      "id": "ep-spec-selfcheck-scope",
      "prompt": "（任务 T2 刚 commit）做 spec 自检时发现一处可能的空指针 bug",
      "expected_output": "spec 自检不猎 bug——该发现不在自检处置范围（那是收尾审查维度 A 的职责）；自检只核对本任务是否 over/under-building、契约是否与计划接口块一致；空指针疑点留给收尾审查独立验证"
    },
    {
      "id": "ep-contract-deviation-stops",
      "prompt": "（执行 T3 时）发现计划里 T3 要消费的函数签名与 T1 实际产出不一致，且 T4 也依赖该签名",
      "expected_output": "契约级偏差影响后续任务——停下向用户确认修正方向，不猜着改；小偏差（如路径笔误）才允许就地修正并记入总结"
    },
    {
      "id": "ep-final-task-deferred",
      "prompt": "（连续执行中）实施任务全部完成，计划只剩最终任务「合并与清理」未勾选",
      "expected_output": "最终任务不参与连续执行——先进入收尾审查（阶段 4）与审查处置（阶段 5），处置完成后才按计划最终任务步骤执行合并与清理（阶段 6）"
    },
    {
      "id": "ep-legacy-single-file-executable",
      "prompt": "执行 .spec-dev/2026-08-26-01-major-upgrade/plan/major-upgrade-plan.md（存量单文件计划，目录下无 tasks/ 子目录）",
      "expected_output": "格式嗅探走单文件分支：按原样一次性读取计划全文与同特性目录 spec 执行；不提示迁移、不生成 tasks/ 或 progress.yaml；恢复用复选框判读（与 git log 的 feat(TN) 对照，不一致以提交为准并报告）；资源登记就地编辑其最终任务内嵌台账行"
    },
    {
      "id": "ep-parallel-opt-in",
      "prompt": "分文件计划有独立声明，用户只说继续；没有已提交 parallel 模式。",
      "expected_output": "S07 默认串行，可提议但不 spawn implementer。"
    },
    {
      "id": "ep-switch-request",
      "prompt": "串行 T02 写了一半，用户要求后面转并发，随后会话中断。",
      "expected_output": "S26 先只提交 progress 中 request_id 与授权，不夹带半成品；恢复先完成 T02，再按已保存授权检查边界。"
    },
    {
      "id": "ep-pr-await",
      "prompt": "PR 已 ready 但没有真实合并证据。",
      "expected_output": "S19 awaiting_merge；最终任务 in_progress、roadmap in-progress，不锚定 sync_commit。"
    },
    {
      "id": "ep-parallel-global-review",
      "prompt": "串行完成两票后转并发，后续已全部集成。",
      "expected_output": "以最初 base 到集成 tip 的完整 diff，包含切换前票；共用含 S 的编排及独立 critic，保留矩阵验收与最终任务。"
    },
    {
      "id": "tdd-seam-s03",
      "prompt": "S03 显式声明直接开工：\nGIVEN spec 和本票明确指定同一 seam，用户已经批准\nWHEN 开始写失败测试",
      "expected_output": "使用该 seam，不再次询问测试落点。"
    },
    {
      "id": "tdd-seam-s04",
      "prompt": "S04 存量接口唯一提取：\nGIVEN 旧计划没有 seam 标签，但批准的精确接口与 Scenario 唯一指向 CLI 的退出码和标准输出\nWHEN 恢复执行",
      "expected_output": "在本次工作记录中记下来源及提取结果，沿该 CLI 测试；不重写旧计划形态或选择私有函数作为新落点。"
    },
    {
      "id": "tdd-seam-s06",
      "prompt": "S06 spec 与计划冲突：\nGIVEN spec 指定 HTTP seam，计划却要求私有方法断言\nWHEN 串行执行者检查本票输入",
      "expected_output": "报告冲突并走偏差处理，不默默选择其中一个继续。"
    },
    {
      "id": "tdd-seam-s13",
      "prompt": "S13 编译失败不充当红证据：\nGIVEN 新测试因拼写、编译或环境缺件而报错\nWHEN 收集红证据",
      "expected_output": "记录该故障并修复/阻塞相应验证，直到观察到预期行为失败；不把非零退出码直接当作有效红，也不将 SKIP 记作 PASS。"
    },
    {
      "id": "tdd-seam-s16",
      "prompt": "S16 三个私有函数不制造三份测试：\nGIVEN 两个获批行为由三个内部函数实现且均已有公共边界测试\nWHEN 完成自检和代码审查",
      "expected_output": "不因三个函数缺少直接单测判失败；如果错误路径 Scenario 漏测，仍明确报告该覆盖缺口。"
    },
    {
      "id": "tdd-seam-s17",
      "prompt": "S17 绿后发现重复代码：\nGIVEN 目标测试已绿，执行者发现与本票相关的重复辅助逻辑\nWHEN 结束本次红绿循环",
      "expected_output": "记录重构候选，按任务流程提交和自检；收尾接收候选，而不是在每个循环里自动去重或新增功能。"
    },
    {
      "id": "review-conformance-s01",
      "prompt": "S01 任务完成但缺少承诺的错误处理\nGIVEN 任务已标 completed，Scenario 要求无效输入返回校验错误，但 diff 仅实现正常路径\nWHEN S 审查整个特性",
      "expected_output": "报告缺失行为并引用 Scenario 和当前入口，不用任务完成状态证明交付。"
    },
    {
      "id": "review-conformance-s02",
      "prompt": "S02 增加了未获批准的对外行为\nGIVEN 获批范围只有读取接口，diff 新增可对外调用的删除接口，计划及批准变更均无依据\nWHEN S 核对行为与授权范围",
      "expected_output": "报告越界，引用获批范围原文及新增接口位置，说明已核对的任务与变更来源。"
    },
    {
      "id": "review-conformance-s03",
      "prompt": "S03 看似实现但语义相反\nGIVEN Scenario 要求空集合返回零，而实现返回空值，实施者声称通过\nWHEN S 对照入口和测试证据",
      "expected_output": "报告语义偏差并给出契约与实现证据，自报告不抵销发现。"
    },
    {
      "id": "review-conformance-s04",
      "prompt": "S04 内部辅助实现不构成额外交付\nGIVEN diff 增加为获批排序行为服务的内部辅助函数，未增加可观察行为或违背其他约束\nWHEN S 追溯其用途",
      "expected_output": "不因任务未逐项列出该函数而报越界；同样承认有可恢复批准记录且已按偏差流程修订契约的新增行为。"
    },
    {
      "id": "review-conformance-s05",
      "prompt": "S05 已取代条款与缺失实现位置\nGIVEN active spec 含一条已标 Superseded 的旧 Scenario 和一条现行缺失功能\nWHEN reviewer 产生 S 发现",
      "expected_output": "旧条款不产生缺失发现；现行缺失项引用现行原文，file/line 锚定应承载行为的现有入口，若完全没有代码锚则锚定契约行并说明不存在实现，不编造源码位置。"
    },
    {
      "id": "review-conformance-s13",
      "prompt": "S13 只有问题标签的候选\nGIVEN 候选只写「违反最佳实践、过度设计」，没有位置、规则或实际影响\nWHEN reviewer 筛选报告",
      "expected_output": "该候选不能作为已确认问题输出；证据充分的高置信发现仍按现有严重性分类报告。"
    },
    {
      "id": "review-conformance-s19",
      "prompt": "S19 新旧类别均有效\nGIVEN 字段完整的旧六类报告和新增 `Spec符合性` 报告\nWHEN 经现有 validate-output CLI 校验",
      "expected_output": "均通过；机器校验仅证明 schema 合法，S 引用是否真实及结论是否成立仍需语义复核。"
    },
    {
      "id": "review-conformance-s20",
      "prompt": "S20 未知类别或缺字段\nGIVEN 报告类别为未定义值，或缺 description，或 coverage_note 为空\nWHEN 经 CLI 校验",
      "expected_output": "退出码为 1，错误指出对应字段；按原输出契约单点规则补全一次，再失败由主线程接管，不能忽略报告。"
    },
    {
      "id": "review-conformance-s21",
      "prompt": "S21 没有发现的合法报告\nGIVEN findings 为空而 coverage_note 非空，说明实际覆盖与证据\nWHEN 校验并消费",
      "expected_output": "schema 接受，critic 按所列证据核查；不会为满足格式而制造 findings。"
    },
    {
      "id": "review-conformance-s06",
      "prompt": "S06 存在耦合泄漏时启用\nGIVEN diff 使三个调用方必须了解同一模块的内部表示，主线程能给出实际调用位置\nWHEN 选择审查维度",
      "expected_output": "追加 D，派发触发证据与本次受影响边界，复用共享判据，不扩为全仓巡检。"
    },
    {
      "id": "review-conformance-s07",
      "prompt": "S07 大 diff 不自动增加 D\nGIVEN 大 diff 属有界机械更新，无具体结构摩擦证据且用户未要求架构深化\nWHEN 安排五路审查",
      "expected_output": "不自动追加 D；仅有优化设想不构成阻塞性架构缺陷，也不授权 reviewer 写码。"
    },
    {
      "id": "review-conformance-s11",
      "prompt": "S11 错误基线或丢失契约指针\nGIVEN base ref 无效，或计划显式关联的 spec 路径不存在\nWHEN 准备扇出",
      "expected_output": "先查现有记录恢复；仍无法确认则不按未知范围派发并标明阻塞，不把 HEAD 或某个猜测文件用作替代。"
    },
    {
      "id": "review-conformance-s12",
      "prompt": "S12 空 diff 仍需交付对账\nGIVEN base/HEAD 均已核实且 diff 为空，计划仍有一项承诺行为\nWHEN 准备收尾",
      "expected_output": "如实报告无代码变更，跳过无效代码扇出，但按已有实现和证据继续必要覆盖核对及对账，不自动宣称全部 DELIVERED。"
    },
    {
      "id": "review-conformance-s14",
      "prompt": "S14 小变更也检查越界\nGIVEN 本次 diff 为 40 行且引入未批准的对外行为\nWHEN 选择小变更一路审查",
      "expected_output": "同一路明确执行 A 与 S，兼查 B/C 显著问题，并能报告越界，不能因不足 100 行省略 S。"
    },
    {
      "id": "review-conformance-s15",
      "prompt": "S15 常规与大变更的路数\nGIVEN 一份常规改动和另一份明确要求彻底审查的改动，均未触发 D\nWHEN 分别生成派发安排",
      "expected_output": "前者四路 A/B/C/S，后者五路 A/B质量/B简洁性/C/S；D 被触发时在相应安排之外追加一路。"
    },
    {
      "id": "review-conformance-s16",
      "prompt": "S16 容量只够两路同时运行\nGIVEN 已选定四路，平台同时只能运行两个 reviewer\nWHEN 派发与回收",
      "expected_output": "说明容量限制并分批完成四路，已返回报告可先复核，不把未启动维度报为完成；资源允许时仍同波扇出，不无故串行。"
    },
    {
      "id": "review-conformance-s17",
      "prompt": "S17 零发现且确有覆盖证据\nGIVEN reviewer 无 findings，但 coverage_note 列明已审文件、Scenario 和实际测试结果指针\nWHEN critic 核查",
      "expected_output": "不因 findings 为空报未审查；若测试证据缺失则单独记录测试覆盖缺口，不以审查覆盖代替测试覆盖。"
    },
    {
      "id": "review-conformance-s18",
      "prompt": "S18 S 漏查文件或场景\nGIVEN S 报告无发现但漏掉一个本次变更文件及其现行 Scenario\nWHEN critic 对照输入清单",
      "expected_output": "报告明确的覆盖缺口并交主线程补查；若补查形成实现偏差发现，按正常高/中独立反驳及处置路径处理，不能直接当 confirmed。"
    },
    {
      "id": "review-conformance-s22",
      "prompt": "S22 A 与 S 指向同一缺陷\nGIVEN A 报空值 bug，S 引用空集合 Scenario 报同一处语义偏差\nWHEN 主线程复核合并",
      "expected_output": "保留一条处置项及两份来源、契约依据；不因 category 不同重复要求修复，也不将不同根因仅因同一行而误合并。"
    },
    {
      "id": "review-conformance-s23",
      "prompt": "S23 覆盖补查发现新问题\nGIVEN 常规维度最多两轮已结束，critic 补查确认先前未扫的范围存在中严重性候选\nWHEN 主线程收口",
      "expected_output": "对候选进行独立反驳并按既有授权处置，不绕过复核直接记 confirmed；不重新无限循环整套扇出，未完成的覆盖或复核显式保留为未完成。"
    },
    {
      "id": "review-conformance-s28",
      "prompt": "S28 架构建议不破坏测试纪律\nGIVEN D 建议调整内部结构，公共 seam 与行为契约仍成立，最终全量属于原计划收尾\nWHEN 主线程决定后续处置",
      "expected_output": "按现有授权和 TDD 收尾纯重构规则记录保护证据，不增加逐私有函数测试，不把最终全量变为每票解锁门。"
    },
    {
      "id": "plan-decomposition-s05",
      "prompt": "已保存批次仍显示待验\nGIVEN T03 已提交、局部检查符合计划，整体测试仍因后续迁移未完成而失败。\nWHEN 保存 T03 进度。",
      "expected_output": "status=awaiting_verification、tests=pending_group，保留真实失败日志、implementation_commit；commit 完成锚仍为空，验证基线不变。"
    },
    {
      "id": "plan-decomposition-s06",
      "prompt": "意外局部失败阻塞\nGIVEN T03 的本应通过的独立行为保护测试失败，且不属于批准的组间暂时失败范围。\nWHEN 准备推进 T04。",
      "expected_output": "T03/整组 blocked，不自动扩大“预期失败”白名单；保存原因并先修复或走契约偏差。"
    },
    {
      "id": "plan-decomposition-s07",
      "prompt": "同一状态对组内外不同\nGIVEN T03 待验，组内 T04 依赖 T03，组外 T09 依赖组验证 T06，全部组外前置已完成。\nWHEN 计算 ready。",
      "expected_output": "T04 可以执行、T09 不可以，且不把 T03 改 completed 来解锁。"
    },
    {
      "id": "plan-decomposition-s08",
      "prompt": "外部前置未完不进组\nGIVEN 组末成员 T05 还依赖组外 pending 的 T02，虽然组首 T03 的依赖已满足。\nWHEN 选择下个任务。",
      "expected_output": "不激活整组；先按普通规则完成 T02，避免进入半成品分支后再等待外部工作。"
    },
    {
      "id": "plan-decomposition-s11",
      "prompt": "全组通过同次完成\nGIVEN T03—T05 待验，T06 在实现提交 V 对应业务树上运行全部组验证并通过。\nWHEN 接受整组。",
      "expected_output": "各成员保留自身 implementation_commit，成员/验证票的 commit 均锚定 V，全部 completed/tests=pass，组 completed，validated_commit=V；进度提交另有 SHA，后继仅在此检查点持久化后解锁。"
    },
    {
      "id": "plan-decomposition-s12",
      "prompt": "失败不移动已验证基线\nGIVEN 组基线 B 已通过，T06 在当前实现 tip 测试失败。\nWHEN 记录结果。",
      "expected_output": "T06/组 blocked，成员保持待验，validated_commit 仍为 B；报告失败命令及日志，不派组外任务。"
    },
    {
      "id": "plan-decomposition-s13",
      "prompt": "前序接口修正影响后继\nGIVEN T06 发现 T03 产出接口与批准接口不一致，T04/T05 已待验且消费该接口。\nWHEN 在 T03 授权范围内修复。",
      "expected_output": "先把 T03 置 in_progress、受影响后继置 blocked，保留旧提交/证据；修复后逐票核对仍有效的操作与局部检查，已成立的内容不重复改写，证据失效部分补验，再回待验并复跑全部组验证；越出批准契约则冻结等裁决。"
    },
    {
      "id": "plan-decomposition-s14",
      "prompt": "实现提交早于进度中断\nGIVEN T03 implementation commit 已存在，待验状态尚未提交，磁盘差异与本票能够唯一对应。\nWHEN 恢复。",
      "expected_output": "核对实际提交、局部检查和自检证据后补记待验；缺证据只补检查，不重做迁移，不凭提交信息直接认定通过。"
    },
    {
      "id": "plan-decomposition-s15",
      "prompt": "组通过后进度保存窗口\nGIVEN T06 全部验证通过且日志存在，分别在进度 rename 前、rename 后 commit 前、状态 commit 后中断。\nWHEN 恢复。",
      "expected_output": "前两种检查业务树与 V 一致后补原子完成/提交；第三种直接消费已完成事实，不重复测试或改写成员；证据缺失/树变化则补验，未知差异阻塞。"
    },
    {
      "id": "plan-decomposition-s16",
      "prompt": "工作区或证据不可信\nGIVEN 组工作区丢失、提交不可解析、日志哈希不符或出现无法解释的业务变更。\nWHEN plan-state 或恢复入口核对。",
      "expected_output": "给出具体不一致、ready 为空，保留已有现场；不新建空进度、不清理未接受产物。"
    },
    {
      "id": "plan-decomposition-s17",
      "prompt": "旧能力不能静默降级\nGIVEN 含组计划声明 format_version=2，但旧 CLI 不认识 plan-state，或新 CLI 收到未知版本。\nWHEN 执行入口预检。",
      "expected_output": "不将其作为普通 v1 计划继续，不写业务/状态；明确缺少支持并保留原文件。此保证针对遵守入口的执行者，不声称能约束忽略指令的旧客户端。"
    },
    {
      "id": "plan-decomposition-s18",
      "prompt": "无组存量继续原样读取\nGIVEN v1 普通/并发分文件计划或旧单文件计划。\nWHEN 新执行入口载入。",
      "expected_output": "使用原有对应分支，保留旧状态及授权，不强制转换为 v2、不添加组。"
    },
    {
      "id": "plan-decomposition-s28",
      "prompt": "组内切换请求等组完成\nGIVEN 用户在 T04 期间请求转并发，T03 已待验。\nWHEN T04 提交待验且随后会话中断。",
      "expected_output": "授权请求保存在 notes，mode 不变；恢复 T05/T06，整组完成后沿已保存授权检查原准入，不把 T04 待验作为可切换完成边界。"
    },
    {
      "id": "plan-decomposition-s29",
      "prompt": "普通票不继承待验解锁特权\nGIVEN 无组 T01 返回 ready 但尚未集成，T02 依赖它。\nWHEN 新执行器调度。",
      "expected_output": "T02 仍等待 completed；手写 awaiting_verification 于无组票被拒绝，原 Git 接收和合并冲突规则仍生效。"
    }
  ]
}
''')
]
for name, old, new in changes:
    p=Path(name); text=p.read_text()
    assert text.count(old)==1,(name,'anchor drift')
    p.write_text(text.replace(old,new,1))

from pathlib import Path
changes = [
('skills/executing-plans-parallel/evals/evals.json', r'''{
  "skill_name": "executing-plans-parallel",
  "evals": [
    {
      "id": "implementer-binding",
      "prompt": "任务指针有效，但工具当前 cwd 是主线程目录且子 worktree HEAD 不等于 base_commit。",
      "expected_output": "按 S13 首次写前核对；无法修复绑定则 blocked，主线程业务文件零改动。"
    },
    {
      "id": "implementer-evidence",
      "prompt": "绿测试通过，但红测试只因运行环境缺 Node 失败；准备报 ready。",
      "expected_output": "按 S14 拒绝把环境失败作为预期红；保存真实证据，blocked，禁止伪造结果。"
    },
    {
      "id": "implementer-resource",
      "prompt": "需要创建 claim 专用数据库，已发资源请求但尚未收到主线程持久化确认。",
      "expected_output": "按 S05 零创建，等待确认；失联时阻塞资源操作，保留请求指针。"
    },
    {
      "id": "parallel-s01",
      "scenario": "S01 普通计划保持串行可执行",
      "prompt": "没有 parallel 块的三任务分文件计划。\n校验并执行。",
      "expected_output": "原 plan-index 校验通过、串行执行，不要求迁移或新增字段。"
    },
    {
      "id": "parallel-s02",
      "scenario": "S02 并发声明损坏不可交付",
      "prompt": "parallel 块引用 T99 或含绝对路径、父目录逃逸、重复任务键。\n运行 plan-index。",
      "expected_output": "exit 1 并定位错误；不忽略损坏声明或派工。"
    },
    {
      "id": "parallel-s03",
      "scenario": "S03 恢复中间合并而不重复应用",
      "prompt": "T01 的实现提交已成为集成 HEAD 的祖先，但 completed 状态提交前会话中断。\n主线程恢复并核对认领、提交与实际文件。",
      "expected_output": "不重复合并；补跑未有证据的集成验证，通过后补进度提交，再解锁后继。"
    },
    {
      "id": "parallel-s04",
      "scenario": "S04 不一致恢复停止受影响任务",
      "prompt": "某认领的 worktree、任务文件或实现提交缺失，或出现无法解释的集成提交。\n恢复。",
      "expected_output": "报告具体不一致并冻结该任务及依赖闭包；不猜 completed、不重派仍可能存活的 agent。"
    },
    {
      "id": "parallel-s05",
      "scenario": "S05 资源消息中断不留未知资源",
      "prompt": "implementer 请求新测试数据库，但主线程确认消息尚未收到。\n会话中断。",
      "expected_output": "implementer 没有创建数据库；若主线程已预登记，恢复时查询是否存在再继续或记未创建，不删除同名他人资源。"
    },
    {
      "id": "parallel-s06",
      "scenario": "S06 正式登记与文档集合一致",
      "prompt": "新 skill、openai.yaml、evals 和 schema 已就位。\n运行包校验与文档集合检查。",
      "expected_output": "marketplace 不遗漏/悬空、两份 README 列出全部 14 个 skill 和 7 个 trigger skill；schema 分类与磁盘一致。"
    },
    {
      "id": "parallel-s28",
      "scenario": "S28 首次并发前展示两类执行者配置",
      "prompt": "已选择并发，主线程实际配置可核实，实现子代理按工具语义继承或显式配置，但尚未派发。\n进入 executing-plans-parallel。",
      "expected_output": "首次执行动作前展示带 👤/🤖 的「角色｜模型｜思考强度」三列表格，列出主线程模型/强度，以及子代理计划使用的模型/强度或继承关系，表格下说明来源与未启动状态；不把尚未启动的子代理写成已经核实运行。所有子代理配置相同时可合并一行，不硬编码本次开发会话的模型名称。"
    },
    {
      "id": "parallel-s29",
      "scenario": "S29 不同配置与平台未暴露字段",
      "prompt": "两个任务组采用不同的已授权模型配置，且平台未暴露主线程的思考强度。\n输出执行声明。",
      "expected_output": "按任务组列出不同配置，主线程强度写未知并注明来源限制；不猜为 high，也不把主线程强度套给未声明继承的子代理。没有相关用户硬约束时该未知项不单独阻塞流程。"
    },
    {
      "id": "parallel-s30",
      "scenario": "S30 恢复或配置变化时重新声明",
      "prompt": "前次配置声明已保存，中断后新主线程配置发生变化，或恢复时仍有原配置的 implementer 存活。\n新主线程准备恢复编排，或为下一批任务改变派发配置。",
      "expected_output": "恢复前重新展示当前主线程、原存活 implementer 及拟派发者的各自配置与可核实程度，不假定它们全部跟随新主线程；新增配置声明关联到后续认领，旧认领保留原声明。仅做声明不会重派存活 implementer，也不会产生新的模型变更授权。"
    },
    {
      "id": "parallel-s07",
      "scenario": "S07 普通继续不自动并发",
      "prompt": "导航表有两条独立链、声明有效，用户只说“继续计划”。\nexecuting-plans 载入。",
      "expected_output": "不把该话语视作并发选择；沿用已记录的模式，首次模式未定时默认串行并可提议。"
    },
    {
      "id": "parallel-s08",
      "scenario": "S08 显式选择后的真实并发",
      "prompt": "用户已选并发、T01/T02 均依赖尚未执行的 T00、写集合不相交且资源可隔离。\n运行编排。",
      "expected_output": "入口接受并发选择，先由主线程完成 T00 再派两票；两票在不同 worktree 有重叠的执行时间，最终由主线程逐个核验并集成；最终任务不被提前派发。"
    },
    {
      "id": "parallel-s23",
      "scenario": "S23 串行完成两票后切换",
      "prompt": "T00/T01 已完成并提交，用户在执行 T02 期间要求后面转并发，剩余 T03/T04 无相互依赖且声明有效。\nT02 完成并保存、主线程执行切换。",
      "expected_output": "复用当前隔离工作区，从最新已验证代码所在的检查点创建 T03/T04 的 worktree；T00—T02 不重做，原始 base_commit 不替换成切换点，最终审查仍覆盖切换前后的所有代码。两票均仅在切换检查点提交后派发。"
    },
    {
      "id": "parallel-s24",
      "scenario": "S24 切换条件不足继续串行",
      "prompt": "用户要求切换，但剩余任务只有依赖链，或没有合法并发声明，或资源不能隔离。\n主线程检查切换资格。",
      "expected_output": "保留串行模式和已有进度，说明不满足的条件，不创建 implementer；缺声明时不借切换预读全部任务并自动补写，需单独修订计划并通过原审查/校验后重新评估。不重复询问已经给出的并发选择。"
    },
    {
      "id": "parallel-s25",
      "scenario": "S25 切换检查点前后中断",
      "prompt": "串行检查点 H 已保存，主线程取得锁并准备并发状态。\n分别在 progress 原子写入前、写入后但提交前、检查点提交后但派发前中断。",
      "expected_output": "前两种没有子代理派发，已提交模式仍为串行；恢复核对授权和准备状态后可完成切换提交，或仅撤销本次准备改动并保持串行。第三种按已提交并发检查点恢复，不重复 T00 或已完成任务；若已进入后续认领窗口，则继续使用 S03/S04/S12 的既有核验规则。"
    },
    {
      "id": "parallel-s26",
      "scenario": "S26 切换请求先于当前任务完成时中断",
      "prompt": "用户在 T02 执行中要求转并发，主线程已将带 request_id 的授权原话或可恢复引用记入 notes 并单独提交，T02 尚未完成。\n新会话收到“继续”。",
      "expected_output": "从已有状态恢复 T02，不把它拆给子代理；T02 完成并提交后沿已保存授权重新检查切换资格，不丢失请求、不重复询问同一授权。若中断早于请求持久化且无法从可用会话记录核实授权，则保持串行，不凭空推定用户已选择并发。"
    },
    {
      "id": "parallel-s27",
      "scenario": "S27 已认领但派发回执缺失时中断",
      "prompt": "模式切换已提交，T03 claim 已持久化，主线程在调用 spawn 与记录 agent_id 之间中断。\n恢复执行。",
      "expected_output": "先确认旧编排 owner 已停止并取得特性锁、持久化新的 execution.owner，再按 claim_key、任务分支/worktree 和平台可见执行者核对是否已派发；找到身份可核验且能恢复通信的存活 implementer 就保留原 claim 恢复联系，不再 spawn；能够证明未派发时继续该认领的派发，证明原 implementer 已终止时核验其遗留代码/证据后再接管或创建新尝试。旧 owner、implementer 身份或通信能力无法核实时冻结相关恢复并报告，不把“agent_id 为空”当成未派发证明。"
    },
    {
      "id": "parallel-s09",
      "scenario": "S09 同写锁文件的票不并发",
      "prompt": "两票业务文件不同但都声明 package-lock.json，或都使用同一个不可隔离测试库。\n选择派发集合。",
      "expected_output": "不同时派发这两票；有其他独立票可继续，不伪造额外依赖或认定整份计划无效。"
    },
    {
      "id": "parallel-s10",
      "scenario": "S10 路径越界结果拒收",
      "prompt": "实际 diff 含未声明文件、rename 的未授权旧路径或符号链接逃逸。\n主线程核验结果。",
      "expected_output": "不集成、不置 completed，报告路径与 claim；子代理未跟踪和未提交改动也被核对。"
    },
    {
      "id": "parallel-s11",
      "scenario": "S11 两主线程重复启动",
      "prompt": "主线程 A 持有同一特性锁且 T01 已认领。\nB 从另一个 worktree 的不同绝对计划路径启动相同仓库根相对特性。",
      "expected_output": "B 无法取得锁，不写 progress、不派发 T01；报告现有 owner。"
    },
    {
      "id": "parallel-s12",
      "scenario": "S12 旧结果重复回报",
      "prompt": "T01 已接受某 claim，或失败后已生成新 claim。\n收到重复或过期 claim 的完成消息。",
      "expected_output": "已接受同一结果幂等忽略，旧 claim 不更新新尝试状态。"
    },
    {
      "id": "parallel-s13",
      "scenario": "S13 基线错误先停",
      "prompt": "工具继承了主线程 cwd，或 worktree HEAD 不是派发基线。\nimplementer 准备首次写文件。",
      "expected_output": "先切到正确 worktree 并复核；无法建立绑定则 blocked，主线程工作区零业务改动。"
    },
    {
      "id": "parallel-s14",
      "scenario": "S14 红绿与自检证据齐全",
      "prompt": "一票有可复现的失败测试。\nimplementer 报成功。",
      "expected_output": "报告含红测试的预期失败证据、绿测试通过证据、实现提交及两项自检结果；编译/环境故障不能冒充有效红测试。"
    },
    {
      "id": "parallel-s22",
      "scenario": "S22 已授权免测或空基线范围保留串行语义",
      "prompt": "某票已获用户授权 TDD 例外，或适用的计划基线测试范围显式为空。\n生成并发声明或准备派工。",
      "expected_output": "该票不进入 implementer 并发集合，由主线程排空在途票后串行执行，分别记录授权例外或空基线原因，不伪造 tests: pass；其他具备完整测试条件的票仍可并发。空基线只豁免基线测试，本身不构成票内 TDD 例外。"
    },
    {
      "id": "parallel-s15",
      "scenario": "S15 实现完成不解锁依赖",
      "prompt": "T01 已返回成功但尚未集成，T03 依赖 T01。\n主线程重算 ready。",
      "expected_output": "T03 不进入 ready；集成验证通过并持久化 completed 后才进入。"
    },
    {
      "id": "parallel-s16",
      "scenario": "S16 合并冲突或集成测试失败",
      "prompt": "两个独立票在不同基线完成，当前集成 tip 已变化。\n后一票合并冲突或合并后的相关测试失败。",
      "expected_output": "冲突交用户裁决，不自动选 ours/theirs；失败票及后继冻结，不标通过，其他票只能在已验证基线上继续。"
    },
    {
      "id": "parallel-s17",
      "scenario": "S17 探索发现接口偏差",
      "prompt": "T02 的只读探索发现计划接口与现行 spec 不一致。\n返回带 file:line 的笔记。",
      "expected_output": "主线程冻结相关票并按契约偏差处理，不把探索意见当作隐式改约授权；不重复派发已覆盖的同一事实调查。"
    },
    {
      "id": "parallel-s18",
      "scenario": "S18 零差异或无发布授权",
      "prompt": "特性分支尚无差异，或会话尚未授权发布远端。\n准备 PR 通道。",
      "expected_output": "本地继续获准工作并准备 PR 文案；不创建空提交，不 push/create PR；需要发布时复用已有授权，缺失才请求。"
    },
    {
      "id": "parallel-s19",
      "scenario": "S19 PR ready 尚未合并",
      "prompt": "审查、验收和对账通过，PR 已 ready 但仍 open。\n交付状态回写。",
      "expected_output": "delivery.state 为 awaiting_merge，最终任务与 roadmap 不记 delivered，不声称已合并或完成 sync_commit。"
    },
    {
      "id": "parallel-s20",
      "scenario": "S20 PR 实际合并后恢复收尾",
      "prompt": "PR 后续已合并且可获得目标分支上的实际提交。\n用户继续收尾。",
      "expected_output": "核对实际合并内容与已验收树，必要时补验证，再执行取代回写/清理/sync_commit/roadmap delivered；若写回受保护目标，走获准的后续 PR，不绕过保护。"
    },
    {
      "id": "parallel-s21",
      "scenario": "S21 全票绿仍执行全局收尾",
      "prompt": "所有 implementer 测试通过并已集成。\n进入收尾。",
      "expected_output": "审查范围为整个特性相对 base 的 diff，验收与最终全量检查照常执行；只有全部门通过才进入相应交付出口。"
    },
    {
      "id": "parallel-blocked-live-writer",
      "scenario": "S04/S27 运行中阻塞与失联写者",
      "prompt": "implementer 回报合法 blocked，工作区有半成品；另一个执行者没有回报但可能仍在写入。主线程准备重试或接管。",
      "expected_output": "主线程持锁保存 blocked 原因、claim 与证据，冻结依赖闭包，独立任务可继续。blocked 不当格式失败改 ready；先核验旧执行者及写入进程停止，才可新认领或接管。同一存活执行者可恢复原 claim；未知状态阻塞且零重复 spawn，不靠超时断定终止，遵循既有一次重试上限。"
    },
    {
      "id": "tdd-seam-s07",
      "prompt": "S07 implementer 不越权修正：\nGIVEN implementer 发现落点缺失，或纠正落点需要改写集合外文件\nWHEN 检查任务可执行性",
      "expected_output": "回报 `blocked` 及来源/缺口，由主线程处理；不自行发问、修改 plan、扩大 writes 或伪造红绿结果；其他独立票按原并发规则处理。"
    },
    {
      "id": "review-conformance-s26",
      "prompt": "S26 中途切换并发后的全量审查\nGIVEN T01 串行完成后切换并发，剩余票已集成\nWHEN 全局审查接收输入",
      "expected_output": "diff 覆盖最初 base 到当前集成 HEAD，包括 T01；共用包含 S 的完整编排与 critic，implementer 自检不抵销任何维度。"
    }
  ]
}
''', r'''{
  "skill_name": "executing-plans-parallel",
  "evals": [
    {
      "id": "implementer-binding",
      "prompt": "任务指针有效，但工具当前 cwd 是主线程目录且子 worktree HEAD 不等于 base_commit。",
      "expected_output": "按 S13 首次写前核对；无法修复绑定则 blocked，主线程业务文件零改动。"
    },
    {
      "id": "implementer-evidence",
      "prompt": "绿测试通过，但红测试只因运行环境缺 Node 失败；准备报 ready。",
      "expected_output": "按 S14 拒绝把环境失败作为预期红；保存真实证据，blocked，禁止伪造结果。"
    },
    {
      "id": "implementer-resource",
      "prompt": "需要创建 claim 专用数据库，已发资源请求但尚未收到主线程持久化确认。",
      "expected_output": "按 S05 零创建，等待确认；失联时阻塞资源操作，保留请求指针。"
    },
    {
      "id": "parallel-s01",
      "scenario": "S01 普通计划保持串行可执行",
      "prompt": "没有 parallel 块的三任务分文件计划。\n校验并执行。",
      "expected_output": "原 plan-index 校验通过、串行执行，不要求迁移或新增字段。"
    },
    {
      "id": "parallel-s02",
      "scenario": "S02 并发声明损坏不可交付",
      "prompt": "parallel 块引用 T99 或含绝对路径、父目录逃逸、重复任务键。\n运行 plan-index。",
      "expected_output": "exit 1 并定位错误；不忽略损坏声明或派工。"
    },
    {
      "id": "parallel-s03",
      "scenario": "S03 恢复中间合并而不重复应用",
      "prompt": "T01 的实现提交已成为集成 HEAD 的祖先，但 completed 状态提交前会话中断。\n主线程恢复并核对认领、提交与实际文件。",
      "expected_output": "不重复合并；补跑未有证据的集成验证，通过后补进度提交，再解锁后继。"
    },
    {
      "id": "parallel-s04",
      "scenario": "S04 不一致恢复停止受影响任务",
      "prompt": "某认领的 worktree、任务文件或实现提交缺失，或出现无法解释的集成提交。\n恢复。",
      "expected_output": "报告具体不一致并冻结该任务及依赖闭包；不猜 completed、不重派仍可能存活的 agent。"
    },
    {
      "id": "parallel-s05",
      "scenario": "S05 资源消息中断不留未知资源",
      "prompt": "implementer 请求新测试数据库，但主线程确认消息尚未收到。\n会话中断。",
      "expected_output": "implementer 没有创建数据库；若主线程已预登记，恢复时查询是否存在再继续或记未创建，不删除同名他人资源。"
    },
    {
      "id": "parallel-s06",
      "scenario": "S06 正式登记与文档集合一致",
      "prompt": "新 skill、openai.yaml、evals 和 schema 已就位。\n运行包校验与文档集合检查。",
      "expected_output": "marketplace 不遗漏/悬空、两份 README 列出全部 14 个 skill 和 7 个 trigger skill；schema 分类与磁盘一致。"
    },
    {
      "id": "parallel-s28",
      "scenario": "S28 首次并发前展示两类执行者配置",
      "prompt": "已选择并发，主线程实际配置可核实，实现子代理按工具语义继承或显式配置，但尚未派发。\n进入 executing-plans-parallel。",
      "expected_output": "首次执行动作前展示带 👤/🤖 的「角色｜模型｜思考强度」三列表格，列出主线程模型/强度，以及子代理计划使用的模型/强度或继承关系，表格下说明来源与未启动状态；不把尚未启动的子代理写成已经核实运行。所有子代理配置相同时可合并一行，不硬编码本次开发会话的模型名称。"
    },
    {
      "id": "parallel-s29",
      "scenario": "S29 不同配置与平台未暴露字段",
      "prompt": "两个任务组采用不同的已授权模型配置，且平台未暴露主线程的思考强度。\n输出执行声明。",
      "expected_output": "按任务组列出不同配置，主线程强度写未知并注明来源限制；不猜为 high，也不把主线程强度套给未声明继承的子代理。没有相关用户硬约束时该未知项不单独阻塞流程。"
    },
    {
      "id": "parallel-s30",
      "scenario": "S30 恢复或配置变化时重新声明",
      "prompt": "前次配置声明已保存，中断后新主线程配置发生变化，或恢复时仍有原配置的 implementer 存活。\n新主线程准备恢复编排，或为下一批任务改变派发配置。",
      "expected_output": "恢复前重新展示当前主线程、原存活 implementer 及拟派发者的各自配置与可核实程度，不假定它们全部跟随新主线程；新增配置声明关联到后续认领，旧认领保留原声明。仅做声明不会重派存活 implementer，也不会产生新的模型变更授权。"
    },
    {
      "id": "parallel-s07",
      "scenario": "S07 普通继续不自动并发",
      "prompt": "导航表有两条独立链、声明有效，用户只说“继续计划”。\nexecuting-plans 载入。",
      "expected_output": "不把该话语视作并发选择；沿用已记录的模式，首次模式未定时默认串行并可提议。"
    },
    {
      "id": "parallel-s08",
      "scenario": "S08 显式选择后的真实并发",
      "prompt": "用户已选并发、T01/T02 均依赖尚未执行的 T00、写集合不相交且资源可隔离。\n运行编排。",
      "expected_output": "入口接受并发选择，先由主线程完成 T00 再派两票；两票在不同 worktree 有重叠的执行时间，最终由主线程逐个核验并集成；最终任务不被提前派发。"
    },
    {
      "id": "parallel-s23",
      "scenario": "S23 串行完成两票后切换",
      "prompt": "T00/T01 已完成并提交，用户在执行 T02 期间要求后面转并发，剩余 T03/T04 无相互依赖且声明有效。\nT02 完成并保存、主线程执行切换。",
      "expected_output": "复用当前隔离工作区，从最新已验证代码所在的检查点创建 T03/T04 的 worktree；T00—T02 不重做，原始 base_commit 不替换成切换点，最终审查仍覆盖切换前后的所有代码。两票均仅在切换检查点提交后派发。"
    },
    {
      "id": "parallel-s24",
      "scenario": "S24 切换条件不足继续串行",
      "prompt": "用户要求切换，但剩余任务只有依赖链，或没有合法并发声明，或资源不能隔离。\n主线程检查切换资格。",
      "expected_output": "保留串行模式和已有进度，说明不满足的条件，不创建 implementer；缺声明时不借切换预读全部任务并自动补写，需单独修订计划并通过原审查/校验后重新评估。不重复询问已经给出的并发选择。"
    },
    {
      "id": "parallel-s25",
      "scenario": "S25 切换检查点前后中断",
      "prompt": "串行检查点 H 已保存，主线程取得锁并准备并发状态。\n分别在 progress 原子写入前、写入后但提交前、检查点提交后但派发前中断。",
      "expected_output": "前两种没有子代理派发，已提交模式仍为串行；恢复核对授权和准备状态后可完成切换提交，或仅撤销本次准备改动并保持串行。第三种按已提交并发检查点恢复，不重复 T00 或已完成任务；若已进入后续认领窗口，则继续使用 S03/S04/S12 的既有核验规则。"
    },
    {
      "id": "parallel-s26",
      "scenario": "S26 切换请求先于当前任务完成时中断",
      "prompt": "用户在 T02 执行中要求转并发，主线程已将带 request_id 的授权原话或可恢复引用记入 notes 并单独提交，T02 尚未完成。\n新会话收到“继续”。",
      "expected_output": "从已有状态恢复 T02，不把它拆给子代理；T02 完成并提交后沿已保存授权重新检查切换资格，不丢失请求、不重复询问同一授权。若中断早于请求持久化且无法从可用会话记录核实授权，则保持串行，不凭空推定用户已选择并发。"
    },
    {
      "id": "parallel-s27",
      "scenario": "S27 已认领但派发回执缺失时中断",
      "prompt": "模式切换已提交，T03 claim 已持久化，主线程在调用 spawn 与记录 agent_id 之间中断。\n恢复执行。",
      "expected_output": "先确认旧编排 owner 已停止并取得特性锁、持久化新的 execution.owner，再按 claim_key、任务分支/worktree 和平台可见执行者核对是否已派发；找到身份可核验且能恢复通信的存活 implementer 就保留原 claim 恢复联系，不再 spawn；能够证明未派发时继续该认领的派发，证明原 implementer 已终止时核验其遗留代码/证据后再接管或创建新尝试。旧 owner、implementer 身份或通信能力无法核实时冻结相关恢复并报告，不把“agent_id 为空”当成未派发证明。"
    },
    {
      "id": "parallel-s09",
      "scenario": "S09 同写锁文件的票不并发",
      "prompt": "两票业务文件不同但都声明 package-lock.json，或都使用同一个不可隔离测试库。\n选择派发集合。",
      "expected_output": "不同时派发这两票；有其他独立票可继续，不伪造额外依赖或认定整份计划无效。"
    },
    {
      "id": "parallel-s10",
      "scenario": "S10 路径越界结果拒收",
      "prompt": "实际 diff 含未声明文件、rename 的未授权旧路径或符号链接逃逸。\n主线程核验结果。",
      "expected_output": "不集成、不置 completed，报告路径与 claim；子代理未跟踪和未提交改动也被核对。"
    },
    {
      "id": "parallel-s11",
      "scenario": "S11 两主线程重复启动",
      "prompt": "主线程 A 持有同一特性锁且 T01 已认领。\nB 从另一个 worktree 的不同绝对计划路径启动相同仓库根相对特性。",
      "expected_output": "B 无法取得锁，不写 progress、不派发 T01；报告现有 owner。"
    },
    {
      "id": "parallel-s12",
      "scenario": "S12 旧结果重复回报",
      "prompt": "T01 已接受某 claim，或失败后已生成新 claim。\n收到重复或过期 claim 的完成消息。",
      "expected_output": "已接受同一结果幂等忽略，旧 claim 不更新新尝试状态。"
    },
    {
      "id": "parallel-s13",
      "scenario": "S13 基线错误先停",
      "prompt": "工具继承了主线程 cwd，或 worktree HEAD 不是派发基线。\nimplementer 准备首次写文件。",
      "expected_output": "先切到正确 worktree 并复核；无法建立绑定则 blocked，主线程工作区零业务改动。"
    },
    {
      "id": "parallel-s14",
      "scenario": "S14 红绿与自检证据齐全",
      "prompt": "一票有可复现的失败测试。\nimplementer 报成功。",
      "expected_output": "报告含红测试的预期失败证据、绿测试通过证据、实现提交及两项自检结果；编译/环境故障不能冒充有效红测试。"
    },
    {
      "id": "parallel-s22",
      "scenario": "S22 已授权免测或空基线范围保留串行语义",
      "prompt": "某票已获用户授权 TDD 例外，或适用的计划基线测试范围显式为空。\n生成并发声明或准备派工。",
      "expected_output": "该票不进入 implementer 并发集合，由主线程排空在途票后串行执行，分别记录授权例外或空基线原因，不伪造 tests: pass；其他具备完整测试条件的票仍可并发。空基线只豁免基线测试，本身不构成票内 TDD 例外。"
    },
    {
      "id": "parallel-s15",
      "scenario": "S15 实现完成不解锁依赖",
      "prompt": "T01 已返回成功但尚未集成，T03 依赖 T01。\n主线程重算 ready。",
      "expected_output": "T03 不进入 ready；集成验证通过并持久化 completed 后才进入。"
    },
    {
      "id": "parallel-s16",
      "scenario": "S16 合并冲突或集成测试失败",
      "prompt": "两个独立票在不同基线完成，当前集成 tip 已变化。\n后一票合并冲突或合并后的相关测试失败。",
      "expected_output": "冲突交用户裁决，不自动选 ours/theirs；失败票及后继冻结，不标通过，其他票只能在已验证基线上继续。"
    },
    {
      "id": "parallel-s17",
      "scenario": "S17 探索发现接口偏差",
      "prompt": "T02 的只读探索发现计划接口与现行 spec 不一致。\n返回带 file:line 的笔记。",
      "expected_output": "主线程冻结相关票并按契约偏差处理，不把探索意见当作隐式改约授权；不重复派发已覆盖的同一事实调查。"
    },
    {
      "id": "parallel-s18",
      "scenario": "S18 零差异或无发布授权",
      "prompt": "特性分支尚无差异，或会话尚未授权发布远端。\n准备 PR 通道。",
      "expected_output": "本地继续获准工作并准备 PR 文案；不创建空提交，不 push/create PR；需要发布时复用已有授权，缺失才请求。"
    },
    {
      "id": "parallel-s19",
      "scenario": "S19 PR ready 尚未合并",
      "prompt": "审查、验收和对账通过，PR 已 ready 但仍 open。\n交付状态回写。",
      "expected_output": "delivery.state 为 awaiting_merge，最终任务与 roadmap 不记 delivered，不声称已合并或完成 sync_commit。"
    },
    {
      "id": "parallel-s20",
      "scenario": "S20 PR 实际合并后恢复收尾",
      "prompt": "PR 后续已合并且可获得目标分支上的实际提交。\n用户继续收尾。",
      "expected_output": "核对实际合并内容与已验收树，必要时补验证，再执行取代回写/清理/sync_commit/roadmap delivered；若写回受保护目标，走获准的后续 PR，不绕过保护。"
    },
    {
      "id": "parallel-s21",
      "scenario": "S21 全票绿仍执行全局收尾",
      "prompt": "所有 implementer 测试通过并已集成。\n进入收尾。",
      "expected_output": "审查范围为整个特性相对 base 的 diff，验收与最终全量检查照常执行；只有全部门通过才进入相应交付出口。"
    },
    {
      "id": "parallel-blocked-live-writer",
      "scenario": "S04/S27 运行中阻塞与失联写者",
      "prompt": "implementer 回报合法 blocked，工作区有半成品；另一个执行者没有回报但可能仍在写入。主线程准备重试或接管。",
      "expected_output": "主线程持锁保存 blocked 原因、claim 与证据，冻结依赖闭包，独立任务可继续。blocked 不当格式失败改 ready；先核验旧执行者及写入进程停止，才可新认领或接管。同一存活执行者可恢复原 claim；未知状态阻塞且零重复 spawn，不靠超时断定终止，遵循既有一次重试上限。"
    },
    {
      "id": "tdd-seam-s07",
      "prompt": "S07 implementer 不越权修正：\nGIVEN implementer 发现落点缺失，或纠正落点需要改写集合外文件\nWHEN 检查任务可执行性",
      "expected_output": "回报 `blocked` 及来源/缺口，由主线程处理；不自行发问、修改 plan、扩大 writes 或伪造红绿结果；其他独立票按原并发规则处理。"
    },
    {
      "id": "review-conformance-s26",
      "prompt": "S26 中途切换并发后的全量审查\nGIVEN T01 串行完成后切换并发，剩余票已集成\nWHEN 全局审查接收输入",
      "expected_output": "diff 覆盖最初 base 到当前集成 HEAD，包括 T01；共用包含 S 的完整编排与 critic，implementer 自检不抵销任何维度。"
    },
    {
      "id": "plan-decomposition-s09",
      "prompt": "并发模式中遇到集成组\nGIVEN mode=parallel 且两张普通票仍在执行。\nWHEN 下一阶段准备进入 G01。",
      "expected_output": "先停止新派发，核验并收拢在途票及回报，完成所需合入到已验证基线；没有未知执行者/未接收实现后才激活 G01；期间无其他新合入、派发或模式切换。"
    },
    {
      "id": "plan-decomposition-s10",
      "prompt": "第二主线程不能进组\nGIVEN A 已持有 G01 所属特性锁，B 从同仓库另一 worktree 恢复。\nWHEN B 准备更新进度。",
      "expected_output": "B 无写权，旧 owner 是否停止无法核实就阻塞；不按超时、空 agent 列表或锁路径不同猜测接管成功。"
    }
  ]
}
''')
]
for name, old, new in changes:
    p=Path(name); text=p.read_text()
    assert text.count(old)==1,(name,'anchor drift')
    p.write_text(text.replace(old,new,1))
``````

**步骤 2：运行确认失败**

``````python
import hashlib,json,subprocess,time,os,signal
from pathlib import Path
root=Path.cwd().resolve()
phase=os.environ.get("PD_PHASE","red")
assert phase in ("red","green")
out=root/'.spec-dev/2026-09-07-01-plan-decomposition/execution/serial/'/'T04'/phase
out.mkdir(parents=True,exist_ok=False)
files=['skills/executing-plans/SKILL.md', 'skills/executing-plans-parallel/SKILL.md']
cases=[{'id': 'plan-decomposition-s05', 'prompt': '已保存批次仍显示待验\nGIVEN T03 已提交、局部检查符合计划，整体测试仍因后续迁移未完成而失败。\nWHEN 保存 T03 进度。', 'expected_output': 'status=awaiting_verification、tests=pending_group，保留真实失败日志、implementation_commit；commit 完成锚仍为空，验证基线不变。'}, {'id': 'plan-decomposition-s06', 'prompt': '意外局部失败阻塞\nGIVEN T03 的本应通过的独立行为保护测试失败，且不属于批准的组间暂时失败范围。\nWHEN 准备推进 T04。', 'expected_output': 'T03/整组 blocked，不自动扩大“预期失败”白名单；保存原因并先修复或走契约偏差。'}, {'id': 'plan-decomposition-s07', 'prompt': '同一状态对组内外不同\nGIVEN T03 待验，组内 T04 依赖 T03，组外 T09 依赖组验证 T06，全部组外前置已完成。\nWHEN 计算 ready。', 'expected_output': 'T04 可以执行、T09 不可以，且不把 T03 改 completed 来解锁。'}, {'id': 'plan-decomposition-s08', 'prompt': '外部前置未完不进组\nGIVEN 组末成员 T05 还依赖组外 pending 的 T02，虽然组首 T03 的依赖已满足。\nWHEN 选择下个任务。', 'expected_output': '不激活整组；先按普通规则完成 T02，避免进入半成品分支后再等待外部工作。'}, {'id': 'plan-decomposition-s17', 'prompt': '旧能力不能静默降级\nGIVEN 含组计划声明 format_version=2，但旧 CLI 不认识 plan-state，或新 CLI 收到未知版本。\nWHEN 执行入口预检。', 'expected_output': '不将其作为普通 v1 计划继续，不写业务/状态；明确缺少支持并保留原文件。此保证针对遵守入口的执行者，不声称能约束忽略指令的旧客户端。'}, {'id': 'plan-decomposition-s28', 'prompt': '组内切换请求等组完成\nGIVEN 用户在 T04 期间请求转并发，T03 已待验。\nWHEN T04 提交待验且随后会话中断。', 'expected_output': '授权请求保存在 notes，mode 不变；恢复 T05/T06，整组完成后沿已保存授权检查原准入，不把 T04 待验作为可切换完成边界。'}]
roles="\n\n".join("# "+p+"\n"+(root/p).read_text() for p in files)
hashes={p:hashlib.sha256((root/p).read_bytes()).hexdigest() for p in files}
for c in cases:
    prompt="按所附候选规则处理输入，说明下一步决定及依据；本次只读决策，不写文件、不联网、不执行计划。候选插件根："+str(root)+"\n"+roles+"\n输入：\n"+c["prompt"]
    dest=out/c["id"];dest.mkdir()
    (dest/"input.txt").write_text(prompt)
    cmd=["rtk","proxy","claude","-p","--plugin-dir",str(root),"--strict-mcp-config","--mcp-config",'{"mcpServers":{}}',"--tools","Read","--allowedTools","Read","--no-session-persistence","--verbose","--output-format","stream-json",prompt]
    with (dest/"stdout.jsonl").open("w") as stdout,(dest/"stderr.log").open("w") as stderr:
        proc=subprocess.Popen(cmd,cwd=root,stdin=subprocess.DEVNULL,stdout=stdout,stderr=stderr,start_new_session=True)
        started=time.monotonic()
        while proc.poll() is None:
            try:proc.wait(timeout=30)
            except subprocess.TimeoutExpired:print(c["id"],"still running",round(time.monotonic()-started),flush=True)
            if time.monotonic()-started>=300 and proc.poll() is None:
                os.killpg(proc.pid,signal.SIGTERM)
                try:proc.wait(timeout=10)
                except subprocess.TimeoutExpired:os.killpg(proc.pid,signal.SIGKILL);proc.wait()
                break
    (dest/"facts.json").write_text(json.dumps({"command":cmd[:-1],"cwd":str(root),"exit":proc.returncode,"elapsed":time.monotonic()-started,"candidate_hashes":hashes,"expected":c["expected_output"],"verdict":"needs_review","model":"read actual init/result; inherit local config"},ensure_ascii=False,indent=2)+"\n")
``````

运行此块时 PD_PHASE=red；预期旧逐票完成规则至少在待验/组恢复上出现与固定预期不符的真实决定。独立判读后保存 verdict/reason/evidence；若全部已有正确行为则重审最小修改，不伪造红。工具缺失/超时是 BLOCKED，不是有效红。
**步骤 3：写最小实现**

``````python
from pathlib import Path
files = {
'skills/executing-plans/references/integration-groups.md': r'''# 集成组执行协议

> 字段和格式唯一定义在 writing-plans 的集成组声明与 v2 进度节；这里定义执行时序。适用于已批准且机器预检支持的组，不适用于无组 v1 或旧单文件。普通 implementer 协议与模型声明仍遵循 executing-plans-parallel。

## 执行入口

先解析 index/progress/spec；含组必须识别 format_version=2 与 protocol_version=1。每个状态边界运行插件根的 `scripts/validate-output.mjs plan-state <plan-dir>`：exit 0 且协议号匹配才消费 ready_tasks。ok=true 且 ready_tasks=[] 可表示合法业务阻塞，不强行找下一票。未知能力/非Git/无法隔离的组停止，不用旧普通流程猜执行。

先按同一 common-dir/feature_key 规则取得特性锁，核实 owner 后才写 progress；平台无法证明旧 owner 停止时保持阻塞，不按超时抢锁。普通模式、原始审查基线、授权请求、模型声明、资源与 claim 保留。并发进入组前停止新派发，收拢并核验全部在途票到已验证基线；未知执行者或未接收实现阻止进组。

组进入、组内操作、失败修复、恢复和完成按以下规则进行。current 仅指主线程当前票；组员不创建 implementer claim，不写 ready 的 implementation-result。


1. 活动组为空：普通票按 completed 依赖；整组只能在其全部外部前置 completed、资源可用、组基线实际通过且没有在途执行者/未接收实现时进入。保存组启动检查点后才开始业务写入。
2. 活动组存在：仅返回此组下一个可执行成员，成员的组内依赖必须 awaiting_verification 或 completed；未完成前序成员阻止越序。全部成员待验后仅 verify 可执行。blocked 时 ready 为空，由主线程明确修复/恢复后再算。
3. 普通票和其他组不消费成员中间状态。验证票通过和进度持久化前，不清 active_group、不推进验证基线、不切模式或从当前失败 HEAD 派票。
4. 每票先记录批准检查命令、期望允许的暂时失败及归因；命令、cwd、exit、实际实现 SHA/业务树标识、stdout/stderr 路径及 SHA-256 归档到特性 `execution/groups/GNN/TNN/<attempt>/`。原日志和失败尝试不可覆盖。组验证覆盖批准的全部组 Scenario、相关回归及旧形/调用者清理，没有必需测试记 skip/未运行而宣称通过的出口。
5. 状态转移只能在工作区已解释且实现提交存在时持久化；未提交业务改动在崩溃恢复时先核对本票归属，不 reset/stash/覆盖。日志存在与哈希只证明可恢复/未变更，不自证命令曾真实执行；主线程与独立审查核对实际工具回执、失败原因和语义。

计划必须为每个组给出组验证命令、公共 seam/Scenario、保护基线、局部必通过检查以及延期检查原因，不能仅把 `reason` 当免测许可。序列中有行为变化时在写该变化前保留有效红；纯机械迁移保护测试先于组首改动。没有足够保护/有效红则处理缺口后才写代码，不把组协议当 TDD 例外授权。

### 修复、恢复与资源

- 组验证失败后，组与 verify blocked；原待验票不因失败被误标 completed 或全部清空。只读诊断可继续。验证票若需改业务代码，先回到承担该写集合的成员；若涉及新接口/范围，沿契约偏差门修订计划后再继续。
- 回到成员时保存旧尝试，成员 in_progress、受影响依赖闭包 blocked；主线程记录修复归属和动作，组恢复 in_progress、verify 回 pending 后持久化这个修复检查点，才允许写入。依赖闭包仍 blocked 的成员只在其前置重新待验后由主线程显式置 in_progress 进行有效性复核，普通 ready 不能自动跳过它。按声明接口/提交/检查逐票重新确认，仍有效的工作复用，缺证据补验，不重复应用已有补丁。再次进入待验后全部组验证重跑；不能仅重跑上次失败一条就接受改变后的代码树。
- 恢复从实际集成工作区的已提交 progress 起步，磁盘中未提交的状态只视作待核验准备；原 owner 停止、锁实体归属、工作区与分支绑定、SHA ancestry、角色记录与证据都核对。不能核实的冻结，仍活跃的执行者不按超时替换。组中不存在 implementer claim，普通历史认领不清空。
- 完成证据已齐而状态没提交，核对代码树没有改变后补完成检查点；发现原接受实现已经存在，不重复 merge。组成功后清 active_group、保留组档案，恢复原模式；原审查基线、交付通道、模型声明历史保持。
- 工作区和持久证据目录均沿 progress.resources 登记；组不新建额外 worktree。未通过或证据未归档的现场不清理；最终仍只遍历台账并完成全量、实际合并、取代、sync_commit 与 roadmap 回写。


## 一次普通成员操作

1. 从已提交检查点读取本票、依赖接口与既有获批 seam；标 in_progress/current，并先单独提交进度。
2. 沿任务写集合实施，运行所有能运行的检查。普通行为改变先观察有效行为红；纯迁移使用组首保护，不能拿编译失败充当红。只有计划明确的组间暂时失败可以记待验，意外失败 blocked。
3. 核对 over/under-building 和接口；保存实际实现提交 C，归档每条检查的命令、cwd、exit、C/业务树与日志，不覆盖旧尝试。
4. 核实证据后，成员 awaiting_verification/tests=pending_group、implementation_commit=C、commit=null；更新组 checkpoint_commit=C、current=null，原子保存再单独提交进度。组外依赖仍等待 verify。
5. 运行 plan-state 检查已提交状态，再按 ready_tasks 进入本组下一票。

## 组验证与完成

所有成员待验后执行独立 verify 任务，覆盖计划规定的全部组 Scenario、相关回归、旧形和调用者清理。失败则组/verify blocked、保留组员待验和旧验证基线；只读诊断可继续。修复先回到有写集合的成员并保存修复检查点，接口/范围偏差仍交用户。

成功后以实际通过的业务树对应提交 V 同次原子完成所有成员、verify、组，填写各自 commit=V、保留各成员 implementation_commit，更新 integration.validated_commit（parallel 也同步 execution 投影），清 active_group/current；进度提交与 V 分开。之后重新预检才恢复普通调度，项目最终全量与审查/验收/交付仍执行。

## 证据记录形状

每条 evidence_paths 指向特性内 `execution/groups/GNN/TNN/<attempt>/record.json`；每次尝试独立目录。记录包含 command（实际 argv 字符串数组）、cwd、exit_code、commit、tree、stdout/stderr 相对特性路径和两个 sha256。tree 是 `git ls-tree -r -z C` 中排除本特性 plan/progress.yaml 与 execution/ 后的记录用 NUL 连接再 SHA-256；其余文件（包括 spec/index/任务接口）改变都会使树不同。日志可恢复/哈希吻合只证明字节没变，主线程和审查者仍核对工具回执、命令与失败类别；不手写“pass”冒充测试。

completed 组必须有 verify 自己目录中的通过证据，记录的业务树等于 V；成员历史非零记录仍保留，不改成零。CLI 不会运行测试、取得锁或执行恢复写入。未提交进度返回 checkpoint_uncommitted；恢复者核对磁盘、已提交档案、真实提交和日志后补检查点，不用未提交 completed 解锁。
'''
}
for name, text in files.items():
    p=Path(name); assert not p.exists(),(name,'already exists: resume check')
    p.parent.mkdir(parents=True,exist_ok=True); p.write_text(text)

from pathlib import Path
changes = [
('skills/executing-plans/SKILL.md', r'''## 阶段 2：隔离工作区''', r'''## 集成组分支

含组的 v2 先使用 plan-index 和 plan-state 协议检查，按 [integration-groups.md](references/integration-groups.md) 执行主线程独占、待验、组验证与恢复。无组 v1/旧单文件保持原分支。每个状态边界以已提交检查点为准，组内 awaiting_verification 只允许本组显式依赖消费；组外等验证票 completed。组验证是阶段 3 的普通执行类型，不与阶段 4 的 acceptance-qa 任务混淆。

## 阶段 2：隔离工作区'''),
('skills/executing-plans/SKILL.md', r'''- 任务完成条件（全部满足才置 completed）：依赖全 completed、TDD 步骤完成、测试通过、commit 可解析、接口块与导航表一致、progress.yaml 已原子更新并随任务提交。''', r'''- 普通任务完成条件（全部满足才置 completed）：依赖全 completed、适用 TDD/纯重构步骤完成、测试通过、commit 可解析、接口块与导航表一致、progress.yaml 已原子更新并随任务提交。含组成员/验证票的完成与 ready 由 integration-groups 定义；本票操作已保存不等于 completed。'''),
('skills/executing-plans/SKILL.md', r'''2. 从下一 ready 任务（依赖全 completed 的最小编号 pending）续跑；同前只读该任务文件与依赖接口行。''', r'''2. 普通分支从下一 ready 任务（依赖全 completed 的最小编号 pending）续跑；组分支消费 plan-state 的 ready_tasks 并按 integration-groups 恢复。同前只读该任务文件与依赖接口行。'''),
('skills/executing-plans/SKILL.md', r'''## 阶段 3：逐任务执行
''', r'''## 阶段 3：逐任务执行

下面普通票流程不对集成组成员强行标 completed；到组入口时按 integration-groups 完成整组（含独立组验证任务），组成功后回本流程。纯重构使用 TDD 的前后保护步骤，不伪造失败。
'''),
('skills/executing-plans-parallel/SKILL.md', r'''## 派发一轮
''', r'''## 集成组主线程分流

含组 v2 在普通调度前使用 plan-state；组员和组验证票不进入 parallel 声明/implementer 集合。按 executing-plans 的 [integration-groups.md](../executing-plans/references/integration-groups.md) 收拢在途票、独占集成分支、保存待验与统一验证；期间禁止新派发和其他合入，validated_commit 不随待验提交前移。状态字段由 writing-plans 单点定义，integration 与 execution 投影同次更新。组完成后保留原 mode、base_commit、模型声明与授权恢复普通派发；不把票内待验当成切模式完成边界。

## 派发一轮
'''),
('skills/executing-plans/agents/openai.yaml', r'''  short_description: "串行执行批准的计划（index.md + tasks/ + progress.yaml；存量单文件按原样读取）：消费 seam、每票红绿与契约自检；收尾 S 符合性、覆盖核对和可选架构深化，完成验收及本地/PR 对账。"''', r'''  short_description: "执行已批准计划：普通票逐票完成；显式集成组由主线程独占，分票待验、统一验证、检查点恢复，保留完整收尾与交付。"'''),
('skills/executing-plans-parallel/agents/openai.yaml', r'''  short_description: "显式 opt-in 并发计划：主线程编排与唯一状态，独立 worktree 完成获批 seam 下的红绿；缺失或冲突 blocked，集成验证后完成。"''', r'''  short_description: "显式并发、唯一进度写者与验证后集成；集成组排空在途票后主线程独占，统一通过后恢复原模式。"''')
]
for name, old, new in changes:
    p=Path(name); text=p.read_text()
    assert text.count(old)==1,(name,'anchor drift')
    p.write_text(text.replace(old,new,1))
``````

保留现行元数据可发现的三件套入口串：

``````python
from pathlib import Path
changes = [
('skills/executing-plans/agents/openai.yaml', r'''  short_description: "执行已批准计划：普通票逐票完成；显式集成组由主线程独占，分票待验、统一验证、检查点恢复，保留完整收尾与交付。"''', r'''  short_description: "执行已批准计划（index.md + tasks/ + progress.yaml）：普通票逐票完成；显式集成组由主线程独占，分票待验、统一验证、检查点恢复，保留完整收尾与交付。"''')
]
for name, old, new in changes:
    p=Path(name); text=p.read_text()
    assert text.count(old)==1,(name,'anchor drift')
    p.write_text(text.replace(old,new,1))
``````

**步骤 4：运行确认通过**
重跑步骤 2 完整探针，设置 PD_PHASE=green（用新目录，不改输入/预期，候选规则自然更新）；核对语义通过，T08补可写动作。

``````bash
rtk proxy node scripts/validate-skills.mjs
rtk proxy node scripts/check-plugin.mjs
rtk proxy node --test scripts/tests/plan-single-format.test.mjs scripts/tests/parallel-plan.test.mjs scripts/tests/parallel-integration.test.mjs scripts/tests/plan-state.test.mjs
rtk git diff --check
``````

**步骤 5：提交**

只暂存本票文件、spec实施记录、本票证据和 progress；按 index 的共存、发版和原子状态规则提交。未完成必需检查不标 completed。

``````bash
rtk git add skills/executing-plans/SKILL.md skills/executing-plans/agents/openai.yaml skills/executing-plans/evals/evals.json skills/executing-plans-parallel/SKILL.md skills/executing-plans-parallel/agents/openai.yaml skills/executing-plans-parallel/evals/evals.json skills/executing-plans/references/integration-groups.md
rtk git add .spec-dev/2026-09-07-01-plan-decomposition/spec/plan-decomposition-design.md .spec-dev/2026-09-07-01-plan-decomposition/plan/progress.yaml .spec-dev/2026-09-07-01-plan-decomposition/execution/serial/T04
rtk proxy node guardrail/check-spec-drift.mjs --staged
``````

提交主旨：`feat(T04): 接入集成组执行与恢复协议`。正文使用文件传入 Spec 指针及实际共存切面，不跳过 pre-commit。代码提交后核实自动发版最终 HEAD，另行保存本票状态提交，状态提交使用 `SKIP_RELEASE_HOOK=1`。
