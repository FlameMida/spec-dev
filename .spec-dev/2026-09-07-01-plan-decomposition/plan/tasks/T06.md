### T06：统一前置重构与组验证纪律

**文件**：
- `skills/test-driven-development/SKILL.md`
- `skills/test-driven-development/agents/openai.yaml`
- `skills/test-driven-development/evals/evals.json`
- `skills/test-strategy/SKILL.md`
- `skills/test-strategy/agents/openai.yaml`
- `skills/test-strategy/evals/evals.json`

**接口**：
- 消费：T05 类型化任务和共享判据；已获批公共seam
- 产出：TDD/test-strategy 前置/组/收尾验证时机与有效红边界
- Scenario：S06、S12、S30、S31。公共测试落点沿 index 与 spec，不新增私有测试接口。

**步骤 1：写失败测试**

``````python
from pathlib import Path
changes = [
('skills/test-driven-development/evals/evals.json', r'''{
  "skill_name": "test-driven-development",
  "evals": [
    {
      "id": "tdd-red-first",
      "prompt": "实现一个 retryOperation 函数：失败自动重试，最多 3 次",
      "expected_output": "沿获批 seam 先写失败测试，确认目标行为失败后最小实现转绿；重构候选交收尾，不插入循环，不先写实现。"
    },
    {
      "id": "tdd-delete-untested-code",
      "prompt": "（已经先写了 200 行实现代码，还没有测试）继续完成这个功能",
      "expected_output": "指出违反铁律：删除未经测试先行的实现（不留作参考、不边写测试边改造），从失败测试重新开始"
    },
    {
      "id": "tdd-bugfix-with-test",
      "prompt": "修复：空邮箱也能提交表单的 bug",
      "expected_output": "先写复现该 bug 的失败测试（如 rejects empty email），确认失败后再修实现，测试转绿即证明修复并防回归；不允许不带测试直接修"
    },
    {
      "id": "tdd-seam-s18",
      "prompt": "S18 各入口与图示一致：\nGIVEN TDD 正文、DOT、description/openai、反模式 5、串行和 quick-fix 引用及已有 eval\nWHEN 沿任一入口执行红绿",
      "expected_output": "都没有循环内重构步骤，串并行每票仍保持五步；纯壳引用指向同一定义，元数据仍为中文。"
    },
    {
      "id": "tdd-seam-s19",
      "prompt": "S19 纯重构复用已有保护：\nGIVEN 已授权的结构整理不改变公共接口且已有相关行为测试\nWHEN 实施整理",
      "expected_output": "记录前后通过证据，不伪造失败或强行添加同义测试；一旦可观察行为回归就修复回归，不把它当作允许的重构差异。"
    },
    {
      "id": "tdd-seam-s20",
      "prompt": "S20 缺少保护先刻画：\nGIVEN 待整理路径缺乏公共行为保护测试\nWHEN 准备纯重构",
      "expected_output": "先依据现行契约补行为刻画并观察当前实现通过，记录其为刻画而非红证据，再进行结构调整；若需求期望与当前行为不同则转行为缺陷修复。"
    },
    {
      "id": "tdd-seam-s21",
      "prompt": "S21 并发红绿协议不被豁免：\nGIVEN implementer 被派发的票实际只需纯重构保护，无法提供新增行为的有效红证据\nWHEN 执行准入检查",
      "expected_output": "不伪造红结果，回主线程按现有例外/阻塞串行路径处理；不改变 implementation-result phase 或接收器对普通实施票的红绿要求。"
    },
    {
      "id": "tdd-seam-s08",
      "prompt": "S08 拒绝同构自证：\nGIVEN 被测函数用 reduce 求和，测试又用同一 reduce 算出期望值\nWHEN 检查断言",
      "expected_output": "指出同义反复，改用有来源的已知样例结果，如 `[2, 5, 8]` 的手算值 `15`，而非换个函数名继续复制算法。"
    },
    {
      "id": "tdd-seam-s09",
      "prompt": "S09 重构后脆弱测试失败：\nGIVEN 私有协作者改名导致 mock 断言失败，而获批公共行为保持不变\nWHEN 诊断失败测试",
      "expected_output": "把测试移回公共边界，不为保留脆弱断言恢复旧内部结构；若公共行为实际改变，则按行为回归处理。"
    },
    {
      "id": "tdd-seam-s10",
      "prompt": "S10 数据库测试边界对照：\nGIVEN 一个测试声称验证 HTTP 响应却只查内部表，另一个明确验证获批数据库适配器契约\nWHEN 审查测试落点",
      "expected_output": "前者补回 HTTP 可观察行为断言，后者可在自身契约边界验证真实数据库；不一概禁止 DB 测试。测试夹具准备和清理不等于旁路行为断言。"
    }
  ]
}
''', r'''{
  "skill_name": "test-driven-development",
  "evals": [
    {
      "id": "tdd-red-first",
      "prompt": "实现一个 retryOperation 函数：失败自动重试，最多 3 次",
      "expected_output": "沿获批 seam 先写失败测试，确认目标行为失败后最小实现转绿；重构候选交收尾，不插入循环，不先写实现。"
    },
    {
      "id": "tdd-delete-untested-code",
      "prompt": "（已经先写了 200 行实现代码，还没有测试）继续完成这个功能",
      "expected_output": "指出违反铁律：删除未经测试先行的实现（不留作参考、不边写测试边改造），从失败测试重新开始"
    },
    {
      "id": "tdd-bugfix-with-test",
      "prompt": "修复：空邮箱也能提交表单的 bug",
      "expected_output": "先写复现该 bug 的失败测试（如 rejects empty email），确认失败后再修实现，测试转绿即证明修复并防回归；不允许不带测试直接修"
    },
    {
      "id": "tdd-seam-s18",
      "prompt": "S18 各入口与图示一致：\nGIVEN TDD 正文、DOT、description/openai、反模式 5、串行和 quick-fix 引用及已有 eval\nWHEN 沿任一入口执行红绿",
      "expected_output": "都没有循环内重构步骤，串并行每票仍保持五步；纯壳引用指向同一定义，元数据仍为中文。"
    },
    {
      "id": "tdd-seam-s19",
      "prompt": "S19 纯重构复用已有保护：\nGIVEN 已授权的结构整理不改变公共接口且已有相关行为测试\nWHEN 实施整理",
      "expected_output": "记录前后通过证据，不伪造失败或强行添加同义测试；一旦可观察行为回归就修复回归，不把它当作允许的重构差异。"
    },
    {
      "id": "tdd-seam-s20",
      "prompt": "S20 缺少保护先刻画：\nGIVEN 待整理路径缺乏公共行为保护测试\nWHEN 准备纯重构",
      "expected_output": "先依据现行契约补行为刻画并观察当前实现通过，记录其为刻画而非红证据，再进行结构调整；若需求期望与当前行为不同则转行为缺陷修复。"
    },
    {
      "id": "tdd-seam-s21",
      "prompt": "S21 并发红绿协议不被豁免：\nGIVEN implementer 被派发的票实际只需纯重构保护，无法提供新增行为的有效红证据\nWHEN 执行准入检查",
      "expected_output": "不伪造红结果，回主线程按现有例外/阻塞串行路径处理；不改变 implementation-result phase 或接收器对普通实施票的红绿要求。"
    },
    {
      "id": "tdd-seam-s08",
      "prompt": "S08 拒绝同构自证：\nGIVEN 被测函数用 reduce 求和，测试又用同一 reduce 算出期望值\nWHEN 检查断言",
      "expected_output": "指出同义反复，改用有来源的已知样例结果，如 `[2, 5, 8]` 的手算值 `15`，而非换个函数名继续复制算法。"
    },
    {
      "id": "tdd-seam-s09",
      "prompt": "S09 重构后脆弱测试失败：\nGIVEN 私有协作者改名导致 mock 断言失败，而获批公共行为保持不变\nWHEN 诊断失败测试",
      "expected_output": "把测试移回公共边界，不为保留脆弱断言恢复旧内部结构；若公共行为实际改变，则按行为回归处理。"
    },
    {
      "id": "tdd-seam-s10",
      "prompt": "S10 数据库测试边界对照：\nGIVEN 一个测试声称验证 HTTP 响应却只查内部表，另一个明确验证获批数据库适配器契约\nWHEN 审查测试落点",
      "expected_output": "前者补回 HTTP 可观察行为断言，后者可在自身契约边界验证真实数据库；不一概禁止 DB 测试。测试夹具准备和清理不等于旁路行为断言。"
    },
    {
      "id": "plan-decomposition-s30",
      "prompt": "组身份不豁免有效红\nGIVEN 一张组员票实际改变错误处理行为，现有失败仅是导入编译错误。\nWHEN 执行者准备写实现。",
      "expected_output": "不将编译错误当红，不因有组就跳过有效行为复现；取得有效红或冻结回设计，不能静默把该票改称纯重构。"
    },
    {
      "id": "plan-decomposition-s31",
      "prompt": "前置纯重构缺保护先刻画\nGIVEN T01 有批准范围但当前路径没有行为保护。\nWHEN 准备结构调整。",
      "expected_output": "先补契约行为刻画并在当前实现观察通过，再调整并记录通过；不伪造一次失败、不拖到功能实现之后才做前置重构。"
    }
  ]
}
''')
]
for name, old, new in changes:
    p=Path(name); text=p.read_text()
    assert text.count(old)==1,(name,'anchor drift')
    p.write_text(text.replace(old,new,1))

from pathlib import Path
changes = [
('skills/test-strategy/evals/evals.json', r'''{
  "skill_name": "test-strategy",
  "evals": [
    {
      "id": "ts-lane-annotation",
      "prompt": "spec 验收矩阵有一行 integration 维度的 DB 回归检查，项目用 PostgreSQL",
      "expected_output": "该行标注 PR lane；writing-plans 翻译的测试步骤引用 db-testing 处方（job 级一容器 + 模板克隆/事务回滚），运行命令归入 PR lane；不出现每测试一容器"
    },
    {
      "id": "ts-governance-order",
      "prompt": "用户抱怨 CI 又慢又经常无关红，想先上测试选择系统",
      "expected_output": "按治理顺序 flaky→时长→选择劝阻：先清 flaky 再治时长（拓扑/磁盘/并行），选择系统仅在人数/时长触发条件满足后引入"
    },
    {
      "id": "tdd-seam-s14",
      "prompt": "S14 内部实现与外部替身对照：\nGIVEN 支付业务通过声明的支付客户端边界调用外部服务，同时有内部折扣计算模块\nWHEN 设计测试替身",
      "expected_output": "可替换支付客户端并检查真实业务结果，保留内部计算；不把“自己写的外部适配器”按所有权误判为禁止替换的内部细节。"
    },
    {
      "id": "tdd-seam-s15",
      "prompt": "S15 四类依赖与集成信心：\nGIVEN 测试涉及远端 API、数据库、时钟/随机源、文件系统\nWHEN 选择策略及 Lane",
      "expected_output": "每类按上述规则得到真实依赖或替身方案；纯内存替身可以进入 fast，真实 IO 按 PR/nightly 安排；fast 全绿不能证明真实 DB/网络集成通过。"
    }
  ]
}
''', r'''{
  "skill_name": "test-strategy",
  "evals": [
    {
      "id": "ts-lane-annotation",
      "prompt": "spec 验收矩阵有一行 integration 维度的 DB 回归检查，项目用 PostgreSQL",
      "expected_output": "该行标注 PR lane；writing-plans 翻译的测试步骤引用 db-testing 处方（job 级一容器 + 模板克隆/事务回滚），运行命令归入 PR lane；不出现每测试一容器"
    },
    {
      "id": "ts-governance-order",
      "prompt": "用户抱怨 CI 又慢又经常无关红，想先上测试选择系统",
      "expected_output": "按治理顺序 flaky→时长→选择劝阻：先清 flaky 再治时长（拓扑/磁盘/并行），选择系统仅在人数/时长触发条件满足后引入"
    },
    {
      "id": "tdd-seam-s14",
      "prompt": "S14 内部实现与外部替身对照：\nGIVEN 支付业务通过声明的支付客户端边界调用外部服务，同时有内部折扣计算模块\nWHEN 设计测试替身",
      "expected_output": "可替换支付客户端并检查真实业务结果，保留内部计算；不把“自己写的外部适配器”按所有权误判为禁止替换的内部细节。"
    },
    {
      "id": "tdd-seam-s15",
      "prompt": "S15 四类依赖与集成信心：\nGIVEN 测试涉及远端 API、数据库、时钟/随机源、文件系统\nWHEN 选择策略及 Lane",
      "expected_output": "每类按上述规则得到真实依赖或替身方案；纯内存替身可以进入 fast，真实 IO 按 PR/nightly 安排；fast 全绿不能证明真实 DB/网络集成通过。"
    },
    {
      "id": "plan-decomposition-s06",
      "prompt": "意外局部失败阻塞\nGIVEN T03 的本应通过的独立行为保护测试失败，且不属于批准的组间暂时失败范围。\nWHEN 准备推进 T04。",
      "expected_output": "T03/整组 blocked，不自动扩大“预期失败”白名单；保存原因并先修复或走契约偏差。"
    },
    {
      "id": "plan-decomposition-s12",
      "prompt": "失败不移动已验证基线\nGIVEN 组基线 B 已通过，T06 在当前实现 tip 测试失败。\nWHEN 记录结果。",
      "expected_output": "T06/组 blocked，成员保持待验，validated_commit 仍为 B；报告失败命令及日志，不派组外任务。"
    }
  ]
}
''')
]
for name, old, new in changes:
    p=Path(name); text=p.read_text()
    assert text.count(old)==1,(name,'anchor drift')
    p.write_text(text.replace(old,new,1))
``````

**步骤 2：运行确认失败**

``````python
import hashlib,json,subprocess,time,os,signal
from pathlib import Path
root=Path.cwd().resolve()
phase=os.environ.get("PD_PHASE","red")
assert phase in ("red","green")
out=root/'.spec-dev/2026-09-07-01-plan-decomposition/execution/serial/'/'T06'/phase
out.mkdir(parents=True,exist_ok=False)
files=['skills/test-driven-development/SKILL.md', 'skills/test-strategy/SKILL.md']
cases=[{'id': 'plan-decomposition-s30', 'prompt': '组身份不豁免有效红\nGIVEN 一张组员票实际改变错误处理行为，现有失败仅是导入编译错误。\nWHEN 执行者准备写实现。', 'expected_output': '不将编译错误当红，不因有组就跳过有效行为复现；取得有效红或冻结回设计，不能静默把该票改称纯重构。'}, {'id': 'plan-decomposition-s31', 'prompt': '前置纯重构缺保护先刻画\nGIVEN T01 有批准范围但当前路径没有行为保护。\nWHEN 准备结构调整。', 'expected_output': '先补契约行为刻画并在当前实现观察通过，再调整并记录通过；不伪造一次失败、不拖到功能实现之后才做前置重构。'}, {'id': 'plan-decomposition-s06', 'prompt': '意外局部失败阻塞\nGIVEN T03 的本应通过的独立行为保护测试失败，且不属于批准的组间暂时失败范围。\nWHEN 准备推进 T04。', 'expected_output': 'T03/整组 blocked，不自动扩大“预期失败”白名单；保存原因并先修复或走契约偏差。'}, {'id': 'plan-decomposition-s12', 'prompt': '失败不移动已验证基线\nGIVEN 组基线 B 已通过，T06 在当前实现 tip 测试失败。\nWHEN 记录结果。', 'expected_output': 'T06/组 blocked，成员保持待验，validated_commit 仍为 B；报告失败命令及日志，不派组外任务。'}]
roles="\n\n".join("# "+p+"\n"+(root/p).read_text() for p in files)
hashes={p:hashlib.sha256((root/p).read_bytes()).hexdigest() for p in files}
for c in cases:
    prompt="按所附候选规则处理输入，说明下一步决定及依据；本次只读决策，不写文件、不联网、不执行计划。候选插件根："+str(root)+"\n"+roles+"\n输入：\n"+c["prompt"]
    dest=out/c["id"];dest.mkdir()
    (dest/"input.txt").write_text(prompt)
    cmd=["rtk","proxy","claude","-p","--plugin-dir",str(root),"--strict-mcp-config","--mcp-config",'{"mcpServers":{}}',"--tools","Read","--allowedTools","Read","--no-session-persistence","--verbose","--output-format","stream-json",prompt]
    with (dest/"stdout.jsonl").open("w") as stdout,(dest/"stderr.log").open("w") as stderr:
        proc=subprocess.Popen(cmd,cwd=root,stdin=subprocess.DEVNULL,stdout=stdout,stderr=stderr,start_new_session=True)
        started=time.monotonic()
        while proc.poll() is None:
            try:proc.wait(timeout=30)
            except subprocess.TimeoutExpired:print(c["id"],"still running",round(time.monotonic()-started),flush=True)
            if time.monotonic()-started>=300 and proc.poll() is None:
                os.killpg(proc.pid,signal.SIGTERM)
                try:proc.wait(timeout=10)
                except subprocess.TimeoutExpired:os.killpg(proc.pid,signal.SIGKILL);proc.wait()
                break
    (dest/"facts.json").write_text(json.dumps({"command":cmd[:-1],"cwd":str(root),"exit":proc.returncode,"elapsed":time.monotonic()-started,"candidate_hashes":hashes,"expected":c["expected_output"],"verdict":"needs_review","model":"read actual init/result; inherit local config"},ensure_ascii=False,indent=2)+"\n")
``````

PD_PHASE=red。目标是已批准 T01 时机及组内记录的行为决定，不把元数据缺句或工具超时当有效红。
**步骤 3：写最小实现**

``````python
from pathlib import Path
changes = [
('skills/test-driven-development/SKILL.md', r'''## 收尾纯重构
''', r'''## 批准的前置纯重构与集成组

批准计划中解除实际实施阻碍的前置纯重构在 T01 执行，复用下方前后公共行为保护规则；不能等到收尾才解除阻碍。执行中额外发现的重构机会仍交收尾，不借此前置规则扩大授权。

集成组不是 TDD 例外：行为变化在实现前仍获得有效行为失败；纯机械/行为保持迁移先取得组基线保护。每票照常跑能运行的检查，仅对计划逐项声明的命令/范围/原因延后整体绿，保存真实 exit 与待验状态。意外局部失败阻塞，环境/编译错误不能当行为红。没有足够保护或无法获得行为红时回到设计，不把该票改名“重构”来跳过。

组员的公共行为最终在唯一组验证票上检验，普通功能票仍保持五步和有效红绿。没有有效红的纯重构与组员/组验证票不派给普通 implementer，不改变 implementation-result phase 或原接收条件。组验证完成不替代最终特性审查、相关回归和收尾全量。

## 收尾纯重构
'''),
('skills/test-driven-development/SKILL.md', r'''有计划则由现有收尾审查接收候选，修复后复审受影响维度；quick-fix 使用自身收尾，不强制开启可选 acceptance-qa。''', r'''计划中的前置纯重构与组迁移按已批准时机执行，额外候选由现有收尾审查接收，收尾修复后复审受影响维度；quick-fix 使用自身收尾，不强制开启可选 acceptance-qa。'''),
('skills/test-strategy/SKILL.md', r'''## 治理顺序铁律：flaky → 时长 → 选择''', r'''## 集成组的验证归属

普通行为票的有效红绿与原完成条件保持。获批的不可独立验证集成组，在组首记录公共行为保护基线，各票保留可运行检查与真实结果；明确延期的相关验证在组验证票统一运行通过，才能完成全组。组首保护或局部必通过检查失败不能记待验放行；编译/环境失败不作为行为红。组内记录 pending_group 与日志，不能写 tests:pass；组完成记录的 pass 指组合验证，不改写历史失败。最终全量继续在原收尾时机执行。

## 治理顺序铁律：flaky → 时长 → 选择'''),
('skills/test-driven-development/agents/openai.yaml', r'''  short_description: "TDD 纪律：沿获批公共落点先红后绿；纯重构在收尾保绿；例外单点与授权复用；独立真值和测试反模式防护。"''', r'''  short_description: "行为变化先有效红再绿；批准的前置/收尾纯重构保绿，集成组逐票留证、统一验证，额外重构交收尾。"'''),
('skills/test-strategy/agents/openai.yaml', r'''  short_description: "测试策略：按 IO 分三 Lane，治理 flaky、时长与选择；四类外部依赖和模型边界策略；静态快检不替代行为验证，票内与收尾验证沿原计划执行。"''', r'''  short_description: "按 IO 划分 fast/PR/nightly；区分局部检查、集成组验证和最终全量，模型冒烟与静态证据分别记账。"''')
]
for name, old, new in changes:
    p=Path(name); text=p.read_text()
    assert text.count(old)==1,(name,'anchor drift')
    p.write_text(text.replace(old,new,1))
``````

**步骤 4：运行确认通过**
设置 PD_PHASE=green，重跑步骤2相同输入和预期；保留现有seam与普通implementer测试。

``````bash
rtk proxy node scripts/validate-skills.mjs
rtk proxy node scripts/check-plugin.mjs
rtk proxy node --test scripts/tests/plugin-root.test.mjs scripts/tests/parallel-integration.test.mjs scripts/tests/plan-single-format.test.mjs
rtk git diff --check
``````

**步骤 5：提交**

只暂存本票文件、spec实施记录、本票证据和 progress；按 index 的共存、发版和原子状态规则提交。未完成必需检查不标 completed。

``````bash
rtk git add skills/test-driven-development/SKILL.md skills/test-driven-development/agents/openai.yaml skills/test-driven-development/evals/evals.json skills/test-strategy/SKILL.md skills/test-strategy/agents/openai.yaml skills/test-strategy/evals/evals.json
rtk git add .spec-dev/2026-09-07-01-plan-decomposition/spec/plan-decomposition-design.md .spec-dev/2026-09-07-01-plan-decomposition/plan/progress.yaml .spec-dev/2026-09-07-01-plan-decomposition/execution/serial/T06
rtk proxy node guardrail/check-spec-drift.mjs --staged
``````

提交主旨：`feat(T06): 统一前置重构与组验证纪律`。正文使用文件传入 Spec 指针及实际共存切面，不跳过 pre-commit。代码提交后核实自动发版最终 HEAD，另行保存本票状态提交，状态提交使用 `SKIP_RELEASE_HOOK=1`。
