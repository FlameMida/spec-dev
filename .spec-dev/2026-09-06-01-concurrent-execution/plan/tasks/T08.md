### 任务 8：全量验证、取代回写、合并与清理

> 仅在全局审查、处置与 T07 验收完成后由主线程执行；不参与连续实施循环。

**文件**：
- 修改：本特性 `spec/concurrent-execution-design.md`、`plan/progress.yaml`。
- 修改：`.spec-dev/2026-08-27-01-plan-single-format/spec/plan-single-format-design.md`、`.spec-dev/2026-09-03-02-portability-hygiene/spec/portability-hygiene-design.md`。
- 修改：`.spec-dev/roadmaps/2026-09-03-01-skill-ecosystem-absorption.md` 的 #2 行与胶囊。

**接口**：
- 消费：已审查的完整特性 diff、T07 证据、资源台账与来源/原始基线。
- 产出：来源 main 上可核验交付、sync_commit 锚点、已清理台账与 roadmap #2 delivered。

**步骤 1：全量验证与归属裁决**

在实施 worktree 运行 index 全量命令，确认 T07 后没有未验证的实现变更。失败在范围内则修复并复跑；范围外失败在干净来源基线上复核，既有失败交用户裁决，不静默略过。检查 `rtk git diff --check` 与任务拥有的 staged diff。nightly 未验证项如实保留，必需 fast/PR 不得伪绿。

**步骤 2：测试退役检查**

只检查 index 相关范围内按 Scenario 命名且其 Requirement 已删除/取代的孤儿测试；现行场景、历史不合命名约定的测试保留。无候选记无；有候选列明并待用户同意才删除，不借取代删回归保护。

**步骤 3：部分取代回写**

两份旧 spec 保持 active。仅在下列 Requirement 标题下添加对应的 `> **Superseded**：由 [concurrent-execution](后继相对路径) 的对应 Requirement 部分取代。`，同时移除激活阶段 H1 下本次 pending 行，不动别的后继标记。

| 旧 spec | 标题 | 后继相对路径 |
|---|---|---|
| plan-single-format | plan 单一形态；渐进执行与断点恢复；资源登记纪律 | ../../2026-09-06-01-concurrent-execution/spec/concurrent-execution-design.md |
| portability-hygiene | README 漂移修正 | ../../2026-09-06-01-concurrent-execution/spec/concurrent-execution-design.md |

当前 spec 的取代与共存已明确这些范围，存量单文件保持冻结。Accepted ADR-0007 不改正文；覆盖路径不删除，不以 trailer 代替实际 drift 检查。将 pending 翻转与本次交付放同一合并组，未交付不能提前让旧保护消失。

**步骤 4：合并来源分支**

本计划默认本地交付；原有会话未授权远端发布，因此不 push。先核对源工作区 `/Users/maverick/feature-dev` 的分支仍为 main、干净且不存在他人未提交工作，来源已变化则检查并验证影响；未知改动或 merge 冲突停止并报告，不 reset/stash/强制覆盖。

```bash
rtk proxy git -C /Users/maverick/feature-dev status --short
rtk proxy git -C /Users/maverick/feature-dev branch --show-current
rtk proxy git -C /Users/maverick/feature-dev merge plan/2026-09-06-01-concurrent-execution
```

若执行期用户选择 PR，则服从已交付 delivery-channels：准备具体文案后只执行已有授权动作；ready/open 保持最终任务 in_progress、roadmap in-progress；取得实际目标树证据后再完成剩余回写。不得因为平台 squash 使 implementer SHA 不为祖先就误报代码未交付，改查实际合并树。

**步骤 5：清理台账**

先把 acceptance 证据归档并提交，再逐行执行 progress.resources。临时 fixture 未接受或有失联执行者时保留，不能递归删除；其他共享缓存不动。任务台账提供 .parallel-qa、setup 文件和本特性 worktree/branch 的精确标识。清理命令失败保留该行并报告；已经不存在记 notes。移除 worktree 前确认干净且实现已合并，使用正常 `git worktree remove` 和 `git branch -d`，不强制。此前已隔离的情况交原机制收尾，不删除非本计划创建的 worktree。

**步骤 6：锚定与 roadmap**

来源工作区执行 `rtk proxy git rev-parse HEAD` 取得实际合并后已验证提交；把真实 SHA 写本 spec 的 sync_commit。将介绍中的「尚未实施」改为本次真实交付情况；roadmap #2 改 delivered，胶囊补入口、字段定义点、验收边界与 #3 接续指针。progress 记录最终任务完成的实际证据和已清理台账；任务 commit 指向已接受合并提交，不引用接下来的文档锚定提交。

窄暂存以上文档，手动运行 staged drift，`SKIP_RELEASE_HOOK=1` 提交同步锚点；核对干净状态、任务提交图与只存在计划所有的资源。未完成清理/实际合并/文档闭环时如实标未完成，不能抢先 roadmap delivered。
