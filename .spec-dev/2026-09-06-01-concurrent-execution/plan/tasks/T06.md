### 任务 6：接通串行入口、共享交付与平台映射

**文件**：
- 修改：`skills/executing-plans/SKILL.md`、`skills/executing-plans/agents/openai.yaml`、`skills/executing-plans/evals/evals.json`。
- 修改：`skills/executing-plans/references/review-orchestration.md`、`skills/requirement-analysis/references/codex-compat.md`。
- 修改：`guardrail/templates/CLAUDE.md.snippet`、`guardrail/templates/AGENTS.md.snippet`、`guardrail/README.md`、`guardrail/README.zh-CN.md`。

**接口**：
- 消费：executing-plans-parallel 入口/恢复协议；delivery-channels local/pr 完成判据。
- 产出：executing-plans 的 opt-in 委托、边界切换、恢复路由与共同交付入口。

**Scenario / 验收锚点**：S07、S17—S27；保留存量单文件与既有审查。

**步骤 1：写失败测试**

向现有 executing-plans evals 数组追加以下四例，保留旧用例：

```json
[
  {"id":"ep-parallel-opt-in","prompt":"分文件计划有独立声明，用户只说继续；没有已提交 parallel 模式。","expected_output":"S07 默认串行，可提议但不 spawn implementer。"},
  {"id":"ep-switch-request","prompt":"串行 T02 写了一半，用户要求后面转并发，随后会话中断。","expected_output":"S26 先只提交 progress 中 request_id 与授权，不夹带半成品；恢复先完成 T02，再按已保存授权检查边界。"},
  {"id":"ep-pr-await","prompt":"PR 已 ready 但没有真实合并证据。","expected_output":"S19 awaiting_merge；最终任务 in_progress、roadmap in-progress，不锚定 sync_commit。"},
  {"id":"ep-parallel-global-review","prompt":"串行完成两票后转并发，后续已全部集成。","expected_output":"S21/S23 使用最初 base 到集成 tip 完整审查，保留矩阵验收与最终任务，不只审并发后半段。"}
]
```

**步骤 2：运行测试确认失败**

逐条走查旧文本，记录未定义委托、请求恢复、PR 等待和原始基线的缺口；运行 `rtk proxy node --test scripts/tests/plugin-root.test.mjs scripts/tests/plan-single-format.test.mjs scripts/tests/resource-ledger-split.test.mjs scripts/tests/search-clause.test.mjs` 记录结构基线。文档改动不另造镜像字符串测试，不伪称真实模型红绿。

**步骤 3：写最小实现**

在 executing-plans 的「恢复入口」之前插入以下完整章节：

```markdown
### 可选并发与任务边界切换

默认串行不变。分文件计划有合法 parallel 声明、导航拓扑至少存在两个无相互依赖路径且写集合/资源可隔离的实施票时，可以提议 executing-plans-parallel；只有用户明确选择才委托。共同 T00 不影响入口资格，实际派发等依赖 completed。普通「继续」不构成新的并发选择；已提交 progress.execution.mode=parallel 时进入该 skill 的恢复入口，先定位原集成工作区与锁，不从旧来源副本重新调度。

用户在串行票中途要求切换时，先按 executing-plans-parallel 协议保存 request_id/授权到 notes 并单独提交，当前票由主线程继续完成；其完成检查点干净可核验后，复用原隔离 worktree 检查剩余资格并委托，不重做 T00。原始审查基线保持不变；模型表、特性锁与激活检查点顺序由该 skill 定义。条件不足继续已授权串行，缺声明不自动改计划；恢复先读取已提交请求，不丢失授权或重派半成品。

并发分支完成全部实施票集成后回到本 skill 阶段 4—6。只有该分支的 implementer 可以写获准代码，其他探索/审查子代理继续只读。存量单文件按现有冻结分支执行，不新增并发元数据。
```

将阶段 2 的降级句替换为：

```markdown
**降级**：非 git 仓库或无法建立隔离时，按 using-git-worktrees 的既有纪律记录并处理；来源分支受保护不是原地实施理由，仍在隔离特性分支工作。交付与权限遵循 references/delivery-channels.md，策略无法查询时记未知。
```

在阶段 6 标题后插入：

```markdown
分文件计划的本地/PR 交付统一遵循 [delivery-channels.md](references/delivery-channels.md)。ready 不等于已合并；尚 awaiting_merge 时最终任务与 roadmap 保持未完成，不提前 sync_commit。存量单文件保持原读取与记录形态。
```

executing-plans frontmatter description 在现有首段末补：`默认串行；已明确选择且声明/拓扑/资源合格时委托 executing-plans-parallel，任务边界切换及恢复由该 skill 编排。` 同步 openai.yaml 的这一行，其余字段不变：

```yaml
  short_description: "执行已批准的书面计划：默认主线程串行 TDD；显式选择且条件合格时委托并发 skill；共享完整审查、验收与本地/PR 交付闭环。"
```

在 `skills/requirement-analysis/references/codex-compat.md` 追加：

```markdown
## 并发 implementer（仅显式选择的执行分支）

executing-plans-parallel 可派发有界实施子任务；主线程先读 agents/implementer.md，在 prompt 中提供其绝对路径并要求子代理先读，不能假设 spawn_agent 自动加载 agent 定义。模型声明见 executing-plans-parallel；参数选择仍服从当前工具与用户约束，不硬编码模型、强度或并发上限。

工具没有 cwd/worktree 参数时，主线程先建立并验证独立 worktree，派发绝对路径、branch/base_commit/claim_key 与指针输入，要求每条命令显式指定工作目录。首次写前 implementer 核对实际绑定；父 cwd 不构成隔离证明。不能可靠隔离或工具不允许写码代理时串行降级，不虚构原生 isolation 参数，不自行扩大授权。

按当前工具查询 agent 生命周期并处理返回 ID；回执缺失先查 claim 与平台可见性，空列表不能证明终止。恢复须区分旧编排 owner 与存活 implementer，规则以 executing-plans-parallel 为准。通用映射、外部检索和失败隔离沿本文件原定义。
```

在 `skills/executing-plans/references/review-orchestration.md` 追加：

```markdown
## 并发分支的审查输入

executing-plans-parallel 全票集成后复用本文件完整编排。输入必须包含最初 base_commit 到当前集成 HEAD 的完整 diff、spec、progress、research 与 execution 证据指针；中途串行转并发不把 base 换成切换检查点。code-reviewer 继续只分析与复跑，implementer 自检不抵销任何审查维度、completeness critic 或矩阵验收。最终本地/PR 完成判据见 delivery-channels.md。
```

两个 guardrail snippet 中，在英文工作流段后插入英文句、中文工作流段后插入中文句；保留既有义务和标记。双语 guardrail README 各在「判定逻辑/Detection logic」节末添加对应句：

```text
Explicit parallel execution uses `executing-plans-parallel` only with declared independent writes and isolated resources; default execution stays serial. Only its implementer writes claimed code in isolated worktrees. `Spec: <repo-relative spec path>` is a traceability trailer, not a drift-guard bypass.

显式选择并发且写集合与资源可隔离时委托 `executing-plans-parallel`，默认仍串行；只有其 implementer 在独立 worktree 写认领代码。`Spec: <仓库根相对 spec 路径>` 仅作追溯，不代替 spec 同步或漂移检查。
```

**步骤 4：运行测试确认通过**

复跑步骤 2 的结构测试、check-plugin、validate-skills；逐条走查新增四例和既有七例，分离此前已登记的无关陈旧 eval（不顺手修复）。SKILL/openai 同时窄暂存后执行 check-openai-sync；shared reference 不复述插件根解析、TDD 或通用映射表。

**步骤 5：提交**

按 index 提交纪律窄暂存、手动运行 staged drift、解释本 spec 分面共存；提交 `feat(T06): 接通并发执行入口与共享交付`，带本特性 Spec trailer，记录实际 SHA。
