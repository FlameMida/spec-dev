interface:
  display_name: "Test Strategy"
  short_description: "划分 fast/final/manual；final 先于审查验收，变更后按影响补验，真实模型与静态证据分开记账。"
  default_prompt: "Use $test-strategy to design test lanes, translate an acceptance matrix into plan tasks, or set up the AI-agent testing skeleton. / 用 $test-strategy 设计测试分层、把验收矩阵翻译为计划任务、或搭 AI Agent 测试骨架。"

policy:
  allow_implicit_invocation: true
